# Completed backlog items 95–99

Bounded per [`README.md`](README.md)'s own instruction: `items-89-NN.md` reached 153,463 characters once item 95's own Return-to-paused-ride scroll carry-over follow-up was added, past the ~150,000-character soft cap, so that follow-up (and item 95 itself, entire and unrewritten) moved here instead. `items-89-NN.md` is renamed `items-89-94.md` and closed at item 94; items 89 through 94 are complete and final there. This file's own range now closes at item 99: once item 99's `0.4.13` follow-up and its dated iPhone acceptance note were added, item 104 — also originally recorded here, implemented ahead of pending items 100–103 — moved out to what was then `items-100-NN.md` (since split at item 101, so item 104's text now lives in [`items-104-109.md`](items-104-109.md)) instead, so this file could close cleanly at item 99. Items 95 through 99 are complete and final here.

**These are historical accounts of what shipped and why, at the time each was recorded.** Where later work has changed or superseded a detail described here, current source and tests are authoritative — but the rationale, rejected alternatives, and real regressions documented below remain valuable and are preserved rather than edited to match the present state. See the root [`CLAUDE.md`](../../../CLAUDE.md) for the required reading order.

---

<a id="item-95"></a>

## Item 95 — Route-switch prompt visibility and action hierarchy — done

_Category: Route Library interaction reliability_

95. **Route-switch prompt visibility and action hierarchy — done**
    - **Outcome.** A confirmed, fail-first-proven presentation defect fixed: the inline route-switch guard prompt (item 73/73-follow-up) now scrolls the complete chosen route card into view — accounting for the sticky top navigation and the visible viewport's bottom/safe area — instead of only the nested prompt sub-panel. The action order and styling were also corrected to the approved hierarchy: `End and switch`/`Discard and continue` first, `Return to paused ride` styled as the positive primary action second, `Cancel` last but still the default keyboard focus. Item 73's state machine, item 91's coordinator characterisation, and `App.tsx`'s conflict/retry/return semantics are unchanged.
    - **Baseline.** `9a215f8` (the item 94 iPhone-acceptance documentation commit, itself on top of `7fe8471` / `0.4.3`), clean, in sync with `origin/main`.
    - **Confirmed root cause.** `RouteListItem.tsx`'s switch-prompt effect held a ref (`switchPanelRef`) only on the nested `<div class="route-delete-confirm" role="alertdialog">` panel — the last child inside the card — and called `scrollIntoView({ block: "nearest" })` on it alone whenever the prompt's message changed. `block: "nearest"` has no awareness of the separate `position: sticky; top: 0` header (`App.tsx`'s `<header class="app-header--sticky">`, z-index 10): nothing in the codebase used `scroll-margin-top`/`scroll-padding` to compensate for its visual overlay (confirmed by a repository-wide grep returning zero matches), and the header's own rendered height is not a fixed CSS constant (`padding-top: calc(var(--safe-area-inset-top) + var(--space-8))` plus the nav row's own content height plus a 1px border), so nothing could reliably offset for it as a static value either. There was also no visibility check at all — the effect scrolled unconditionally on every message change, regardless of whether the card was already fully visible.
    - **Fail-first evidence, run against unmodified baseline before any production change.**
      - **Component-level (deterministic, source-confirmed defect).** A rewritten `describe("scroll-into-view target (backlog item 95)")` block in `RouteListItem.test.tsx`, run via `npx vitest run src/ui/library/RouteListItem.test.tsx -t "backlog item 95"` against unmodified `RouteListItem.tsx`: **3 failed, 2 passed**. The failures proved, deterministically: the scroll call targeted the nested panel (`switchPanelRef`'s element), not the outer `[data-route-id]` card, with `{ block: "nearest" }` rather than `{ block: "end" }`; there was no skip-when-already-visible branch (a card whose fabricated `getBoundingClientRect()` was comfortably within a headerless, un-cushioned viewport still triggered a scroll call); and the reduced-motion assertion inherited the wrong `block` value alongside the correct `behavior`.
      - **Real-browser (Chromium/Playwright), treated as a hypothesis until measured, not assumed.** `e2e/rideSessionSwitchGuard.spec.ts` was extended with header-clearance and card-bottom-visibility assertions (using `window.visualViewport`'s `offsetTop`/`height`, falling back to `window.innerHeight`) across its existing lower-route, portrait, short-landscape and 200%-enlarged-text tests, plus a new "already fully visible, no unnecessary jump" test and a DOM-order assertion. Run via `npx playwright test e2e/rideSessionSwitchGuard.spec.ts --project=chromium` against the unmodified baseline build (`npx vite build`, bypassing the locally-blocked `tsc -b` step — see Verification below): **9 passed, 3 failed**. The three failures, all genuine and distinct, confirm the hypothesis empirically rather than by assumption: the lower-route/portrait test failed only on button order (its geometry passed cleanly at that viewport/list-length combination — the sticky-header occlusion did not manifest there); the 844×390 short-landscape test failed on real header occlusion (`dialogBox.y` measured `43`, 22px above the header's own measured bottom of `65`); and the 200%-enlarged-text test (extended with 8 filler routes to push the card below the fold) failed dramatically, with the card's own bottom edge measured at `861`, 17px below the 844px visible viewport bottom.
    - **Implementation.**
      - `App.tsx`: added a read-only `stickyHeaderRef` (`useRef<HTMLElement>(null)`), attached to the existing `<header className="app-header--sticky">`, and threaded into `<RouteLibrary>` as a new optional prop — mirroring `routesScrollYRef`'s own established "App owns a page-chrome fact a screen component needs" shape (`RouteLibraryProps.restoreScrollYRef`). No other `App.tsx` change: the `PendingRideSwitch` state machine, `checkRideTransition`, `confirmPendingSwitch`, `retryPendingSwitchCheck`, `returnToPausedRide`, `handleSwitchTargetMissing` and the `origin: "route-card" | "global"` distinction are all byte-for-byte unchanged (confirmed via `git diff` before staging).
      - `RouteLibrary.tsx`: added the same optional `stickyHeaderRef` prop, doc-commented like `restoreScrollYRef`, passed straight through unchanged to every `<RouteListItem>` row (one shared ref, not per-row).
      - New `src/ui/library/routeSwitchCardVisibility.ts`: a small pure function, `isCardAlreadyFullyVisible(cardRect, headerBottomPx, bottomCushionPx, visibleTopPx, visibleBottomPx)`, following this directory's existing pure-function-sibling convention (`routeDeleteFocus.ts`, `routeLibraryView.ts`). Its own `routeSwitchCardVisibility.test.ts` covers the fully-visible, header-occluded, viewport-bottom-occluded, cushion-boundary and visible-top-exceeds-header-bottom cases directly with fabricated numbers — the geometry arithmetic is proven independently of any DOM/jsdom limitation.
      - `RouteListItem.tsx`: replaced `switchPanelRef` with `cardRef` (`useRef<HTMLLIElement>(null)`) on the outer `<li className="route-card stack">`, which also gains a conditional `route-card--switch-pending` modifier class whenever `switchPrompt` is non-null. The scroll effect (still gated on `switchPrompt.message` changing, preserving the exact "same message → no extra scroll; changed message → re-check" contract already proven by the pre-existing test) now reads the card's own `getBoundingClientRect()`, `stickyHeaderRef?.current?.getBoundingClientRect().bottom ?? 0`, the card's own computed `scrollMarginBottom` (for the safe-area cushion), and the visible viewport band via `window.visualViewport` (`offsetTop`/`offsetTop + height`, matching the reconciliation between visual- and layout-viewport coordinate spaces that `getBoundingClientRect()` uses) falling back to `[0, window.innerHeight]`. When `isCardAlreadyFullyVisible` reports the card is already within that band, the effect returns without scrolling; otherwise it calls `cardEl.scrollIntoView({ block: "end", behavior: prefersReducedMotion() ? "auto" : "smooth" })` — `block: "end"` was chosen deliberately over `"nearest"` because `"nearest"` is a no-op precisely when an element is larger than the scrollport and already straddles both edges (the exact "card too tall to fit" case this item needs to handle), the opposite of the required behaviour; the explicit visibility skip-check is what restores `"nearest"`-like idempotence for the ordinary, already-visible case.
      - `index.css`: added `scroll-margin-bottom: calc(var(--safe-area-inset-bottom) + var(--space-8))` scoped to the new `.route-card--switch-pending` modifier class only (reusing the exact token pairing `.ride-immersive-switcher` already uses for the same purpose elsewhere in this file) — not the base `.route-card` rule, since an ordinary card (including one showing the unrelated delete-confirmation panel, which has no scroll-into-view behaviour of its own) has nothing to reserve this space for.
      - Action order and styling (`RouteListItem.tsx`'s switch-prompt button block): reordered from Cancel → Return → Confirm to Confirm → Return → Cancel, a plain JSX reorder (confirmed no `order:`/`row-reverse` exists anywhere in `index.css` that would need removing). `Return to paused ride`'s class changed from `btn-secondary` to `btn-primary` (the established positive/accent treatment — `--colour-accent` is green in both light and dark themes); `Cancel` kept its `autoFocus` attribute unchanged, which continues to focus it on mount regardless of its new last DOM position (React's `autoFocus` is position-independent), so the safe, non-destructive action remains the default focus target despite the destructive/proceed action now rendering first. The stale `RouteSwitchPrompt` doc comment ("Cancel and Return are always secondary-styled") was corrected to state Cancel is secondary and Return is primary/positive.
    - **Negative control, followed by an immediate restore and a green re-run.** `git stash push --keep-index -- src/ui/library/RouteListItem.tsx` (the single file containing both the scroll fix and the action reorder/restyle — reverted together, with the already-rewritten tests left in place, since every failure it produces is independently attributable to the specific assertion it violates): the component suite reproduced exactly 6 failures, cleanly separable by cause — 3 from the scroll-target/skip-logic/reduced-motion assertions (the original fail-first set) and 3 from the order and `btn-primary`-styling assertions the reorder work depends on. The e2e suite, rebuilt against the reverted file, reproduced exactly the same 3 failures as the original baseline run (button order; the 22px short-landscape header occlusion; and, this time, the 200%-enlarged-text card-bottom assertion specifically, since the header-clearance assertion in that test had since been narrowed to the two-tier ordinary-vs-extreme-case logic described below). `git stash pop` restored the fix; both suites returned to green. This confirms every changed assertion is genuinely load-bearing on the production fix, not accidentally passing regardless.
    - **A real, evidence-driven test correction found along the way.** The 200%-enlarged-text scenario (with 8 filler routes added to push the card below the fold, matching the other extended geometry tests) is a genuine instance of the "card too tall to fit between the sticky header and the visible viewport" extreme case the backlog explicitly anticipated: the card measured 1726px tall against only a 766px available band (78–844). Even after the fix, the dialog's own top (containing the heading and message, above the actions) could not be brought fully below the header — mathematically impossible, since the dialog alone (1352px) exceeds the entire available band. Critically, all three actions and the card's own bottom edge were confirmed fully visible and usable (`block: "end"` correctly prioritises them). The test's original single-tier assertion (dialog top must always clear the header) was corrected to a two-tier rule: when the card fits within the available band, the complete prompt (including its top) must be immediately visible, exactly as before; when it genuinely cannot fit, only every action and the card's bottom edge are required to be immediately visible — matching the backlog's own extreme-case fallback wording rather than a stricter, physically-unsatisfiable reading of it.
    - **Files changed.** `src/App.tsx`, `src/ui/library/RouteLibrary.tsx`, `src/ui/library/RouteListItem.tsx`, `src/index.css` (production); new `src/ui/library/routeSwitchCardVisibility.ts` and its `routeSwitchCardVisibility.test.ts`; `src/ui/library/RouteListItem.test.tsx` (rewritten scroll-target tests, corrected order/style assertions); `e2e/rideSessionSwitchGuard.spec.ts` (extended geometry/order assertions across five existing tests, one new test); `package.json`/`package-lock.json` (version `0.4.3` → `0.4.4`); root `CLAUDE.md` (queue advanced to item 96); `docs/project/backlog.md` (item 95 removed); this file; `docs/project/current-status.md`.
    - **Verification.** Full local gate run on the final combined tree: `npm run format` (clean on every file this item touched — the pre-existing root-owned `*-DELETE` leftover directories are untracked and unrelated, documented below), `npm run lint` (clean), `npm run test` — **3059/3059** passed across 151 files, `npm run e2e` — **265/272** passed; the 7 failures are exclusively the already-documented, pre-existing local-sandbox `webkit-smoke` `browserType.launch` gap (missing `libgtk-4.so.1`/`libevent-2.1.so.7`/`libgstcodecparsers-1.0.so.0`/`libavif.so.13`), confirmed identical in name and cause to every prior item's own note on this; a targeted `--project=chromium --project=android-chrome` rerun independently confirmed all 265 non-webkit tests green. `npm run build` succeeded. `npm run typecheck` (`tsc -b`) could not write its incremental cache locally (`node_modules/.tmp` is root-owned from an earlier session, an already-documented pre-existing gap) — confirmed as cache-write-only, not a masked type error, by temporarily pointing both `tsconfig.app.json`/`tsconfig.node.json`'s `tsBuildInfoFile` at a scratch, writable path (the same documented workaround used by prior items), running `tsc -b --force` clean with zero errors, then reverting both files to their original content (confirmed via `git diff` showing no residual change). `npm run format:check` run last, clean on every file this item touched.
    - **Limitations.** No physical iPhone or Android verification is claimed — automated evidence only. `getBoundingClientRect()`/`window.visualViewport` coordinate reconciliation was exercised only under Chromium/Playwright's ordinary and 200%-text-zoom geometry, not under an active pinch-zoom gesture; no such divergence was actually observed during this work, so none is recorded as a residual defect. Real Android Chrome and installed-iPhone Safari-PWA acceptance — including the lower-card prompt's scroll/visibility behaviour, the new action order/hierarchy, and usability at enlarged text and short landscape — remain outstanding; see `docs/project/current-status.md`.
    - **Follow-up (2026-09-03), v0.4.5 — Return-to-paused-ride scroll carry-over.** The user confirmed item 95's principal result on the installed iPhone Home Screen PWA: the inline prompt appears with the chosen card, the card and actions are brought into view correctly, and the action order/hierarchy work as intended. One intermittent follow-up remained: after tapping `Return to paused ride`, the newly opened paused route's Pre-Ride screen sometimes inherited the Route Library's lower scroll position instead of opening at its normal top.
      - **Mechanism.** Two distinct contributors were examined, held to different evidentiary bars as the diagnosis explicitly required. **Source-confirmed** (a plain fact about the code, not an inference): `RouteListItem.tsx`'s switch-prompt scroll effect keyed only on message text, with no `switchPrompt.busy` guard, even though `busy` was already computed and passed — so tapping Return (which flips status to `"returning"`, changing the message to `"Opening your paused ride…"` while `screen` is still `"library"`) re-triggered a second, wholly avoidable `scrollIntoView` call. **A confirmed structural overlap opportunity, not an empirically observed cause**: nothing synchronises the card's own item-95 scroll animation (started when the prompt first opened, on the `"conflict"` message) with `useResetScrollForNewRideContent`'s single, one-shot `window.scrollTo({top:0})` reset once `openRideTarget` succeeds — so that original animation being still mid-flight when the reset fires is timing-plausible, but this repository's own Chromium e2e harness could not catch it in flight in repeated real runs (see Evidence below); it is reported as an opportunity the fix closes structurally, not as a proven cause of the field symptom.
      - **Fix.** (a) `RouteListItem.tsx`: the scroll effect now returns immediately whenever `switchPrompt.busy` is true, and — critically — does so _before_ recording the message into `lastSwitchMessageRef`, so the ref keeps holding the last actionable text throughout any busy interval and a later genuinely-new actionable message (e.g. a failure) still re-arms and re-checks visibility correctly. (d) `useResetScrollForNewRideContent.ts`: only for a genuinely new token (never a plain nav-tab return), the existing one-shot `scrollTo(0)` is followed by a `requestAnimationFrame` loop that re-flattens `window.scrollY` to within a 1px tolerance until it holds there for 3 consecutive frames — a condition-based completion, not a fixed frame count — backstopped by an elapsed-time (not frame-count) safety cap, and aborted immediately the instant a genuine `touchstart`/`pointerdown`/`wheel` event is observed, so it can never fight a deliberate later scroll. All scheduled frames and the input listener are cancelled via the effect's existing `[screen, token]`-keyed cleanup whenever a newer token supersedes, `screen` leaves `"riding"`, or the component unmounts. This makes correctness independent of exactly which animation is racing the reset, or of any unverifiable assumption about whether an instant `scrollTo` reliably aborts an in-flight smooth one on real iOS Safari — a boundary this sandbox cannot test directly. A third candidate (cancelling at the `openRideTarget` transition boundary) was considered and not used as the primary mechanism for the same reason; (d) does not depend on it.
      - **Fail-first evidence.** `RouteListItem.test.tsx`: a busy `"returning"`-shaped prompt produces no second `scrollIntoView` call, and a later non-busy actionable message still scrolls (fails pre-fix: 2 calls; passes post-fix: gated correctly, then re-arms). `useResetScrollForNewRideContent.test.ts`: a scripted late-drift sequence (a simulated nonzero `scrollY` landing on a later frame) is detected and corrected, then the loop terminates once genuinely stable; a pathological never-stabilising sequence terminates at the elapsed-time cap, not indefinitely; a newer token or unmount cancels the pending frame and listener; each of `touchstart`/`pointerdown`/`wheel` stops the loop immediately; a genuine plain nav-tab return (away from Riding and back with no new token) never starts the loop at all. `App.test.tsx`: extended Return-to-paused-ride and a new End-and-switch characterisation, each with a forced off-screen card-geometry stub (jsdom's default zero-rects otherwise make `isCardAlreadyFullyVisible` trivially true, making a raw call-count assertion vacuous), proving no second `scrollIntoView` call during the real, App-wired busy window, that the reset fires exactly once at the correct transition, and that a simulated Library scroll position is still restored on a later return to Routes.
      - **Two independently-isolated, deterministically-graded negative controls**, each reverting only the one production hunk, reconfirming the regression, then restoring and re-verifying green. **A** (busy-gate reverted, reassertion kept): `RouteListItem.test.tsx`'s busy-message test fails again (2 calls). **B** (reassertion reverted, busy-gate kept): `useResetScrollForNewRideContent.test.ts`'s 7 reassertion-mechanism tests fail again (scripted late-drift, safety cap, token/unmount cleanup, all three input-abort cases); the pre-existing plain-tab-return and no-resume-correcting tests correctly stayed green throughout both reverts, confirming they exercise a different contract.
      - **Real-browser (Chromium) evidence, reported honestly as observational, not a graded control.** A new ordinary-motion `e2e/rideSessionSwitchGuard.spec.ts` test (no `reducedMotion:"reduce"`) builds the same long, most-recent-first Routes list as item 95's own reduced-motion test, activates Return via `locator.click({force:true})` (a real, trusted, freshly-positioned CDP click that skips Playwright's stability wait — an earlier coordinate-snapshot approach proved unreliable: a `boundingBox()` measured even slightly before dispatch could go stale enough, while the page was still scrolling, that the click landed on `End and switch` instead), and records `scrollY` every animation frame in-page (immune to Node-side round-trip latency) from before the click through the whole Pre-Ride settle. Across 5+ repeated runs, Pre-Ride reached and held within 1px of the top for the entire post-transition recording every time (`offTopSampleCount=0`). Honestly reported: `motionObservedBeforeReturn` was `false` in every run — this harness's real local IndexedDB reads resolve in roughly 10–80ms total, faster than needed for the item-95 `"conflict"` animation to still be genuinely in flight by the time Return is clicked. This is a reportable data point about this environment's timing, not proof the underlying race cannot occur on slower real hardware, and Chromium's own timing here neither confirms nor refutes anything about real iOS Safari's. A non-race "fully settled" control test and a minimal ordinary-motion End-and-switch analogue (proving the shared `openRideTarget`/busy-gate boundary deliberately, not merely assuming it from code-sharing) were also added, plus a scroll-at-top assertion on the pre-existing reduced-motion test.
      - **Incidental finding, not a defect and not addressed by this fix.** While building the e2e Library-restoration proof, Chromium's own native scroll anchoring was observed shifting `window.scrollY` on its own once the busy message's much shorter text shrinks the card content above the current viewport — a real, separate browser behaviour that makes the _exact_ pixel value restored on a later return to Routes unpredictable from outside a controlled test. The e2e tests assert the qualitative property that matters (`scrollY > 0` after returning — not discarded), while the precise exact-value restoration contract is proven in `App.test.tsx`'s own deterministic, mocked-`scrollTo` environment instead.
      - **Files changed.** `src/ui/library/RouteListItem.tsx`, `src/ui/shared/useResetScrollForNewRideContent.ts` (production); `src/ui/library/RouteListItem.test.tsx`, `src/ui/shared/useResetScrollForNewRideContent.test.ts`, `src/App.test.tsx`, `e2e/rideSessionSwitchGuard.spec.ts` (tests); `package.json`/`package-lock.json` (version `0.4.4` → `0.4.5`); this file; `docs/project/current-status.md`.
      - **Verification.** `npm test` — **3071/3071** across 151 files (+12 from the `3059/3059` baseline). `npm run e2e` — **268/275** passed (+3 new tests; the 7 failures are exclusively the already-documented, pre-existing local-sandbox `webkit-smoke` `browserType.launch` gap, unrelated to any file this follow-up touched); repeated standalone runs of `rideSessionSwitchGuard.spec.ts` alone (5+ full passes) confirmed no flake in the new tests. `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` and `npm run format:check` (run last) all green on the complete final tree; the `tsBuildInfoFile` scratch-path workaround for the already-documented root-owned `node_modules/.tmp` gap was used and reverted before commit, confirmed via `git diff` showing no residual change.
      - **Limitations.** No physical iPhone or Android verification is claimed for this follow-up either. The real-browser evidence is Chromium-only and did not catch the original animation genuinely in flight at Return-click time in this harness (see above) — do not treat this fix's green automated suite, on its own, as field acceptance; the deployed `0.4.5` build needs its own real-device retest, per `docs/project/current-status.md`.
    - **Installed-iPhone acceptance (2026-09-03).** The user retested the deployed `0.4.5` build on the installed iPhone Home Screen PWA and reported: "Yes, it works now and opens at the top." This confirms the specific Return-to-paused-ride scroll-carry-over defect described in the Follow-up above no longer reproduces on real iOS Safari/PWA — choosing `Return to paused ride` opens the paused route's Pre-Ride content at the top rather than carrying over the lower Route Library scroll position. This report is narrow and does not separately re-confirm the Chromium-only evidence caveats already recorded above: the distinct early-tap-during-scroll vs. tap-after-scroll-settles timing variants, or the Route Library's own exact scroll-position restoration on a later return to Routes. Those remain open — see `docs/project/current-status.md`.
    - Item 96 (Grace period for the slow map-imagery notice) is now the selected next implementation item.

---

<a id="item-96"></a>

## Item 96 — Grace period for the slow map-imagery notice — done

_Category: Map imagery status presentation_

96. **Grace period for the slow map-imagery notice — done**
    - **Outcome.** The "Map imagery is taking longer than usual to load. Your route and position are still shown." notice (`MapView.tsx`, `data-testid="map-imagery-delayed-banner"`) now waits a 2-second presentation grace (`SLOW_IMAGERY_NOTICE_GRACE_MS`) before it may render, instead of appearing the instant `styleStructurallyReady && !ready && !usingFallbackStyle && tileErrorMessage === null` (`slowImageryNoticeEligible`) became true with no delay of its own. `STYLE_READY_TIMEOUT_MS` (15s, a wholly separate style-document-readiness fallback), Diagnostics categories, camera/recovery logic, and item 83's external `onImageryStatusChange` presentation seam are all unchanged.
    - **Confirmed cause.** The banner's JSX condition was exactly `slowImageryNoticeEligible`, checked and rendered synchronously on every render — the existing test suite observed it appear immediately after `style.load` with no elapsed time. Predates the CSP work (item 93) and the `0.4.0` release-preparation slice; not a regression from either.
    - **Mechanism actually shipped.** `slowImageryNoticeEligible` (the pre-existing trigger condition, unchanged) is still recomputed fresh every render. A `useState<number | null>` (`elapsedForRetryToken`) is non-null only once a grace timer has genuinely elapsed for the `retryToken` value stored in it, and is set _exclusively_ from inside a `window.setTimeout` callback — never synchronously in the owning effect's body — because this project's `react-hooks/set-state-in-effect` lint rule flags a direct effect-body `setState` call as risking cascading renders. A second, small piece of state (`lastRenderedEligible`) uses React's own documented "adjust state during render" pattern (not a further Effect) to null out `elapsedForRetryToken` the instant `slowImageryNoticeEligible`'s own value changes, in the same render, before paint — this closes the "an already-elapsed marker instantly redisplays once eligibility resumes with the same `retryToken` (e.g. a tile error interrupting, then clearing, with no retry involved)" gap without a second effect-body `setState` call. `imageryGraceEpisodeRef` (a plain `useRef`, incremented once at the very start of every effect run, and again — if unclaimed by a later run — in that same run's cleanup) is read and written _only_ inside the effect and its timer callback, never during render, because this project's `react-hooks/refs` lint rule flags reading a ref value during render. The timer callback checks `imageryGraceEpisodeRef.current !== episode` before calling `setElapsedForRetryToken`, so a callback that becomes runnable despite an intervening cleanup can never mark a superseded episode elapsed. The effect's dependency array is `[slowImageryNoticeEligible, retryToken]` — including `retryToken` explicitly, not relying on `slowImageryNoticeEligible`'s boolean value alone, closes a `retryToken`-bump race: a retry renders once with the _old_ eligibility state still in place (the map-creation effect's own reset to "ineligible" runs later, as a separate effect body), so a boolean-only dependency could silently miss restarting the timer for a retry whose eligibility value happened not to visibly change across it. The final render condition, `slowImageryNoticeVisible = slowImageryNoticeEligible && elapsedForRetryToken === retryToken`, re-derives live eligibility and requires the elapsed marker to belong to the current retry generation — a timer firing is never sufficient by itself.
    - **Rejected/superseded design, corrected during plan review before implementation.** An initial plan proposed a plain boolean effect keyed only on `[slowImageryNoticeEligible]`, with `window.clearTimeout` alone for cancellation and a stored `{retryToken, episode, elapsed}` object whose `episode` field was compared against the ref _during render_. Plan review identified two problems with this: `clearTimeout` cannot neutralise a callback that has already become runnable before React's own passive-effect cleanup gets scheduled, and the callback's own guard (not `clearTimeout` alone, and not a render-time ref read) needed to be what made a stale callback safe. The revised design added the ref-based episode guard _inside_ the timer callback and — per an explicit follow-up correction — also invalidates the episode in the effect's _cleanup_, not only at the start of the next effect run: at unmount specifically, no later effect run ever exists to mint a fresh episode and thereby make a pending one stale by comparison. Once implemented, this exact shape (a stored `{retryToken, episode, elapsed}` object plus a render-time ref comparison) was itself replaced during implementation, after this project's own `react-hooks/set-state-in-effect` and `react-hooks/refs` ESLint rules flagged the direct effect-body `setState` calls and the render-time ref read described above — the final `elapsedForRetryToken`-plus-render-time-reset shape (described in Mechanism above) is what shipped, preserving every one of plan review's correctness requirements without either lint violation.
    - **Fail-first evidence.** Before any production change, the rewritten/added unit tests were run against the unmodified baseline: `npx vitest run src/map/MapView.test.tsx` — **10 failed, 187 passed** (of 197). The 10 failures were exactly the ones expecting the new grace behaviour (8 new tests in the `backlog item 96` describe block, plus 2 existing tests updated to assert the notice is absent immediately after `style.load` and only appears after a 2000ms fake-timer advance) — all failing because the unmodified banner rendered immediately, with no delay.
    - **Negative controls**, each isolating one specific guard, reverting only it, reconfirming a real failure, then restoring and re-verifying the full suite green:
      - **Callback episode-guard alone removed** (`if (imageryGraceEpisodeRef.current !== episode) return;` deleted from the timer callback, render-time reset left intact): the dedicated "ignores an obsolete grace-timer callback from an earlier episode" test failed — a directly-invoked stale callback revealed the banner. Confirms the callback's own guard is independently load-bearing, not redundant with the render-time reset (the two tests use the _same_ `retryToken` throughout, so the render-time reset alone cannot discriminate this case).
      - **Render-time reset alone removed** (`lastRenderedEligible`/`setElapsedForRetryToken(null)` block deleted, callback guard left intact): the dedicated "requires a fresh full grace period... when eligibility returns after an already-visible notice was superseded by a tile error" test failed — the notice redisplayed instantly from a stale marker instead of requiring a fresh 2000ms. Confirms this mechanism is independently load-bearing too.
      - **Cleanup's episode-increment alone removed** (the unmount-specific `if (imageryGraceEpisodeRef.current === episode) { imageryGraceEpisodeRef.current += 1; }` deleted from the effect's cleanup): the full suite, including the dedicated unmount test, stayed green. React 18 silently no-ops a state update reaching an unmounted function component (no throw, no dev warning) — confirmed empirically by this exact comparison — so this specific line's own effect is not independently observable via a black-box unit test in this environment. It is retained as correct, defensive code per the reasoning above (the "no later effect run exists to invalidate a pending episode at unmount" gap is real by inspection, even though this project's test harness cannot force it to manifest as an observable symptom), and the corresponding test is honestly commented as a basic crash-safety net rather than a fix-discriminating assertion.
        All three reverts were restored and the full 197-test file re-verified green before continuing.
    - **Diagnostics separation, proven directly.** Three dedicated tests: an ordinary load resolving inside the grace window records no `"initial-load-timeout"` entry; a load left showing the notice just past its own grace also records none; the pre-existing, unrelated 15-second `STYLE_READY_TIMEOUT_MS` timeout still records its own `"initial-load-timeout"` entry exactly as before, proving the two timers stay independent.
    - **Real-browser evidence.** `e2e/mapStyleReadiness.spec.ts`: a new test installs a `page.addInitScript` `MutationObserver` before navigation (so a transient DOM insertion can't escape polling), reaches ordinary fast local-style readiness, and asserts the notice was never attached at any point — not merely absent at a final snapshot. `e2e/mapImageryRecovery.spec.ts`: one self-contained test (appearance, timing proof, release and disappearance kept together rather than split across tests with implicit cross-test dependencies) uses a new `holdTiles()`/`releaseTiles()`/`heldTileRequestCount()` extension to `installLocalMapStyleWithTileSource`'s `TileFailureController` (`e2e/support/localMapStyle.ts`) to hold real tile requests genuinely pending — each held request's own `page.route` handler invocation stays pending on an internal promise until `releaseTiles()` resolves it, not merely "returned early holding an unresolved `Route`" — while proving `data-route-loaded`/`data-route-coordinate-count` are populated and `data-map-ready` is still `"false"`. The test polls `heldTileRequestCount() > 0` before any slow-path assertion, proving a real tile request was genuinely intercepted and held, not merely that `holdTiles()` was called. Grace duration is measured from `[data-testid="map-loading"]`'s own removal (which corresponds to `styleStructurallyReady` becoming true) to the notice's first DOM attachment — deliberately not from `data-route-loaded`, since route-source processing happens after structural readiness and could make a correct 2-second grace measure shorter than it actually is. The measured delta was asserted `>= 1_900ms` and `< 5_000ms`. Both new e2e tests were run 5 times each standalone with no flake, and the full `mapImageryRecovery.spec.ts` file (11 tests) and `mapStyleReadiness.spec.ts` file (2 tests) passed together.
    - **Files changed.** `src/map/MapView.tsx`, `src/map/MapView.test.tsx` (production/tests); `e2e/mapStyleReadiness.spec.ts`, `e2e/mapImageryRecovery.spec.ts`, `e2e/support/localMapStyle.ts` (e2e); `package.json`/`package-lock.json` (version `0.4.5` → `0.4.6`); root `CLAUDE.md` (queue advanced to item 97); `docs/project/backlog.md` (item 96 removed); this file; `docs/project/current-status.md`. Commit `a503f1e`.
    - **Verification.** `npm run format`, `npm run lint` (clean). `npm run typecheck` (`tsc -b --noEmit`) — the already-documented root-owned `node_modules/.tmp` cache-write gap recurred; confirmed as cache-write-only via the same documented `tsBuildInfoFile` scratch-path workaround used by prior items, run clean, then reverted (confirmed via `git diff` showing no residual change). `npm run build` succeeded. `npm test` — **3087/3087** passed across 151 files (+16 from the `3071/3071` baseline, matching the new describe block's exact test count). `npm run e2e` — **270/277** passed (+2 new tests; the 7 failures are exclusively the already-documented, pre-existing local-sandbox `webkit-smoke` `browserType.launch` gap, unrelated to any file this item touched); a targeted `--project=chromium --project=android-chrome` rerun independently confirmed all 270 non-webkit tests green. `npm run format:check` (run last) clean on every file this item touched — the pre-existing root-owned `*-DELETE` leftover directories are untracked and unrelated. `npm ci` could not complete locally (the same already-documented root-owned `node_modules/.tmp` gap prevents removing two cached `.tsbuildinfo` files); `node_modules` was confirmed intact and functional (`npm ls --depth=0` succeeded, 377 packages) and unaffected by the two version-string-only `package.json`/`package-lock.json` edits, so the rest of the gate proceeded against the existing install — this is a pre-existing local-sandbox permission gap, not evidence of a broken lockfile, and CI's own `npm ci` step is authoritative.
    - **Limitations.** Automated (Vitest/Playwright Chromium) evidence only. No physical iPhone or Android verification is claimed — see `docs/project/current-status.md` for the specific outstanding manual acceptance checklist.

---

<a id="item-97"></a>

## Item 97 — Compact ongoing warning for imported routes without trusted turn cues — done

_Category: Riding navigation trust presentation_

97. **Compact ongoing warning for imported routes without trusted turn cues — done**
    - **Outcome.** For an active ride on an untrusted `gpx-import` route, the existing full warning ("No trusted turn information is available for this imported GPX. Follow the route line on the map.") now shows for the first `UNTRUSTED_GPX_FULL_WARNING_MS` (10,000 ms) of a genuine ride episode, then collapses to a persistent, native `<button type="button">` labelled "No turn cues" (`aria-expanded`/`aria-controls`), which the rider can activate at any time to reveal the exact full explanation again and collapse it back. The compact control is rendered outside the Map-only fixed-shell block, so it remains present and undisturbed in both Map and Profile. The distinct non-GPX-import untrusted message ("Turn information is unavailable for this route.") is byte-for-byte unchanged, still Map-only and untimed; trusted planner and trusted GPX (ACN extension) presentation is unchanged; `hasTrustedManoeuvres`/`selectNextManoeuvre`'s trust boundary is untouched.
    - **Confirmed mount-boundary-as-episode-identity finding, superseding the originally-anticipated design.** The hand-off for this item specified an explicit "session/episode identity" — a counter in `RidingScreen.tsx`, incremented inside `handleStart()`'s existing idle-only branch, threaded down as a prop, mirroring `MapView.tsx`'s item-96 `SLOW_IMAGERY_NOTICE_GRACE_MS` episode-ref shape. Source tracing during planning, and load-bearing tests written to prove it (not merely assert it), established this to be unnecessary: `RidingUntrustedGpxNotice` is rendered by `RidingScreen` only while `nav.geolocationStatus !== "idle"` for an untrusted `gpx-import` route, and `geolocationStatus` (`useRideNavigation.ts`'s `start()`/`stop()`) never transiently passes back through `"idle"` on the mid-ride "error" -> "watching" Try-again retry — `start()` transitions directly from either `"idle"` or `"error"` to `"watching"`, and `stop()` (used by Pause) is the only path back to `"idle"`. Because the notice is deliberately rendered **outside** the Map-only `.ride-content-area` toggle — unlike `RidingNextManoeuvrePanel`, which remounts on every Map<->Profile switch, the exact failure mode the hand-off's "do not restart on component mount" warning was guarding against — React's own mount/unmount lifecycle for the notice already _is_ the required episode boundary: a fresh mount happens only on a genuine `idle -> non-idle` transition (fresh Start, explicit Resume, or the one-use cold `resumeIntentToken` recovery — all reaching `"watching"` in the ordinary case, though a fresh Start that fails immediately can also land directly in `"error"`, which is still a genuine new episode), and the component never unmounts/remounts for GPS fixes, rerenders, map-imagery recovery, Map<->Profile switching, or the mid-ride Try-again retry. `RidingScreen.tsx`'s own pre-existing comment on `activeView` already stated this exact fact independently ("this whole screen unmounts on Pause"), corroborating the finding. Consequence: `RidingUntrustedGpxNotice` carries no props at all, `RidingScreen.tsx` needed no new `useState`, and `handleStart()` — a shared, heavily-commented function used by three call sites (the ordinary button, the mid-ride Try-again retry, and the cold-recovery consumer) — was not touched.
    - **Mechanism shipped.** `RidingUntrustedGpxNotice.tsx`: a mount-once `useEffect(() => {...}, [])` starts a single `window.setTimeout(..., UNTRUSTED_GPX_FULL_WARNING_MS)` that sets `autoCollapsed` to `true`; its cleanup calls `window.clearTimeout`. Because the effect has no reactive dependency, it can never re-run while the component stays mounted, so there is at most one live pending timeout per mounted instance at any time — this is explicitly documented in the component's own comment as the reason no `MapView`-style episode ref is needed (that ref exists there specifically because that effect _can_ re-run while mounted, on `slowImageryNoticeEligible`/`retryToken` changes; this effect has no such trigger). The comment also explicitly avoids claiming "runs exactly once per mount": React Strict Mode's development-only setup-cleanup-setup double-invoke can genuinely execute the effect body twice for one logical mount, and the correctness property that must hold regardless is only-ever-one-live-timer, not exactly-once-ever — cleanup makes this Strict-Mode-safe by construction. A second `isExpanded` boolean state, toggled by the compact button, is independent of the timer and is never re-armed by it. `RidingScreen.tsx` adds one derived boolean, `showUntrustedGpxNotice = nav.geolocationStatus !== "idle" && !isTrustedForNavigation && route.source.kind === "gpx-import"`, computed beside the existing `isTrustedForNavigation`, and renders `<RidingUntrustedGpxNotice />` once, as a sibling immediately after the existing Map-only `RidingNextManoeuvrePanel` block and before `.ride-content-area` — outside the Map/Profile toggle entirely. `RidingNextManoeuvrePanel.tsx`'s own `gpx-import`-untrusted branch (previously rendering the full warning text itself) now returns `null`, delegating entirely to the new component; its doc comment was updated from "four mutually exclusive states" to three, noting the delegated case. The compact button and its revealed explanation reuse only existing tokens/classes (`.status-row`, `.btn-secondary`, `.stack`, and the bare `button` rule's default 44 px `min-height`/`min-width`) — no new CSS. `aria-controls` is `undefined` (not a dangling reference) while collapsed, mirroring `RouteSummaryPanel.tsx`'s own established convention; the revealed explanation paragraph carries no `role` at all, so re-expanding never produces a fresh live-region announcement, and the wrapping `<div className="stack">` carries no role/`aria-live` either, so the automatic collapse itself is never announced. No animation was introduced, so `prefers-reduced-motion` handling was moot.
    - **Fail-first evidence.** Before any production change, three new/changed test files were run against the unmodified baseline: `RidingUntrustedGpxNotice.test.tsx` (new — failed to even resolve, the module not yet existing), `RidingScreen.untrustedGpxNotice.test.tsx` (new — **4 of 8 failed**, exactly the four fake-timer lifecycle tests expecting the 10-second collapse and Map/Profile persistence, since the baseline's warning never collapsed and was still nested inside the Map-only block), and the updated assertion in `RidingNextManoeuvrePanel.test.tsx` (**1 of 14 failed**, expecting `null` where the baseline still rendered the full text). All failures were exactly the ones the new behaviour was expected to fix. A further pre-existing test in `RidingScreen.mapProfileViews.test.tsx` ("Profile view shows the elevation window selector and chart, with Map-only controls unreachable") asserted the old Map-only-hiding behaviour for this exact message and was updated in the same commit, with a comment explaining the item 97 change, once the new cross-view-visible behaviour was confirmed intentional.
    - **Negative controls**, each isolating one specific claim, reverting only it, reconfirming a real, narrowly-targeted failure, then restoring and re-verifying the full suite green:
      - **Timer cleanup removed** (the effect's `return () => window.clearTimeout(timeoutId)` deleted): the dedicated "clears the pending timer on unmount so a stale callback cannot affect a later mount" test failed (`clearTimeout` spy never called); the other 6 tests in that file stayed green.
      - **Render-gate placement narrowed to nested-inside-Map-only** (temporarily changed to `showUntrustedGpxNotice && activeView === "map"`, reproducing today's `RidingNextManoeuvrePanel` remount-on-view-switch behaviour): the dedicated "does not restart the deadline across a Map -> Profile switch" test failed exactly at the point of asserting the full warning remained visible after switching to Profile; the other 7 tests in that file stayed green. This is the discriminating proof that placement outside the Map-only toggle, not just the timer logic itself, is load-bearing.
      - **Render-gate condition narrowed from `!== "idle"` to `=== "watching"`** (rather than mutating `handleStart`'s shared state machine, which risked disturbing well-tested, multi-call-site logic for a throwaway mutation): the dedicated mid-ride error/Try-again continuity test failed exactly at the point of asserting the full warning remained visible immediately after the geolocation error (the notice unmounted the instant status became `"error"`, losing its state); the other 7 tests in that file stayed green. This isolates the exact claim the mount-boundary finding depends on: that `"error"` must count as "still the same episode," not merely `"watching"`.
        All three reverts were restored and the full riding test directory (36 files, 751 tests at the time) re-verified green before continuing.
    - **Real-browser evidence.** `e2e/ridingNextManoeuvre.spec.ts`'s existing `"ordinary imported GPX"` block gained two new tests, both driving the real app against the local MapLibre style and the `smoke-route.gpx` fixture via a real "Start riding" click, at an iPhone-sized 390×844 viewport: (1) a comprehensive test proving the full warning appears immediately, waits a real, unshortened ~10 s (`toBeVisible({ timeout: 12_000 })`, the production constant never reduced and no test-only hook added) before compacting; proves — as **structural layout evidence** (element `boundingBox()` geometry, not rendered-pixel screenshotting) — that the notice's own box shrinks while `.ride-content-area--immersive`'s own box (a `flex: 1` sibling filling whatever vertical space its fixed-size siblings leave, per `RidingScreen.tsx`'s own "backlog item 56" comment) genuinely grows; confirms the compact button is ≥44×44 px and fully contained in the viewport; confirms it survives a real Map -> Profile -> Map switch undisturbed; and drives keyboard-only disclosure (a bounded real `Tab`-press loop reaching the button, mirroring `ridingWakeLock.spec.ts`'s/`ridingClimbView.spec.ts`'s own documented gotcha that a scripted `.focus()` call does not reliably engage Chromium's `:focus-visible` heuristic, then asserting `outlineWidth !== "0px"`, then `Enter` to expand/collapse). (2) A 200%-text-size and short-landscape (844×390) test, reusing this suite's exact established `document.documentElement.style.fontSize = "200%"` idiom, structured to capture the extreme-combination content-area height **before** compaction (still within the initial full-warning window) and **after** (once compact), asserting the after-height is never less than the before-height — a robust relative claim rather than a fragile absolute pixel floor. This surfaced a genuine, honestly-recorded finding: at 200% text size **and** short landscape simultaneously, `.ride-content-area--immersive`'s own height was already squeezed to 0 px by the pre-existing header/status-card/switcher chrome **before** this item's own change (confirmed by isolating the notice's DOM contribution and remeasuring, and by directly reproducing the old permanently-full-warning behaviour at the same viewport/zoom) — a pre-existing extreme-edge-case limitation of the fixed shell at this specific combination, not a regression introduced here; the compact state modestly improves it (0 px -> 5 px) but does not fully resolve it, and the test therefore does not assert absolute usability of the _expanded_ explanation at this exact combination, only that it stays free of horizontal clipping and that collapsing again recovers the more-usable compact state. All 6 tests in the file (4 pre-existing plus 2 new) were run 4 times total with no flake.
    - **Files changed.** `src/ui/riding/RidingUntrustedGpxNotice.tsx` (new), `src/ui/riding/RidingUntrustedGpxNotice.test.tsx` (new), `src/ui/riding/RidingScreen.untrustedGpxNotice.test.tsx` (new), `src/ui/riding/RidingNextManoeuvrePanel.tsx`, `src/ui/riding/RidingNextManoeuvrePanel.test.tsx`, `src/ui/riding/RidingScreen.tsx`, `src/ui/riding/RidingScreen.mapProfileViews.test.tsx` (production/tests); `e2e/ridingNextManoeuvre.spec.ts` (e2e); `package.json`/`package-lock.json` (version `0.4.6` → `0.4.7`, hand-patched, no `npm install`); root `CLAUDE.md` (queue advanced to item 98); `docs/project/backlog.md` (item 97 removed); this file; `docs/project/current-status.md` (item 96's phone observation recorded, item 97's outstanding-acceptance entry added).
    - **Verification.** `npm run format`, `npm run lint` (clean; the pre-existing root-owned `test-results-root-owned-leftover-DELETE/`/`_stale-root-owned-artifact-DELETE/` write errors during `format` are the same already-documented untracked, unrelated leftovers as prior items). `npm run typecheck` (`tsc -b --noEmit`) — the already-documented root-owned `node_modules/.tmp` cache-write gap recurred; confirmed cache-write-only via the same documented `tsBuildInfoFile` scratch-path workaround used by prior items, run clean, then reverted (confirmed via `git diff` showing no residual change to either tsconfig file). `npm run build` succeeded (same workaround). `npm test` — **3102/3102** passed across 153 files (+15 tests, +2 files from the `3087/151` baseline, matching the 15 new tests in the two new test files). `npm run e2e` — **272/279** passed (+2 new tests from the `270/277` baseline); the 7 failures are exclusively the already-documented, pre-existing local-sandbox `webkit-smoke` `browserType.launch` missing-shared-library gap, unrelated to any file this item touched; a targeted `--project=chromium --project=android-chrome` rerun independently confirmed all 272 non-webkit tests green. The new e2e file's own tests were additionally run 4 times total (an initial run plus 3 repeats) with no flake. `npm run format:check` (run last) — clean on every one of the 13 files this item actually touched (confirmed via a scoped `prettier --check` invocation listing exactly those paths); the 232-file warning from the unrestricted repo-wide check is entirely the pre-existing, untracked, root-owned `*-DELETE` leftover directories, confirmed via the same scoped check excluding them. `git diff --check` clean. `npm ci` could not complete locally (the same already-documented root-owned `node_modules/.tmp` gap prevents removing two cached `.tsbuildinfo` files); `node_modules` was confirmed intact and functional (`npm ls --depth=0` resolved cleanly) and unaffected by the two version-string-only `package.json`/`package-lock.json` edits, so the rest of the gate proceeded against the existing install — a pre-existing local-sandbox permission gap, not evidence of a broken lockfile; CI's own `npm ci` step is authoritative. `package-lock.json`'s diff was confirmed to be exactly the two version-string lines, with no dependency resolution change.
    - **Limitations.** Automated (Vitest/Playwright Chromium) evidence only. No physical iPhone or Android verification is claimed, and no screen-reader (VoiceOver) behaviour has been confirmed — see `docs/project/current-status.md` for the specific outstanding manual acceptance checklist. The 200%-text-plus-short-landscape extreme combination's pre-existing content-area squeeze (see Real-browser evidence above) remains a known, unresolved limitation of the fixed shell generally, tracked as a candidate for item 103's broader visual-consistency work rather than fixed here.

---

<a id="item-98"></a>

## Item 98 — Direction-aware active-Riding route-segment layering — done

_Category: Riding map presentation_

98. **Direction-aware active-Riding route-segment layering — done**
    - **Outcome.** During active route Riding only, a short current/near-ahead route interval is now painted above the whole-route macro/micro presentation, so at a geographic overlap the colour belongs to the route occurrence the rider is actually travelling. The confirmed field defect — grey completed outbound covering green remaining return immediately ahead on an exactly overlapping return leg — is fixed. Planning, the pre-ride overview, warnings, direction arrows, markers, route matching, gradient thresholds, climb/descent detection and persistence are all unchanged, and the existing global remaining-then-completed base order is deliberately preserved.
    - **Baseline.** `21e31be` (this slice's own item-104 acceptance documentation commit) on top of `8ed4b68` / `0.4.9`, clean and in sync with `origin/main`.
    - **Physical pre-fix evidence.** Deployed `0.4.9` on the installed iPhone PWA, 2026-09-07: on an exactly overlapping out-and-back return leg the grey completed outbound trace was visibly painted above the green remaining return trace immediately ahead of the rider. That route reports `0 m ascent`, so it evidences the ordinary completed-versus-remaining case only, not the climb-versus-descent palette case.
    - **Confirmed root cause.** `MapView.tsx` adds `acn-route-remaining-line` (`#0a5f38`) and then `acn-route-completed-line` (`#8a8f8c`, `lineOpacity: 0.7`), and this codebase uses no `beforeId` anywhere — `mapAdapter.ts`'s `addLayer` calls take no position argument — so z-order is purely add-order and completed grey wins at an exact overlap. Reversing those two globally would merely invert the fault onto the outbound leg, where the still-remaining future return occurrence would cover genuinely-completed road behind the rider. The correction is therefore local, and a dedicated `MapView` test now pins the global base order as unchanged.
    - **Gate 0: fail-first rendered evidence, measured on the unmodified tree.** Both scenarios drive the real import/analysis/Riding path with the project-owned local map style, and prove which colour a real browser actually painted by sampling the WebGL canvas — no repository test had previously proved which of two MapLibre canvas layers is on top. Sampling is anchored to the live position marker's own pixels rather than screen coordinates, and restricted to a narrow band around the route, which is what keeps DOM chrome composited into the element screenshot out of the counts. Because the completed layer is 0.7-alpha, the pre-fix composite is **not** `#8a8f8c`: the predicted values `#648173` (grey over remaining green) and `#929693` (grey over itself over the `#dedede` local style background) were confirmed by measurement as `#648173` and `#929694`, and the matching radius was tightened to 10 accordingly, since `#929694` and `#a3a7a5` are only ~30 apart.
      1. **Flat/unclassified exact out-and-back**, rider 900 m onto the return leg: immediately ahead, `#648173` = **166 px** and the current-direction green = **0 px**. The defect, reproduced deterministically. Behind the rider, `#929694` = **234 px**.
      2. **Classified coincident climb/descent** (production-verified as a `category-3` climb over route 440–2000 m averaging 10.56% and a `very-steep` descent over 2000–2597 m averaging −11.01%), rider 200 m below the summit: with **nothing selected** the road ahead already read `very-hard-climb` red (**164 px**, descent 0) — the existing micro-gradient path masks the defect in that state, exactly as suspected, and this is recorded as a no-regression control rather than a defect. With the **return descent explicitly selected through a real map tap**, the road ahead read descent navy (**164 px**, climb 0): the rider is climbing, and the colour immediately ahead belongs to the opposite direction.
    - **A structural negative result, established rather than assumed.** On an exact retrace with elevation a function of position, macro classification is necessarily _symmetric_: at any physical point the outbound is a climb exactly when the return is a descent, with equal length and equal |gradient|, so both pass or fail every eligibility gate together. "Flat outbound coincident with a recognised return descent" therefore cannot be constructed. And on the return leg the macro/micro layers already clip to `[matched, routeEnd]`, which excludes the outbound occurrence entirely. The classified defect is consequently reachable only on the **outbound** leg, and only when the active climb's own micro detail is not covering the road — i.e. when a different feature is explicitly selected.
    - **The interval, and why 300 m.** `ACTIVE_DIRECTION_AHEAD_METRES = 300`, measured in route distance and named in the pure module rather than in the screen, so its boundary is testable without going through the UI. It is comfortably below `MIN_FEATURE_LENGTH_METRES` (500 m), so the overlay can never span an entire recognised climb or descent and always reads as local emphasis rather than a re-colouring; and it is roughly 40 seconds of riding at road-bike speed. The rendered on-screen extent of 300 m depends on the follow camera's zoom, pitch and offset and is deliberately **not** part of the definition — no viewport claim is made. The window clamps at route start/end only; it deliberately does **not** truncate at a recognised-feature boundary, because truncating there would leave the road just past a summit or apex uncorrected, which is precisely the turnaround case. Spans _inside_ it clip exactly at feature and micro boundaries, so no colour bleeds across an edge and adjacent pieces share exact seam coordinates.
    - **No look-back, and that is load-bearing.** `ACTIVE_DIRECTION_BEHIND_METRES = 0`. Road behind the rider is legitimately completed, the completed/remaining split already meets the overlay exactly at progress, and a look-back would both hide real progress and destroy the outbound evidence that this is a local correction rather than a global layer reversal. A dedicated test asserts the first span starts exactly at progress, so a later "helpful" look-back fails immediately. The gap-under-the-marker case the user asked to be checked empirically did not arise: the marker is found intact in every rendered sample, which is itself proof it still paints above every route line.
    - **Composition rule.** `buildActiveDirectionSpans` resolves each sub-span at its **midpoint** — never at an endpoint, since `findFeatureAtDistance` and `findClassifiedSegmentAtDistance` are both inclusive at both ends with first-match-wins — to the micro key where the caller's already-narrowed `microDetailSegments` cover it, else the containing feature's macro key via the now-exported `visualKeyOf`, else `"ordinary-route"`. That reproduces exactly what the existing stack would paint for that occurrence absent overlap, so nothing is recoloured and the overlay is invisible except where it wins an overlap. On an unclassified route every span is the ordinary remaining-route green, which is why the confirmed defect is fixed by a pure z-order win with **no colour change at all**.
    - **Why one composed layer, not two clipped copies.** Re-emitting the existing macro and micro collections clipped to the window as two extra layers cannot work: at a turnaround the farther piece may be micro while the nearer piece is macro, and micro-above-macro across two layers would still let the wrong occurrence win. Nearest-wins must dominate the macro/micro nesting, and only a single layer can express that.
    - **Overlap priority.** Each span carries `paintPriority`, the negated route-distance separation between its start and current progress, so the span nearest the rider in route order has the highest value. The layer binds MapLibre's documented `line-sort-key` layout property to it ("features with a higher sort key will appear above"), and features are additionally emitted farthest-first so source order agrees. `line-sort-key` is a **layout** property, so `addLineLayer` gained an optional fourth `LineLayerOptions` argument rather than widening `LineLayerPaint`; it must be the data-driven `["get", …]` form, since a constant is `isConstant()` and would silently disable sorting altogether.
    - **Independence from explicit selection.** The overlay's geometry is chosen by route distance from canonical progress, never by what is selected, so an explicitly selected opposite-direction feature can no longer decide the colour of the road under the rider. With the descent selected the near-ahead correctly resolves to the climb's macro colour — self-consistent, because the base stack has already given the climb's micro detail to the selection — while the selection stays truthfully represented in the details panel and everywhere outside the window.
    - **Reliability, without a new state machine.** `RidingScreen` supplies the overlay only when `geolocationStatus !== "idle"` (this file's own established active boundary, so a paused session's restored progress never leaks into the pre-ride overview) and a reliable `presentationDistanceFromStartMetres` exists, so the window freezes with every other trusted figure on a stale or strongly off-route fix and inherits item 104's tied-regression hold unchanged. `MapView` then clips it by the same `matchedDistanceFromStartMetres` every other overlay already uses, which removes any need for a bespoke off-route gate: if the live match runs ahead of the frozen window the overlay is trimmed rather than repainting ridden road, and empties entirely once the match passes the whole window. Measured directly: across a 133 m off-route excursion the window's far end stays put while its near end is trimmed.
    - **Layering and failure isolation.** The new `acn-route-active-direction` source and `acn-route-active-direction-line` layer are added immediately after the micro gradient layer and entirely before the warning group, at the same `recedingWidthStops(5)` footprint as the micro layer — so no new visual weight, the selected-feature halo still shows as a ring, and warnings keep their established priority. Setup lives in its **own** `try/catch`, deliberately not joined to the existing climb/descent block, and the two existing isolation tests were strengthened to assert each block survives the other's failure. The source is registered in `APP_OWNED_SOURCE_IDS`: without that, a source rewritten on every GPS tick would have cleared the tiles-unavailable banner continuously throughout a ride — covered by its own regression test. It is not added to route-feature tap hit-testing, so tap priority stays warning-first and unchanged.
    - **Negative controls**, each applied alone with a file backup, the result recorded, then reverted and re-verified green:
      1. Overlay removed from `RidingScreen`: the flat rendered assertion fails.
      2. `"ordinary-route"` colour replaced: the flat rendered assertion fails (green drops to 0).
      3. Macro key resolved from the wrong feature: 9 pure builder tests fail.
      4. Active layer moved above the warning group: the layer-order test fails.
      5. Overlay populated during pre-ride: 5 `RidingScreen` tests fail.
      6. Frozen progress replaced by the raw live match: the freeze test fails (window advances 0.01629 vs 0.012287). **First attempt did not discriminate** — it changed the value but not the `useMemo` dependency, so memoisation masked it; recorded because it is a real trap for anyone repeating this.
      7. `paintPriority` semantics inverted: the overlap-band assertion fails (climb pixels 166 → 70).
      8. `line-sort-key` binding removed entirely: **did not discriminate** — with it gone, the farthest-first emission order alone still produces the correct result, because MapLibre only re-sorts a line layer's features when the sort key is non-constant, so source order governs otherwise. Both mechanisms are kept deliberately: the sort key makes the contract a declared style property rather than an inferred implementation detail, and the emission order is belt-and-braces. Recorded honestly rather than claimed as proof.
    - **A sampling correction worth recording.** The first overlap control also failed to discriminate, because the sampled band sat entirely _below_ the region where the window's own two halves coincide. Measuring the rendered bands rather than assuming their extent showed the self-overlap lands roughly 90–160 px above the rider on this fixture, and a second, farther sample was added; only then did control 7 fail. Separately, an exact `toBe(0)` on absent colours proved intermittently flaky at one stray antialiased pixel, so absent-colour assertions use a floor of 10 — two orders of magnitude below the ~166 pixels each defect actually produces, and far below every "present" assertion's own floor of 100, so they stay decisive rather than vacuous.
    - **Tests added.** `src/map/activeDirectionLayer.test.ts` (22 new); `src/map/MapView.test.tsx` (+9, including layer order, clipping, isolation, `APP_OWNED_SOURCE_IDS`, fallback/retry and hit-testing); `src/ui/riding/RidingScreen.activeDirectionOverlay.test.tsx` (6 new, including a direction test that distinguishes the outbound from the return occurrence purely by the direction the overlay's geometry runs, which is the only thing that differs on exactly coincident legs); `src/map/mapAdapter.test.ts` (+2 for the layout binding); `src/navigation/routeFeaturePalette.test.ts` (+1 pinning the reused colours). `e2e/ridingActiveDirectionLayering.spec.ts` (new, 2 tests) carries the rendered proof.
    - **Files changed.** `src/map/activeDirectionLayer.ts` (new), `src/map/MapView.tsx`, `src/map/mapAdapter.ts`, `src/map/routeFeatureLayer.ts` (`visualKeyOf` exported), `src/navigation/routeFeaturePalette.ts`, `src/ui/riding/RidingScreen.tsx` (production); the five test files above plus `e2e/ridingActiveDirectionLayering.spec.ts`; `package.json`/`package-lock.json` (`0.4.9` → `0.4.10`, hand-patched, no `npm install`, the lockfile diff being exactly the two root version-string lines); root `CLAUDE.md`, `docs/project/backlog.md`, `docs/project/current-status.md` and this file (documentation). No storage, GPX, service-worker, CSP, dependency, projection, matching or off-route change.
    - **Limitations.** Automated evidence only — Vitest and Playwright Chromium. No physical iPhone or Android verification of this item is claimed; see `docs/project/current-status.md` for its outstanding real-device checklist. The rendered proof runs in the `chromium` project only, since the spec is not an `android*.spec.ts`. A tap on coincident road still resolves to the macro layer's topmost feature (the later occurrence), so tap-to-select asymmetry at an overlap persists deliberately — this item scopes the change to local colour emphasis. Direction arrows reuse the remaining-route source, whose LineString doubles back on a coincident out-and-back, so arrows on coincident road remain ambiguous; that is pre-existing, is not addressed here, and the rendered proof deliberately asserts nothing about arrow pixels. On a warned coincident section a warning's dashes still partly mask the direction colour, which is intended and follows the surface-data priority rule.

    ***
    - **Follow-up (2026-09-07): reliable overlay geometry and selection-independent detail, `0.4.11`.** Two corrections to what `0.4.10` actually shipped, both approved by the user and implemented together in one slice. Baseline: `cee20f2` / `0.4.10`, clean and in sync with `origin/main`; GitHub Actions run `34119688139` for that exact SHA was independently confirmed with direct job conclusions (`Verify and build`, `End-to-end tests`, `Deploy`, all `success` on attempt 1) rather than inferred from the deployed site alone, which is how earlier verification in this file typically recorded it.
      1. **Reliability: the whole overlay now freezes, not just its far end.** `0.4.10` built spans from the frozen/reliable `presentationDistanceFromStartMetres` in `RidingScreen`, but `MapView`'s own effect then clipped the emitted GeoJSON using the raw, live `matchedDistanceFromStartMetres` as its lower bound. Whenever raw progress ran ahead of the frozen window — a stale or strongly off-route fix, exactly the case the freeze exists to protect — the near end was trimmed, and the overlay emptied entirely once raw progress passed the far end. `RidingScreen.activeDirectionOverlay.test.tsx`'s own freeze test recorded this in a comment ("its near end may legitimately be trimmed") and its assertion checked only the farthest longitude; that was proof of what `0.4.10` actually did, not of the intended contract. The follow-up removes MapView's raw-progress clip for this one overlay — it now clamps only to the route's own `[0, routeEnd]` domain, matching every other overlay's route-domain clamp without matching their live-clip behaviour — so the whole window freezes with the rest of the trusted presentation. Fail-first, on the unmodified `0.4.10` tree: the strengthened `MapView.test.tsx` assertion (four raw `matchedDistanceFromStartMetres` values — equal to, ahead of, beyond, and behind the frozen interval — held against one non-empty baseline) failed, and the strengthened `RidingScreen.activeDirectionOverlay.test.tsx` freeze test (comparing the complete overlay, not just its farthest longitude, and independently proving the raw match itself moved via the still-live-clipped completed/remaining split) also failed.
      2. **Selection: the active overlay's local detail is independent of any explicit selection again.** `0.4.10` deliberately let `buildActiveDirectionSpans`'s `microSegments` input follow `selectedFeature ?? activeFeature` — documented above as "Independence from explicit selection" — so with the opposite descent explicitly selected, the climbed road immediately ahead fell back to the climb's macro category colour rather than its own local micro band. That was a departure from the originally reviewed plan, which the user has now approved reverting. `RidingScreen` now derives a dedicated value from `activeFeature` alone — reusing `microDetailSegments` when nothing overrides it, and the already-memoised `activeClimbDetailSegments` for an active climb, so no feature is reclassified twice on the same GPS tick — and only computes a fresh classification for the one genuinely new case: an active, unselected **descent**, which `activeClimbDetailSegments`'s own climb-only scope never covered. The original Gate-0 measurements above (no-selection climb ahead reading micro red, 164 px, descent 0; the opposite descent selected reading macro yellow, 164 px, climb 0) remain accurate historical evidence of `0.4.10`'s own, now-superseded behaviour — not of the corrected one. Fail-first: the amended `e2e/ridingActiveDirectionLayering.spec.ts` assertion (expecting the climb's own micro red rather than its macro yellow once the opposite descent is selected) failed against the unmodified `0.4.10` build with `microClimb` measured at 0 px in a real browser; two new `RidingScreen.activeDirectionOverlay.test.tsx` component tests (a different feature explicitly selected while riding, and an active descent whose own short local band differs from its macro band) also failed against unmodified `0.4.10`, both reading the wrong colour.
      3. **The reported imagery-grace-period test failure: cause unconfirmed, corrected.** The original item-98 verification separately reported a `mapImageryRecovery.spec.ts` grace-period test failure. Observing item 32's own, differently-located flake reproduce on an untouched baseline does not establish a shared cause with a different test and assertion — that comparison is a non-discriminating control for this specific question, not evidence either way. Its cause remains unconfirmed; later clean reruns are recorded as subsequent observations only, not as resolution evidence.
      4. **An unidentified unit-test failure from that same verification pass: identity and cause not captured, corrected.** A separate unit-test failure was also seen during that original verification, and its specific identity and cause were never captured at the time. This narrow limitation is recorded honestly rather than inventing a test name or attribution; later successful runs establish neither its identity nor its cause.
      5. **Retrace symmetry, narrowed.** The claim above that an exact retrace makes macro classification "necessarily symmetric... equal length... only one classified failure state" describes a theoretical geometric property, not a proven general law about this project's own classification pipeline. Geometric coincidence between two occurrences of the same physical road does not by itself establish that elevation-derived classification — smoothing, run merging, minimum-length gating — treats both directions identically for every possible fixture; it establishes only that the SAME imported points, read in each direction, happen to classify a particular way here. The fixture's own two recognised features are not, by themselves, evidence of a general outbound/return symmetry law: its climb (`440–2000 m`) and descent (`2000–2597 m`) spans differ in length precisely because they are two different physical features on the SAME outbound leg (a climb immediately followed by a descent to the turnaround), not a matched outbound/return pair. The fixture's own measured outcomes stand; no detection algorithm changed here, and no universal impossibility is asserted.
      6. **CI**, restated for clarity: `34119688139`'s three job conclusions were confirmed directly (see above), distinct from an inference drawn from the deployed site's own build id/version, which is how earlier items in this file typically recorded CI evidence. This follow-up's own final pushed commit and its job conclusions are recorded in its own verification note below.
      - **Fixes.** `src/map/MapView.tsx`'s `activeDirectionOverlay` effect no longer takes `matchedDistanceFromStartMetres` as a clip bound or effect dependency — it clamps only to the route's own `[0, routeEnd]`. `src/ui/riding/RidingScreen.tsx` adds `activeFeatureMicroDetailSegments`; it and `activeDirectionOverlay`'s own `useMemo` were relocated to after `activeClimbDetailSegments`'s declaration, since the new value reuses it and a hook must not reference a later `const` during render.
      - **Negative controls**, each applied alone via an explicit file backup, reverted and re-verified green: (1) MapView's raw-progress clip alone restored — the new whole-overlay freeze test failed; (2) RidingScreen's `activeDirectionOverlay` `microSegments` input, and its dependency array reverted consistently with it (so stale memoisation on the still-present `activeFeatureMicroDetailSegments` dependency could not mask the substitution), reverted alone to `microDetailSegments` — the selection-invariance and active-descent tests both failed.
      - **Tests changed.** `src/map/MapView.test.tsx` (the raw-clipping test replaced by a whole-overlay freeze test); `src/ui/riding/RidingScreen.activeDirectionOverlay.test.tsx` (the existing freeze test strengthened to compare the complete overlay and independently prove the raw match moved via the completed/remaining split; two new tests for selection-invariance and active-descent local detail, using a new non-retracing classified climb/descent fixture — deliberately not an out-and-back, so a fix can target any route distance directly with no self-intersection continuity to resolve first, which an initial out-and-back-based draft of these two tests needed and a simple flat-region draft of one of them then turned out not to discriminate the bug at all, since a descent's macro band and its own dominant local band shared the same name there); `e2e/ridingActiveDirectionLayering.spec.ts` (the selected-opposite-descent and turnaround-overlap assertions corrected to expect the climb's own micro colour, plus its own header comment updated to describe the corrected contract).
      - **Verification.** `corepack npm@11.16.0 ci` (fresh, in an isolated user-owned worktree — the primary checkout's `node_modules` carries root-owned leftovers from an earlier privileged process, already documented by prior items); `format`, `lint`, `typecheck` clean; `build` clean. `npm test` — **3176/3176** passed across 155 files (+2 from the `3174`-test baseline: two new `RidingScreen.activeDirectionOverlay.test.tsx` tests, the `MapView.test.tsx` raw-clipping test replaced net-zero). `npm run e2e` — **276/276** passed across the `chromium` and `android-chrome` projects combined; the same 7 `webkit-smoke` tests failed with the identical, already-documented missing-library error (`libgtk-4.so.1`, `libevent-2.1.so.7`, `libgstcodecparsers-1.0.so.0`, `libavif.so.13`) — a pre-existing local-sandbox gap, not a new regression, confirmed directly rather than assumed. The decisive amended `ridingActiveDirectionLayering.spec.ts` scenario was re-run 3 times against the corrected build with no flake. `format:check` run last, clean. `git diff --check` clean. `npm audit`/`npm audit --omit=dev` could not reach the registry from this sandbox (`self-signed certificate in certificate chain`, the same pre-existing network-egress limitation prior items recorded) — no dependency changed in this slice (`package-lock.json`'s diff is exactly the two version-string lines), so this does not block; CI's own audit step is authoritative. Local Node is `v24.13.0` against the pinned `24.18.0` (a pre-existing, already-documented sandbox mismatch, not fixable locally); WebKit fails to launch locally (confirmed directly, not merely assumed) — relied on the pinned CI's own WebKit project instead. [The pushed commit's own SHA and CI/deployment confirmation are recorded once available — see the note added after push.]
      - **Limitations.** Automated (Vitest, Playwright Chromium) evidence only — this follow-up carries no new physical acceptance of its own. Item 98's physical checklist in `docs/project/current-status.md` remains open and is updated by this same slice to describe the new automated coverage accurately.

---

<a id="item-99"></a>

## Item 99 — Route Library sorting by distance and total ascent — done

_Category: Route Library organisation_

99. **Route Library sorting by distance and total ascent — done**
    - **Outcome.** The Route Library `Sort by` control now offers six choices — the existing `Most recent`/`Name A–Z` plus four new ones: `Distance: shortest first`, `Distance: longest first`, `Total ascent: least first`, `Total ascent: most first` — ordering by each saved route's own canonical `PlannedRoute.distanceMetres`/`ascentMetres`. Presentation-only: no route recalculation, no GPX/route-data change, no storage-schema change. Pin priority, search, focus restoration, rename/delete and persistence are all unaffected.
    - **Baseline.** `6674333` / `0.4.11` (this commit's own stated hand-off baseline — the item 98 follow-up above), fetched and fast-forwarded onto a locally stale `main` before starting; `git status` clean, no unrelated local work to preserve.
    - **Confirmed storage fact, established by direct source reading before writing anything.** `StoredRouteLibraryPreferences.sortOrder` (`src/storage/db.ts`) is a plain, non-indexed `string` — the Dexie `stores()` schema for `routeLibraryPreferences` is `"id"` only, and all validation lives in `mapping.ts`'s `isRouteLibrarySortOrder` guard; the repository does no validation of its own. Widening the accepted values is therefore a pure TypeScript-level change with **no Dexie version bump and no migration**, confirmed rather than merely assumed per this item's own backlog contract.
    - **Comparator design.** `routeLibraryView.ts`'s two-branch if/else was replaced with an exhaustive `switch` over all six `RouteLibrarySortOrder` values, with a `default: { const exhaustive: never = sortOrder; ... }` branch — a future unhandled value is now a TypeScript compile error, not a silent fall-through to `most-recent`. A new `compareKnownOrNullLast(a, b, direction)` helper gives total-ascent sorting its null policy in one place: a route with unknown ascent (`ascentMetres: null`) always sorts after every route with a known ascent, in **both** directions — descending does not resurrect unknown routes to the front — and known zero ascent is compared as a real value, never conflated with null. Every order is tie-broken by the existing, unchanged `compareIds`. `selectRouteLibraryGroups`/`sortPinnedRoutes` needed **no changes at all**: the existing pipeline already applies `sortOrder` only to the unpinned partition and always orders pinned routes by `pinnedAt` via `sortPinnedRoutes`, so pin priority is preserved by the pre-existing pipeline shape, not by anything new written for this item.
    - **Fail-first evidence, genuinely re-derived at each layer (not merely written once and assumed to transfer).** Storage (`mapping.test.ts`, `routeLibraryPreferencesRepository.test.ts`) and view (`routeLibraryView.test.ts`) tests were run against the unmodified two-value baseline by temporarily setting aside the three production files (`git stash push -u` scoped to those paths, never a bare `stash`/`stash pop`, per this project's own shared-stash-stack caution) and rerunning: **13/13** new storage/view tests failed for the expected reason (new sort values rejected, falling back to `most-recent`). The same procedure at the component layer (`RouteLibrary.test.tsx`) produced **7/7** new-test failures (`"Value ... not found in options"`), including the strengthened ≥2-pinned-route test described below. Production files were restored and re-verified green after each pass; no stash entry was left behind.
    - **The e2e fail-first pass surfaced, and corrected, a genuine tooling trap.** The first attempt ran `npm run build` on the reverted baseline; its `tsc -b` step failed (the _test_ files still reference the new sort values), so `vite build` never re-ran and Playwright silently served the **stale, already-fixed** `dist/` from the prior successful build — a false "5 passed" that proved nothing. Recognised and corrected by building with `vite build` directly (bypassing the type-check gate, which does not affect what is actually bundled, since test files are never reachable from the production entry point): the genuinely reverted build then produced **3/3** real e2e failures (`locator.selectOption` timing out — the option genuinely does not exist), which is the fail-first evidence actually reported below. Recorded here as a specific, concrete instance of this project's own established "e2e preview serves stale dist/" caution — this is the same class of trap, discovered again from a different angle (a failing `tsc -b`, not merely a skipped `build`).
    - **Real-browser layout finding, investigated rather than special-cased away.** The first attempt at a 200%-enlarged-text Route Library e2e check failed on whole-document `scrollWidth`. Direct in-browser measurement (a small diagnostic script sampling every overflowing element's bounding box) identified the overflowing element as the app shell's own bottom-navigation `Settings` label — confirmed, by rerunning the identical measurement with the sort select left at its original short `"Most recent"` label, to overflow by the exact same 9 px regardless of which sort option is selected. This is pre-existing app-shell chrome, unrelated to this item's own longer option labels, and fixing primary-navigation chrome is out of this item's scope (see item 103). The two new enlarged-text/short-landscape checks were rescoped to measure the `Sort by` control's and the route-card list's own bounding boxes directly — what this item's backlog contract actually governs ("the toolbar and route cards remain contained") — rather than the whole document, which would otherwise fail on an unrelated defect. Not fixed here; noted for whoever next touches primary-navigation chrome (item 102/103).
    - **A second, smaller real-browser finding.** Playwright's `locator.selectOption()` does not reliably leave a native `<select>` focused in Chromium the way a genuine user interaction does; the pre-`selectOption` focus assertion initially failed even against the corrected build. Fixed by clicking the select first (a real user gesture) before calling `selectOption`, mirroring how a rider actually operates the control — not by loosening the assertion.
    - **Negative controls**, each applied alone via a small Python-scripted patch (not a destructive checkout) to `routeLibraryView.ts`, run against the targeted tests, reverted, and re-verified green:
      1. Swapped `distance-asc`'s subtraction direction (behaving like `distance-desc`) — the targeted `sortRoutesForLibrary` "distance-asc" unit test failed.
      2. Routed the pinned partition through `sortRoutesForLibrary(pinned, sortOrder)` instead of `sortPinnedRoutes` — failed the pinned-order unit test, the item-99 component test, **and** the new dedicated e2e pinning test (verified directly in a real browser, not assumed to transfer from the unit result).
      3. Coerced `compareKnownOrNullLast`'s null handling to treat `null` as `0` — failed all three null/zero-distinction unit tests (least-first, most-first-still-last, multiple-unknown-tie-break).
    - **Pin-priority test design, strengthened beyond the original draft per review.** The existing `name-asc` pin-vs-sort precedent only ever pins **one** route before changing sort, which can prove pinned-vs-unpinned partitioning but not that the pinned block's own _internal_ order survives. Both the new component test and the new dedicated `routeLibraryPinning.spec.ts` e2e test instead pin **two** routes whose pin-recency order actively conflicts with their distance order (the older pin is the longer route, the newer pin the shorter), with unpinned routes interleaved at distances between them — so a bug that (re)sorts the whole list, not just the unpinned partition, scatters the pinned pair rather than merely transposing two adjacent items. Negative control 2 above confirms this is genuinely discriminating, in both the component test and a real browser.
    - **E2E canonical-value verification before relying on order (a review amendment, not the original draft).** Three new synthetic GPX fixtures (`e2e/fixtures/sort-short-flat-route.gpx` ~0.6 km flat/0 m ascent, `sort-no-elevation-route.gpx` ~1.6 km with **no `<ele>` tag on any point at all**, `sort-long-hilly-route.gpx` ~4.3 km climbing ~210 m) are imported through the real GPX upload path — never a personal file. Before any ordering assertion, the new `routeLibrarySearchSort.spec.ts` test parses each imported card's own rendered `.route-card-meta` text (the production `formatDistanceKm`/`formatAscent` output) and asserts the three distance values are genuinely increasing and that the elevation-free import genuinely rendered `"ascent not available"` — i.e. a real `ascentMetres: null`, not an accidentally-zero fixture — before trusting any subsequent sort-order assertion. This guards against exactly the kind of non-discriminating fixture this item's own instructions warned against.
    - **Tests changed.** `src/storage/mapping.test.ts` (+4, `it.each` round-trip for the four new values); `src/storage/routeLibraryPreferencesRepository.test.ts` (+4, `it.each` save/retrieve); `src/ui/library/routeLibraryView.test.ts` (+10: 4 distance-order tests, 5 ascent-order/null-policy tests, 1 pin-priority test; `buildRoute`'s trailing optional parameters replaced by a single `overrides` object — `Partial<Pick<PlannedRoute, "pinnedAt" | "distanceMetres" | "ascentMetres">>` — per review, to remove the risk of a positional-argument mix-up as fields grow); `src/ui/library/RouteLibrary.test.tsx` (+7: six-option select contents, distance/ascent reorder, remount persistence, error-and-revert, focus-after-reorder, and the strengthened ≥2-pinned-route distance test); `e2e/routeLibrarySearchSort.spec.ts` (+3: the combined distance/ascent/search/focus/reload test, and two enlarged-text/short-landscape containment tests); `e2e/routeLibraryPinning.spec.ts` (+1, the ≥2-pinned-route distance test). Three new e2e fixture files (`e2e/fixtures/sort-*.gpx`), synthetic only.
    - **Files changed (production).** `src/storage/mapping.ts` (`RouteLibrarySortOrder` widened to six values; `isRouteLibrarySortOrder` extended); `src/ui/library/routeLibraryView.ts` (exhaustive-switch comparator, `compareKnownOrNullLast`); `src/ui/library/RouteLibrary.tsx` (four new `<option>`s only). `src/index.css` was **not** touched — investigated and measured to be unnecessary (see the layout finding above); `src/storage/db.ts` and `routeLibraryPreferencesRepository.ts` were **not** touched, per the confirmed no-migration finding above.
    - **Verification.** `corepack npm@11.16.0 ci` (fresh, isolated user-owned worktree `.claude/worktrees/item99-sorting` — the primary checkout's `node_modules` still carries the already-documented root-owned leftovers). `format`, `lint` (three real errors surfaced and fixed — an unnecessary-conditional `??`, a redundant `String()` wrapper, and a forbidden non-null assertion replaced by a proper narrowing `if`/`throw`, all in the new e2e spec), `typecheck` clean. `npm run build` clean. `npm test` — **3201/3201** passed across 155 files (+25 over the `3176`-test baseline, exactly matching the count of tests listed above). `npm run e2e`: a dedicated `e2e/routeLibrary*.spec.ts` run passed **10/10** cleanly across `chromium` every time it was run in isolation; the full `npm run e2e` gate totalled **287** tests every time it was run (six full runs across this slice), consistently split as 280 across `chromium`+`android-chrome` (+4 over the `276`-test baseline, matching the four new e2e tests) plus the 7 already-documented `webkit-smoke` failures (missing `libgtk-4.so.1`/`libevent-2.1.so.7`/`libgstcodecparsers-1.0.so.0`/`libavif.so.13`, a pre-existing local-sandbox gap). Across those six full-parallel runs, between 0 and 3 **unrelated** `chromium` tests (`rideSessionSwitchGuard.spec.ts` twice by name, `ridingImmersiveShell.spec.ts`'s "Finish ride restores the normal app shell", `ridingFinishAndEnd.spec.ts`'s completion test — all already named in this file's own item-32 monitored-flake entry) intermittently failed under heavy contention (`/proc/loadavg` showed roughly 118 on a 72-core host during this session — genuine external load, not simulated). Directly reproduced, not merely asserted: the identical class of flake (a different, equally unrelated test) also occurred on the untouched `git stash`-restored baseline under the same full-parallel load, confirming this is pre-existing sandbox contention rather than a regression from this item — no Route Library test ever failed in any of the six runs. `git diff --check` clean. `npm audit`/`npm audit --omit=dev` could not reach the registry (`self-signed certificate in certificate chain`, the same already-documented sandbox network-egress limitation); no dependency changed in this slice (`package-lock.json`'s diff is exactly the two version-string lines), so this does not block; CI's own audit step is authoritative. `format:check` run last, immediately before committing, clean. Local Node `v24.13.0` against the pinned `24.18.0` (pre-existing, already-documented, not fixable locally); WebKit fails to launch locally — relied on CI's own pinned WebKit project instead.
    - **Documentation.** This entry moves item 99 from `docs/project/backlog.md` to this file; root `CLAUDE.md`'s queue index now selects item 100 next and folds 99 into the completed-items range; `docs/project/current-status.md` gained one new, narrowly-scoped outstanding real-device entry (layout/legibility of the six-option control, correct sort/pin-priority/persistence behaviour) — no existing item 98 wording was touched. `docs/project/README.md` was not touched: `items-95-99.md`'s own byte size after this entry and Prettier formatting stays comfortably under the ~150 kB split threshold.
    - **Limitations.** Automated (Vitest, Playwright Chromium) evidence only — no physical iPhone or Android verification of this presentation-only change is claimed. The primary-navigation 200%-text overflow finding above is recorded as an out-of-scope observation, not fixed here.

    ***
    - **Follow-up (2026-09-07): the user reviewed the shipped six-option menu and found it unnecessarily bloated.** They explicitly chose exactly one direction per criterion, matching the pre-item-99 design's own existing asymmetry (dates newest-first, names A–Z): distance should show longest-first only, total ascent should show most-first only. This follow-up retires `distance-asc`/`ascent-asc`, leaving four active choices with two of the four labels also changed:

      | Stored value    | Label               |
      | --------------- | ------------------- |
      | `most-recent`   | `Most recent`       |
      | `name-asc`      | `Name A–Z`          |
      | `distance-desc` | `Longest route`     |
      | `ascent-desc`   | `Most total ascent` |

    - **Baseline reconciliation, before any edit.** The local `main` checkout was found one commit behind `origin/main` (still at `6674333`/`0.4.11`, the item 98 commit) — item 99's own `f8b455e`/`0.4.12` commit was already pushed upstream but not yet pulled locally. `git fetch` plus `git merge --ff-only origin/main` confirmed a clean, non-diverging fast-forward (`git log 6674333..f8b455e` showed exactly the one expected commit) before any file was touched.
    - **Root-owned `node_modules` recurred, and a stray leftover-worktree preview server was found and cleared.** The primary checkout's `node_modules/.bin` (and 378 other top-level entries) were still root-owned, blocking `corepack npm@11.16.0 ci` (`EACCES` on `unlink`) exactly as item 99's own entry above already documents — per the user's own explicit safeguard for this follow-up, no ownership repair was attempted; a fresh, isolated, user-owned worktree (`.claude/worktrees/item99-followup`, new branch `item99-followup` off the just-fast-forwarded `main`) was created instead, following the same per-item-worktree convention item 99 itself used. Separately, the first e2e run against the real four-option select still showed six stale options — port `4173` was already held by an **orphaned** `vite preview` process (`PPID 1`, started in a prior session, `cwd` the leftover `.claude/worktrees/item99-sorting`) left over from item 99's own work, silently serving that worktree's old six-option `dist/` to Playwright's `reuseExistingServer` (`playwright.config.ts`'s `reuseExistingServer: !process.env.CI`) instead of starting a fresh preview of this worktree's build. Confirmed orphaned and harmless to stop (a static file server holding no state), killed, port re-verified free, and the same e2e run then passed cleanly against a freshly started preview of the real build — a fresh, concrete instance of this project's own documented "Playwright worktree port sharing" caution.
    - **Compatibility design.** `src/storage/mapping.ts`'s `RouteLibrarySortOrder` narrows from six to four values (`"most-recent" | "name-asc" | "distance-desc" | "ascent-desc"`); `isRouteLibrarySortOrder` narrows to match. A new `resolveLegacyRouteLibrarySortOrder(value: string)` helper, placed and commented to mirror this file's own pre-existing `normalizeStoredElevationWindowMetres` retired-value precedent, recognises exactly `"distance-asc"` and `"ascent-asc"` and maps each to its surviving direction; `fromStoredRouteLibraryPreferences` now tries an active value first, then this legacy resolver, and only then falls back to `DEFAULT_ROUTE_LIBRARY_SORT_ORDER` for anything else (a genuinely corrupt or future-unknown string). `StoredRouteLibraryPreferences.sortOrder` (`src/storage/db.ts`) is untouched — still a plain, non-indexed, unvalidated `string` — so this is **read-time normalisation only**: no Dexie version bump, no migration, no write-on-read. A device that saved a `distance-asc`/`ascent-asc` row on `0.4.12` or earlier keeps its chosen **criterion**, resolved to the one supported **direction**, on every subsequent read; an ordinary later save then naturally persists an active value.
    - **Comparator simplified.** `src/ui/library/routeLibraryView.ts`'s exhaustive `switch` drops the `case "distance-asc"`/`case "ascent-asc"` branches (the `never`-typed `default` branch keeps this compiler-checked, not merely conventionally maintained). `compareKnownOrNullLast(a, b, direction)` — now only ever called with one direction — was simplified to `compareAscentDescendingWithUnknownLast(a, b)`, removing the now-dead `direction` parameter and the unreachable ascending branch rather than leaving them for cosmetic parity with `0.4.12`.
    - **Fail-first evidence, run against the freshly fetched, unmodified `0.4.12`/`f8b455e` tree before any production edit.** (1) A raw stored `distance-asc` row fed to `fromStoredRouteLibraryPreferences` round-tripped unchanged to `{ sortOrder: "distance-asc" }` instead of normalising to `distance-desc` — failed as predicted. (2) The same for a raw `ascent-asc` row against `ascent-desc` — failed as predicted. (3) `RouteLibrary.test.tsx`'s rewritten "exactly four sort choices" assertion found six real `<option>`s (`distance-asc`/`ascent-asc` still present) instead of four — failed as predicted. (4) A new Playwright assertion reading the real, browser-rendered `#route-library-sort option` values/labels likewise found six entries against a genuine `npm run build` of the unmodified tree — failed as predicted, and (once the stray port-4173 process above was found and cleared) against a confirmed-fresh preview server, not a stale one.
    - **Negative controls**, each applied alone to the finished implementation, confirmed to fail for the right reason, then reverted and re-verified green:
      1. `distance-asc`'s branch removed from `resolveLegacyRouteLibrarySortOrder` — its dedicated `mapping.test.ts` test, `routeLibraryPreferencesRepository.test.ts` test (raw-row insert via `db.routeLibraryPreferences.put`), and `RouteLibrary.test.tsx` legacy-render test all failed (each normalising instead to `most-recent`, the generic corrupt-value fallback).
      2. Same for `ascent-asc`'s branch — the same three test layers (mapping/repository/component) all failed.
      3. A retired `<option value="distance-asc">` temporarily re-added to `RouteLibrary.tsx`'s select — both the exact-four-option component test and the new Playwright option-count assertion failed (5 options found).
    - **Tests changed.** `src/storage/mapping.test.ts` (the item 99 `it.each` narrowed to the two active values; +2 new legacy-normalisation tests); `src/storage/routeLibraryPreferencesRepository.test.ts` (same narrowing; +2 new raw-row legacy tests bypassing the repository's own save path); `src/ui/library/routeLibraryView.test.ts` (the `distance-asc`/`ascent-asc` describe blocks trimmed to their surviving descending-only tests, each losing one direction-specific test and its ascending half of shared assertions; the pinned-vs-metric-sort test trimmed to its already-self-sufficient `distance-desc` half); `src/ui/library/RouteLibrary.test.tsx` (the six-option assertion rewritten to four with the new labels; the two combined asc/desc reorder tests trimmed to their surviving desc-only half; the error-and-revert and focus-after-reorder tests repointed off retired values; the ≥2-pinned-route test's `distance-asc` half dropped; +2 new legacy-preference-renders-correctly tests); `e2e/routeLibrarySearchSort.spec.ts` (the core ordering test gains an explicit real-select four-option assertion and drops its `distance-asc`/`ascent-asc` blocks; its enlarged-text/short-landscape describe renamed "four-option"); `e2e/routeLibraryPinning.spec.ts` (the ≥2-pinned-route distance test's `distance-asc` half dropped). Net unit/component test count is unchanged (3201 both before and after this follow-up) — the removed asc-specific tests are offset one-for-one by the new legacy-normalisation tests.
    - **Files changed (production).** `src/storage/mapping.ts` (`RouteLibrarySortOrder` narrowed to four values; `isRouteLibrarySortOrder` narrowed; new `resolveLegacyRouteLibrarySortOrder`; `fromStoredRouteLibraryPreferences` extended with the legacy-normalisation step); `src/ui/library/routeLibraryView.ts` (two comparator cases removed; `compareKnownOrNullLast` simplified to `compareAscentDescendingWithUnknownLast`); `src/ui/library/RouteLibrary.tsx` (two `<option>`s removed, two of the remaining four relabelled). `src/storage/db.ts` and `routeLibraryPreferencesRepository.ts` were **not** touched — no schema/migration involved.
    - **Verification.** Fresh, isolated, user-owned worktree `.claude/worktrees/item99-followup` (see above); `corepack npm@11.16.0 ci` clean (554 packages). `npm run format` reformatted only line-wrapping in the two files this follow-up's own edits touched (`mapping.ts`, `routeLibrarySearchSort.spec.ts`) — confirmed cosmetic only via `git diff`. `npm run lint` initially surfaced two real errors (`@typescript-eslint/no-empty-function` on `vi.spyOn(console, "error").mockImplementation(() => {})` in the two new legacy-render component tests), fixed to `() => undefined`, matching this codebase's own pre-existing `storageHealth.test.ts` convention for the identical pattern; clean thereafter. `npm run typecheck` clean (the narrowed `RouteLibrarySortOrder` surfaced eight real compile errors across five test files on the first pass, exactly at every remaining `distance-asc`/`ascent-asc` reference — each fixed per the tests-changed list above). `npm run build` clean. `npm test` — **3201/3201** passed across 155 files, unchanged from the pre-follow-up baseline (see the net-test-count note above). `npm run e2e` — **287** total, **280** passed across `chromium`+`android-chrome` (unchanged from the item 99 baseline: no route-library e2e test count changed, only their content), **7** failed under `webkit-smoke`, confirmed to be the exact same already-documented missing-system-library gap (`libgtk-4.so.1`/`libevent-2.1.so.7`/`libgstcodecparsers-1.0.so.0`/`libavif.so.13`) as every recent item's own entry — and, checked freshly for this follow-up, `webkit-smoke`'s own `testMatch` (`playwright.config.ts`) only ever runs `smoke.spec.ts`/`csp.smoke.spec.ts`, neither of which exercises Route Library sorting at all, so this gap is confirmed orthogonal to everything this follow-up changed, not merely assumed to be. `npm audit` and `npm audit --omit=dev` both reached the registry this session once the shell's proxy environment variables were unset first (this sandbox's own documented workaround) and both reported **0 vulnerabilities**; no dependency changed (`package-lock.json`'s diff is exactly the two version-string lines, confirmed by direct `git diff`). `git diff --check` clean. `npm run format:check` run last, immediately before committing, clean. Local Node `v24.13.0` against the pinned `24.18.0` (pre-existing, already documented, not fixable locally) — CI's own pinned toolchain and container remain authoritative.
    - **Documentation.** This dated follow-up appended under item 99's own existing anchor (the original `0.4.12` six-option account above is left byte-for-byte intact as historical record). `docs/project/current-status.md`'s item 99 outstanding entry rewritten to describe the final four-option control, its labels, and the same not-yet-physically-verified status — no acceptance is claimed by this change. `docs/project/README.md`, item 100's specification and selected-next status, and every other item's entry are untouched.
    - **Version.** `0.4.12` → `0.4.13`, hand-patched in `package.json` and both root version fields of `package-lock.json` only (no `npm install`/`--package-lock-only`).
    - **Limitations.** Automated (Vitest, Playwright Chromium/Chromium-emulated Android) evidence only — no physical iPhone or Android re-verification of this presentation-only simplification is claimed; the outstanding item 99 real-device checklist in `docs/project/current-status.md` now targets the four-option control described above.
    - **Acceptance (2026-09-07): the user checked the deployed `0.4.13` four-option control on the installed iPhone Home Screen PWA and reported: "It works as intended."** This confirms ordinary on-device use of the compact `Sort by` control (`Most recent`, `Name A–Z`, `Longest route`, `Most total ascent`) and its intended sorting behaviour. It does **not** by itself confirm the short-landscape or enlarged (accessibility) text-size containment checks, pin-priority preservation, reload persistence, the synthetic unknown-ascent ("ascent not available" always sorts last) edge case, or physical Android behaviour — those remain open in `docs/project/current-status.md`.
