# Completed backlog items 81–88

Bounded per [`README.md`](README.md)'s own instruction: `items-69-80.md` was already at 143,212 characters, close to the ~150,000-character soft cap, so item 81 started a new range file rather than risk pushing it over. This file's own range closed at item 88: item 89's own required content (coverage figures, two full import-cycle graphs, a complete jsdom-warning disposition table, and WebKit-feasibility evidence) would have pushed this file past that same bound, so item 89 started [`items-89-NN.md`](items-89-NN.md) instead. Items 81 through 88 are complete and final here.

**These are historical accounts of what shipped and why, at the time each was recorded.** Where later work has changed or superseded a detail described here, current source and tests are authoritative — but the rationale, rejected alternatives, and real regressions documented below remain valuable and are preserved rather than edited to match the present state. See the root [`CLAUDE.md`](../../../CLAUDE.md) for the required reading order.

---

<a id="item-81"></a>

## Item 81 — Preserve Riding zoom through stale-GPS and imagery-retry recovery — done

_Category: Riding camera reliability investigation_

81. **Preserve Riding zoom through stale-GPS and imagery-retry recovery — done**
    - Field evidence: during an active route ride (the PWA remained active throughout — never paused, backgrounded or suspended), the camera intermittently ended at approximately whole-world zoom after a combination believed to involve stale GPS and pressing `Retry map imagery`. Follow was already active; pressing Follow again did not restore a normal zoom, which is correct, since Follow preserves the rider's selected zoom rather than resetting it. Pressing North-up also did not change zoom, which is likewise correct, since North-up only changes bearing/pitch. Manual pinch/`+` zoom restored a normal useful view. Kept separate from item 66 (an unreproduced, accepted-for-now-monitored route-Riding fresh-Start overview-zoom symptom) and item 74 (a confirmed and shipped fix for a _different_ free-roam fresh-Start zoom-corruption bug, via the `hasAppliedCameraCommand` settle-provenance latch) — this item's evidence is mid-ride, tied specifically to a stale-GPS-plus-imagery-retry combination, not a fresh Start.
    - **Confirmed root cause, source-verified before any change, then proven with a failing test against unmodified code.** `src/map/MapView.tsx`'s `appliedCameraCommandGenerationRef` (item 74's own settle-provenance latch) answers only "has _any_ real `cameraTarget` command ever been applied on this map instance's current generation" — set once, synchronously, immediately after the `cameraTarget` effect calls `setCamera`, and never reset except by a full map recreation. `onCameraSettled`'s `hasAppliedCameraCommand` payload field was computed purely as `appliedCameraCommandGenerationRef.current === generation` — a coarse per-_generation_ latch, not a per-_settle-event_ correlation. Once true for a generation, **every subsequent settle on that instance — regardless of what actually produced it — was reported `hasAppliedCameraCommand: true`**, indistinguishable from a genuine settle of the tracked command. `src/ui/riding/rideCamera.ts`'s `"follow-zoom-settled"` reducer case trusts this boolean completely: if a settle carrying a value that does not correspond to the rider's real command reaches it while `mode === "following"`, `awaitingFreshFix === false` and the latch is already true, `followZoomLevel` is unconditionally overwritten. That field is read directly by `useRideCamera.ts`'s `persistableCameraState` and by every subsequent `followCommand(...)` call, so the corruption is persisted on the very next accepted GPS fix (unrelated to the corrupting settle) and does **not** self-correct on a later fresh fix, since `"fresh-fix"` explicitly preserves `state.followZoomLevel` rather than re-deriving it — matching the field report's every detail: Follow's own re-press faithfully (and correctly, per its own contract) reproduces the corrupted value rather than resetting it; manual zoom "fixes" it only because `"follow-zoom-changed"` steps _away from_ the corrupted value additively rather than re-deriving it from a trustworthy source; and the corruption survives a subsequent fresh fix rather than self-correcting, since only genuine reducer-state corruption (not a transient display glitch) explains that persistence.
    - **A previously-ruled-out hypothesis, not re-proposed.** Item 66's own investigation (`docs/project/current-status.md#item-66`) had already directly read the installed MapLibre 6.0.0 source and confirmed `Map.resize()` does not independently fire its own corrupting settle at a stale transform — it only updates cached container dimensions and leaves an in-flight ease running. This investigation did not reopen that finding.
    - **Two other hypotheses traced and closed, not merely assumed safe.** A "Follow re-pressed while GPS is stale, after an earlier manual gesture" path was traced in full: a genuine gesture drops `rideCameraReducer`'s `mode` to `"free"`, which also nulls `cameraTarget`; re-requesting Follow with a stale (`freshCoordinate: null`) fix produces `NO_COMMAND` and keeps `cameraTarget` at `null`, so the `cameraTarget` effect never runs and `appliedCameraCommandGenerationRef` correctly stays stale for that generation — this path is not reachable as a corruption source. An "overview-fit effect re-firing mid-ride" hypothesis was closed by inspecting `hasActionableCameraTarget` (`useRideCamera.ts`): a sticky, monotonic latch, explicitly documented as existing to prevent exactly this ("un-latching then would make MapView's overview-fit effect re-run and incorrectly re-fit the whole route mid-ride"), which stays true for the rest of a route-open session once Follow has engaged once, regardless of later staleness or mode churn.
    - **Fail-first evidence, exercising the real causal path, not a fabricated reducer event.** A new `MapView.test.tsx` test (`describe("recoverable errors, staged readiness, and retry", ...)`) drives the real, unmodified `cameraTarget` effect and `onCameraSettled` handler via the file's existing mock map factory: a genuinely non-default zoom (18, never `NAVIGATION_ZOOM` or the corrupting settle's own 0) settles and is correctly reported `hasAppliedCameraCommand: true`; a tile/style failure and manual `retry-map-imagery-button` click trigger a real map-instance recreation (a new attach generation); the new generation's own `cameraTarget` effect genuinely re-applies the same real command once style becomes ready (`setCameraSpy` called twice, correctly); then `mock.triggerCameraSettled({coordinate:[0,0], zoom:0, ...})` — MapLibre's own confirmed pre-style-ready settle at its raw default transform, delivered _after_ the real command rather than before it, which nothing in production code bounds to arrive first — is fired on the same generation. **Pre-fix, this settle was reported `hasAppliedCameraCommand: true`** (confirmed by running the test against the unmodified tree, `git stash`ed away from the fix, before writing any production change), passing all four of the reducer's guard conditions and, per the second test below, genuinely corrupting `followZoomLevel`. A companion `RidingScreen.test.tsx` integration test reproduces the complete field sequence end to end — Start, a fresh fix, two real `Zoom in` presses to `NAVIGATION_ZOOM + 2`, a genuine `stub.emitError(...)` (retained-stale-fix, no Follow re-press, no manual gesture), a genuine tile error, a manual `Retry map imagery` click, the new generation's correct real reapplication, the same unrelated late settle, then a fresh stationary fix followed by a fresh moved fix. **Pre-fix, the moved fix's resulting `setCamera` call carried `zoom: 0`** — the field-reported "approximately whole-world zoom" — instead of the rider's real `18`, confirmed deterministic across repeated runs before any production change. (The stationary-fix step in this same test is a legitimate but weaker check: a genuinely unmoved fix does not reissue any command at all under `rideCamera.ts`'s existing "stationary fixes don't jitter the camera" no-op guard, so it cannot itself distinguish a corrupted from an uncorrupted `followZoomLevel` — the moved fix is what carries the pre-fix failure.) Two genuine test-level races were found and fixed while constructing this evidence, neither a production defect: `map.triggerTileError()`/`map.triggerCameraSettled(...)` are raw synchronous calls bypassing React Testing Library's automatic `act()` flushing (mirroring this file's own already-documented `emitError` gotcha), requiring `await screen.findByTestId(...)` rather than a synchronous query; and the post-retry `waitFor` assertion needed an explicit `setCameraSpy.mockClear()` beforehand, since without it the assertion could trivially pass against a stale pre-retry call that happened to already match by coincidence, racing ahead of the real generation-2 reapplication.
    - **The fix: a coordinate-provenance correlation, not a zoom clamp or a broader gate.** `MapView.tsx` gained `settleMatchesLastAppliedCommand(settleCoordinate, lastApplied)`, comparing the settle's own coordinate against `lastAppliedCameraTargetRef.current` — the exact target the most recently applied command actually specified — within a `1e-6`-degree tolerance (mirroring `e2e/ridingCamera.spec.ts`'s own `CAMERA_VALUE_TOLERANCE`, for negligible floating-point read-back noise on a genuinely-caused settle, far tighter than any real distinguishable coordinate difference). `hasAppliedCameraCommand` becomes `appliedCameraCommandGenerationRef.current === generation && settleMatchesLastAppliedCommand(camera.coordinate, lastAppliedCameraTargetRef.current)`. Deliberately **coordinate-only, not zoom/bearing/pitch**: MapLibre's own min/max zoom clamping can legitimately shift a genuinely-caused settle's zoom away from the exact commanded value (`rideCamera.test.ts`'s own clamped-zoom reconciliation coverage proves this is a real, intended case), so correlating on zoom would risk exactly the false negative this project's own "do not globally clamp deliberate zoom" requirement forbids; a raw/default MapLibre transform, by contrast, is never the rider's real route/GPS coordinate, so coordinate alone fully and safely discriminates the untrustworthy case. A command with a null coordinate (north-up-only, via `rideCamera.ts`'s `"north-up-requested"`) always drops `rideCameraReducer`'s own mode to `"free"` — `"follow-zoom-settled"`'s own mode guard already excludes that settle regardless of this function's result, confirmed by direct source reading before relying on it, so falling back to `true` (generation-only) when no coordinate was ever commanded needed no special-casing.
    - **Deliberately not done, per the fail-first evidence's own scope.** No `cameraRestorePendingGenerationRef` skip-once guard was added to the `cameraTarget` effect (mirroring the sibling overview-fit/warning-selection-fit effects): traced and found structurally unreachable for `useRideCamera.ts`, since a genuine gesture always nulls `cameraTarget` before any diverged-and-still-following state could exist. `lastAppliedCameraTargetRef`'s reset was left unconditional (not made divergence-aware like its four sibling one-shot dedup refs), since making it conditional risks regressing the already-tested "retry preserves and re-applies a live cameraTarget" guarantee and the fail-first evidence gave no reason to touch it. `rideCameraReducer`'s `"follow-zoom-settled"` guard itself was left completely unchanged — it already correctly trusts an accurate boolean; the defect was entirely in what produced that boolean.
    - **Free roam: no production changes needed, confirmed by tracing, not merely assumed.** `useFreeRoamCamera.ts` consumes the exact same shared `rideCameraReducer` and forwards `MapView.tsx`'s `hasAppliedCameraCommand` verbatim into the same `"follow-zoom-settled"` event, via the same `onCameraSettled` payload MapView produces — since the fix lives entirely inside `MapView.tsx`'s shared handler, free roam inherits the correction automatically. The complete existing `useFreeRoamCamera.test.ts` and `e2e/freeRoam.spec.ts` suites were re-run unmodified and pass, confirming the shared fix introduces no free-roam regression; no new free-roam-specific test was added, since no free-roam-only production code changed and this item's own scope explicitly authorises extending only if a shared defect is proven — here the shared fix is already the smallest safe correction.
    - **Real-browser regression.** A new `e2e/mapImageryRecovery.spec.ts` test combines this file's own manual-retry mechanism with `ridingCamera.spec.ts`'s Zoom-in-to-a-non-default-zoom technique and `ridingStatusCardRecovery.spec.ts`'s genuine `context.clearPermissions()` stale-fix mechanism: establishes a genuinely non-default zoom, induces a real mid-ride tile failure and clicks the real `Retry map imagery` button, induces a real `PERMISSION_DENIED` geolocation error, then recovers both (re-granting permission and pressing the real "Try again", and letting the tile retry's own success clear the imagery banner), asserting `data-camera-zoom` equals the rider's exact pre-failure selected zoom for both a stationary and a subsequently moved fresh fix, and that `Follow my location` stays pressed throughout. A genuine test-level settle-timing race was found and fixed while writing this: `triggerFreshTileFailure`'s own success signal (a new tile request failing) is an independent async completion from the camera's own ease settling at the new zoom, so the "selected zoom" reference value must be captured only after an explicit poll confirms the camera has itself genuinely changed, not merely immediately after the tile-failure helper resolves. This e2e test is real-MapLibre regression/non-regression coverage for the fix, not the prove-first evidence itself — the exact out-of-order-settle race the fix closes is proven deterministically at the component/integration level, where settle ordering is under full test control; a real browser gives no reliable way to force that same internal ordering without manufacturing an artificial, unrepresentative race, and this item's own instructions explicitly warn against doing so.
    - **Files changed.** Production: `src/map/MapView.tsx` (`settleMatchesLastAppliedCommand`, `CAMERA_PROVENANCE_TOLERANCE_DEGREES`, and `onCameraSettled`'s `hasAppliedCameraCommand` computation — the entire production diff). Test: `src/map/MapView.test.tsx` (one new test), `src/ui/riding/RidingScreen.test.tsx` (one new test), `e2e/mapImageryRecovery.spec.ts` (one new test). `package.json`, `package-lock.json`.
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2872/2872 across 144 files. `npm run e2e` — 204/204 across both the `chromium` and `android-chrome` projects, no flakes observed. `npm run format:check` run last, immediately before committing — clean. The new fail-first tests were confirmed to fail for the predicted reason against the unmodified tree (`git stash`) before the production fix was written, then confirmed to pass once it was restored, per this repository's own established prove-first discipline. Version bumped to 0.3.65 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed. Real-device acceptance remains outstanding: a genuine mid-ride stale-GPS-plus-imagery-retry combination, at a deliberately non-default selected zoom, on the installed iPhone PWA and real Android Chrome, confirming the selected zoom, Follow state and below-centre look-ahead anchor all survive recovery exactly as this item's own automated evidence proves in Chromium. See `docs/project/current-status.md` for the tracked outstanding item.
    - Item 66 is left completely untouched: nothing in this investigation establishes a shared cause with its own unreproduced fresh-Start symptom, and this item's own backlog text already forbids reinterpreting it without such proof — none was found.

---

<a id="item-82"></a>

## Item 82 — Unify the active status control and make the climb cue fully readable — done

_Category: Immersive Riding interface_

82. **Unify the active status control and make the climb cue fully readable — done**
    - One bounded active-Riding presentation slice covering route Riding and free roam. Must not change session, GPS, wake-lock or climb state machines.
    - Replace the current checkbox-plus-information-button wake-lock presentation (item 68's `.wake-lock-row`/`.wake-lock-info-button`, carried into item 75's status card) with one compact, large-target `Screen on` action integrated into the right-hand portion of the existing status card (item 75's `.ride-status-card-top-row`), adding no separate vertical row.
    - The action must be comfortably tappable while riding, use the existing restrained green treatment when active and a neutral/grey treatment when inactive, retain a non-colour state indication, and keep correct pressed/checked semantics and accessible naming.
    - Use the same control in route Riding (`RidingStatusCard`) and free roam (`FreeRoamStatusCard`).
    - Move the explanatory wake-lock information, including the honest battery-consumption warning, to Settings. Remove the active-card information popover only when implementing this slice; do not change wake-lock acquisition, release, persistence, retry, or unsupported-browser behaviour (`RidingWakeLockControl`'s existing lifecycle logic).
    - Replace free roam's `Tracking` heading with `Location` (and suitable existing-state variants) — free roam does not record a track, progress or location history.
    - Reuse route Riding's compact GPS terminology and freshness/age formatting rules in free roam rather than maintaining a second wording convention. Share formatting logic where that is the smallest safe implementation, but do not merge `RidingStatusCard` and `FreeRoamStatusCard` merely for cosmetic reuse — they remain semantically distinct components.
    - In the active-climb Map cue (`RidingClimbCue`, items 57/71), keep the large one-tap `View climb` action, but ensure `Climb active` and the remaining-distance text are fully readable without ellipses at ordinary phone portrait sizes. Adjust the cue's internal allocation, padding and text sizing as needed without changing cue timing, dismissal, or Profile-transition behaviour (items 71/80).
    - Preserve large touch targets, enlarged-text fallback, portrait/short-landscape support and the fixed, non-scrolling immersive layout (item 68's immersive shell).
    - **Field context.** Real-device route Riding testing after item 75's deployment (2026-08-25) reported the checkbox-plus-information-button control was awkward to operate one-handed while riding, and that free roam's `Tracking` heading was misleading since free roam records no track, progress or location history — both tracked as this item, alongside the separately-observed climb-cue ellipsis truncation.
    - **One integrated `Screen on` toggle, styled off `aria-pressed` itself, not a separately-toggled class.** `RidingWakeLockControl.tsx`'s `<label><input type="checkbox">` plus `About Screen on` information button and its anchored popover were replaced by a single `<button type="button" aria-pressed={desired}>`, containing two stacked spans: a visible `Screen on` label (which, once the state span below is excluded, is also the button's own accessible name) and a visible but `aria-hidden` `On`/`Off` state span — the non-colour cue the backlog requires, without double-announcing what `aria-pressed` already conveys to assistive technology. `isInfoOpen`, `infoId` (`useId`), `controlRef`, `rowRef`, `infoButtonRef` and the whole outside-click/Escape-key effect were deleted outright — nothing needs popover anchoring any more, and `.wake-lock-row`'s wrapper div became unnecessary once there was only one interactive child. The existing visually-hidden success `<span role="status">Screen staying awake.</span>` and the existing failure/retry `<div role="alert">` are unchanged in substance, now direct children of `.ride-wake-lock-control` alongside the button. `useScreenWakeLock`'s three-effect acquisition/release/reacquire/retry lifecycle, its `status`/`retry` return shape, and `isWakeLockSupported()`'s caller-side gating were not touched at all — confirmed both by inspection and by every existing lifecycle-shaped test continuing to exercise the same hook contract.
    - **CSS: the existing binary-toggle idiom, not the multi-option one.** `.wake-lock-toggle[aria-pressed="true"]` reuses the same `background: var(--colour-accent-soft); border-color: var(--colour-accent); box-shadow: inset 0 0 0 2px var(--colour-accent); color: var(--colour-accent);` idiom already established by `.main-nav-button[aria-current="page"]` and `.ride-immersive-switcher-button.is-selected` — a real binary on/off feature toggle — rather than `.elevation-window-button.is-selected`'s deliberately colourless multi-option-selector idiom, which would have been the wrong precedent for a genuine on/off control. No separate "off" rule exists, matching all three precedents: the unstyled base `button` rule (grey border, `--colour-bg`) is already the neutral/off state. `.wake-lock-toggle` itself needed an explicit `display: flex` — easy to miss, since the base `button` rule sets no `display` at all, so a bare `flex-direction: column` would silently have no effect. `.wake-lock-row`, `.wake-lock-label`, `.wake-lock-checkbox`, `.wake-lock-info-button`, `.wake-lock-popover` and `.wake-lock-popover p` were deleted once nothing referenced them.
    - **A shared GPS-status-line formatter, not a shared component.** `FreeRoamStatusCard.tsx`'s `Tracking`/`Tracking — signal lost` became `Location`/`Location — signal lost`; its separate `GPS accuracy: ±N m — Live/Stale (age)` wording, and route Riding's own `GPS ±N m · Live/Stale (age)` wording (previously duplicated inline, including two byte-identical local `formatFixAge` copies), were unified into a new `src/ui/riding/rideStatusText.ts` exporting `formatFixAge`/`formatGpsStatusLine`. This is a fresh implementation match for an existing project precedent found while designing it: `manoeuvreLabels.ts`, a tiny module holding one pure formatter shared between two sibling Riding components, whose own doc comment explains the shape is needed so neither component file mixes a component export with plain function exports (a real Vite Fast-Refresh boundary concern, not merely a style preference) — the same rationale applies here, and folding the formatter into the route/Planning-shared `routeSummary.ts` instead was rejected as a scope mismatch (that file holds static route-shape facts; this is live-tracking, Riding-only state). One real behavioural correction fell out of the unification: free roam's previous inline wording showed a fix age whenever `fixAgeMs` was non-null, even for a genuinely fresh (non-stale) fix — the backlog's own "fresh status should not display an unnecessary age" requirement is satisfied for free roam simply by adopting the already-correct shared logic, with a new regression test added for exactly this case. `RidingStatusCard`/`FreeRoamStatusCard` themselves were not merged — only the formatter — and free roam kept its existing `.ride-status-card-detail` class unchanged (its styling was already identical to `.ride-status-card-gps` via a shared comma-selector; only the text content changed).
    - **Climb cue: reclaim width and height budget first, wrap instead of truncate, prove containment before trusting it.** Three coordinated CSS changes to `.ride-climb-cue` and its children, all within the backlog's own "adjust flex allocation, gap, padding, font sizing or wrapping" boundary: (1) a new `.ride-climb-cue-action` class on the `View climb` button (`className="btn-primary ride-climb-cue-action"`) with tighter horizontal padding than the base button rule's `var(--space-16)`, plus `flex-shrink: 0; white-space: nowrap;`, reclaiming the width the button was previously eating from the text column; (2) `.ride-climb-cue`'s own `gap` and vertical padding tightened from `var(--space-8)` to `var(--space-4)`, buying back height headroom under `.map-status-overlay`'s `top: 72px` clearance budget (that offset's own doc comment already documents it as empirically coupled to the cue's worst-case height); (3) `.ride-climb-cue-title, .ride-climb-cue-detail`'s `overflow: hidden; white-space: nowrap; text-overflow: ellipsis;` replaced with `overflow-wrap: break-word;` — no clipping ancestor sits between these paragraphs and the cue's own padded box, and `.ride-climb-cue-text` keeps `min-width: 0`, so wrapped text stays fully visible and contained. `.ride-climb-cue` itself also gained `flex-wrap: wrap; align-items: flex-start;` as a structural safety net (the button can drop to its own line below the text if a row is ever too narrow to hold both), though in practice every required viewport/zoom combination below was proven to fit on one line without ever needing that fallback to actually trigger. Font-size reduction was deliberately never used — it would contradict "fully readable" — and `.map-status-overlay`'s own `top: 72px` offset was left untouched, since real Playwright measurement (below) proved the tightened design already clears it with margin at every required condition; touching that offset remained an explicitly reserved last resort, not exercised.
    - **Settings: a fourth panel, appended after Elevation and climbs, following the established disclosure-wrapped-prose convention exactly.** `SettingsScreen.tsx` gained `<section className="panel stack" aria-labelledby="riding-heading"><h2 id="riding-heading">Riding</h2><details className="settings-disclosure"><summary>Screen on</summary>…</details></section>`, mirroring every existing Settings panel's pattern of keeping supplementary explanation inside a collapsed-by-default disclosure rather than as bare visible prose. The disclosure carries the same substance the old in-context popover held (display stays on while an active Riding/free-roam screen is visible; may increase battery use) plus the two clarifications the backlog requires: scope-limited to a visible active screen, not background tracking, and no guarantee of staying on if the browser doesn't support the feature. No new toggle was added to Settings — explanation only, per the backlog's explicit "do not duplicate the live toggle" instruction.
    - **Fail-first evidence.** `RidingWakeLockControl.test.tsx`'s pre-existing 15 tests (all built around checkbox role, the `About Screen on` button, or `role="note"` popover mechanics) were confirmed to fail 13/15 for the predicted reason against the new unmodified-elsewhere component before being replaced wholesale with 7 jsdom-appropriate tests (accessible name, `aria-pressed`, visible on/off text, callback invocation, keyboard activation, failure/retry) — deliberately excluding ≥44×44 sizing and focus-outline visibility, which jsdom cannot meaningfully measure and which moved to Playwright instead. The climb-cue readability fix was proven fail-first at the Playwright layer specifically: `git stash`-reverting only `src/index.css`/`RidingClimbCue.tsx` and re-running the two new geometry tests reproduced `titleStyle.overflow` reading `"hidden"` — the exact predicted symptom — before the fix was restored and both tests passed.
    - **Playwright: geometry/paint stays in Playwright, not jsdom, throughout.** `e2e/ridingWakeLock.spec.ts` was substantially rewritten: `aria-pressed` assertions replace checkbox-role queries throughout; a real ≥44×44 CSS-pixel bounding-box check and a genuine `:focus-visible` outline-width check (reached via a bounded real-Tab-press loop rather than a hard-coded single Tab, since the exact number of intervening focusable header elements is an implementation detail this test shouldn't hard-code) replace the old popover-containment assertions; the full acquisition/release/reacquire/retry lifecycle and unsupported-browser-absence coverage carried over unchanged in substance. `e2e/ridingMapProfileViews.spec.ts`'s own `About Screen on` popover-containment test was replaced with an equivalent "pressing the toggle never resizes the map" proof; while building it, a genuine gap was found and fixed in that test's own long-standing inline `navigator.wakeLock` stub (`{ request: () => Promise.resolve({ release: () => Promise.resolve() }) }`, present since before this item and shared by two other, unaffected tests in the same file) — it had no `addEventListener`/`removeEventListener`, which `browserWakeLockSource.request()` calls unconditionally; since that function is `async`, the missing method threw and turned the returned promise into a silent rejection, driving `useScreenWakeLock` to `"unavailable"` and rendering the failure row, which was the real, fully mundane cause of an apparent ~52px map resize this item's own new assertion first caught. `e2e/ridingClimbView.spec.ts` gained a new local `expectClimbCueTextFullyReadable` helper (computed-style proof that `overflow`/`text-overflow` no longer clip, plus both text elements' own bounding boxes fully contained within the cue's box via the file's existing `isFullyWithin` helper, plus a no-document-horizontal-overflow check) — applied to the existing 390×844 test and two new tests, short landscape (844×390) and 200%-enlarged text, neither of which existed before this item. A pre-existing, already-shipped test in `e2e/mapImageryRecovery.spec.ts` ("the map-status overlay never visually overlaps the active-climb cue when both are shown simultaneously," genuinely combining a forced tile failure with an active climb) already covered the specific non-overlap-with-`.map-status-overlay` requirement in a meaningful, non-trivial way — re-run and confirmed still passing rather than duplicated with a lower-value always-empty-box version in `ridingClimbView.spec.ts`. `e2e/androidRiding.spec.ts` and `e2e/ridingImmersiveShell.spec.ts` (found via a repository-wide grep after the initially-scoped file list proved incomplete) needed the same checkbox-role-to-button-role mechanical update, including switching Playwright's checkbox-only `.check()` helper to a plain `.click()`. `e2e/freeRoam.spec.ts`, `e2e/rideSessionSwitchGuard.spec.ts` and `e2e/androidFreeRoam.spec.ts`'s `/GPS accuracy:/` regex assertions, and `e2e/ridingStatusCardRecovery.spec.ts`'s `"GPS accuracy"` string assertions, were updated to the unified `GPS ±` convention.
    - **Files changed.** Production: `src/ui/riding/RidingWakeLockControl.tsx`, `src/ui/riding/RidingStatusCard.tsx`, `src/ui/riding/FreeRoamStatusCard.tsx`, `src/ui/riding/RidingClimbCue.tsx`, `src/ui/settings/SettingsScreen.tsx`, `src/index.css`; new `src/ui/riding/rideStatusText.ts`. Test: `src/ui/riding/RidingWakeLockControl.test.tsx` (rewritten), `src/ui/riding/RidingStatusCard.test.tsx`, `src/ui/riding/FreeRoamStatusCard.test.tsx`, `src/ui/riding/FreeRoamScreen.test.tsx`, `src/ui/riding/FreeRoamScreen.pause.test.tsx`, `src/ui/riding/RidingScreen.test.tsx`, `src/ui/riding/RidingScreen.pause.test.tsx`, `src/ui/riding/RidingScreen.finishEndRide.test.tsx`, `src/ui/settings/SettingsScreen.test.tsx`; `e2e/ridingWakeLock.spec.ts` (rewritten), `e2e/ridingMapProfileViews.spec.ts`, `e2e/ridingClimbView.spec.ts`, `e2e/androidRiding.spec.ts`, `e2e/ridingImmersiveShell.spec.ts`, `e2e/freeRoam.spec.ts`, `e2e/rideSessionSwitchGuard.spec.ts`, `e2e/androidFreeRoam.spec.ts`, `e2e/ridingStatusCardRecovery.spec.ts`. `package.json`, `package-lock.json`.
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2891/2891 across 146 files. `npm run e2e` — 215/215 across both the `chromium` and `android-chrome` projects, no flakes observed in the targeted or full runs for any file this item touched. One already-documented class of pre-existing, environment-contention-driven Vitest flake (item 32/68's own established precedent) was observed twice across repeated full-suite runs during this item's own verification — once in `src/gpx/parseAcnExtension.test.ts` (the exact file item 68's own history entry already names for this), once in `src/routing/openRouteServiceAdapter.test.ts` — both in files this item's diff never touches, both passing cleanly in isolation and in subsequent full clean reruns, and confirmed via a `git stash` baseline run on the unmodified tree reproducing the same intermittent-but-unrelated pattern. `npm run format:check` run last, immediately before committing — clean. The climb-cue CSS fix was confirmed failing for the predicted reason against the unmodified tree (`git stash`) before being relied on; `RidingWakeLockControl.test.tsx`'s full rewrite was confirmed failing 13/15 against the new component before being replaced. Version bumped to 0.3.68 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed. Real-device acceptance remains outstanding: one-handed/gloved use of the integrated `Screen on` toggle in both route Riding and free roam; its active/inactive legibility in bright light relying on the non-colour `On`/`Off` cue, not colour alone; genuine wake-lock acquisition/failure/retry and persistence through real visibility changes; the new Settings explanation's clarity and discoverability; free roam's `Location`/GPS wording reading correctly at a glance; the climb cue's complete `Climb active`/remaining-distance text and `View climb` usability while mounted and genuinely moving; true portrait/landscape rotation, real device text scaling and safe-area behaviour; and the installed iPhone PWA and physical Android Chrome checked separately. See `docs/project/current-status.md` for the tracked outstanding item.
    - **Follow-up (2026-08-26, v0.3.69): the status card's dead-space gap, measured from a real installed-iPhone screenshot.** A post-deployment screenshot of route Riding showed the new `Screen on` toggle's style and behaviour working exactly as intended, but exposed a genuine layout defect this item's own automated coverage never checked for: `.ride-status-card-top-row` only ever contained the status label and the wake-lock control — the remaining-distance/ascent and GPS-freshness lines were later siblings of the whole card, not of that row, so they rendered _below_ the taller button, leaving a large blank gap beneath the first line. Confirmed as a genuine composition defect by direct source inspection, not merely inferred from the screenshot.
      - **Fix: a two-column main region, not a taller top row.** `.ride-status-card-top-row` was renamed `.ride-status-card-main` and its `align-items` changed from `flex-start` to `stretch` — the load-bearing change: flexbox now makes the text column and the control column equal to the taller one's natural height, so the control genuinely spans the text stack with no dead space beneath it. A new `.ride-status-card-text` (`display: flex; flex-direction: column; gap: var(--space-4); flex: 1 1 auto; min-width: 0`) now wraps the status label plus (for route Riding) the remaining-distance/ascent and GPS lines, or (for free roam) the single GPS detail line, as one grouped left column beside the wake-lock control — both `RidingStatusCard.tsx` and `FreeRoamStatusCard.tsx` restructured identically in this respect, each keeping its own distinct text content. `.ride-wake-lock-control` gained `display: flex; flex-direction: column; flex: 0 0 auto`, and `.wake-lock-toggle` gained `justify-content: center; flex: 1 0 auto`, so the button visually grows to fill its stretched column while its two stacked spans stay centred — a no-op whenever the button is already at its natural height (e.g. the wake-lock failure-row state, where the control column is taller than the text: `align-items: stretch` then stretches the _text_ column to match, while its own rows stay pinned to the top via its default `justify-content: flex-start`, exactly as required). The error row and offline row needed no change — they were already direct children of `.ride-status-card`, siblings of the row wrapper, and remain full-width below the two-column region. No session, GPS, wake-lock (`useScreenWakeLock`), or climb state was touched; the button's colours, ring, wording, `aria-pressed` semantics, accessible name and 44×44 minimum target are all unchanged.
      - **`flex-wrap: wrap` carried over, not newly proven.** The old row's wrap behaviour was kept on `.ride-status-card-main` as a narrow-width safety net, but it was deliberately _not_ assumed to be a safe fallback for enlarged text or short landscape without measurement: `.ride-status-card-text`'s `min-width: 0` lets flexbox shrink the text column arbitrarily narrow (forcing many internal text-wrap lines) rather than actually wrapping the control onto its own line, so whether real overflow/clipping/unreachability occurs had to be checked, not presumed. It was checked directly (see below) at 200% text and 844×390 short landscape, and held safely with no added breakpoint — a media query was deliberately not introduced since none proved necessary.
      - **Fail-first evidence.** Six Vitest structural assertions were added across `RidingStatusCard.test.tsx`/`FreeRoamStatusCard.test.tsx` (wake-lock control inside `.ride-status-card-main` but not `.ride-status-card-text`; status/remaining/GPS or status/detail lines sharing one `.ride-status-card-text` ancestor; error/offline rows outside `.ride-status-card-main` and direct children of `.ride-status-card`) — all six confirmed failing against the unmodified tree (the new classes did not exist yet) before the fix, then passing after. Two Playwright geometry tests (`ridingWakeLock.spec.ts` for route Riding, `freeRoam.spec.ts` for free roam) captured the actual `.wake-lock-toggle` button's own box — not merely the outer `.ride-wake-lock-control` wrapper, which could mask a wrapped-below-the-stack layout — and asserted the inter-line gap, left-column alignment, every text row sitting entirely left of the button, the button's top aligning with the status row's top, and the button's bottom reaching at least the GPS/detail line's _bottom_ (not merely its top, since a button that had wrapped onto its own line below the stack could still satisfy a top-only check). Both were confirmed failing against the unmodified tree (a ~38px dead-space gap measured, against an asserted <12px) before the fix, then passing after.
      - **Additional coverage.** New 200%-text and 844×390-short-landscape Playwright tests in `ridingWakeLock.spec.ts` measured (rather than assumed) that the existing `flex-wrap` plus `min-width: 0` produces no horizontal document overflow, keeps the text rows grouped as one ordered column, and keeps the toggle a reachable ≥44×44 target — all passed without needing a new breakpoint. `ridingStatusCardRecovery.spec.ts` gained one focused bounding-box assertion per row (route Riding and free roam) proving the error and offline rows' width is close to the card's own content width and their left edge is close to the card's left edge — genuinely full-width beneath the two-column region, not merely absent from document-overflow — rather than resting on the pre-existing no-overflow checks alone. `ridingMapProfileViews.spec.ts`'s pre-existing "toggle never resizes the map" and short-landscape non-overlap assertions were re-run unchanged and confirmed still passing.
      - **Files changed.** Production: `src/index.css`, `src/ui/riding/RidingStatusCard.tsx`, `src/ui/riding/FreeRoamStatusCard.tsx`, `src/ui/riding/RidingScreen.tsx` (comment only). Test: `src/ui/riding/RidingStatusCard.test.tsx`, `src/ui/riding/FreeRoamStatusCard.test.tsx`, `e2e/ridingWakeLock.spec.ts`, `e2e/freeRoam.spec.ts`, `e2e/ridingStatusCardRecovery.spec.ts`. Docs: this entry, `docs/project/current-status.md`, and one factual-accuracy correction to item 83's still-pending backlog text (`docs/project/backlog.md`), which named `.ride-status-card-top-row` as its target location — updated to name `.ride-status-card-main`/`.ride-status-card-text` since item 83 is the next selected implementation item and would otherwise reference a class that no longer exists. `package.json`, `package-lock.json`.
      - **Verification.** `npm run typecheck` and `npm run build` clean after each production edit. Targeted `npm test` (both status-card test files, 35/35) and targeted `npm run e2e` (`ridingWakeLock.spec.ts`, `freeRoam.spec.ts`, `ridingStatusCardRecovery.spec.ts`, `ridingMapProfileViews.spec.ts`, `chromium` project, 41/41) confirmed green after the fix, following the fail-first evidence above. The full repository gate (`format`, `lint`, `typecheck`, `build`, `test`, `e2e` on both `chromium` and `android-chrome`, `format:check` last) was run before committing. Version bumped to 0.3.69 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
      - This follow-up ships with automated evidence only. The report that prompted it is real-device evidence of the _original_ defect, not acceptance of this correction — a new deployment and a fresh installed-iPhone check are still required before the corrected two-column layout can be marked accepted. See `docs/project/current-status.md` for the tracked outstanding item.

---

<a id="item-83"></a>

## Item 83 — Make offline and map-imagery recovery unobstructive — done

_Category: Map imagery and tile reliability_

83. **Make offline and map-imagery recovery unobstructive — done**
    - A presentation/composition slice around the already-shipped item 67 recovery state machine (`.map-status-overlay`, retry/episode tracking, camera preservation across recreation) and item 75's compact status card, item 82's two-column layout. Not authorised to rewrite networking or camera recovery.
    - Represent connectivity compactly within the existing status-card main region using a recognisable online/offline status symbol with an accessible textual name, without another full-width status row. Move the imagery-failure explanation and retry action out of the central route-viewing area, into existing status chrome, without duplicating item 67's state machine.
    - **Field context.** Real installed-iPhone testing after item 82 shipped (2026-08-25) reported the map-imagery-unavailable overlay and `Retry map imagery` button could still cover the upcoming route, including while an active-climb cue was present, and the status card's separate full-width `Offline` row (item 75's `.ride-status-card-offline`) consumed unnecessary vertical space — tracked as this item, alongside item 82's own field observations.
    - **A narrow typed seam, not a lifted state machine.** `MapView.tsx` gained two new exported types — `MapImageryRecoveryStatus` (`{ kind: "load-error" | "tile-error" | "fallback" }`) and `ImageryRetryCommand` (`{ requestId: string }`, mirroring `OrientNorthCameraTarget`'s own requestId-only, deduped-by-requestId-not-value shape exactly, since a "Retry" press is a discrete explicit action with no automatic/value-carrying variant) — and two new optional `MapViewProps`: `onImageryStatusChange` and `imageryRetryCommand`. A derived `currentImageryStatusKind` (one source of truth: `load-error` > `tile-error` > `fallback` > `none`, computed from the existing `loadState`/`tileErrorMessage`/`usingFallbackStyle`/`ready` state) drives a new reporting `useEffect`, dependent only on that primitive kind and a `hasExternalImageryPresentation = onImageryStatusChange !== undefined` boolean — never on the raw callback reference, which is instead mirrored into a ref exactly like the existing `onCameraSettledRef`/`onUserCameraInteractionRef` idiom, so an unrelated parent rerender (a GPS-tick-driven `currentPosition` change, for instance) can never retrigger a duplicate report. A `lastReportedImageryStatusKindRef`, reset to `"none"` whenever `hasExternalImageryPresentation` goes false, makes the effect idempotent and guarantees a caller that starts listening again mid-episode gets the current truth immediately rather than staying silently stale. A second new effect, deduped by `requestId` against `lastAppliedImageryRetryRequestIdRef`, calls the exact same `handleRetryImagery()` the in-map button already calls — no second/parallel retry mechanism, identical diagnostic recording and `retryToken` bump. `handleRetryImagery` itself was converted from a plain per-render function into a `useCallback` (keyed only on `tileSource.id`, its one real dependency besides the always-stable `setRetryToken`) purely so this new effect could include it in its own dependency array without violating `react-hooks/exhaustive-deps` or re-running every render — its body and behaviour are byte-identical.
    - **JSX suppression stayed independent per-condition, not derived from the new single-valued kind — a real bug caught by the existing suite before it shipped.** The first implementation collapsed the three in-map terminal banners' render conditions onto `currentImageryStatusKind`, which is deliberately mutually-exclusive for the _reporting_ effect's sake. But a pre-existing test ("does not create a retry loop from repeated errors on the fallback map itself") proves `tileErrorMessage` and `usingFallbackStyle` can both be true at once in one synthetic edge case (a `source-or-tile` error fired while already on the fallback style — unreachable for a real fallback style, which has no sources, but the mock doesn't respect that constraint and the existing test doesn't assert exclusivity). Under the collapsed derivation this silently hid the fallback banner in that case, a genuine regression the full suite caught immediately. Fixed by reverting the JSX to the original three fully independent conditions (`loadState === "load-error"`, `tileErrorMessage !== null`, `usingFallbackStyle && ready`), each gaining only an added `&& !hasExternalImageryPresentation` clause — `currentImageryStatusKind` is used solely by the reporting effect, which can only represent one kind at a time by construction and does not need to match this edge case's own doubled truth.
    - **A shared presentation module, not per-card copy.** New `src/ui/riding/mapImageryRecoveryPresentation.ts` exports `describeMapImageryRecovery(kind)`, returning `{ role, message, testId }` per kind — reusing MapView's own three existing banner strings and `data-testid`s verbatim (`map-load-error`/`role="alert"`, `tiles-unavailable-banner`/`role="status"`, `map-fallback-banner`/`role="status"`), so wording can never drift between `RidingStatusCard` and `FreeRoamStatusCard`, and the large existing e2e suite's testid-based locators keep working unmodified regardless of the relocation. The relocated row reuses `retry-map-imagery-button`/`.map-status-retry-button` verbatim too (confirmed safe: the base `button` rule already sets its own theme-aware `background`/`color`, so the class isn't tied to the map's dark-overlay context) — at most one of {map-owned, card-owned} ever renders per screen, so reuse minimises churn rather than risking drift.
    - **A new `ConnectivityIcon`, matching the existing `PinIcon`/`NavIcon`/`ManoeuvreIcon` convention exactly.** `viewBox="0 0 24 24"`, `aria-hidden="true"`, all styling inline (never a CSS class, since this project's Vitest config sets `test: { css: false }`), a `sizePx` prop defaulting to 16px. Online is a signal/broadcast glyph (a dot plus two nested arcs); offline is the identical glyph plus one added diagonal `<line>` — a genuine shape difference, not a `currentColor` swap, satisfying "never colour/shape alone" structurally rather than merely by convention.
    - **Placement: inside `.ride-status-card-text`, sharing the status label's own line via a new `.ride-status-card-status-row` wrapper — not a new direct child of `.ride-status-card-main`.** A new column there would need its own `align-self` override to avoid the main region's load-bearing `align-items: stretch` (item 82's own mechanism), and would visibly separate from the text column via `justify-content: space-between` whenever `wakeLock` is absent — the empty-reserved-column look the backlog explicitly warns against. Nesting inside the always-present text column adds zero height in the common case and inherits the existing wrap-safety net. The icon stays `aria-hidden`; the adjoining `Online`/`Offline` word is the sole accessible name and the sole visible prose stating connectivity, since `.ride-status-card-offline` was removed once both cards' only two call sites were replaced (confirmed via grep: no other production or e2e reference remained).
    - **The relocated imagery-recovery row stays a full-width direct child of `.ride-status-card`, deliberately.** Unlike the connectivity indicator, this row replaces the map's own banner rather than duplicating an existing fact, so a full-width row (analogous to, and placed after, the existing geolocation-error row) is correct and explicitly permitted by the backlog text. Role varies by kind — `alert` only for the terminal `load-error` kind, `status` for the two recoverable kinds — matching MapView's own existing distinction exactly, now expressed via a `--alert` CSS modifier class rather than a second component.
    - **Screen wiring: gated per-render on `showStatusCard`, cleared via React's own "adjusting state when a prop changes" pattern, not an effect.** `RidingScreen.tsx`/`FreeRoamScreen.tsx` each gained `imageryStatus`/`imageryRetryCommand` state, passed into `<MapView>` as `onImageryStatusChange={showStatusCard ? setImageryStatus : undefined}`/`imageryRetryCommand={showStatusCard ? imageryRetryCommand : undefined}` and into the status card as `imageryRecoveryStatus`/`onRetryImagery={() => setImageryRetryCommand({ requestId: generateId() })}` (the existing `src/platform/idGenerator.ts` helper, already used by `boundsTarget`/`centreTarget`/`orientNorthTarget`/`zoomTarget`). Planning gained neither prop, so it keeps the default map-contained presentation automatically — no Planning file was touched. A genuine edge case was identified during planning: without clearing these two states when `showStatusCard` goes false, a rider who ends a ride mid-tile-failure and starts a new one later could see a stale row, since MapView has no reason to re-report unchanged internal state to a callback that only just started listening again after the trouble had already cleared during the gap. The natural fix — a `useEffect` calling `setImageryStatus(null)`/`setImageryRetryCommand(null)` when `!showStatusCard` — is flagged by this repository's `react-hooks/set-state-in-effect` lint rule for any non-ref-derived condition (see `feedback_react_hooks_set_state_in_effect_ref_gotcha` precedent, a different but adjacent gotcha); the actual fix used React's own documented "adjusting state when a prop changes" pattern instead — a `wasStatusCardShown` state mirror compared directly during render, with the reset `setState` calls made synchronously in the render body rather than inside an effect, which the rule does not flag.
    - **Fail-first evidence.** A new `describe("backlog item 83: external imagery-recovery presentation seam", ...)` block in `MapView.test.tsx` (9 tests) was written and run against the inert (types/props added, no behaviour) tree first: 7 of 9 failed for the predicted reason, the other 2 (an anti-loop proof and a default-path regression guard) legitimately held trivially true both before and after, as intended. Two of the initial 7 test bodies themselves needed correction during this process — an attempted "reports the fallback kind, then the load-error kind" scenario tried to reach the terminal `load-error` state via a `style-request-or-parse` error fired _after_ a prior `triggerLoad()`, which production code correctly treats as ordinary post-load tile trouble once the local fallback has genuinely loaded (`hasLoaded` is true), not "the fallback itself failed to ever load"; split into two independently-reachable tests instead, one mirroring the existing "shows a terminal load-error state" test's own two-errors-with-no-load-in-between pattern. `RidingStatusCard.test.tsx`/`FreeRoamStatusCard.test.tsx`'s pre-existing "keeps the error row and offline row outside the main region" test (asserting `.ride-status-card-offline` was a sibling of `.ride-status-card-main`) was split into a preserved error-row assertion and a new, inverted connectivity-indicator containment assertion, proven against the restructured component.
    - **Playwright: relocation proven positively, not merely left passing by coincidence.** `e2e/mapImageryRecovery.spec.ts`'s own direct regression test for the original field-reported bug ("the map-status overlay never visually overlaps the active-climb cue…") was rewritten to assert the stronger real claim — the relocated row never overlaps the map container at all, via `intersects()`, plus explicit `.closest(".ride-status-card")`/`.closest(".map-status-overlay")` DOM-ancestry checks — and its 390px-viewport containment test now asserts non-overlap with the map plus full containment within `.ride-status-card`, inverting the old "banner lives inside the map box" assumption. The offline-alone test gained a positive assertion that the connectivity indicator is present with the correct accessible name, alongside its existing negative assertions that no imagery-failure banner appears. The Planning test gained an explicit regression-guard assertion that its own banner stays inside `.map-status-overlay`, proving the opt-in truly is per-render, not a screen-wide constant. The primary route-Riding and free-roam "never blocks controls" tests each gained a positive DOM-location assertion. `e2e/ridingStatusCardRecovery.spec.ts`'s "simultaneous offline and a geolocation error" tests (route Riding and free roam) had their `offlineBox.width >= cardBox.width * 0.7` assertions — which directly encoded the old full-width row — replaced with containment-within-`.ride-status-card-main` plus a `< 0.5 * cardBox.width` compactness bound. `e2e/freeRoam.spec.ts`'s existing local-fallback test gained a location assertion for the relocated fallback banner.
    - **Files changed.** Production: `src/map/MapView.tsx`, `src/ui/riding/RidingStatusCard.tsx`, `src/ui/riding/FreeRoamStatusCard.tsx`, `src/ui/riding/RidingScreen.tsx`, `src/ui/riding/FreeRoamScreen.tsx`, `src/index.css`; new `src/ui/riding/ConnectivityIcon.tsx`, `src/ui/riding/mapImageryRecoveryPresentation.ts`. Test: `src/map/MapView.test.tsx`, `src/ui/riding/RidingStatusCard.test.tsx`, `src/ui/riding/FreeRoamStatusCard.test.tsx`, `src/ui/riding/RidingScreen.test.tsx` (including a new `triggerSourceData` capability added to its own stub map factory), `src/ui/riding/FreeRoamScreen.test.tsx` (same stub extension, plus a new `describe("map-imagery recovery relocation (backlog item 83)", ...)` block closing a genuine pre-existing coverage gap — this file had no mid-session-tile-error test at all before this item); new `src/ui/riding/ConnectivityIcon.test.tsx`; `e2e/mapImageryRecovery.spec.ts`, `e2e/ridingStatusCardRecovery.spec.ts`, `e2e/freeRoam.spec.ts`. `package.json`, `package-lock.json`.
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean (lint required two follow-up fixes beyond the initial pass: the `react-hooks/set-state-in-effect` pattern above, and wrapping `handleRetryImagery` in `useCallback` to satisfy `react-hooks/exhaustive-deps` on the new retry-command effect). `npm test` — 2932/2932 across 147 files (one `src/gpx/parseAcnExtension.test.ts` timeout was observed on the first full-suite run, confirmed a pre-existing CPU-contention flake unrelated to this item's files by passing cleanly both in isolation and on a clean full-suite rerun). `npm run e2e` — 204/204 on `chromium` (one `ridingFinishAndEnd.spec.ts` completion-detection failure was observed on the first full run, matching the already-monitored, already-documented item 32 flake exactly — confirmed passing cleanly in isolation) and 14/14 on `android-chrome`. `npm run format:check` run last, immediately before committing — clean. Version bumped to 0.3.70 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed. Real-device acceptance remains outstanding: compact Online/Offline symbol comprehension in bright light without relying on colour; genuine offline-to-online transitions while already-rendered imagery remains useful; a genuine tile/style failure and manual Retry on an installed iPhone PWA; automatic disappearance of the recovery affordance after real imagery recovery without a pan/zoom; the route, rider marker, climb cue, controls, attribution and Map/Profile switcher remaining usable during a real failure; Profile's elevation controls and the status-card retry action remaining reachable together; camera/Follow/free-pan preservation across a real failure and retry; one-handed/gloved use, true portrait/landscape rotation, real device text scaling and safe-area behaviour; and the installed iPhone PWA and physical Android Chrome checked separately. See `docs/project/current-status.md` for the tracked outstanding item.

---

<a id="item-84"></a>

## Item 84 — Restore visibly rendered, zoom-adaptive route-distance badges — done

_Category: Map presentation reliability_

84. **Restore visibly rendered, zoom-adaptive route-distance badges — done**
    - A focused visibility/reliability correction to an already-implemented feature (commit `7ca6b85`), not a redesign: badge placement, the 1/5/10/20 km interval family, fixed absolute coordinates, active-Riding passed-badge filtering, merged loop/out-and-back labels and the separate white route-direction arrows are all unchanged.
    - **Diagnosed root cause, source-verified and then proven with fail-first composite-pixel evidence.** Route-distance badges (`.distance-badge-marker`) and Planning's waypoint markers (`.planning-waypoint-marker`) are both plain DOM `<div>`s wrapped in real `maplibregl.Marker` instances; MapLibre appends every marker as a sibling of the WebGL `<canvas>` inside its own internal `.maplibregl-canvas-container` (confirmed directly against the installed `maplibre-gl` package's own `marker.ts`/CSS). The canvas and `.maplibregl-marker` both default to `position: absolute` with `z-index: auto`, so ordinary DOM-append order alone already puts a marker above the canvas — this is why waypoint markers, which set no `z-index` at all, rendered correctly all along. `.distance-badge-marker` carried an explicit `z-index: -1`; because the element is already `position: absolute`, that value made it establish its own stacking context at a negative level, painted at CSS 2.1 Appendix E's step 2 — strictly before (behind) the canvas's own step-6 box in their shared stacking context. The adjacent code comment's claim that the canvas "lives in a separate stacking context below every DOM marker regardless" was real-browser-proved false, not merely suspected: a temporary runtime override of only the badge's inline `style.zIndex` (no other change) flipped a failing composite-pixel sample to passing, isolating the property itself as the causal mechanism, distinct from marker removal, opacity or any other rule.
    - **The fix is a paired stacking contract, not a one-line deletion.** Simply removing the negative `z-index` would fix "badge above canvas" but badges are DOM-appended after waypoint markers whenever both exist (Planning's own waypoint-marker effect runs before its badge effect in `MapView.tsx`), so a bare `z-index: auto` badge would then paint _above_ a coincident waypoint marker — inverting the explicit requirement that a waypoint/start/finish marker keep priority. `.distance-badge-marker` now carries `z-index: 1` (moving it to the painting algorithm's ascending-order step 7, above the canvas's step 6) and `.planning-waypoint-marker`'s base rule now carries `z-index: 2` (also step 7, but ordered above the badge by value, independent of DOM order). Both stay far below every real overlay's `z-index: 5`/`10`. The stale, factually-wrong comment was replaced with an accurate one explaining the shared-stacking-context mechanism and the step-2-vs-step-7 distinction, cross-referenced from both rules so neither value is ever tuned in isolation later. No other production file changed: `mapAdapter.ts`'s `setMarkers`/`setDistanceBadges` and `MapView.tsx`'s two marker effects were already structurally symmetric between the two marker types — confirmed by direct reading, not merely assumed.
    - **Fail-first evidence, using region-based composite-pixel coverage, not a single fragile pixel.** `e2e/distanceBadges.spec.ts` gained a `verifyBadgePaint` helper: crops exactly to a badge's border box via `page.screenshot({ clip })`, decodes the PNG with a project-owned, dependency-free decoder (copied from the proven `ridingSelectedFeatureSummary.spec.ts` precedent per this repo's no-shared-e2e-helpers convention, not imported), then measures the _fraction_ of pixels in the left/right padding-strip interior matching the badge's white fill and in a small bounded search window near each edge matching its dark border — deliberately excluding the rounded corners and the centred text glyph, and scanning inward (rather than assuming row 0 is the border) since `boundingBox()`'s fractional CSS coordinates do not always align with rasterised device-pixel rows. This is paired with a supporting, not sole, relative check: the badge's own inline `display` is toggled to `none` and the identical clip re-captured, requiring the hidden crop's coverage of the badge's own colours to drop substantially and a meaningful fraction of the whole crop to differ — restored in `finally`. Confirmed failing (coverage 0, both on ordinary local imagery and on the local fallback background) against the unmodified `z-index: -1` tree before any production change, and confirmed passing once the fix landed.
    - **Zoom-band behaviour proven without asserting screen coordinates.** A new zoom-band test crosses a real badge-density-band boundary via MapLibre's own `Shift+=` `KeyboardHandler` (not a synthetic wheel/drag gesture, mirroring `lowZoomLegibility.spec.ts`'s own established preference for this exact reason), on an 80 km fixed-geometry route chosen so the tested bands are observable without `MAX_ACTIVE_DISTANCE_BADGES`'s cap forcing unwanted coarsening. It asserts the finer subset is a strict superset of the coarser one and that every shared label/aria-label survives identically — deliberately not asserting anything about the badges' screen pixel position, since projection legitimately moves a fixed geographic point to a different pixel as zoom changes; the underlying fixed-route-coordinate invariant for a shared distance stays covered at the existing unit level (`distanceBadgeLayer.test.ts`, `MapView.test.tsx`).
    - **Waypoint-over-badge precedence proven with a forced, deterministic overlap and an ancestry check, not a bare z-index-number comparison.** Planning's own "Unknown surface" warning spans an entire route with no supplied surface data, and Planning explicitly blocks placing or moving a waypoint while a route/warning segment is selected — a click landing precisely on the thin rendered route line (as any position chosen to exactly match a badge's own coordinate necessarily would) selects that warning instead of adding a waypoint, making an organically-produced badge/waypoint coincidence impractical to reach through ordinary interaction on a route with no surface data anywhere along it. The new test instead adds a third waypoint through ordinary empty-space clicking (proven safe, mirroring the pre-existing "updates badges after an edit" test's own click positions), then forces the geometric precondition itself via a temporary inline `transform: translate(...)` on "Waypoint 2" — deliberately not the newly-added third waypoint, which becomes the new Finish and is styled white-with-dark-border, visually identical to a badge and therefore useless for a "whose colour is on top" comparison — restored in `finally`. A new `haveStackingSafeAncestry` helper walks both elements' ancestor chains to their common ancestor and confirms nothing in between establishes its own stacking context (a non-static position with non-auto z-index, sub-1 opacity, a transform, a filter, isolation, a non-normal mix-blend-mode, or a layout/paint-affecting will-change/containment) before the numeric z-index comparison is trusted; only once that holds does the test sample the real composite pixels in the forced overlap region and confirm the waypoint's own orange fill, not the badge's white/dark, is what actually painted on top. The same ancestry helper is reused for a second, lighter test proving the badge's z-index sits below every real always-on Planning overlay (`.map-attribution`, `.planning-map-controls`, `.planning-map-zoom-controls`) and that `pointer-events: none` is retained.
    - **Two genuine test-infrastructure bugs found and fixed while building this evidence, neither a production defect.** `pickNonOverlappingBadge`'s viewport-boundedness check compared a candidate badge's box against `page.viewportSize()` — the whole browser viewport, which includes Planning's own sticky header above the map. After a real 90° bearing rotation, a badge's projected position moved above the map's own top edge into the header area (confirmed directly via `document.elementsFromPoint()`, landing inside the "Plan" nav button's own icon) while still passing a page-viewport-only check; fixed by bounding against the map container's own `boundingBox()` instead. Separately, the rotation test's original mouse-drag gesture through MapLibre's `DragRotateHandler` was replaced with the same deterministic `Shift+ArrowRight` `KeyboardHandler` technique `planning.spec.ts`'s own `establishManualRotationAndPitch` already uses for the identical, previously-documented reason (CLAUDE.md future-backlog item 21's drag/inertia flakiness class) — investigated and ruled out as the actual cause here (the failure was proven identical and fully reproducible with keyboard-only rotation too, before the real cause — the viewport-vs-map-container bug above — was found), but kept as the safer, already-proven mechanism regardless; the gesture's own rotation amount was also reduced from a full ~90° to a single ~15° press once measurement showed a full 90° turn pushed this particular wide/short fallback route's remaining badges outside the map area's own shallow aspect ratio entirely, independent of any stacking defect.
    - **Free roam confirmed empty, in the screen that actually shows the assertion's context.** `e2e/freeRoam.spec.ts`'s existing "Start free roam shows a live position" test gained a zero-`.distance-badge-marker`-count and zero-matching-accessible-name assertion — free roam already passes an empty `points` array to `MapView`, so this closes a genuinely uncovered absence claim rather than duplicating existing coverage.
    - **Android-Chromium-emulated coverage, proportionate to the one new claim it needs to add.** New `e2e/androidDistanceBadges.spec.ts` repeats only the normal-imagery real-paint proof under the `android-chrome` project's `Pixel 7` emulation (mobile viewport, touch UA, `deviceScaleFactor` 2.625) — not a second full re-proof of every desktop scenario, mirroring the existing `androidPlanning.spec.ts`/`androidRiding.spec.ts` narrow-scope-per-file convention. Uses an in-browser PNG decode (`Image()` plus a throwaway `<canvas>`) fed a base64-encoded Playwright screenshot buffer, rather than duplicating the ~90-line Node-side decoder a third time for one assertion — and deliberately never calls `canvas.toDataURL()` on MapLibre's own WebGL canvas, which would not reflect genuine composited output under `preserveDrawingBuffer: false`; the only canvas this file's own decode touches is a fresh one that merely decodes the already-composited screenshot PNG bytes, which has no such limitation.
    - **Files changed.** Production: `src/index.css` (the paired `z-index` values and the corrected/relocated comment only — no other file). Test: `e2e/distanceBadges.spec.ts` (substantially extended: the region-based `verifyBadgePaint`/`pickNonOverlappingBadge`/`haveStackingSafeAncestry`/`waitForCameraSettled` helpers, and new or paint-extended tests across normal imagery, the fallback background, zoom-band crossing, the forced waypoint/badge overlap, control/attribution precedence, rotation, retry and Riding), `e2e/freeRoam.spec.ts` (one new absence assertion in an existing test); new `e2e/androidDistanceBadges.spec.ts`. `package.json`, `package-lock.json`.
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean (lint required two follow-up fixes: an `import()` type annotation replaced with a top-level type import, and two arrow-function-shorthand void-expression returns given explicit braces). `npm test` — 2932/2932 across 147 files, unchanged from item 83's own baseline (no unit tests were added or changed by this item); one `src/gpx/parseAcnExtension.test.ts` timeout was observed on a full-parallel-load run, confirmed a pre-existing CPU-contention flake unrelated to this item's own files by passing cleanly in isolation and by reproducing identically on a `git stash`-clean baseline run before this item's changes were restored. `npm run e2e` — 224/224 across both the `chromium` and `android-chrome` projects, with the full `distanceBadges.spec.ts` suite and the new `androidDistanceBadges.spec.ts`/`freeRoam.spec.ts` assertions additionally re-run several times in isolation with no flakes observed. `npm run format:check` run last, immediately before committing — clean. The primary fail-first regressions (normal-imagery and fallback-background paint) were confirmed failing against the unmodified `z-index: -1` tree for the predicted reason before the production fix, and confirmed passing once it landed; a temporary runtime `style.zIndex` override, never committed, additionally isolated the property itself as the causal mechanism ahead of the permanent CSS change. Version bumped to 0.3.71 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed, and Playwright's `android-chrome` project is Chromium emulation of a mobile viewport/UA/DPR, not real Android Chrome or WebView. Real-device acceptance remains outstanding: readable badges in both Planning and route Riding on the installed iPhone PWA and physical Android Chrome; restrained 1/5/10/20 km density at close, ordinary, wide and very-wide real zoom levels; readability over both bright and detailed real map imagery; fixed absolute labels while genuinely zooming and while real ride progress advances; passed-badge disappearance during a genuine ride; portrait/landscape rotation, real pitch/bearing changes and real device text scaling; fallback and imagery-retry behaviour under a genuine failure; waypoint, start/finish marker, control, attribution and climb-cue precedence in real use; and explicit confirmation that free roam shows none. See `docs/project/current-status.md` for the tracked outstanding item.
    - **Follow-up (2026-08-27, v0.3.72): active-Riding badge density decoupled from whole-route length.** Real-device confirmation after v0.3.71 closed the narrow "badges are visibly painted on the installed iPhone PWA" point (see current-status.md's dated entry below), but exposed a genuine density defect this item's own scope never addressed: `selectDistanceBadgeIntervalMetres` always ran against the _whole_ route length before active-Riding's progress filter ever ran, so a long route at a close, useful zoom could still escalate to a coarse (5/10/20 km) interval — e.g. a 45 km route at zoom 16 escalated to 5 km spacing, leaving only two sparse badges ahead of a mid-route rider — even though, once actually riding, only the next handful of upcoming kilometres matter. Fail-first proof: a test driven through the real Riding path (MapView's badge effect and, separately, RidingScreen's own restored-idle-to-active transition) confirmed exactly this against the unmodified tree — `["35", "40"]` instead of the desired ten consecutive 1 km badges — before any production change landed.
      - Added a second, wholly separate zoom-only spacing table for active-Riding only (`selectActiveUpcomingBadgeSpacingMetres`, `src/map/distanceBadgeLayer.ts`): `<=8` zoom → 20 km, `9`–`11` → 10 km, `12`–`13` → 5 km, `14` → 2 km (a new interval, not part of the existing 1/5/10/20 km family), `>=15` → 1 km — banded differently from the whole-route table, which folds zoom 14 into its existing 5 km band. Guarded explicitly with `if (!Number.isFinite(zoom)) return 20000;` at the top (not merely relying on the fallthrough comparison chain): without it, `+Infinity` would incorrectly satisfy `zoom >= 15` and wrongly select 1 km, unlike the whole-route selector's own zoom bucketing where every comparison already happens to fail safely for non-finite input. Never takes route length as a parameter at all.
      - Added a rolling cap of the next ten rendered (post-merge) badges (`MAX_ACTIVE_UPCOMING_DISTANCE_BADGES = 10`), applied through the same shared `place → filterActiveRidingCandidates → mergeCoincidentDistanceBadges → cap` pipeline (`buildDistanceBadgeMarkerSpecs` gained an optional fourth `maxCount` parameter, defaulting to the whole-route cap so every existing call site is unaffected) via a new `buildActiveUpcomingDistanceBadgeMarkerSpecs` entry point. The cap counts merged specs, never raw pre-merge candidates, so a coincident loop/out-and-back pair still costs only one of the ten slots. `MAX_ACTIVE_DISTANCE_BADGES` — always the whole-route cap despite its name — is renamed `MAX_WHOLE_ROUTE_DISTANCE_BADGES` now a real, differently-valued sibling constant exists; `selectDistanceBadgeIntervalMetres`, `filterActiveRidingCandidates`, `mergeCoincidentDistanceBadges` and `placeDistanceBadgeCandidates` are reused entirely unmodified.
      - Active mode with a still-null/non-finite reliable progress (GPS/matching starting just after Start/Resume) falls back to the exact whole-route policy — family, escalation, cap — rather than fabricating a zero-progress window, switching over only once genuine progress exists. This fallback deliberately lives inside `buildActiveUpcomingDistanceBadgeMarkerSpecs` in `distanceBadgeLayer.ts`, not in `MapView.tsx`'s effect, keeping every zoom-threshold/cap/fallback decision in one pure, unit-testable place — `MapView` only ever decides _which_ function to call, never _what policy applies_.
      - A new explicit `MapView` prop, `badgeDensityMode: "whole-route" | "active-upcoming"` (default `"whole-route"`), replaces the previous implicit "progress is non-null" inference. `RidingScreen` derives it from `nav.geolocationStatus !== "idle"` at its single always-mounted `<MapView>` call site — the ride is active as soon as Start/Resume flips the GPS watch on, even before the first reliable fix, while a paused/restored _idle_ ride can already carry a non-null frozen `presentationDistanceFromStartMetres` that must not switch it into active density. Planning, the pre-ride overview and free roam needed zero changes, since the new prop's default preserves their existing whole-route behaviour byte-for-byte; the badge effect's whole-route branch is a verbatim copy of the pre-existing body, and neither branch calls `fitBounds`/`setCamera`, preserving the existing "badge recompute never triggers a camera fit" invariant.
      - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2960/2960 across 147 files (up from item 84's own 2932/2932 baseline; +28 new tests: 20 in `distanceBadgeLayer.test.ts`, 6 in `MapView.test.tsx`, 2 in `RidingScreen.test.tsx`). `npm run e2e` — 226/226 across both the `chromium` and `android-chrome` projects (up from 224/224; +2 new Riding tests, plus the existing Riding test strengthened in place), with `e2e/distanceBadges.spec.ts` additionally re-run three times over under full parallel load with no flakes. One unrelated flake was observed in a full-suite run — `e2e/ridingFinishAndEnd.spec.ts`'s already-monitored completion-detection test (queue item 32) — reproduced identically on a `git stash`-clean pre-change baseline under the same load and passed reliably 3/3 in isolation, confirming it predates and is unrelated to this follow-up. `npm run format:check` run last, immediately before committing — clean. Version bumped to 0.3.72 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched).
      - This follow-up ships with automated evidence only. The real-device "badges paint at all" point is now closed (see current-status.md), but the new active-only density/rolling-window behaviour — the 1/2/5/10/20 km spacing actually reading well and the rolling window feeling natural rather than jarring while genuinely riding, and correct behaviour through the brief awaiting-first-fix window after Start/Resume — has not yet been checked on a real device.

---

<a id="item-85"></a>

## Item 85 — Simplify active Full/2 km/10 km feature inspection — done

_Category: Active-Riding elevation/profile presentation_

85. **Simplify active Full/2 km/10 km feature inspection — done**
    - Field observation: item 80 moved active Climb view and the manual upcoming-climb preview to the compact `Local gradient colours on this climb` disclosure but left active Full/2 km/10 km on the older, combined `GradientColoursDisclosure`. Worse, `microDetailFeature = selectedFeature ?? activeFeature` meant _merely being physically on_ a recognised climb or descent — with no tap at all — already drove local-gradient micro-band chart colouring and auto-opened `RouteFeatureDetailsPanel`'s full pre-ride-grade analytical fact set (detail chart, local-gradient disclosure, max/steepest gradient, climb score) inside the fixed, glanceable active-Riding pane. This item makes Full/2 km/10 km answer "what terrain lies ahead?" with macro colouring only, opening one compact summary solely on an explicit tap.
    - **Component boundary: a new sibling component, not a `RouteFeatureDetailsPanel` variant.** `src/ui/riding/RidingSelectedFeatureSummaryPanel.tsx` is rendered by `RidingScreen` instead of `RouteFeatureDetailsPanel`, only in the new active-standard branch — chosen over threading a suppression prop through the shared component, since `RouteFeatureDetailsPanel` is reused byte-identically by Planning's `RouteSummaryPanel` and by Riding's own pre-ride/Climb/preview paths, none of which may change; a wrapper keeps that shared contract untouched and avoids any risk of the new compact facts accidentally leaking into (or the full facts leaking out of) those other callers. `aria-label="Selected feature summary"` is a deliberately distinct region name from `RouteFeatureDetailsPanel`'s own `"Route feature details"`, so tests (and assistive technology) never conflate the two. Reuses `GradientColourSwatch`/`ROUTE_FEATURE_COLOURS`/`ROUTE_FEATURE_LABELS` for the heading exactly as `RouteFeatureDetailsPanel` does, and the established `routeSummary.ts` formatters (`formatDistanceKm`, `formatGradientPercent`, `formatDistanceKmValue`) plus one small addition, `formatDescentLoss` (mirrors `formatAscent`'s shape, `"224 m loss"`), for the descent counterpart of `formatAscent`.
    - **Ahead/within/passed derivation: a new pure function, not inline JSX branching.** `computeFeatureRelativePosition(feature, presentationDistanceFromStartMetres)` in `src/navigation/routeFeatureDetail.ts` (the existing generic-over-`RouteFeature` home already hosting `buildFeatureDetailSegments`) returns `{kind:"ahead"|"within"|"passed"; ...}` or `null`. "Within" is inclusive of both bounds, mirroring `findFeatureAtDistance`'s own convention. `RidingScreen` calls it once, always against `nav.presentationDistanceFromStartMetres` — the same frozen/reliable value every other Riding presentation fact already uses — never raw/live progress, so wording stays correct through stale-fix and off-route freezing with zero new navigation/projection logic. Wording lives in the component, reusing established precedent verbatim: `` `Starts in ${formatDistanceKm(...)}` `` (exact `RidingClimbPreviewPanel` convention) for ahead, `` `${formatDistanceKm(...)} remaining` `` (exact `RidingClimbCue` convention) for within, and a new `` `Passed ${formatDistanceKm(...)} ago` `` for passed — the one piece of genuinely new product copy this item required, confirmed with the user before implementation. A `null` relative position (no presentation distance yet, e.g. a selection made before the first GPS fix) omits the clause entirely rather than fabricating one.
    - **The standard-view gate: `isActiveStandardFeatureView`, mirroring an existing implicit boundary rather than inventing a new one.** `const isActiveStandardFeatureView = nav.geolocationStatus !== "idle" && effectiveElevationView.kind !== "climb" && effectiveElevationView.kind !== "climb-preview"` — computed once, immediately after `effectiveElevationView` itself. Drives four changes in `RidingScreen.tsx`:
      - the chart-branch disclosure ternary's final `else` arm (previously `GradientColoursDisclosure`) is simply deleted, becoming `null` — the `GradientColoursDisclosure` import is removed from this file entirely (the component itself is untouched, still used by Planning's `RouteSummaryPanel`);
      - all three standard-view `ElevationChart` call sites (`matchedDistanceFromStartMetres === null`, `Full`, and the rolling window) pass `gradientSegments={isActiveStandardFeatureView ? undefined : ...}` instead of the segments unconditionally — omitting the prop (confirmed already-existing `ElevationChart` behaviour, no component change needed) leaves the chart macro-colour-only, which is what actually stops "merely occupying a climb" from painting local-gradient bands;
      - `handleChartTapDistance` passes `isActiveStandardFeatureView ? null : microDetailFeature` (and `[]` for segments) into the existing, unmodified `resolveElevationChartTap` — a `null` feature makes that function's own inner "segment" guard structurally unreachable, so a tap always degrades to macro select-or-noop, with zero changes to `src/navigation/routeFeatures.ts`;
      - the feature-panel render site becomes `isActiveStandardFeatureView ? <RidingSelectedFeatureSummaryPanel feature={selectedFeature} .../> : <RouteFeatureDetailsPanel .../* verbatim existing content */>` — using `selectedFeature` (purely a function of the rider's own explicit tap/map-select) rather than `microDetailFeature` (which also includes the merely-active feature) is the actual fix for "merely occupying auto-opens a summary"; `GradientSegmentDetailsPanel` is wrapped `{!isActiveStandardFeatureView && (...)}`, i.e. made conditional rather than removed, since it's still used pre-ride and by Climb/preview.
    - **A genuine stale-selection leak found and fixed along the way.** `chartSelectedRangeMetres` (the chart's macro emphasis-ring range) previously prioritised `selectedGradientSegment` unconditionally over `selectedFeature`. Since dismissing Climb view to a standard view does not itself clear `selectedGradientSegment` (only `selectRouteFeature`/`handleClearRouteFeatureSelection`/`handleClearGradientSegmentSelection` do), a segment picked earlier while genuinely in Climb view could otherwise leak its own narrow range into the emphasis ring after the rider dismissed to Full/2 km/10 km. Fixed by guarding that branch with `!isActiveStandardFeatureView &&` — `selectedGradientSegment` itself is left un-cleared (no new imperative reset), simply becoming inert while a standard view shows, matching this codebase's existing derivation-over-clearing style. `displayedMicroDetailFeature` (the local variable that fed the now-deleted `GradientColoursDisclosure` arm) became dead code once that arm was removed and was deleted along with its two reassignment sites.
    - **`Clear selection`: extracted into a shared component, not just a shared class.** `src/ui/shared/ClearSelectionButton.tsx` (`{onClick}` → `<button type="button" className="clear-selection-button">Clear selection</button>`) replaces the three previously independent, byte-identical, unstyled `<button>` implementations in `RouteFeatureDetailsPanel.tsx`, `GradientSegmentDetailsPanel.tsx`, and the new `RidingSelectedFeatureSummaryPanel.tsx` — guaranteeing any future CSS fix applies everywhere the action renders, by construction, rather than three call sites kept in sync by hand.
    - **The border defect: investigated live in a real browser; not reproduced; not guessed at.** Built a throwaway diagnostic Playwright spec (build + `npm run preview`, real Chromium) and drove the button into both the pre-ride `RouteFeatureDetailsPanel` context and the new active `RidingSelectedFeatureSummaryPanel` context inside `.ride-profile-pane--immersive`, at baseline (390×844, DPR 1), an iPhone-like DPR of 3, and 200%-enlarged text (the last of which genuinely overflows the pane — `scrollHeight` 985 vs `clientHeight` 476 — a real, not hypothetical, scroll-clipping scenario). In every one of these states the button's own `border-bottom` (`1px solid`, confirmed via `getComputedStyle`) painted fully intact in cropped screenshots, and `scrollHeight === clientHeight` exactly at ordinary sizes (no fractional overflow for Chromium to silently crop). The reported defect could not be reproduced anywhere this project's available Chromium-based tooling can reach — consistent with a real-iOS-Safari-specific rendering quirk (WebKit's own known differences in overflow/border-radius/DPR compositing), which this sandbox has no way to exercise. Per this item's own explicit instruction not to guess: **no border CSS change was made**. The one fix that _is_ independently justified regardless of the border's own cause — `:focus-visible`'s default outward `outline-offset: 2px` sitting on exactly the same last-child-inside-`overflow-y:auto` structure item 76 already proved gets clipped — was applied: `.ride-profile-pane--immersive .clear-selection-button:focus-visible { outline-offset: -2px; }` in `src/index.css`, mirroring `.elevation-window-button:focus-visible`'s own item-76 rule verbatim. The global `button {}`/`button:focus-visible` rules are untouched, and no margin/padding was added anywhere. The border defect itself remains an open, explicitly tracked real-device item (`docs/project/current-status.md`), not silently declared fixed.
    - **Tests.** New: `src/navigation/routeFeatureDetail.test.ts` gained a `computeFeatureRelativePosition` describe block (6 tests: null distance, ahead, within at each inclusive bound, passed). `src/ui/shared/routeSummary.test.ts` gained a `formatDescentLoss` describe block (2 tests). `src/ui/shared/ClearSelectionButton.test.tsx` (new, 2 tests: role/name/class, click). `src/ui/riding/RidingSelectedFeatureSummaryPanel.test.tsx` (new, 7 tests: null feature, climb fields, descent fields, passed wording, omitted-relative-position, absence of every forbidden analytical fact, clear-control gating). `src/ui/riding/RidingScreen.test.tsx` gained a new `describe("active standard-view feature selection (item 85)")` block (6 tests, using a local copy of the two-climb fixture already proven by `describe("current-climb elevation view")`, per this file's own each-block-owns-its-fixture convention): no disclosure in 2 km/10 km; macro colouring before/after selection with only the permitted fact set; no summary from a tap on ordinary geometry; ahead-state wording frozen through three repeated strongly-off-route fixes; an honest passed state after an on-route (non-off-route) advance past the selected feature's own end; Clear selection preserving the chosen window with unchanged `setCameraSpy`/`fitBoundsSpy` call counts. Six existing `RidingScreen.test.tsx` tests were rewritten fail-first (confirmed failing against the unmodified tree for the predicted reason, then confirmed passing once the production change landed), each previously encoding the exact "merely occupying auto-opens a summary" or "chart tap drills into a local-gradient segment" behaviour this item supersedes: the default-windowed/Full macro-colouring test (now proves no auto-opened disclosure/summary while still asserting macro colour persists); the chart-tap-drills-into-segment test (now proves the same tap opens the compact summary, never `GradientSegmentDetailsPanel`); the mid-ride `Try again`-retry preservation test (now proves the compact summary, not a segment panel, survives); two tests literally named for asserting `"Gradient colours"` was present post-Start/in Full view (now assert its absence); and the "never activates Climb view for a recognised descent" test (previously asserted the heading appeared from mere GPS occupancy alone; now proves it's absent until an explicit tap). One further test (`"a stale explicit selection elsewhere does not leak into the preview's details panel"`) had its standard-view-half region query updated from `"Route feature details"` to `"Selected feature summary"`; its Climb-preview half, which item 85 leaves unchanged, was untouched. New `e2e/ridingSelectedFeatureSummary.spec.ts` (8 tests): dismissing Climb view to Full while still on the climb shows no disclosure/summary, then an explicit tap opens the compact summary with only the permitted facts (and none of the forbidden ones, including asserting exactly one elevation-profile chart on screen); a tap on the dip between the two recognised climbs opens nothing; a `describe` block of five border/focus-paint checks (normal text/light, normal text/dark via `page.emulateMedia`, short landscape, 200% enlarged text with a genuine bounded-scroll assertion, and the pre-ride `RouteFeatureDetailsPanel` context) each combining a cropped-screenshot capture with `getComputedStyle`/`getBoundingClientRect` evidence rather than a bare DOM-presence assertion, plus a real-Tab-navigation `:focus-visible` outline-offset check (scripted `.focus()` does not reliably engage Chromium's `:focus-visible` heuristic, mirroring `ridingClimbView.spec.ts`'s own documented gotcha); and Clear selection preserving the chosen Full window. The full existing `android-chrome` project suite (14 tests across 7 files) was re-run unmodified and confirmed passing — this item added no new Android-chrome-project-specific test, following item 80's own precedent for this exact kind of clipping/paint nuance (real Android/iPhone acceptance tracked separately, see below), since the existing suite's own fixtures (e.g. `smoke-route.gpx`, used across many unrelated assertions in `androidMobileLayout.spec.ts`) have no recognised climb/descent to select and widening that file's scope was judged out of proportion to the marginal evidence gained over the desktop-Chromium coverage already added.
    - **Files changed.** Production: `src/navigation/routeFeatureDetail.ts` (`FeatureRelativePosition`, `computeFeatureRelativePosition`), `src/ui/shared/routeSummary.ts` (`formatDescentLoss`), `src/ui/shared/ClearSelectionButton.tsx` (new), `src/ui/shared/RouteFeatureDetailsPanel.tsx`, `src/ui/shared/GradientSegmentDetailsPanel.tsx` (both switched to `ClearSelectionButton`), `src/ui/riding/RidingSelectedFeatureSummaryPanel.tsx` (new), `src/ui/riding/RidingScreen.tsx`, `src/index.css`. Test: `src/navigation/routeFeatureDetail.test.ts`, `src/ui/shared/routeSummary.test.ts`, `src/ui/riding/RidingScreen.test.tsx`; new `src/ui/shared/ClearSelectionButton.test.tsx`, `src/ui/riding/RidingSelectedFeatureSummaryPanel.test.tsx`, `e2e/ridingSelectedFeatureSummary.spec.ts`. `package.json`, `package-lock.json`.
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2895/2895 across 146 files (23 net new tests over item 81's 2872/144 baseline). `npm run e2e` — 212/212 across both the `chromium` (198) and `android-chrome` (14, unmodified) projects; one unrelated, already-tracked pre-existing flake (`ridingFinishAndEnd.spec.ts`'s completion-detection test, item 32) was observed once under full-parallel-load contention and confirmed, by an immediate isolated rerun (4/4 clean) and by this file being completely untouched by this diff, to be the same pre-existing class of flake rather than a regression. `npm run format:check` run last, immediately before committing — clean. The six rewritten `RidingScreen.test.tsx` tests were confirmed failing against the unmodified tree, for the predicted reason, before any production change; confirmed passing once the production diff landed. Version bumped to 0.3.66 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed. Real-device acceptance remains outstanding: the compact active-standard summary's legibility and glanceability while mounted and moving; the ahead/within/passed wording's clarity at a glance; and, specifically, the reported `Clear selection` bottom-border defect on a real iPhone — this item investigated it thoroughly in the available Chromium-based tooling (baseline, DPR 3, and genuinely-overflowing 200% text all painted the border fully intact) but could not reproduce it, applied only the one independently-justified `:focus-visible` fix, and leaves the border itself an open, explicitly tracked real-device diagnosis item rather than a guessed-at one. See `docs/project/current-status.md` for the tracked outstanding item.
    - **Follow-up (2026-08-26, v0.3.67): the border defect measured directly from a real device screenshot, not guessed at.** A new installed-iPhone screenshot (`docs/project/evidence/item-85/clear-selection-bottom-border-iphone-2026-08-26.png`, 1170×2532, DPR 3) still showed a perceptibly narrowed/lighter lower border on `Clear selection`. Two hypotheses were formed and tested against direct measurement before any CSS was touched, and one was rejected by its own evidence rather than shipped:
      - **Rejected: ancestor scrollport coincidence.** A first hypothesis proposed that the button's border sat exactly at `.ride-profile-pane--immersive`'s own `overflow-y: auto` clip boundary, with a `padding-block-end` fix. A throwaway diagnostic script (build + `vite preview`, real Chromium, driving the exact screenshot scenario — Riding → Profile → Full → tap a climb) measured `getBoundingClientRect()` for both the button and the pane directly: a **65.5px gap** between the button's bottom border and the pane's clip edge, at both DPR 1 and DPR 3 — not coincident at all. The hypothesis was rejected by this measurement, and the proposed fix (which would also have manufactured 8px of permanent scroll and required weakening `ridingElevationWindows.spec.ts`'s `scrollHeight <= clientHeight` invariant to hide it) was abandoned before any file was changed.
      - **Measured directly from the real screenshot, pixel by pixel.** Cropping the actual PNG at native resolution and sampling raw RGB values column-by-column along each button's flat/straight border segment (avoiding rounded corners and text glyphs) gave a precise, reproducible comparison table:

        | Button              | Top border (device-px rows)                    | Bottom border (device-px rows)                 | Symmetric?           |
        | ------------------- | ---------------------------------------------- | ---------------------------------------------- | -------------------- |
        | **Clear selection** | 3 rows, `118,118,118` (every column x=90..434) | **2 rows**, `118,118` (every column x=90..434) | **No — 1 row short** |
        | Pause               | 3 rows                                         | 3 rows                                         | Yes                  |
        | End ride            | 4 rows (antialiased red)                       | 4 rows, mirrored                               | Yes                  |
        | 2 km                | 3 rows                                         | 3 rows                                         | Yes                  |
        | Map                 | 3 rows                                         | 3 rows                                         | Yes                  |

        The rounded corners of Clear-selection itself render close to mirror-symmetric (left corner top profile `201,118,118,124,240` vs. bottom `240,124,118,118,201` — same values, reversed order) — the defect is confined to the straight segment, not the corners. Nothing overlaps or crowds the border; the Map/Profile switcher's own divider sits ~53px below it.

      - **What this rules in/out, kept separate from cause.** Ruled out: a page-wide device-pixel grid offset (four other buttons across three different regions of the same screen render symmetrically — this is not a global rendering issue) and the ancestor-scrollport coincidence above (already directly disproven). Consistent with, but **not proven**: something specific to Clear-selection's own instance — of the five buttons sampled, it is the only one whose immediate DOM predecessor is a long run of dynamic, variable-height text (the climb heading, `Passed … ago · … · … average`, and `Route position: …` lines, which can wrap differently depending on content), unlike the statically-laid-out header, elevation-window row, and bottom nav. Confirming this would require real Safari/WebKit devtools layout inspection on the device itself, which this sandbox cannot do (no cached `webkit` Playwright browser, no network egress to fetch one, Linux host) — it remains an explicit **hypothesis about cause**, not a settled mechanism.
      - **The fix shipped: an internal-stroke treatment, not a proven correction.** `src/index.css` gained an unscoped `.clear-selection-button { border-color: transparent; box-shadow: inset 0 0 0 1px var(--colour-border); }`, mirroring this file's own existing inset-ring idiom (`.elevation-window-button.is-selected`'s `box-shadow: inset 0 0 0 2px`, two rules away) at the border's own existing 1px weight, not a new value. This is zero-layout-risk (`border-color` alone never changes `border-width`; `box-shadow` never participates in box sizing) and applies uniformly wherever the shared class renders — active immersive and idle/pre-ride alike — by construction, with one rule, no ancestor/layout/spacing change anywhere. It is deliberately **not claimed to fix the measured asymmetry**: the button's bottom edge position is still derived the same way (`top + height`) whether painted via `border` or `box-shadow`, so if the true cause is a fractional device-pixel vertical origin, box-shadow could in principle exhibit the same asymmetry. It is shipped anyway because it changes the rendering primitive for a border already proven, by the table above, to rasterise asymmetrically on this specific box, at zero risk to anything else.
      - **Tests.** `e2e/ridingSelectedFeatureSummary.spec.ts`'s `expectBottomEdgePainted` helper was rewritten to add genuine pixel evidence rather than only computed-style/bounding-box checks: a small project-owned PNG decoder (`zlib.inflateSync` plus manual scanline unfiltering, 8-bit colour types 2/6, no interlace — built and validated against both a Chromium screenshot and the real iPhone PNG during this diagnosis) crops exactly to the button's own border box, resolves the visible edge's actual colour from computed style (`borderColor` if opaque, else parsed from `boxShadow`'s `rgb(...)` component — necessary now that the colour lives in `box-shadow`), and counts consecutive matching-colour rows from the top and separately from the bottom at a column safely inside the straight segment, asserting the two counts are equal. A genuine implementation pitfall was hit and fixed while writing this: `boundingBox()`'s fractional CSS coordinates do not always align exactly with the screenshot's rasterised device-pixel rows (confirmed empirically — assuming row 0 of an exact-boundingBox crop was the border's own first row produced false zero-row failures on every test), so the helper scans a short, bounded distance inward from each edge to find the border's first matching row before counting its run, rather than assuming the crop's edge and the border's edge coincide. A new, separate fail-first test (`"Clear selection paints its visible edge via an inset box-shadow, not border-color"`) asserts the mechanism itself — `border-color` resolves transparent, `box-shadow` contains `inset` with `0px 0px 0px 1px` offsets/blur/spread and the expected colour (parsed component-by-component, not matched against the full serialised string, since a browser's computed-style ordering of an inset box-shadow's parts is not guaranteed stable) — confirmed failing against the pre-fix tree for the predicted reason (opaque `border-color`, no `box-shadow`), then passing once the CSS change landed. Both this and all five existing border/focus-paint checks (light/dark/short-landscape/200%-text/pre-ride) pass unchanged after the fix; Chromium does not reproduce the real screenshot's measured asymmetry either before or after this change, so a pass here is non-regression evidence only, not proof the real-device defect is fixed.
      - **Files changed.** Production: `src/index.css`. Test: `e2e/ridingSelectedFeatureSummary.spec.ts`. Evidence: `docs/project/evidence/item-85/clear-selection-bottom-border-iphone-2026-08-26.png` (newly tracked). Docs: this file, `docs/project/current-status.md`. `package.json`, `package-lock.json`.
      - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test`, `npm run e2e`, `npm run format:check` (last) — see this follow-up's own commit for exact counts. Version bumped to 0.3.67.
      - Real-device acceptance stays **open**: this sandbox cannot run WebKit/Safari (no cached browser, no network egress, Linux host), so this follow-up's own verification is necessarily limited to the measured comparison above, the mechanistic reasoning for the fix, and a clean Chromium non-regression gate. Confirmation that the fix actually resolves the defect — or a further diagnostic, such as a screenshot of Clear-selection after selecting a feature whose preceding text wraps to a different number of lines, to help confirm or refute the dynamic-preceding-content hypothesis — awaits a new post-deployment iPhone screenshot. See `docs/project/current-status.md` for the tracked outstanding item.
      - **Further follow-up (2026-08-26): the border-specific real-device check closes.** After deployment of the v0.3.67 inset-`box-shadow` change above, the user checked the installed iPhone PWA and reported: "Looks good now and the bottom line is not truncated anymore." This closes the border-specific real-device acceptance point for item 85 — the `Clear selection` lower edge now appears complete and is no longer visibly truncated on a real device. This confirms the shipped paint change produced the desired observed result; it does not itself prove which of the two earlier hypotheses (a page-wide device-pixel offset, ruled out; a fractional/dynamic-preceding-content cause, never confirmed) was the true underlying mechanism, which remains unconfirmed. Item 85's other outstanding real-device checks — the compact active-standard summary's legibility and the ahead/within/passed wording's clarity while mounted and moving, confirmation that merely riding through a recognised climb or descent never auto-opens the summary on a real device, and physical Android acceptance — remain open; see `docs/project/current-status.md` for the live ledger.

---

<a id="item-86"></a>

## Item 86 — Release-readiness audit and hardening roadmap — done

_Category: Investigation, security/privacy/reliability/maintainability audit and planning_

86. **Release-readiness audit and hardening roadmap — done**
    - **Why commissioned.** After 85 completed backlog items and no version bump strictly required by "amount changed since 0.3.0," the user asked for a candid, evidence-based release-readiness audit rather than a momentum-based one: how stable, secure, maintainable and release-ready is the PWA actually, separating confirmed defects from structural smells, hardening opportunities, untested possibilities and accepted product constraints — and, from that, a small ordered set of justified follow-up slices, not the longest possible risk list.
    - **Audited baseline.** Commit `f8c38b9e50c42e0792286e803f72299cd763e884` ("Keep route-switch choices with the selected card (item 73 follow-up)"), package version `0.3.73`, confirmed as the live `origin/main` tip both at the start of the audit and again immediately before its own documentation commit, with a clean working tree throughout.
    - **Scope.** Reproducing the full quality gate; a read-only dependency/supply-chain audit (production vs. development dependencies kept strictly separate); a `.github/workflows/deploy-pages.yml` CI/supply-chain review; an ACN-specific application security/privacy threat review (GPX/extension parsing, the OpenRouteService key's storage and transport, error redaction, DOM-injection surfaces, CSP feasibility); a data-integrity/persistence review (Dexie schema/migrations/atomicity/storage-health); a reliability/lifecycle review (camera-command dedup, off-route classification, route-completion arming, service-worker update deferral, the route-switch/session coordinator); a test-strategy/coverage/browser-device analysis, including a full whole-project Vitest coverage baseline; an architecture/maintainability review (size, git churn, hook density, confirmed import cycles); a performance/bundle-size review; and an accessibility/interface-resilience review. Explicit non-goals, held to throughout: no production TS/TSX/CSS/HTML/PWA-config change, no dependency or lockfile change, no `npm audit fix`, no CI/Dependabot/CodeQL/branch-protection change, no CSP implementation, no coverage threshold, no refactor, no version bump, and no implementation of any item this audit itself created.
    - **Verification performed.** Full local gate reproduced from a clean `npm ci` under this environment's own resolved toolchain (Node `v24.13.0`/npm `11.6.2`, older than the repository's pinned `24.18.0`/`11.16.0` — no matching version-manager install was found, none was installed for this task, and every local result is labelled non-equivalent rather than presented as CI-identical): `format:check`, `lint`, `typecheck`, and a production `build` stamped with the audited commit's own `APP_BUILD_SHA` all passed; `npm test` — **2,992/2,992 across 147 files**; `npm run e2e` — **230/230** (215 desktop Chromium + 15 Android-emulated). Both totals are an exact match to this repository's own item-73 history entry and to the independently-checked GitHub Actions run (#178) for this same commit, which the user inspected directly through the public GitHub web interface (this sandbox cannot reach the GitHub API) and confirmed as a full **Success** across its `verify`, `e2e` and `deploy` jobs. A full-project Vitest coverage baseline (`npx vitest run --coverage`) was also attempted and completed cleanly in one pass — 93.53% statement / 88.78% branch coverage overall — with no reproduction of a previously-observed instrumentation-overhead report against `parseAcnExtension.test.ts`'s intentional >10,000-manoeuvre boundary test.
    - **Headline conclusion.** No release-blocking defect was found. Production dependencies carry zero known `npm audit` advisories across all 32 resolved production-tree entries; the application's own security- and privacy-relevant code paths (geometry-digest-bound GPX extensions, header-only API-key transport, redacted error paths, absence of any DOM-injection sink) were traced directly against source and found consistently defensive, with dedicated tests backing every load-bearing claim checked. Five known advisories exist in transitive **development-only** dependencies (`brace-expansion`, `fast-uri`, `nanoid`, `postcss`, `undici`, all pulled in by ESLint/Vite-PostCSS/`vite-plugin-pwa`'s Workbox tooling/jsdom, none reachable in the shipped browser bundle), alongside ordinary CI-pinning and CSP-absence hardening gaps typical of a single-maintainer static-site pipeline. The one finding this audit treats as genuinely consequential rather than cosmetic: `src/App.tsx`'s route-switch/unfinished-session coordinator is simultaneously the most recently hot-churned production file in the repository (15 of its 19 all-time commit touches landed in roughly the last 90 commits) and the least test-covered one (79.78% statements against a 93.53% project average), with the coverage gap concentrated almost exactly inside that coordinator's own clear-failed/start-free-roam-failed/retry branches. No refactor of that coordinator is proposed by this audit — only closing that specific coverage gap first, per this project's own change-discipline rule that a refactor needs a named, already-characterised seam before it is justified.
    - **Deliverables.** [`docs/project/release-readiness-audit.md`](../release-readiness-audit.md) — the full 20-section report: executive conclusion, method, a nine-row risk register (no Critical or High finding), verified strengths, dependency/CI/threat/data-integrity/lifecycle/test/architecture/performance/accessibility analyses, a three-tier release-decision model (continued `0.3.x`, a proposed `0.4.0` gate, a future `1.0.0` gate), the roadmap below, and accepted risks/deferred decisions. Six new backlog items, 87–92, added to [`backlog.md`](../backlog.md) in dependency order: item 87 (dev-dependency advisory remediation — selected as the next implementation slice, being the smallest, highest-confidence, lowest-behaviour-risk item and the only one plausibly in the "fix before next release candidate" tier); item 88 (CI/supply-chain pinning and automated-advisory evaluation, deliberately kept separate from 87 so a failure in one is never hard to attribute to the other); item 89 (verification-baseline polish: a wired coverage-reporting script with risk-based floors, the two confirmed type-only import cycles, jsdom test-noise triage, and a WebKit-feasibility judgement); item 90 (a Content-Security-Policy compatibility-matrix investigation — feasibility only, no implementation); item 91 (characterization tests for `App.tsx`'s route-switch coordinator, closing the specific coverage gap the audit measured, as the explicit precondition for any future extraction); item 92 (a real `navigator.storage.estimate()`-based quota signal for the currently `db.open()`-only storage-health check).
    - **No runtime, lockfile, or workflow change.** This item's diff is documentation only: `docs/project/release-readiness-audit.md` (new), this history entry, `docs/project/backlog.md` (items 87–92 appended), `docs/project/README.md` (index entry), and root `CLAUDE.md`'s queue table. `package.json`, `package-lock.json`, and every `.github/workflows/*.yml` file are byte-identical to the audited commit.
    - **No version bump.** Following this project's established handling for investigation/documentation-only slices (see, for example, item 30's e2e-hardening follow-ups and the reverse-route/edit-copy save-race hardening items), package version stays `0.3.73` — the deployed application's bytes and behaviour are unchanged by this slice, so there is nothing for a version number to mark.
    - **Commit and push.** This entry deliberately does not record this slice's own eventual commit SHA or push confirmation, to avoid a commit describing itself before it exists — that information belongs in, and was reported in, the assistant's final hand-off to the user for this task, not here.

---

<a id="item-87"></a>

## Item 87 — Dev-dependency advisory remediation — done

_Category: Supply-chain / dependency hygiene (from the release-readiness audit, item 86)_

87. **Dev-dependency advisory remediation — done**
    - **Outcome.** `npm audit` (full graph) went from **5 vulnerable package entries (1 moderate, 4 high by npm's own package-level severity classification) comprising 10 distinct GHSA advisory records** to zero. `npm audit --omit=dev` stayed at zero throughout ("five advisories" is deliberately avoided here as ambiguous — `brace-expansion` and `undici` each carry multiple GHSA IDs under a single package-level entry).
    - **Baseline reproduced.** Against `origin/main` at `b5acbbedf15d9207453f71314571d76065676969` (one documentation-only commit past item 86's own audited commit, confirmed via `git diff --stat` to touch no source or test file), a fresh `npm audit --json` reproduced item 86's exact baseline: `brace-expansion` — GHSA-mh99-v99m-4gvg, GHSA-rgw5-rvv9-x895 (2 records, 2 lockfile nodes); `fast-uri` — GHSA-7p8r-x3mc-p8w7 (1); `nanoid` — GHSA-2v37-7h3g-55p8 (1); `postcss` — GHSA-fxqj-rqcc-2cmp (1); `undici` — GHSA-8xcm-r25x-g524, GHSA-4cwx-7wf7-3272, GHSA-m8rv-5g2x-5cg5, GHSA-jr45-8vmc-qm54, GHSA-v3r7-h72x-cjcm (5). Total: 5 package entries, 10 GHSA records. `npm audit --omit=dev --json` — 0. Dependency totals: 32 prod / 574 dev / 607 total (608 lockfile package entries including the root).
    - **Before/after versions.**

      | Package           | Node   | Before | After  | Parent range (unchanged) | Dependency path                                                                                                                                         |
      | ----------------- | ------ | ------ | ------ | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
      | `brace-expansion` | root   | 5.0.8  | 5.0.9  | `^5.0.5`                 | `eslint@10.7.0 → minimatch@10.2.5 → brace-expansion`                                                                                                    |
      | `brace-expansion` | nested | 2.1.2  | 2.1.4  | `^2.0.1`                 | `vite-plugin-pwa@1.3.0 → workbox-build@7.4.1 → @trickfilm400/rollup-plugin-off-main-thread → ejs → jake → filelist → minimatch@5.1.9 → brace-expansion` |
      | `fast-uri`        | —      | 3.1.4  | 3.1.6  | `^3.0.1`                 | `vite-plugin-pwa → workbox-build → ajv@8.20.0 → fast-uri`                                                                                               |
      | `nanoid`          | —      | 3.3.16 | 3.3.18 | `^3.3.16`                | `vite@8.1.5 → postcss → nanoid`                                                                                                                         |
      | `postcss`         | —      | 8.5.22 | 8.5.26 | `^8.5.17`                | `vite@8.1.5 → postcss`                                                                                                                                  |
      | `undici`          | —      | 7.28.0 | 7.29.0 | `^7.25.0`                | `jsdom@29.1.1 → undici`                                                                                                                                 |

      `package.json` required **no change** — every fixed version was confirmed, via `npm view "<pkg>@<parent's-declared-range>" version` before touching anything, to already satisfy its existing parent's current semver range.

    - **Environment.** This machine's ambient toolchain (Node `v24.13.0`, npm `11.6.2`) is older than the repository's pinned Node `24.18.0`/npm `11.16.0`, and no nvm/asdf install was available to get exact Node. `corepack` (already present, `0.34.5`) ran the exact pinned npm ad hoc via `corepack npm@11.16.0 <args>` without enabling global shims or touching system Node — confirmed to return `11.16.0` cleanly. Separately, this machine sits behind a corporate TLS-intercepting proxy (`cntlm` on `127.0.0.1:3128`, exported via `http_proxy`/`https_proxy`/`ftp_proxy`/`cntlm_proxy`); routed through it, `npm audit`/`npm ping` fail with `self-signed certificate in certificate chain`, which resembles "no network access" but isn't — unsetting those four variables lets npm reach the real registry directly and succeed cleanly. Every registry-touching command in this task ran with those variables unset and under `corepack npm@11.16.0`. Local script/gate execution (lint/typecheck/build/test/e2e) still ran on ambient Node `v24.13.0` — labelled, per item 86's own precedent, as a non-equivalent local reproduction rather than CI-identical; the pushed GitHub Actions run (exact Node `24.18.0`/npm `11.16.0`) is the corroborating final check.
    - **Resolution mechanism.** Before mutating anything, both candidate commands were dry-run and compared under the exact pinned npm: `npm update brace-expansion fast-uri nanoid postcss undici --package-lock-only --dry-run` and `npm audit fix --package-lock-only --dry-run`. (The `--package-lock-only --dry-run` combination itself printed an uninformative "up to date" summary for both — a reporting quirk of that specific flag pairing, not evidence of no-op; re-running the same two commands with `--dry-run` alone, which does preview a tree diff, showed both proposing the identical 6-package change set.) Applied for real (still `--package-lock-only`, so `node_modules` was left untouched pending a single clean `npm ci`), both `npm update <the 5 packages>` and `npm audit fix` independently produced a **byte-identical** `package-lock.json` diff, confirmed via direct file comparison. The explicitly-scoped `npm update` naming exactly the five known-vulnerable packages was kept as the applied command, in preference to `npm audit fix`, because it is driven by an explicit, reviewable package list rather than by whatever the live npm registry's advisory-bulk endpoint currently returns — `npm audit fix` was tested and rejected only in the sense that it offered no narrower or otherwise more suitable diff, not because it produced a worse one.
    - **The actual lockfile diff, fully characterised.** 28 total changed `packages` entries: the 6 version bumps above, plus **22 packages whose `"peer": true` flag flipped to `false`** with zero accompanying version/`resolved`/`integrity`/`dependencies` change — `@babel/core`, `@csstools/css-parser-algorithms`, `@csstools/css-tokenizer`, `@emnapi/core`, `@emnapi/runtime`, `@testing-library/dom`, `@types/node`, `@types/react`, `@types/react-dom`, `@typescript-eslint/parser`, `@vitest/coverage-v8`, `acorn`, `browserslist`, `eslint`, `react`, `react-dom`, `rollup`, `typescript`, `vite`, `vitest`, `workbox-build/node_modules/ajv`, `zod`. This was investigated before being accepted: reverting and applying `npm audit fix` instead reproduced the exact same 22-package `peer`-flag change (confirmed via a byte-for-byte diff comparison of the two candidates' resulting lockfiles), proving it is not an artefact of which remediation command was chosen but an unavoidable canonicalisation that any supported lockfile-writing operation performs under the correctly-pinned npm 11.16.0 against this repository's currently-committed lockfile. A direct JSON structural comparison (old lockfile vs. new, keyed by `packages` path) confirmed exactly these 28 entries changed, no entry was added or removed, and the total package count is unchanged (608 before and after) — i.e. the dependency graph's _shape_ is identical; only 6 leaf version numbers and 22 stale boolean flags moved. Every one of the 22 affected packages is a package this project's own `package.json` already declares directly (as a `dependencies` or `devDependencies` entry) or that is also required elsewhere as an ordinary (non-peer) dependency — a `"peer": true` marker specifically means "this node exists in `node_modules` solely to satisfy someone else's `peerDependencies` auto-install," which is not an accurate description of a package the project itself directly depends on, so this most plausibly reflects a peer-dependency-flag computation difference (very plausibly a bugfix) between whatever npm version last wrote this lockfile and the exact pinned npm `11.16.0` used here, surfacing only now because this is the first time this repository's lockfile has been mutated under the exact pinned version rather than an ambient substitute. `react`/`react-dom` are among the affected packages but their `version`/`resolved`/`integrity` are byte-identical before and after — nothing about the production dependency graph's actual content changed.
    - **Clean reinstall proof.** `npm ci` under exact npm `11.16.0` reported "added 555 packages" both before and after the fix (identical to the pre-fix `npm ci` in this same session). Post-fix `npm audit --json` → 0 vulnerable package entries; `--omit=dev --json` → 0 (unchanged — production graph was never in scope). `npm ls brace-expansion` / `npm ls postcss nanoid fast-uri undici` showed the same tree shape as the pre-fix baseline, with only the six leaf versions moved to their fixed releases.
    - **Build-comparison result.** Two production builds (`npm run build`, both with `APP_BUILD_SHA=b5acbbedf15d9207453f71314571d76065676969`) — one from the unmodified pre-fix tree, one from the post-fix tree — produced a **byte-for-byte identical `dist/`**: identical file list, identical per-file SHA-256 checksums (including both hashed asset filenames, which are themselves content hashes, and the unhashed icons/`index.html`/`manifest.webmanifest`), and an identical `sw.js` (identical Workbox precache manifest, same hashed-filename entries with `revision: null`, same static-file MD5 revisions).
    - **App-version decision.** Package version stays **`0.3.73`**. The build comparison above proved zero production-behaviour difference and `package.json` required no change, matching this item's own explicit allowance that a purely transitive, behaviour-neutral lockfile diff does not require a version bump.
    - **Verification totals.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — **2,992/2,992 across 147 files**, an exact match to item 86's own baseline for this source tree. `npm run e2e` — **230/230** (215 desktop Chromium + 15 Android-emulated). No flake was observed in this run — the known, monitored `ridingFinishAndEnd.spec.ts` completion-detection flake (item 32) did not appear. `npm run format:check`, run last immediately before staging, clean. Full gate run against the complete final tree (dependency change and this documentation together), per this project's own change-discipline rule that the committed candidate must be the one actually verified — not only against the dependency change before its documentation was added.
    - **No production or CI/workflow change.** `src/`, `e2e/` (test logic), `.github/workflows/deploy-pages.yml`, and every production `dependencies` entry are byte-identical to the pre-fix commit; the diff is confined to `package-lock.json` and documentation (`docs/project/backlog.md`, this file, root `CLAUDE.md`).
    - Item 88 (CI/supply-chain pinning and automated-advisory evaluation) is now the selected next implementation item, per the release-readiness audit's own stated ordering — no dependency on item 87, deliberately kept separate so a failure in one is never hard to attribute to the other.

---

<a id="item-88"></a>

## Item 88 — CI/supply-chain pinning and automated-advisory evaluation — done

_Category: Supply-chain / CI hardening (from the release-readiness audit, item 86)_

88. **CI/supply-chain pinning and automated-advisory evaluation — done**
    - **Outcome.** All seven `uses:` occurrences (five unique actions; `actions/checkout` and `actions/setup-node` each appear once in `verify` and once in `e2e`) in `.github/workflows/deploy-pages.yml` are pinned to immutable full 40-character commit SHAs with a trailing human-readable version comment, GitHub's own documented convention. The `mcr.microsoft.com/playwright` container is pinned to `tag@digest` (keeping the tag live and readable in the actual `image:` value, not merely in a comment) instead of the tag alone. No workflow step, order, permission, trigger, or job dependency changed; `playwright-core` stays `1.61.1` throughout.
    - **Pins applied**, each independently re-resolved live via the official GitHub REST API and, for the container, a direct registry v2 manifest-list request against `mcr.microsoft.com` — twice: once while scoping this slice, once again immediately before editing, both times identical, with every major tag's `/releases/latest` confirmed to still resolve to the same commit:

      | Reference                       | Exact tag       | SHA / digest                                                              |
      | ------------------------------- | --------------- | ------------------------------------------------------------------------- |
      | `actions/checkout`              | `v7.0.1`        | `3d3c42e5aac5ba805825da76410c181273ba90b1`                                |
      | `actions/setup-node`            | `v7.0.0`        | `820762786026740c76f36085b0efc47a31fe5020`                                |
      | `actions/configure-pages`       | `v6.0.0`        | `45bfe0192ca1faeb007ade9deae92b16b8254a0d`                                |
      | `actions/upload-pages-artifact` | `v5.0.0`        | `fc324d3547104276b827a68afc52ff2a11cc49c9`                                |
      | `actions/deploy-pages`          | `v5.0.0`        | `cd2ce8fcbc39b97be8ca5fce6e763baed58fa128`                                |
      | `mcr.microsoft.com/playwright`  | `v1.61.1-noble` | `sha256:5b8f294aff9041b7191c34a4bab3ac270157a28774d4b0660e9743297b697e48` |

      The Playwright digest is the multi-platform manifest-**list** index digest (read from the `docker-content-digest` response header on a manifest-list-accepting HEAD request), verified distinct from either child platform manifest (`docker manifest inspect` showed separate amd64/arm64 child digests, neither matching the pinned value) — and cross-checked against the same digest independently recorded in a real prior GitHub Actions run's "Initialize containers" log. The workflow's own "Verify Playwright package matches the pre-installed browser image" `::error::` message was reworded to say the container's **tag and digest** must be updated together, so a future maintainer bumping `@playwright/test` isn't misled into thinking a tag-only edit suffices.

    - **Update mechanism — GitHub Actions.** `.github/dependabot.yml` (new) adds a `github-actions` ecosystem entry (`directory: "/"`, GitHub's documented location for scanning `.github/workflows`; weekly; `open-pull-requests-limit: 5`) so the five action pins get a reviewed PR whenever an upstream tag advances, rather than staying pinned forever with no update path.
    - **Update mechanism — npm.** A separate `npm` ecosystem entry (weekly, `open-pull-requests-limit: 5`, `versioning-strategy: increase-if-necessary`) flags dependency updates for manual review only; no auto-merge is configured anywhere in the repository. A `dev-minor-patch` group (`dependency-type: development`, `update-types: [minor, patch]`) reduces routine dev-dependency noise into fewer PRs; every production-dependency update and every major-version update (dev or prod) remains individually reviewable. All `groups`/`versioning-strategy`/`commit-message` keys used were cross-checked against GitHub's live `dependabot-options-reference` documentation before writing the file, not assumed. **This npm entry gives version-update visibility only** — it does not, by itself, prove or enable repository-level Dependabot security alerts or automated security updates; those are separate GitHub repository settings and remain unobservable from this environment (see below).
    - **A real gap in the update mechanism: the Playwright container isn't covered by either Dependabot entry.** Dependabot's `github-actions` ecosystem parser only tracks `uses:` action references — it does not support `jobs.<job>.container.image` Docker references ([`dependabot-core`'s `github_actions/file_parser.rb`](https://github.com/dependabot/dependabot-core/blob/main/github_actions/lib/dependabot/github_actions/file_parser.rb) confirms Docker image references are out of scope for that ecosystem). So the new config will never open a PR to bump the container tag/digest. This is recorded as a durable manual rule (added to root `CLAUDE.md`'s "Engineering standards"): every `@playwright/test` version bump must, in the same change, resolve and update the matching container tag, its manifest digest, and the workflow's `expected_playwright` compatibility-check value together; independently of any Playwright bump, a modest periodic (roughly six-monthly) manual check should confirm the pinned digest still resolves.
    - **CodeQL — deferred, not implemented.** The correct authenticated check for default code-scanning setup is `GET /repos/adf-MD/amazing-cycling-navigation/code-scanning/default-setup` (an alerts-list response, empty or otherwise, does not distinguish "default setup enabled, nothing found yet" from "no setup at all"). That endpoint, along with `vulnerability-alerts`, `code-scanning/alerts`, and `dependabot/alerts`, all returned HTTP 401 unauthenticated when checked live from this environment (no `gh` CLI installed/authenticated). Per this item's own decision gate, an advanced CodeQL workflow was not added blind against an unobservable default-setup state — GitHub documents that default setup and an advanced workflow can conflict. **What would change this decision:** an authenticated `gh api repos/adf-MD/amazing-cycling-navigation/code-scanning/default-setup` call, or the repository's own Security → Code security settings page, confirming default setup is not already enabled.
    - **Durable triage rule.** Added to root `CLAUDE.md` under "Engineering standards": a confirmed production-reachable advisory (present in `npm audit --omit=dev`, or shown to run in the shipped bundle) blocks the next release candidate; a confirmed development-only advisory (absent from `--omit=dev`, demonstrated unreachable via `npm explain`/lockfile-path inspection) is tracked and prioritised by realistic exposure but does not itself block deployment; Dependabot/CodeQL findings are investigation inputs, never auto-merge instructions or proof of ACN-specific exploitability; `npm audit fix --force` and blind re-resolution are never used merely to zero a count; pin/digest updates require provenance re-verification and a full green CI run. This directly answers item 86's F-1/F-2 findings and the concrete near-miss they identified (a blind "any advisory blocks deployment" gate would have wrongly blocked item 87's five confirmed dev-only, unreachable advisories).
    - **No dependency, lockfile, or runtime change.** `src/`, `e2e/` (test logic), `package.json`, and `package-lock.json` are byte-identical to the pre-change commit (confirmed via `git diff --stat` after `npm ci`); the diff is confined to `deploy-pages.yml`'s pin/comment lines, the new `.github/dependabot.yml`, and documentation. Application version stays `0.3.73`.
    - **Local verification totals**, run against the exact final tree (workflow, Dependabot config and documentation together): `npm ci` and both `npm audit` commands run through exact npm `11.16.0` via Corepack (`corepack npm@11.16.0`), on ambient Node v24.13.0 (pinned Node `24.18.0` unavailable locally, as in prior sessions) — `npm ci` reported "added 555 packages"; `npm audit --json` (full graph) → **0 vulnerabilities** across 607 resolved entries (prod 34, dev 574, optional 58); `npm audit --omit=dev --json` → **0** (34 prod-tree entries, unaffected by this slice). `npm run format`, `npm run lint`, `npm run typecheck`, `APP_BUILD_SHA=b69126d9d774dd98484dc871fddaf5d55591db53 npm run build` — all clean, on ambient npm (11.6.2) as these scripts have no Corepack-exact path. `npm test` — **2,992/2,992 across 147 files**, an exact match to the established baseline. `npm run e2e` — **230/230** (215 desktop Chromium + 15 Android-emulated), no flake observed (the known, monitored `ridingFinishAndEnd.spec.ts` contention flake, item 32, did not appear). `npm run format:check`, run last immediately before staging, clean. The pinned-toolchain-exact ("Verify pinned toolchain versions") and container-digest gates are only meaningfully exercised by the pushed GitHub Actions run itself, not this local reproduction — reported separately in the assistant's final hand-off, not backfilled into this entry.
    - **Commit and push.** As with item 86's own precedent, this entry deliberately does not record this slice's own eventual commit SHA, Actions run URL, or job conclusions — a commit cannot truthfully describe its own SHA before it exists, and appending that afterwards would require a second, unrelated documentation commit that re-triggers `deploy-pages.yml` and immediately makes its own "final run" reference stale again. That information was reported directly in the assistant's final hand-off for this task.
    - Item 89 (verification-baseline polish: coverage reporting, import-cycle cleanup, test-noise triage, WebKit feasibility) is now the selected next implementation item, per the release-readiness audit's own stated ordering.
    - **Follow-up (2026-08-28): CI now runs on pull requests; npm Dependabot slowed, narrowed, and taught about Playwright's manual-only pin.** `deploy-pages.yml` gained a `pull_request` (base `main`) trigger: `verify`/`e2e` run as real PR checks; `Setup Pages`/`Upload Pages artifact` and the whole `deploy` job are skipped via `if: github.event_name != 'pull_request'`. The `pages` concurrency group moved from workflow scope to the `deploy` job alone, so PR-triggered `verify`/`e2e` runs no longer queue behind or ahead of an actual deployment. Workflow permissions were narrowed to `contents: read` at the top level, with `pages: read` added to `verify` and `pages: write`/`id-token: write` moved to `deploy` only — no job now holds more permission than it uses. `dependabot.yml`'s npm entry moved from weekly to monthly and its open-PR limit from 5 to 2; `@playwright/test` is now ignored via an explicit `version-update:semver-major`/`-minor`/`-patch` list — not a bare dependency-only ignore, which GitHub documents can also suppress security updates — so scheduled version updates stop arriving without the required coupled container-tag/digest/`expected_playwright` change, while a genuine security advisory for it can still surface a PR; `@types/node` and `jsdom` each gained a `semver-major`-only ignore (Node-24 ceiling and deliberate-review-pending, respectively). The `github-actions` ecosystem entry is unchanged (weekly, limit 5). This corrects this item's own "every major-version update ... remains individually reviewable" description above, no longer accurate for the npm ecosystem specifically. No application version bump accompanies this change.
    - **Follow-up (2026-08-31, v0.3.74): the new pull_request checks caught a real fallout from Dependabot's own maplibre-gl 6.0.0 → 6.6.0 bump.** MapLibre's 6.3.0 release made fired/listened map events typed (its own changelog: `map.on("...", ...)` now type-checks and autocompletes), which is what exposed a pre-existing unsafe fallback in `mapAdapter.ts`'s `onError` handler — `ErrorEvent.error` was already typed `ErrorLike = { message: string }` back in 6.0.0 (that part never changed), but the untyped `Listener = (a: any) => any` callback parameter did, so `event.error` only started resolving to its real declared type once 6.3.0's event-typing overhaul landed, letting `@typescript-eslint/no-base-to-string` correctly flag the old `event.error instanceof Error ? event.error.message : String(event.error)` fallback for the first time. Fixed by reading `event.error.message` directly (`ErrorLike.message` is unconditionally present, Error instance or not); `sourceId`/`category` classification untouched. Added a unit test characterising the structural, non-`Error` `ErrorLike` contract with a `{ message: "tile failed" }` payload — not a reproduction of any observed failure; the actual regression was lint-only, and e2e passed both before and after. The full 6.1.0–6.6.0 release-note range was separately reviewed against ACN's actual MapLibre usage (no draggable markers, no custom-fired events, no terrain/globe/fill-extrusion/DEM, no `Popup`, no dynamic `setTiles`/`setUrl`, no zoom-dependent `icon-size`) and found no other migration, API, rendering, interaction or browser-compatibility concern. Full local gate and the PR's own `verify`/`e2e` checks both passed, `deploy` correctly skipped; full and production-only `npm audit` stayed at 0 vulnerabilities. Because this changes the shipped production `maplibre-gl` dependency and production source, not merely CI/Dependabot config, the application version was bumped to 0.3.74.
    - **Follow-up (2026-08-31, v0.3.75): dependency maintenance — Dexie 4.4.4 → 4.4.5.** Dexie's own release notes and source diff (`src/dbcore/virtual-index-middleware.ts`) show a single core-package fix: the internal `indexLookup` dictionary changed from a plain `{}` to `Object.create(null)`, so a table with a primary key or index literally named `constructor` (colliding with `Object.prototype`) no longer crashes `db.open()`. ACN's four Dexie schema versions (`db.ts`) index only `id`, `name` and `createdAt` — none collide with an inherited `Object.prototype` property — so this fix was never reachable here and the update cannot affect existing stored routes, planning drafts, settings or active-ride state. (The release's other fix, in `dexie-cloud-addon`, doesn't apply — ACN depends on plain `dexie` only.) `npm explain dexie` confirms the root `^4.4.4` constraint resolves to `4.4.5`; full and production-only `npm audit` both stayed at 0 vulnerabilities. Production builds from `main` and this PR, built with the same `APP_BUILD_SHA`, are **not** byte-identical: the CSS, MapLibre worker chunk, PWA icons, `manifest.webmanifest` and workbox runtime all matched exactly, but the main JS bundle (which embeds Dexie's own source) differs, cascading into `index.html`'s script reference and `sw.js`'s precache manifest — confirmed genuine (not build non-determinism) by rebuilding `main` twice and getting byte-identical output both times. Per the established versioning rule, a changed production build means the application version bumps; 0.3.74 → 0.3.75.
    - **Follow-up (2026-08-31, v0.3.76): dependency maintenance — the regenerated `dev-minor-patch` group (13 updates).** Vite 8.1.5 → 8.2.2, `@vitejs/plugin-react` 6.0.4 → 6.1.1, ESLint 10.7.0 → 10.9.1, `typescript-eslint` 8.65.0 → 8.68.0, Vitest and `@vitest/coverage-v8` 4.1.10 → 4.1.11, the three Testing Library packages, `@types/react`/`@types/react-dom`, `eslint-plugin-react-refresh` and `globals` — all devDependencies, plus their transitive monorepo siblings (`@typescript-eslint/*`, `@vitest/*`, Vite's `rolldown`/`@rolldown/binding-*` native-bundler toolchain). `@playwright/test` is correctly absent, confirmed by diff: the container tag, digest and `expected_playwright` value in `deploy-pages.yml` are all byte-identical to `main`; the only "playwright" match anywhere in the diff is `@vitest/browser-playwright`, an uninstalled optional peer-dependency version string inside `vitest`'s own manifest (Vitest browser mode is never enabled here), not a real Playwright package. Release notes for every prioritised package were read directly from source (GitHub releases/changelogs, plus a direct `.d.ts` diff for the two `@types/react*` packages since DefinitelyTyped ships no changelog) and cross-checked against ACN's actual config and code: none of Vite's bundled-dev/rolldown/CSS changes touch anything `vite.config.ts` sets; `@vitejs/plugin-react`'s new React Compiler support is opt-in and unused; the ESLint/typescript-eslint fixes only narrow false positives/negatives, none touching a rule ACN overrides; Vitest's mocker fs-allowlist restriction doesn't affect ACN's same-project `vi.mock` calls; the `@types/react` `Usable<T>` widening is backward-compatible by construction (`RendererUsable<T>` is empty unless a renderer augments it, and ACN never imports `react-dom/canary`/calls `use()`); the three removed `globals.browser`/`globals.node` identifiers (`DisposableStack`, `AsyncDisposableStack`, `SuppressedError`) appear nowhere in `src/`. `npm explain` confirms clean resolution for every prioritised package; full and production-only `npm audit` both stayed at 0 vulnerabilities (602 entries, prod unchanged at 34). Production builds from `origin/main` and this PR, built with the same `APP_BUILD_SHA` (and `main` rebuilt twice first to confirm its own determinism), are **not** byte-identical — but unusually, the difference isn't confined to code that actually changed: the main JS bundle differs as expected, but so does the `maplibre-gl-worker` chunk, compiled from `maplibre-gl`'s own unchanged source. This proves the cause is Vite's updated build tooling itself (a newer bundled minifier producing marginally smaller, functionally equivalent output for identical input), not any dependency's code. CSS, PWA icons, `manifest.webmanifest` and the workbox runtime all stayed byte-identical. The full local gate (format, lint, typecheck, build, 2993 unit tests, 230 e2e, format:check last) was run and its complete output — not just exit codes — checked for new warnings: none found, including no new React `act()` warnings despite `@testing-library/react`'s own `act()`-re-entrancy fix in this range. Per the established versioning rule, a changed production build means the application version bumps; 0.3.75 → 0.3.76.
