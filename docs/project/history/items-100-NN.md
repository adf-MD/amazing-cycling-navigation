# Completed backlog items 100–

This file starts the 100– numeric range. Its first resident was item 104, deliberately implemented ahead of items 100–103 (a route-progress correctness prerequisite for item 98 — see the item's own entry below for why); item 100 has since been completed and inserted **above** it, in ascending numeric order. Stable item numbers never change regardless of which file their text lives in. Item 105 was completed after item 100 and appended in numeric order below item 104. Items 101, 102 and 103, once completed, must likewise be inserted before item 104 — its own higher number does not renumber or reorder them, and item 105's presence does not imply they are finished. Item 104 was originally recorded in [`items-95-99.md`](items-95-99.md) and moved here so that file could close cleanly at item 99.

**These are historical accounts of what shipped and why, at the time each was recorded.** Where later work has changed or superseded a detail described here, current source and tests are authoritative — but the rationale, rejected alternatives, and real regressions documented below remain valuable and are preserved rather than edited to match the present state. See the root [`CLAUDE.md`](../../../CLAUDE.md) for the required reading order.

---

<a id="item-100"></a>

## Item 100 — Reusable route tags and tag-based organisation — done

_Category: Route Library organisation_

100. **Reusable route tags and tag-based organisation — done**
     - Confirmed current state: no tag or folder concept exists anywhere in the codebase (`src/domain`, `src/storage`, `src/ui`) — this is genuinely new work, not an extension of a partially-built feature.
     - Use user-facing **tags**, not exclusive folders. A saved route may naturally belong to several concepts, such as `commute`, `gravel` and `weekend`.
     - When editing a route's tags, offer existing tags as suggestions and allow a new tag to be created without leaving the editor.
     - Specify deterministic normalisation for whitespace, empty values, duplicates and case. Preserve one stable display spelling while preventing accidental case-only duplicates.
     - Existing routes migrate safely to an empty tag collection. No route becomes hidden or inaccessible after the migration.
     - Initial tags are local ACN library metadata. Do not silently alter GPX import/export or place tags inside the geometry-digest-bound `<acn:navigation>` GPX extension without a separately reviewed format/version decision.
     - This is approved staged implementation work, not a feasibility study — stage the delivery so each slice ships independently, rather than an unreviewable vertical expansion in one commit. Later stages depend on the tag foundation earlier stages build, not on a feasibility gate:
       1. **Data model and storage:** inspect migration implications and the compact phone editor's interaction pattern, then add the backward-compatible storage schema/migration for an empty-by-default tag collection per route.
       2. **Tag editing:** add reusable-tag suggestions and new-tag creation in the route editor, built on stage 1's storage.
       3. **Filtering and organisation:** add tag filtering and a useful organisation view without duplicating a multi-tag route confusingly across many sections, built on stage 2's tag data.
       4. **Full lifecycle and acceptance:** add tag editing, deletion/rename and empty-state coverage plus real-device acceptance for the complete feature.
     - Prefer filtering and tag chips before inventing nested folders. If a later grouped presentation is proposed, mock up how multi-tag routes appear before implementation.
     - Preserve search, pinning, sorting (including item 99's new fields once delivered), import/export privacy, storage migrations and route-switch behaviour. Adding a third-party taxonomy or cloud synchronisation is out of scope.
     - **Stage 1 progress (8 September 2026): data model and storage foundation shipped, no UI.** Added `tags?: string[]` to `PlannedRoute` (`src/domain/types.ts`), plus a new `LibraryRoute` type requiring it, which `storage/routesRepository.ts`'s `getRoute()`/`listRoutes()` now return — so a route read through the Route Library repository never needs a defensive `route.tags ?? []`. A new pure module, `src/domain/routeTags.ts`'s `normalizeRouteTags()`, is the single normalisation authority: non-array input and non-string entries are discarded; each surviving tag is Unicode-NFC-normalised, trimmed, with internal whitespace runs collapsed to one ordinary space; empty/whitespace-only results are discarded; two tags are the same identity when equal after NFC normalisation, the whitespace handling above, and plain ECMAScript `toLowerCase()` (deliberately not `.toLocaleLowerCase()`, for cross-machine/CI determinism) — documented explicitly as **not** full Unicode case folding (e.g. a German eszett spelling and its double-s expansion stay distinct tags) and **not** diacritic folding (an accented and unaccented spelling likewise stay distinct); the first spelling seen per identity is kept, in first-occurrence order. `routesRepository.ts` canonicalises tags on both save and read, and gains a narrow `updateRouteTags(id, tags)` primitive for stage 2 to build on — not called by anything in this stage. No Dexie version bump was needed: `routes`' `id, name, createdAt` index is unchanged since v1, and `tags` was added as a plain non-indexed field, the same precedent already established for `pinnedAt`/`manoeuvreProvenance`/`planningProvenance`/`surfaceSummary`.
       - **Fail-first, in explicit order.** Before any production file changed, one test using only pre-existing exports (`saveRoute`, `getRoute`, no new module or type) asserted a freshly saved, untagged route's `tags` equalled `[]` through `getRoute()` — confirmed failing on the unmodified tree with `expected undefined to deeply equal []`, a genuine runtime assertion failure rather than a compilation error. Only then were `domain/routeTags.ts`, the `LibraryRoute` type and the rest of the test suite added together.
       - **Legacy/corrupt-row and new-save-default behaviour.** A raw legacy v4 `routes` row with no `tags` field at all, and a row with a malformed `tags` value (non-array, or an array mixing valid and invalid entries), both load as a canonical `[]`/sanitised array via `getRoute()` and `listRoutes()` — proven with raw `db.routes.put()` rows bypassing the repository's own write path, and with a dedicated Dexie-schema test (`routes tags field (no schema version bump)`) confirming `db.verno` stays `4`. A route saved with the `tags` key omitted entirely defaults to `[]`.
       - **Both real production ingress paths, not a synthetic object alone.** The existing Planning save test (`PlanningScreen.test.tsx`, "saving clears the draft...") now additionally asserts the saved route's `tags` equal `[]`, exercising the real `PlanningScreen.tsx` save flow end-to-end. A new test in `gpx/importGpx.test.ts` drives the exact `importGpxFile()` → `saveRoute()` sequence `ui/library/ImportGpxButton.tsx` performs and asserts the same.
       - **GPX/privacy proof.** A distinctive probe tag is proven absent from exported XML; a route exported then reimported has no tags on the raw import result; and — closing the loop through the actual Route Library boundary rather than stopping at that raw result — saving and re-reading the reimported route produces a canonical `[]`. A second test confirms tags never change `<acn:navigation>`'s `geometrySha256` or add any new element/attribute, with the geometry-digest-bound extension itself untouched.
       - **Negative controls, two of which did not initially discriminate — reported honestly rather than dropped.** Disabling write-time canonicalisation in `saveRoute()` and separately in `updateRouteTags()` produced no test failure at first, because `getRoute()`'s own read-time normalisation silently compensated — a redundant-mechanism gap, not a sound design. Fixed by adding two further assertions that read the raw Dexie row directly (bypassing the repository's read-side normalisation) to prove the persisted data itself, not merely what is returned on read, is canonical; both controls then failed as expected. The remaining five controls (read-time normalisation, case-insensitive dedup, first-spelling-wins, whitespace collapsing, and a simulated GPX leak into a `<desc>` element) all discriminated correctly on the first attempt.
       - **Stage-2 design note (not implemented).** `ui/library/RouteListItem.tsx` already has a one-at-a-time, mutually-exclusive inline-state pattern (rename/pin/delete-confirm/switch-prompt) lifted to `RouteLibrary.tsx`; a future tag editor would add one more such state and an `onTagsSave` callback wired to `updateRouteTags`, the same way `onRename` wires to `renameRoute` today. A reusable-tag suggestion corpus would most naturally be a `useMemo` over the `LibraryRoute[]` that `listRoutes()`'s live query already returns, mirroring how item 99's `selectRouteLibraryGroups` is computed. Neither needs any stage-1 code to change.
       - Stages 2–4 (tag editing/suggestions, filtering/organisation, full lifecycle and physical acceptance) remain undone. Shipped as `0.4.15`; this is an invisible storage-layer change with no UI and no physical-device acceptance claim.
     - **Stage 2 progress (8 September 2026): compact tag editor and reusable suggestions shipped, browser-tested, not yet physically accepted.** `ui/library/RouteListItem.tsx`'s card body is now a three-way ternary (`isRenaming` / `isEditingTags` / the ordinary card), mutually exclusive by construction — the button that opens each mode only exists in the ordinary-card branch. Assigned tags render as a non-interactive `<ul aria-label="Tags">` chip list; a new "Add tags"/"Edit tags" action sits between Rename and Export. The tag editor is a `<div className="tag-editor">` showing the route's name/meta for context, a small inner `<form>` (label "Add a tag" + input + "Add tag" button, so Enter never submits the whole editor) and a single deterministically-sorted toggle-button grid — `sortTagsForDisplay(normalizeRouteTags([...tagSuggestions, ...tagDraft]))` — that handles selecting a suggestion, removing a currently-assigned tag and keeping a freshly-typed tag visible/removable pre-save, all as one mechanism. `RouteLibrary.tsx` derives `tagSuggestions` via `useMemo(() => collectTagSuggestions(routes ?? []), [routes])` from the full, unfiltered live-query result (never `viewRoutes`), and wires a thin `handleTagsSave` wrapper to the already-built `updateRouteTags`.
       - **Identity, never display-string equality.** Five small `domain/routeTags.ts` additions: `tagIdentityKey(tag)` extracts `normalizeRouteTags`'s own dedup-key computation into a reusable, exported function (a behaviour-preserving refactor — `normalizeRouteTags`'s existing test suite passes unmodified); `tagsEqualByIdentity(a, b)` is order-independent, count-aware set equality; `sortTagsForDisplay(tags)` is a fresh, domain-local `Intl.Collator("en-GB", { sensitivity: "base", numeric: true })` sort (not imported from `ui/library/routeLibraryView.ts` — domain must not depend on ui); `resolveTagSpelling(candidate, knownTags)` returns `null` for a whitespace-only candidate, otherwise the candidate's own normalised spelling unless an identity-equivalent tag already exists in `knownTags`, in which case that established spelling is returned instead; `collectTagSuggestions(routes)` is `sortTagsForDisplay(normalizeRouteTags(routes.flatMap(r => r.tags)))`, reusing `normalizeRouteTags` over the flattened cross-route corpus for cross-route dedup and first-occurrence-spelling-wins with no new dedup logic. Every draft-membership, pressed-state, toggle and duplicate check in `RouteListItem.tsx` compares via `tagIdentityKey`, never raw `.toLowerCase()` or `===` — proven directly: a route whose own stored tag is `"gravel"` while another route has established `"Gravel"` renders **one** merged, pressed entry labelled `"Gravel"`; toggling it off removes `"gravel"` by identity despite the differing display spelling; saving _without_ touching it preserves the original `"gravel"` spelling unchanged (canonicalisation happens only on an explicit type/toggle interaction, never silently).
       - **The editor never closes onto stale chips.** `onTagsSave`'s promise resolves as soon as the underlying write settles — a separate, later step from `useLiveQuery`'s `listRoutes()` subscription actually re-firing and this card's `route` prop reflecting the new tags. Closing on promise-resolution alone risked a render where the editor was closed but the chips still showed the pre-save tags. Fixed with a `savedTagsAwaitingSyncRef`, set once the write settles, consumed by a `useEffect` keyed on `route.tags` that only closes the editor once `tagsEqualByIdentity(route.tags, savedTagsAwaitingSyncRef.current)` is true — mirroring `RouteLibrary.tsx`'s own `lastRenamedIdRef`/`lastRenamedNameRef` "wait until the live query demonstrably reflects an async write" precedent. Proved through the real repository/live-query pipeline, not a manually rerendered prop: an integration test spies on `routesRepository.listRoutes` (the function the live query actually calls) to inject a real, undelayed-write/delayed-read artificial gap, then samples repeatedly across the whole widened window — never checking once — confirming the editor is never observed closed while the chips still show the pre-save tags.
       - **Save/Cancel/Escape are guarded synchronously, independent of React's render timing.** The load-bearing guard for `handleSaveTags`/`handleCancelTags`/Escape is a `useRef<boolean>` (`isSavingTagsRef`), checked and set immediately — a `useState` value alone is not reliably synchronous against two invocations landing before React has processed the first one's update. Proved with two techniques: the project's existing two-synchronous-`fireEvent.click()` convention, and a strengthened test invoking the Save handler twice inside one `act()` call with zero intervening re-render. The strengthened test is the one that actually matters here — see the negative control below.
       - **Fail-first.** One test against the unmodified `0.4.15` tree rendered `RouteListItem` with `tags: ["Gravel"]` and asserted an "Edit tags" button plus visible "Gravel" text — confirmed failing with a genuine `TestingLibraryElementError` (element not found), not a compile error.
       - **Automated evidence.** 30 domain tests (`routeTags.test.ts`, 17 new), 80 `RouteListItem.test.tsx` tests (25 new, covering every hand-off-required case plus the identity/sync/guard mechanics above), 72 `RouteLibrary.test.tsx` tests (7 new, real fake-indexeddb integration — cross-route suggestion reuse and spelling adoption, suggestion availability despite an active search filter, name-only search preserved, all four sort modes plus pin priority unaffected by a tag save, persistence across unmount/remount, a rejected write retaining the draft with retry, and the delayed-`listRoutes` stale-chips proof). Two new Playwright specs: `routeLibraryTags.spec.ts` (chromium; the full journey — add/reuse/dedupe/remove/reload — plus 390×844, 200%-text and 844×390-landscape geometry/44×44 evidence) and `androidRouteLibraryTags.spec.ts` (android-chrome; no existing Route Library spec ran on this project before, so this is a new file, deliberately narrower, following the established `android*.spec.ts` convention).
       - **Negative controls, two of which needed strengthening or were reported honestly rather than silently dropped.** Calling `onTagsSave` from the suggestion-toggle handler instead of only `handleSaveTags` did not initially fail the "Cancel discards the draft" test, because that test only exercised typing a tag, never toggling a suggestion — the test was strengthened to cover both mutation paths, after which the control correctly failed. Reverting the `isSavingTagsRef`-based guard to read the `isSavingTags` _state_ value instead did **not** fail the project's own standard two-`fireEvent.click()` test (React's `act()` happens to flush state between the two calls in that harness) but **did** fail the strengthened single-`act()`-double-invocation test — reported honestly rather than treated as a pass, and the reason the strengthened technique was added at all. The remaining six controls (suggestions from `viewRoutes` instead of `routes`; bypassing `resolveTagSpelling`; closing the editor unconditionally on any settled promise, which discriminated both the rejected-write and the stale-chips tests at once; removing `aria-pressed` and removing the decorative check-mark independently; including tags in the name-search predicate) all discriminated correctly on the first attempt.
       - **A genuine, reproducible full-suite timing sensitivity, investigated to a verified root cause rather than papered over.** Running the complete ~290-test Playwright suite repeatedly showed the two new specs occasionally timing out waiting for the editor to close after Save, under full-suite CPU contention — the same class of environment characteristic already documented for item 32's `ridingFinishAndEnd.spec.ts`. Not a functional defect: the underlying wait mechanism is correct and always eventually settles (confirmed clean in isolation every time, and via a stashed clean-baseline comparison showing unrelated pre-existing specs — not just these two — flake under the same full-suite load). Raising the two affected assertions' timeout from Playwright's 5000ms default to 15,000ms, then 25,000ms, reduced but did not eliminate the failures; this sandbox reports 72 CPUs, and Playwright's default worker count scales off that figure, almost certainly oversubscribing whatever real concurrent scheduling capacity the container actually has. Re-running the complete suite at a moderate, CI-realistic `--workers=4` (rather than the sandbox's own oversubscribed default) passed every project cleanly (WebKit's launch failure aside), confirming the instability is this local sandbox's own worker-count/CPU-count mismatch, not the tag editor's write/live-query mechanism. The generous `{ timeout: 25_000 }` on the specific assertions immediately following a real write+live-query round trip is kept as a genuine, reasoned robustness margin regardless (test-only, no production-code change) — real GitHub Actions runners have far fewer cores and are not expected to reproduce this sandbox's oversubscription. The exact pushed commit's own CI run is the authoritative confirmation, not this sandbox's default-parallelism local runs.
       - Decorative check-mark glyphs inside suggestion buttons are `aria-hidden="true"`; `aria-pressed` alone carries the accessible pressed-state, so assistive tech never double-announces a stray glyph.
       - Stage 3 (filtering and organisation) and stage 4 (full lifecycle and physical-device acceptance) remain undone. Shipped as `0.4.16`; browser-tested (Vitest + Playwright chromium/android-chrome; WebKit unavailable in this sandbox, deferred to CI) but not physically accepted on a device — see `docs/project/current-status.md`.
       - **Correction (8 September 2026): the CPU-contention conclusion above was wrong on the substance, not just the numbers — CI exposed a genuine production ordering race.** Commit `39e45fe` (this stage-2 work, still version `0.4.16`) was pushed on the strength of the analysis above. CI's "Verify and build" job passed, but "End-to-end tests" failed at the same boundary in both the `chromium` and `android-chrome` Playwright projects during run 226, so Deploy was skipped and `0.4.16` never reached the live site. That failure cannot be dismissed as a contention-only test flake: CI exposed a genuine production ordering race in both browser projects during run 226. (One CI run reproducing the same failure in two projects is strong evidence of a real bug, not proof the race resolves the same way on every run — CPU scheduling could still influence which side of the race wins on a given run — so this is stated as a genuine race CI exposed, not as deterministic on every run or as something contention plays no part in.)
       - **Root cause.** `RouteListItem.tsx`'s tag editor closes only once two independent async facts are both true: the save write has settled, and `route.tags` (via `RouteLibrary`'s live query) reflects the saved tags by identity. The write-settled fact was held in a plain `useRef` (`savedTagsAwaitingSyncRef`), set inside the write promise's `.then()`. A ref mutation never triggers a re-render or effect re-run. The closing effect depended only on `route.tags`. This worked whenever the write settled _before_ `route.tags` updated — but broke whenever `route.tags` updated _first_: the effect ran once, found the ref not yet set, no-opped; when the write later settled and the ref was finally set, nothing caused the effect to run again, and the editor stayed on "Saving…" forever. The same failure mode also affected a successful no-op save (re-saving already-canonical tags), where `route.tags` might never change again at all.
       - **Fix: a genuine two-signal handshake, in `useState`, not a ref.** `savedTagsAwaitingSyncRef` became `pendingSyncTags`, a `useState<string[] | null>` — so setting it in the write's `.then()` is itself a state update that causes React to reconsider the closing condition, keyed on both `route.tags` and `pendingSyncTags`, wherever the two-signal handshake sits in the component (moved from an effect to a during-render adjustment — see below). No ref-only signal, no microtask delay, no `setTimeout`, and no increased Playwright timeout is part of the fix itself.
       - **A second lint-driven correction along the way.** The straightforward `useEffect` version of the new two-state-dependency closing check tripped this project's `react-hooks/set-state-in-effect` rule, which only exempts a ref-gated "consume once" shape — not this one, since `pendingSyncTags` is deliberately state. Following the exact convention already established in `RouteLibrary.tsx` (`pendingRouteSwitch`/`previousPendingRouteSwitch`) and `RidingScreen.tsx`, the closing check was moved to React's own documented "adjust state during render" pattern instead of an effect; it is self-terminating with no extra "previous value" mirror needed, because clearing `pendingSyncTags` is itself one of the condition's own inputs. `isSavingTagsRef`'s own reset on this success path can't happen during render either (`react-hooks/refs`), so it was split into a small separate effect keyed on `isSavingTags`.
       - **New fail-first evidence for the exact ordering CI hit, plus the orderings it didn't.** `RouteListItem.test.tsx` gained an explicit "Ordering A" test (write settles first — already worked, kept as a regression guard, replacing the previous less explicit version), a new "Ordering B" test (`route.tags` arrives first — the load-bearing fail-first case, confirmed failing against the unmodified `39e45fe` code with a genuine `findByRole` timeout, confirmed passing after the fix), and a new no-op-save test (route already canonical, no further `route.tags` change ever required to close — also confirmed failing pre-fix). `RouteLibrary.test.tsx` gained a real-repository/live-query integration test for ordering B: it performs the real `updateRouteTags` write immediately (so the live query can react to it for real) while holding the write's own caller-visible promise on a manually-released deferred, _proves_ the live query has already propagated — before ever releasing that deferred — by checking the new tag now appears as a reusable suggestion on a second, different route's editor, then confirms the first route's card is still open, releases the write, and confirms it then closes; this test was independently confirmed to fail against the unmodified `39e45fe` code (the same real-device symptom: the card stuck open) and to pass after the fix. Counts: `routeTags.test.ts` stays 30; `RouteListItem.test.tsx` 80 → 82; `RouteLibrary.test.tsx` 72 → 73.
       - **The 25-second timeout padding is removed.** `SAVE_ROUND_TRIP_TIMEOUT` and its explanatory comment are deleted from both `routeLibraryTags.spec.ts` and `androidRouteLibraryTags.spec.ts`; the affected assertions are back to Playwright's own default timeout. With the real fix in place, five repeats of each spec on both `chromium` and `android-chrome`, plus two full-suite runs, all passed in well under a second per assertion — the timeout padding was never the correct fix and is not needed.
       - Version stays `0.4.16` — the original run's Deploy job never ran, so this correction's own successful CI run is what will deploy it for the first time.
     - **Stage 3 progress (8 September 2026): tag filtering and organisation shipped, browser-tested, not yet physically accepted.** A third `.route-library-field` — a labelled "Filter by tags" region, placed after Search and Sort, omitted entirely when no saved route has any tag — renders one native `<button>` per distinct tag identity across the full unfiltered route corpus (`role="group"` labelled by a visible `<span>`, `aria-pressed` plus a decorative `aria-hidden` check mark as the non-colour selected cue, new `.tag-filter-chip`/`.tag-filter-check` classes mirroring the stage-2 suggestion grid's own shape but kept separate since both can render on screen at once). Several tags select together with **AND** semantics: a new `filterRoutesByTags<T extends LibraryRoute>(routes, tagKeys: ReadonlySet<string>)` in `ui/library/routeLibraryView.ts` matches by `tagIdentityKey`, never display-string equality; `selectRouteLibraryGroups` gained this as a required fourth parameter (matching `query`'s own no-default convention) and now runs it between the existing name filter and the pinned/unpinned partition, so both groups are narrowed from the identical post-both-filters set by construction. `filterRoutesByTags` is bound to `LibraryRoute` (not a defensively-optional `PlannedRoute`) since its only real caller always supplies `LibraryRoute[]` — confirmed by grep before committing to the tighter bound; `routeLibraryView.test.ts`'s `buildRoute()` fixture now returns `LibraryRoute` with a `tags` override, and the "missing `tags` field" case is deliberately not retested here, since it is now type-unrepresentable at this boundary and already covered by `routesRepository.ts`'s own legacy-row tests.
       - **Filter-choice source and stale-key reconciliation.** Available chips reuse the existing `tagSuggestions` memo (`collectTagSuggestions(routes ?? [])`, from the full unfiltered live-query result — unchanged from stage 2) rather than a second derivation, so choices never narrow to the currently-filtered view. Selected identities are tracked as `ReadonlySet<string>` state (`selectedTagFilters`); a tag identity whose last backing route is retagged or deleted is genuinely pruned from that state — not merely masked at read time — via a render-time adjustment (the same "adjust state during render" idiom this file already uses for `pendingRouteSwitch`/`previousPendingRouteSwitch`), so an identically-spelled tag recreated later in the same session cannot silently reactivate a filter the rider never re-selected. That pruning, and the session-restoration sync described below, are both gated on an explicit `tagFiltersHydrated` flag (set once, in the hydration effect) together with `routes !== undefined` — `useLiveQuery` returns `undefined` until IndexedDB resolves, so without this gate the pruning logic would see a legitimately-empty corpus during that window and immediately erase a just-restored selection before real data ever loaded.
       - **Disappearance-focus repair, and the exact race class this stage had to avoid repeating.** A tag save that removes a route's last matching filter tag can unmount its card — including mid-edit, before the stage-2 child's own "Saving…" handshake would otherwise have closed it — so `RouteLibrary.tsx`'s existing rename-focus-repair `useEffect` (keyed on `[viewRoutes, routes]`) was extended with a second, independent marker (`lastTagsSaveIntentRef`, a `{ id, tags }` object) rather than a parallel effect, since two effects with identical dependencies would race to update the shared `previousViewRoutesRef` first; the `previous` snapshot itself was hoisted to the top of the effect, read once by both blocks. An initial draft set this marker inside `updateRouteTags(...).then()`, exactly reproducing stage 2's own `39e45fe → 6f3e2d3` ordering race: if the live query updated `routes` before the write's promise settled, the effect's own trailing `previousViewRoutesRef` update would advance past the change with no marker in place, and nothing would re-trigger the effect once one was finally set. Caught in review before this shipped; the fix mirrors `handleRename`'s own already-working pattern — `lastTagsSaveIntentRef` is set synchronously **before** `updateRouteTags` is even called, as one atomic object (a guarded, identity-compared clear on rejection, not two separately-nullable refs), so the consuming effect is correct regardless of which async signal — write settlement or live-query update — arrives first. The stage-2 child handshake inside `RouteListItem.tsx` (`pendingSyncTags`, governing only when the _editor_ itself closes) is untouched. Fallback chain on disappearance: next visible route → previous visible route → the Clear tag filters button → the region's own label → the search field.
       - **Same-session restoration**, mirroring `restoreSearchQueryRef`'s exact contract: a new `routesTagFilterKeysRef` (`readonly string[]` of identity keys, App-owned, never one-shot-nulled, reset only on a full remount) is hydrated once per `RouteLibrary` mount via a `useLayoutEffect` declared immediately after the search-query one, which also sets the explicit `tagFiltersHydrated` flag; a dedicated sync-back `useEffect` (not an inline write-through like search's own, because the render-time pruning above can also change `selectedTagFilters`, and writing a ref during render trips `react-hooks/refs`) keeps the ref current, gated on `tagFiltersHydrated` so the initial pre-hydration empty selection can never overwrite a not-yet-consumed restoration ref. Scroll restoration's existing gate gained an explicit `tagFiltersHydrated` dependency too — a deliberate choice to depend on the flag directly rather than on effect declaration order, per plan review. `onTargetMissing` (the item 73 follow-up pending-switch safety net) needed no change at all: it already depends only on `[pendingRouteSwitch, viewRoutes]`, and `viewRoutes` already incorporates the tag filter once `groups` picks it up.
       - **Fail-first.** Before any production edit, a test rendered several tagged routes through `RouteLibrary` on the unmodified `0.4.16` tree and asserted a "Filter by tags" region existed and a chip narrowed the list — confirmed failing with a missing-element error, not a compile error.
       - **Automated evidence.** `routeLibraryView.test.ts` 34 → 48 (9 new `filterRoutesByTags` tests, 5 new `selectRouteLibraryGroups` tag-filter tests, the 9 existing `selectRouteLibraryGroups` call sites updated to the new required parameter); `RouteLibrary.test.tsx` 73 → 98 (25 new: region omission/derivation, single- and two-tag AND, `aria-pressed` plus the visible `is-selected` class, spelling-insensitive matching, search composition, pin/sort preservation including a non-matching pinned route, Clear plus focus, live-editor interaction, three disappearance-focus fallback tiers, two-route stale-key pruning by deletion and by edit with same-session non-reactivation proof, `onTargetMissing` cancellation, both combined no-match copy variants, four session-restoration tests including the routes-still-loading race, and three dedicated ordering tests — live-query-before-settlement, settlement-before-live-query, and a failed save clearing only its own pending intent); `App.test.tsx` 59 → 62 (3 new, mirroring the existing search-restoration block: filter survives navigate-away/return, a full remount does not restore it, filter and search restoration compose without clobbering each other). New Playwright spec `routeLibraryTagFiltering.spec.ts` (chromium; one full journey covering import/tag/AND/search-composition/Clear/pin-and-sort-survival/route-open-return-restoration/safe edit-caused disappearance/reload-resets-filter-not-tags, plus 390×844-200%-text and 844×390-landscape geometry-and-touch-target `describe`s); one added test in `androidRouteLibraryTags.spec.ts` (chip selection narrows the list, touch-target-checked), matching that file's own established narrower scope. Fixing this stage's real-browser evidence also surfaced and corrected two pre-existing e2e defects unrelated to the new feature's own logic: `getListItemForName`'s `li:has(.route-card-title:text-is(...))` locator cannot find a route whose own editor is currently open (its title renders as a bare `<h2>` in that mode, per `RouteListItem.tsx`) — fixed with an OR-selector matching either form, applied to `routeLibraryTagFiltering.spec.ts`, `routeLibraryTags.spec.ts` and `androidRouteLibraryTags.spec.ts` alike; and several previously-unambiguous `getByRole("button", { name: "Gravel" })`-style queries in the pre-existing stage-2 specs became genuinely ambiguous once a same-named filter chip could coexist with an open editor's own suggestion button, fixed by scoping each to the specific card's own `<li>`.
       - **Negative controls, all ten confirmed discriminating.** OR instead of AND in `filterRoutesByTags`; display-string instead of identity-key route-tag comparison; filter choices derived from `viewRoutes` instead of the full `routes` corpus; the tag filter applied only to the unpinned partition (a non-matching pinned route leaked through); the render-time stale-key pruning disabled (masking only) — confirmed via a genuinely invisible active filter silently excluding the very route a later step needed to retag, not merely the originally-anticipated reactivation symptom; `aria-pressed` removed from the filter chip; the visible `is-selected` class removed (this discovered a real, if minor, gap — no existing test asserted the class specifically, only `aria-pressed` — closed by extending the existing toggle test rather than adding a parallel one); the tagless-region-omission guard removed; the session-restoration sync effect removed; and, most importantly, the tag-save intent moved back into `updateRouteTags(...).then()` — this last control reproduces the exact ordering race described above and is caught by the dedicated live-query-before-settlement ordering test, proving the fix is load-bearing rather than incidental.
       - Stage 4 (full lifecycle and physical-device acceptance) remains undone. Shipped as `0.4.17`; browser-tested (Vitest — 157 files, 3323 tests; Playwright chromium and android-chrome — 288 tests; WebKit unavailable in this sandbox, deferred to CI) but not physically accepted on a device — see `docs/project/current-status.md`.
       - **Follow-up (9 September 2026): a successful "Save tags" press could leave the card's own title hidden under the sticky header or above the fold, with only the focused tag button guaranteed visible — fixed, browser-tested.** Reported from the installed iPhone Home Screen PWA: after scrolling down within a tall tag editor (many tags) and pressing Save tags, the ordinary card returned with the "Edit tags"/"Add tags" button visibly focused, but the card's own title was not necessarily visible.
         - **Fail-first.** Before any production edit, a real-browser Playwright journey (390×844) imported a target route below ten filler routes (most-recent sort pushes it off-screen), added 100 short tags to its editor, pressed the real Save tags button and measured the sticky header, card, title and tag-button boxes in one atomic `page.evaluate()` against the unmodified `0.4.17` build. Confirmed: the tag button ended up fully visible (native "scroll nearest into view" succeeded for the focused element) while the title's own top sat at a negative, off-screen coordinate (e.g. `-235px` against a `65px`-tall header) — proving the reported defect and ruling out the alternative hypothesis that the whole card was simply misplaced.
         - **Root cause.** `RouteListItem.tsx`'s close/focus effect returned focus to the tags button identically for every close reason (Save, Cancel, Escape) with a plain `tagsButtonRef.current?.focus()`. A browser's own focus-scroll only guarantees the focused element's visibility, has no notion of `.app-header--sticky`'s (`z-index: 10`) overlap with the scrollport top, and no notion of the title several rows above the button once the card has grown tall from many tags. No `scroll-margin-top` (or equivalent) exists anywhere in `src/index.css` — only `scroll-margin-bottom` on `.route-card--switch-pending`, for the unrelated item-95 switch-prompt reveal — so a bare `scrollIntoView({block:"start"})` could not have reliably compensated for the sticky header's own dynamic height either.
         - **Fix.** A new signal, `tagsSaveSucceeded` (`useState`, not a ref — set only inside the existing two-signal `pendingSyncTags`/`route.tags` render-time handshake block that already closes the editor on success, and reset defensively inside `openTagEditor()` alongside its other three resets), distinguishes a successful-save close from Cancel/Escape without touching that handshake's own contract; the two paths are structurally mutually exclusive already, since both no-op while `isSavingTagsRef`/`isSavingTags` stay true for the entire window a save is in flight. On the success path only, the close effect calls `tagsButtonRef.current?.focus({ preventScroll: true })` (suppressing the browser's own competing scroll) and then performs exactly one `window.scrollBy({ top, left: 0, behavior })`, sized by a new pure helper (`src/ui/library/routeCardTopReveal.ts`'s `computeTopRevealScrollDelta`) that brings the card's own top edge and the bottom of its title row (a newly-reffed `.route-card-title-row`) below the sticky header plus a small gap — top-prioritising, deliberately separate from item 95's own bottom-prioritising, whole-card `isCardAlreadyFullyVisible`: when the card is taller than the available band, the delta is clamped so the top is never sacrificed to also show the bottom. `left: 0` guarantees horizontal scroll position is never touched; the call is skipped entirely (no-op) when the band is already suitably placed; `prefersReducedMotion()` is reused unchanged. Cancel and Escape keep today's plain `.focus()`, native scroll included.
         - **Real-browser smooth-scroll timing.** The reveal's `scrollBy` call uses `behavior: "smooth"` unless reduced motion is requested, so the first e2e attempt (reading geometry immediately after the click) measured a mid-animation snapshot and failed with numbers indistinguishable from the pre-fix defect. Fixed in the tests, not production, by polling (never a fixed sleep) until `window.scrollY` has genuinely stopped changing across ten consecutive animation frames before measuring — the same convention `rideSessionSwitchGuard.spec.ts`'s own item-95 tests already established for exactly this class of race.
         - **Disappearing-filtered-route interaction needs no new guard.** `RouteLibrary.tsx`'s `viewRoutes` is a synchronous `useMemo` over the live-query `routes`; a save that drops a route out of the active tag filter excludes it from `viewRoutes` in the same render pass that updates `routes`, so that `RouteListItem` never re-renders with the new tags at all — it simply unmounts. The render-time success block therefore never runs, `tagsSaveSucceeded` never becomes true, and the reveal effect never fires — a structural guarantee, not a runtime check. Verified by attempting a substitute negative control (rendering every route instead of only `viewRoutes`, so a disappearing card would keep re-rendering with its new tags): this broke `routeLibraryTagFiltering.spec.ts`'s very first filtering assertion, well before the disappearance step, confirming the existing suite would catch any structural regression of this kind. `RouteLibrary.tsx`'s own established disappearance fallback chain (next route → previous route → Clear tag filters → filter heading → search input) is untouched.
         - **Automated evidence.** New `src/ui/library/routeCardTopReveal.ts`/`.test.ts` (8 tests: already-clear no-op, header-occlusion, visible-top-above-header, below-the-fold, the taller-than-viewport clamp, tolerance boundary, default-gap). `RouteListItem.test.tsx` 82 → 90 (8 new: successful save issues `focus({preventScroll:true})` plus one correctly-signed, `left:0` `scrollBy`; a successful no-op save also reveals; an already-visible card skips the scroll but still returns focus; Cancel and Escape both keep plain `focus()` and never call `scrollBy`; a rejected save never scrolls and a subsequent Cancel behaves ordinarily; re-opening after a successful save resets the signal so a later Cancel doesn't spuriously reveal; reduced motion uses `behavior:"auto"`). `routeLibraryTags.spec.ts` gained 5 tests (the fail-first journey made permanent with a documented 2px tolerance; an already-visible no-op case asserting `scrollY` is unchanged; a net-new Cancel journey — deliberately without an exact-`scrollY` assertion, since Cancel's own unchanged native focus-scroll may legitimately make a small adjustment of its own — asserting draft-discard, correct focus and comfortable visibility instead; and one reveal-geometry variant each inside the existing 200%-text and 844×390-landscape `describe`s). `androidRouteLibraryTags.spec.ts` gained one narrow reveal test under Android emulation, matching that file's established narrower scope. `routeLibraryTagFiltering.spec.ts`'s existing disappearance scenario gained a no-console-error assertion (test count unchanged). Vitest: 157 → 158 files, 3323 → 3339 tests. Playwright chromium + android-chrome: 288 → 294 tests.
         - **Negative controls.** (1) Disabling the `scrollBy` call: the four reveal-driving component tests fail as expected, discriminating. (2) Dropping `preventScroll` on the success path: the two component tests asserting `focus` call arguments fail, discriminating. (3) Applying the reveal unconditionally (including Cancel/Escape): the Cancel, Escape, rejected-save-then-cancel and re-open-then-cancel component tests all fail, discriminating. (4) There is no togglable disappearance guard to remove — the safety is structural (`RouteLibrary`'s synchronous `useMemo` chain); the substitute control described above (render every route, not just `viewRoutes`) is reported honestly rather than forcing a contrived toggle into production code, and it discriminates even more broadly than anticipated, breaking filtering itself before ever reaching the disappearance step.
         - Confirmed pre-existing, unrelated to this change: `rideSessionSwitchGuard.spec.ts`'s two "genuine ordinary-motion scroll" tests and `ridingFinishAndEnd.spec.ts`'s completion-detection test (item 32) flake under this sandbox's full-suite parallel CPU contention on a stashed clean baseline exactly as they do with this fix applied; none of the touched files are involved.
         - Stage 4 remains undone and out of this follow-up's scope. Shipped as `0.4.18`.
     - **Stage 4A progress (9 September 2026): the global tag lifecycle shipped, browser-tested, not yet physically accepted.** Stage 4 was deliberately split: **4A** is this coded slice (global rename, merge and delete, plus the remaining empty-state and accessibility coverage); **4B** is physical-device acceptance and documentation closure, which is a field-acceptance step rather than a second coding project and remains open in `docs/project/current-status.md`. The split exists because real iPhone/Android acceptance cannot truthfully be performed by the implementer, and it lets the lifecycle ship as one coherent, reviewable unit without the feature being called physically accepted. No persistent tag registry and no Dexie schema change were needed: tags stay derived from what saved routes currently carry, so the last use of a tag disappearing removes it from suggestions, filters and the manager alike.
       - **A compact `Manage tags` panel, using the screen's own vocabulary.** A `.btn-secondary` toggle sits in the Route Library toolbar under exactly the same condition as the stage-3 filter region (`tagSuggestions.length > 0`), and deliberately **outside** the `role="group"`/"Filter by tags" region — that region is the scoping anchor for the existing chip queries in `RouteLibrary.test.tsx` and the e2e helpers, and a control inside it would have made them ambiguous. The panel itself is a new presentational `ui/library/RouteTagManager.tsx` (`role="group"` labelled "Manage tags", a new `.tag-manager` class mirroring `.tag-editor`'s left-accent card); all state, the confirmation and the repository call stay in `RouteLibrary.tsx`, exactly as `RouteListItem` is driven.
       - **A native `<select>` for the source tag, chosen on accessible-name grounds rather than layout.** This screen has already twice had to scope queries because an open card editor's suggestion button and a filter chip can share a name (see `RouteLibrary.test.tsx`'s own group-scoped chip lookup and `.tag-filter-chip`'s note in `index.css`); a third set of buttons named "Gravel" would have made existing queries ambiguous, whereas `<option>`s are not buttons. Options are `value={tagIdentityKey}` labelled `Gravel (3 routes)`, counted by a new `countRoutesByTagIdentity()` over the **full unfiltered corpus** (never `viewRoutes`), and the first option is a neutral `Choose a tag` placeholder rather than an auto-selected first tag, matching the pre-ride climb selector's own established behaviour.
       - **The chosen source is derived, not stored — and it governs behaviour, not just display.** `tagLifecycleTarget.ts`'s `resolveEffectiveSourceKey()` falls back to the placeholder the moment the corpus stops carrying the chosen tag, and that derived value is the single input to the Rename/Delete disabled state, the preview and confirmation copy, the target resolution and the operation handed to storage. Deriving only the rendered value would have been actively unsafe: a `<select>` whose value matches no option silently displays the first one, so every other path would have kept acting on a stale key and could have armed a destructive action against the wrong tag.
       - **Three-way spelling resolution, reusing `resolveTagSpelling` rather than reimplementing it.** `resolveTagLifecycleTarget()` calls the stage-2 helper with the **source tag excluded** from the known corpus. That exclusion is load-bearing: passing the full corpus would resolve a deliberate case-only rename ("Gravel" → "gravel") straight back to the established spelling and silently make it a no-op. The three outcomes are: the typed identity equals the source's, so the typed spelling wins (display-only respelling); it matches another existing tag, so that tag's established spelling wins and this is a merge; otherwise the typed spelling wins (plain rename).
       - **Rename applies immediately; merge and delete require explicit confirmation.** A plain rename is reversible and touches only tag metadata, so it applies without a dialog, with a live preview line stating the scope beforehand ("Rename “Gravel” on 4 routes.") and the repository's own affected-route count in the success status. Because the rider cannot know in advance whether a typed name collides, the submit handler **detects the collision and switches to the confirmation path**, and the action's own label changes to "Merge tags" first — a rename can never silently become a merge. Both confirmations are hand-rolled in the panel with `useId()`-scoped `aria-labelledby`/`aria-describedby`, mirroring `RouteListItem.tsx`'s per-card delete confirmation (which `ui/shared/ConfirmDialog.tsx`'s own doc comment cites as its precedent). The shared component was considered and rejected for a concrete reason: it hardcodes `aria-labelledby="confirm-dialog-title"`, and `App.tsx` can render its own page-level `ConfirmDialog` for a pending ride switch while the Route Library is on screen, so two dialogs could share one id. That hardcoded id is recorded as a known pre-existing defect; fixing it would touch five screens and was out of scope here.
       - **One atomic repository operation, and the row-selection rule it must obey.** `routesRepository.applyRouteTagLifecycle(operation)` runs a single `db.transaction("rw", db.routes, …)` (the house pattern from `providerKeyRepository.ts`), re-reads the rows **inside** the transaction, and builds and returns its outcome from inside the callback too, so nothing throwable runs after the `await` — confirmed against Dexie 4.4.5's own source, where `oncomplete` resolves and emits `storagemutated` while `onabort` only rejects, so a rejection always implies no commit and no live-query invalidation. Candidate rows are selected by the **source identity alone**, so a route that only ever carried the target is left byte-identical. Within a selected row, `applyTagRename` maps the target identity too — that is what settles the surviving spelling there, and it is emphatically **not** what deduplicates a merge, since `normalizeRouteTags` already collapses two identity-equal entries on its own (see the negative controls below). Consequence, stated rather than hidden: a merge does not repair spelling drift that already existed among target-only rows; it cannot create any, because a rename to a novel identity has no target rows and a display-only respelling has source identity equal to target identity, so every carrying row is a source row. A row is written only when the result differs from the **raw** stored value by exact element-wise equality, so a legacy non-canonical row carrying the source is corrected rather than skipped as "unchanged". `sourceRouteCount` (rows carrying the source) drives every rider-facing message; `writtenRouteCount` exists so "writes only what actually changes" is directly testable and is deliberately never used for copy, since "0 routes updated" would be nonsense for a successful no-op. There is no `source-missing` status: `{ status: "applied", sourceRouteCount: 0 }` says the same with one fewer untested branch. No Dexie version or index change — `tags` remains the plain non-indexed field stage 1 established, and `db.verno` is still asserted to be `4`.
       - **One marker, one reconciler, one `setSelectedTagFilters` call.** Stage 3's inline stale-key pruning moved into a new pure `ui/library/tagLifecycleReconciliation.ts`, which now also carries the pending rename's filter follow and the manager's own completion. They must share one computation: two render-time blocks each deriving a next selection from the same stale render scope would clobber one another, and whichever ran second would win. Stage 3's existing filter tests pass unmodified and are the behaviour-preserving safety net for that move, exactly as `normalizeRouteTags`'s suite was for `tagIdentityKey`'s extraction in stage 1.
       - **A genuine two-signal handshake, with the complete terminal predicate.** The single `pendingTagLifecycle` state carries the repository's own counts (never a bare string, so completion words the result from what storage actually returned rather than a pre-submit UI count the corpus may already have invalidated). The write signal is the outcome being promoted to `applied` inside `.then()`; the corpus signal is per operation — `has(target) && !has(source)` for an identity-changing rename or merge, the established **spelling** for a display-only respelling (identity keys never change there, so a key-based predicate could not settle it), `!has(source)` for a delete, and **immediate** whenever `writtenRouteCount` is `0`, because the corpus will never fire again and waiting would hang the panel on "Applying…" forever — precisely the successful-no-op case that broke stage 2. Nothing short of the complete terminal predicate may clear the marker: on a promise-first **merge** the target already existed before the operation began, so a weaker "the target is present" test would be true immediately, the marker would be cleared while the source was still present, and the live-query removal that followed would prune the selection instead of carrying it across. While a rename marker is live but not yet terminal the source key is **protected from pruning**, or a live-query update arriving before the write settled would leave the follow nothing to follow; protection is never permanent masking, since the marker is always cleared by the terminal predicate, by a bounded-termination guard when both identities have vanished, or explicitly on failure. The follow only ever moves a filter the rider had **already** selected, so a global rename performed with no source filter active never spontaneously activates the target filter, and `Set` membership is itself the merge collapse when source and target were both selected. A delete needs no marker for the filter at all: stage 3's pruning already removes an identity whose last route stopped carrying it, and on a rejected delete the corpus never changed, so filters stay exactly as they were.
       - **Focus is never moved during rendering.** The render-time adjustment only records where focus must go; a `useLayoutEffect` consumes that hand-off exactly once (reading its marker ref **inside** the effect and performing no `setState`, the same shape as this file's own `pendingPinFocusIdRef` effect) and performs it. Final-tag deletion is an explicit two-phase transition: the panel is closed in the same render pass that records the hand-off, and Search is focused in the layout effect before the browser paints, so focus is never observably lost with the panel it was in. The panel's own render condition is `isTagManagerOpen` — **not** `tagSuggestions.length > 0` — which is what makes the live-query-first ordering safe: the corpus can empty before the write promise settles, and the panel simply shows an explicit "No tags left." state until both signals land. Every other manager focus move (cancel, retry after failure) goes through the same hand-off, after a real defect was found doing it inline: the confirmation disables the Rename/Delete buttons while it is open, and a browser ignores `.focus()` on a disabled element, so focusing in the same handler that cleared the confirmation silently dropped focus to `<body>`.
       - **A real admission check, not a notification after the fact.** `RouteListItem` reports two things upward — whether it has an inline interaction on screen, and whether a tag save is in flight (including the post-write handshake window, when the editor is still open) — and **deregisters both on unmount**, because a stale route id left registered would permanently prevent the manager from ever appearing. Opening the manager **refuses outright**, with a visible `role="status"` reason, while any tag save is in flight, while a route deletion is running, or while a route-switch prompt is **busy**, leaving that in-flight interaction completely untouched; an idle switch prompt or delete confirmation is cancelled instead, following `handleDeleteRequest`'s own established preamble. Otherwise it bumps a dismissal token and the panel renders only once no card interaction is open, so the manager and a card editor are never on screen together. In the other direction a synchronous `requestInlineEditorOpen()` is asked **before** a card editor opens, returning `false` while a lifecycle operation is applying. An in-flight route **rename** needs no signal, and the claim is narrowed rather than assumed: `handleRenameSubmit` calls `onRename` and immediately closes the editor, so nothing is open during that write, and `renameRoute` writes only `{ name }`, field-independent from the lifecycle's `{ tags }`.
       - **Fail-first, with one honest admission.** Before any production edit, a full journey test seeded three routes, tagged two "Gravel", opened Manage tags and renamed to "Trail" — confirmed failing on the unmodified `0.4.18` tree with a genuine `TestingLibraryElementError` at the "Manage tags" step, **after** the tagging journey itself had succeeded, not a compile error. For the repository there is **no honest fail-first for a brand-new storage function**: the obvious form (drive a loop of `updateRouteTags` calls and assert it is not atomic) fails only because the test itself chose a non-atomic implementation, which proves nothing about pre-existing product behaviour and is the same category as a manufactured compile failure. That is reported as such rather than dressed up; atomicity is instead proven positively against the real API and made load-bearing by negative control 1.
       - **Automated evidence.** `routeTags.test.ts` 30 → 56; `routesRepository.test.ts` gains a `tag lifecycle` block (17 tests, including the atomicity proof); new `tagLifecycleReconciliation.test.ts` (15), `tagLifecycleTarget.test.ts` (9), `tagLifecycleMessages.test.ts` (11), `RouteTagManager.test.tsx` (13) and `RouteLibrary.tagManagement.test.tsx` (27 real-`db`, real-live-query integration tests, kept out of the already ~3,000-line `RouteLibrary.test.tsx` following `src/App.pwaUpdateNotice.test.tsx`'s own split precedent). New Playwright spec `routeLibraryTagManagement.spec.ts` (chromium: the full rename → reload → merge → final-tag-delete journey with filter reconciliation, focus and empty state; Escape/Cancel; a case-only rename; 390×844 at 200% text; 844×390 landscape) plus one narrow rename journey added to `androidRouteLibraryTags.spec.ts`. The chromium spec's header states plainly that the **write-failure path is not e2e-testable** — IndexedDB writes never pass through `page.route()` — and is covered by Vitest instead.
       - **Negative controls, one of which did not discriminate and is reported rather than dropped.** Replacing the transaction with independent writes failed exactly the atomicity test and nothing else; comparing display strings instead of `tagIdentityKey` failed eight repository tests; selecting candidate rows by source ∪ target failed the target-only-row-untouched test; omitting the filter follow failed three integration tests; deriving the manager's choices from the filtered view failed three; bypassing the destructive confirmation failed six; closing the manager on a rejected write failed two; removing prune-protection failed two reconciler tests; clearing the marker on "target present" alone failed the promise-first merge test; and following regardless of whether the source was selected failed the no-active-source-filter test. **The exception:** "omit merge deduplication" — removing the target mapping from `applyTagRename` — did **not** discriminate, because `normalizeRouteTags` already collapses identity-equal entries by itself, so dedup was never what that mapping bought. Its real contribution is the surviving **spelling** when an affected row happens to carry the target first under a different spelling. That is a genuine coverage gap, closed by adding a spelling-convergence test (`["TRAIL", "Gravel"]` renamed to "Trail" must store `["Trail"]`, not `["TRAIL"]`), after which the control failed as expected.
       - **Confirmed pre-existing, unrelated to this change.** `RidingScreen.finishEndRide.test.tsx`'s wake-lock cleanup test flakes under this sandbox's full-suite CPU contention — reproduced on a stashed clean baseline under identical load, and passing in isolation every time — the same class already documented for item 32. Separately, three stale `.claude/worktrees/` checkouts in this sandbox contain their own older copies of these test files, which Vitest's default collection also picks up and which fail on the untouched baseline too; test runs here were scoped to the real checkout with `--dir src`. Neither affects CI, which builds from a fresh checkout.
       - Stage 4B (physical-device acceptance and documentation closure) remains open — see `docs/project/current-status.md` for its checklist. Shipped as `0.4.19`; browser-tested (Vitest and Playwright chromium/android-chrome; WebKit could not launch in this sandbox — `libgtk-4.so.1` and `libevent-2.1.so.7` are missing — and is deferred to CI) but **not** physically accepted on a device.
     - **Installed-iPhone field test (9 September 2026, deployed `0.4.19`).** The user worked through both stage 4B checklists on the installed iPhone Home Screen PWA, in portrait at default text size, over a library of eight saved routes (three pinned), with tags treated as disposable test data. The great majority passed — the whole stage-2/stage-3 editor and filter journey, and the global rename, display-only respelling, merge, delete, filter reconciliation, final-tag deletion and empty state, reload persistence and route integrity. **Stage 4B did not close.** Two reveal-scrolling observations were reported (Cancel on a tag edit does not bring the card's top back into view; the Manage tags confirmation is neither scrolled into view when armed nor scrolled back on cancel), enlarged (accessibility) text turned out not to be genuinely exercisable this way — nothing here opts into iOS Dynamic Type, so the system Larger Text setting does not resize the application's own text — and short landscape and the manager's on-screen-keyboard case were declined. `docs/project/current-status.md` is the authoritative ledger and carries the exact mapping, including what stays open; physical Android verification remains separately outstanding.

---

<a id="item-104"></a>

## Item 104 — Preserve route progress through exactly overlapping out-and-back turnarounds — done

_Category: Ride navigation reliability_

104. **Preserve route progress through exactly overlapping out-and-back turnarounds — done** - **Real iPhone field symptom (pre-fix evidence).** On an out-and-back route whose route-before and route-after the turnaround overlap exactly, the app never caught that the rider had reached the turnaround. Continuing physically beyond it in the original direction only ever showed the "Possibly off route" message; turning around and riding home along the coincident return leg instead made the app "go back in time" — displayed progress moved backward instead of continuing forward. This report identifies the pre-fix defect only; it is not evidence that the correction below works on a real device — see the outstanding real-device checklist in `docs/project/current-status.md`. - **Deliberately reprioritised ahead of item 98.** Item 98 (direction-aware active-Riding route-segment layering) is a map-presentation slice whose own contract requires deriving its current/near-ahead overlay from matched canonical route progress, not GPS bearing alone. Building that overlay on top of progress that can run backwards would just make it faithfully highlight the wrong leg. Item 104 is confined to route projection/matching; it adds, reorders or restyles no map layer, and item 98 remains selected next, unimplemented in this slice, and now has a corrected progress input to consume without reopening the matcher (see `docs/project/backlog.md`'s own item 98 cross-reference). - **Fail-first reproduction, measured against unmodified `main`.** A new fixture, `src/test/fixtures/outAndBackCoincidentRoute.ts` (`OUT_AND_BACK_COINCIDENT_ROUTE_POINTS`, ~14 m spacing, ~2099 m per leg, plus a second, deliberately sparse/irregular `OUT_AND_BACK_COINCIDENT_SPARSE_ROUTE_POINTS`, ~320–670 m segments ending in a ~50 m final approach), was built with an EXACT coordinate-for-coordinate retrace of the outbound leg on the return leg — unlike the pre-existing `outAndBackRoute.ts`, whose return leg is deliberately offset ~11 m in latitude (confirmed by direct arithmetic, `0.0001° × 111,320 m/° ≈ 11.13 m`) so its two legs are never tied, which is exactly why it never caught this bug. Two of the required traces genuinely failed against unmodified `src/navigation/projection.ts`, deterministically seeded via a directly-constructed `ProjectionMatch` rather than a fresh reacquire (a fresh whole-route reacquire on this fixture is itself subject to the same ambiguity the fix addresses, since every point on the coincident stretch has a tied mirror somewhere in the array — see the fixture's own module comment):
     - **Trace A** (exact fix at the turnaround, then a fix 20 m onto the return leg): `firstReturnFix.distanceFromStartMetres` measured `2079.32`, required to be greater than `atTurnaround.distanceFromStartMetres` (`2099.32`) — it decreased instead of advancing.
     - **Trace B** (a near-turn fix 8 m short of the turnaround, then a sequence of fixes progressively further along the return leg with no exact apex fix): the first sequence step measured `2074.32`, required to be `>=` the previous step's `2091.32` — it regressed.
     - A third, deliberately constructed "normal forward approach" test (`lastMatch = T − 30`, next fix `T − 20`, both comfortably before the turnaround) did **not** fail against unmodified code — recorded honestly rather than invented, since the pre-fix defect is specifically about resolving the turnaround/return-leg tie, not about ordinary approach. - **Confirmed root cause — with a correction to the original hypothesis.** `src/navigation/projection.ts`'s windowed branch (`projectFixOntoRoute` → `nearestWithinRange`) makes exactly one Turf `nearestPointOnLine` call over the whole ±`WINDOW_RADIUS_METRES` (400 m) window, trusting its single winning candidate unconditionally. Near a turnaround, that window spans both the outbound approach and the return departure (both fall within `[lastMatch.distance − 400, lastMatch.distance + 400]` in cumulative-distance terms, even though they are the same physical line traversed in opposite directions). The installed `@turf/nearest-point-on-line` (v7.4.0, confirmed in `node_modules`) resolves an exact geometric tie via a **strict** `<` comparison (`dist/esm/index.js:80`) — the first-encountered (lowest array index, i.e. outbound) candidate wins on a true tie, and any later exact tie is silently discarded. The original hypothesis was that this would **always** resolve to the outbound occurrence. Direct measurement (see `LATERAL_TIE_TOLERANCE_METRES` calibration below) showed this is not quite right: because the outbound and return coordinates are computed via independently-evaluated floating-point expressions (`0.03 × (i/150)` versus `0.03 − 0.03 × (j/150)`), they are not bit-identical, so which candidate has the marginally smaller computed lateral distance — and therefore wins — is effectively erratic rather than deterministically "always outbound". This is a more precise, and arguably more concerning, characterisation of the field defect than the original hypothesis: the pre-fix behaviour near a turnaround is not simply biased backward, it is unpredictable. - **Why a single split point cannot work — worked counter-example.** Two intermediate designs (splitting the window into two sub-ranges at `lastMatch.pointIndex`, then at the array index nearest to `lastMatch.distanceFromStartMetres`) were tried and rejected before implementation, following review. A single pivot can only separate two tied occurrences into different sub-ranges when it falls _between_ them — but the two tied occurrences near a turnaround are symmetric around the turnaround itself, not around `lastMatch`. Worked example: turnaround `T = 2100 m`, a vertex at `2050 m` (one 50 m segment leading into the turnaround), `lastMatch.distanceFromStartMetres = 2070` (closer to the earlier vertex than to `T`). A fix at the coordinate shared by outbound-`2060` and return-`2140` splits at the nearest-vertex pivot `2050` — **before both tied occurrences** — so both land in the same ("leading") sub-range; that sub-range's own internal Turf call resolves the tie by array order and returns `2060` only, discarding `2140` before any comparison logic runs. The result depends on where `lastMatch` happens to fall relative to nearby vertices, not on the actual route geometry. - **Why gap-size alone cannot justify forward preference — worked counter-example.** An initial decision rule (prefer whichever tied candidate is forward of `lastMatch` whenever the two candidates' route-distance gaps from `lastMatch` differ by no more than a margin) was also rejected. Counter-example: `lastMatch = T − 30` (still 30 m short of the turnaround, ordinary forward approach), next ordinary fix at `T − 20` — tied on this exactly-coincident geometry with its own not-yet-reached return mirror at `T + 20`. `gapTrailing = |(−30) − (−20)| = 10`; `gapLeading = |(−30) − 20| = 50`; difference `= 40`, comfortably within a tens-of-metres margin — wrongly jumping the rider onto the return leg while still on the way out. The root error: `gapLeading − gapTrailing = 2·min(a, b)` (where `a` is how close `lastMatch` was to the crossover and `b` is how close the _new_ fix is) is symmetric in `a` and `b` — small either because `lastMatch` was close, or merely because the new fix happens to be, which does not on its own prove the turnaround was reached. - **The exact correction, in two parts.** `src/navigation/projection.ts`'s windowed branch now, after computing `primary` exactly as before:
     1. `findTiedOccurrences` scans every individual segment in the window (a lightweight two-point `nearestPointOnLine` call each, reusing Turf's own per-segment projection — no new geometry maths), collecting every segment whose own lateral distance is within `LATERAL_TIE_TOLERANCE_METRES` of `primary`'s, and clusters them into distinct **occurrences**. A qualifying segment joins the previous cluster only when it is BOTH array-index-adjacent AND its own matched route-distance is within `OCCURRENCE_SEPARATION_METRES` of the previous one's — array adjacency alone is insufficient, since the last outbound segment and first return segment always share the turnaround vertex and so are always array-adjacent even when they represent two occurrences many metres apart (this specific gap was found and fixed during implementation via a direct debug probe: with adjacency-only clustering, a fix 10.1 m past the turnaround still returned only ONE occurrence, silently discarding the genuinely tied return candidate). If exactly one occurrence survives, `primary` is returned unchanged — ordinary, non-tied fixes are unaffected.
     2. When more than one occurrence survives, `selectAmongOccurrences` picks the occurrence closest in route-distance to `lastMatch` ("continuity-nearest") by default. It only overrides that default when the continuity-nearest occurrence is a **genuine regression** — more than `PROGRESS_EPSILON_METRES` below `lastMatch`'s own distance, not merely equal to or infinitesimally below it — **and** some other occurrence is a **genuine advance** (more than `PROGRESS_EPSILON_METRES` above) whose own gap from `lastMatch` is within `CONTINUITY_PREFERENCE_METRES` of the continuity-nearest occurrence's own gap.

     `findWindowRange`, `isClippedAtEdge`, `nearestWithinRange` itself, and the whole-route reacquire fallback are all byte-for-byte unchanged — this fix is confined to the one call site inside `projectFixOntoRoute`'s windowed branch. - **Three new named constants, each measured, not guessed:**
     - `LATERAL_TIE_TOLERANCE_METRES = 0.01` (1 cm). Calibrated by direct measurement (a dedicated calibration probe, not assumption): across an on-line fix, a mid-segment-interior projection, a fix laterally offset 5 m from the line, and an endpoint-clamped case at the turnaround vertex, plus a sweep over 140 outbound/return mirror pairs, the largest observed lateral-distance delta between two genuinely coincident candidates was `5.7 × 10⁻⁸` m (57 nanometres) — floating-point noise. 1 cm sits roughly five orders of magnitude above that measured floor, and comfortably below `outAndBackRoute.ts`'s own ~11 m offset, which must stay distinguishable, not absorbed into a tie. Handling nearly-parallel-but-physically-distinct geometry (separate carriageways, an adjacent cycleway) is explicitly out of this item's scope and was not, and must not be, smuggled in via a loose tolerance.
     - `CONTINUITY_PREFERENCE_METRES = 30` m. Pinned by TDD against the required traces (Trace B's realistic-sampling sequence, and the "normal forward approach" tests) — related to realistic fix-to-fix route-distance movement at road-bike speeds with ordinary GPS sampling gaps, not GPS accuracy directly. A dedicated boundary test constructs `gapDifference` exactly `±0.1 m` either side of `30` (derived from the constant's own exported value, not a hardcoded literal) and confirms the override fires just under and does not just over — real measurement showed the true boundary is slightly less crisp than the idealised arithmetic suggests (interpolation noise in test-fixture coordinate construction), which is itself informative: even the textbook Trace-A case (`gapDifference = 0` exactly, in principle) measurably fails once `CONTINUITY_PREFERENCE_METRES` is forced to `0` in the negative-control run below, not just Trace B — confirming the constant is more broadly load-bearing than the idealised derivation alone implied.
     - `PROGRESS_EPSILON_METRES = 5` m. Scaled to realistic GPS/coordinate jitter at a stationary or near-stationary fix, deliberately smaller than `CONTINUITY_PREFERENCE_METRES`. A dedicated boundary test constructs a regression magnitude exactly `±0.1 m` either side of `5` and confirms the override fires just over and does not just under.
     - `OCCURRENCE_SEPARATION_METRES = 1` m (a fourth, implementation-level constant, not part of the plan's original three, found necessary during implementation — see the clustering gap above). Kept well under `PROGRESS_EPSILON_METRES`, since a genuine single-occurrence discrepancy between adjacent segment framings is expected to be at most a small fraction of a metre. - **Rejected shortcuts**, per the hand-off's own explicit list, none implemented: a global `Math.max(previous, next)` progress clamp (would leave the matcher stuck forever at the turnaround and corrupt genuine backtracking/reacquisition); always selecting the geographically tied candidate with the largest route distance; always selecting the candidate ahead of `lastMatch` without directional evidence (the rejected gap-size-alone rule, above); GPS bearing as sole authority; suppressing or weakening the `Possibly off route`/`Off route` thresholds; enlarging `WINDOW_RADIUS_METRES` until a fixture happened to pass; source-kind, route-feature-kind or elevation-based special cases; a UI-only correction papering over a wrong navigation core; a production-only testing hook or fixture-specific coordinate exception. - **Accepted, honestly-documented residual ambiguity.** Backtracking that begins very close to the crossover, where the regressing candidate's own gap also happens to fall inside `CONTINUITY_PREFERENCE_METRES`, remains irreducible from route-distance evidence alone — this design deliberately resolves that narrow zone forward, to avoid the strictly worse "goes back in time" symptom. Documented directly in `selectAmongOccurrences`'s own doc comment and exercised (not asserted to a specific winner, since the correct resolution is legitimately boundary-dependent on the pinned constants) by a dedicated "return-vs-backtracking ambiguity characterisation" test on the sparse fixture (`T ≈ 2098`, `lastMatch = T − 27`, tied candidates at `T − 37`/`T + 37`): with the constants above, `gapDifference = 54 > 30`, so the continuity-nearest (regressing) candidate wins — read as "still approaching, wobbled backward" rather than "already turned around", the correct, conservative reading given no other evidence. - **A real, distinct interaction found via the E2E layer, outside item 104's own windowed-branch scope.** While building the required real-browser evidence on a deliberately sparse (100 m-spaced) GPX, a large single fix-to-fix jump (from 600 m to 300 m physical, a 300 m cumulative jump) reproduced the field-class symptom again — but for an unrelated, pre-existing reason: the correct windowed candidate sat at `range.endIndex − 1`, tripping `isClippedAtEdge`'s existing, unmodified "genuinely clipped edge" rejection (item 46's own mechanism, deliberately left untouched by this item), falling through to the whole-route reacquire fallback — which is, by design, still subject to the same Turf tie-break ambiguity this item does not touch. Confirmed via a direct unit-level probe reproducing the exact scenario (`reacquired: true`, resolved to the wrong, `299.66`-distance occurrence). This is not a defect in this item's own fix: `isClippedAtEdge` and the whole-route fallback are explicitly out of scope, and the same rejection would occur for any sufficiently sparse, ordinary (non-coincident) route landing a genuine match near a coarse-grid window edge. Resolved by walking the E2E sequence in smaller, more frequent steps (matching the GPX's own 100 m trkpt spacing) so `lastMatch`'s own window never lands a candidate near a clipped edge — the same discipline `ridingFinishAndEnd.spec.ts`'s own closed-loop test already documents for an analogous reason. Verified via a fast unit-level walk of the full corrected sequence before re-running the slower E2E harness. - **Tests added, four layers, each proving a distinct boundary:**
     - `src/navigation/projection.test.ts`: the normal-forward-approach trace (dense and sparse/both-halves-of-final-segment variants), Traces A–D (plus a "Trace D variant" with a genuinely tied leading candidate present in-window), a stationary/repeated-fix test and a jitter test (both proving the continuity-nearest occurrence is retained, not flapped, on noise alone), the return-vs-backtracking ambiguity characterisation, and three permanent boundary tests (one per named constant, each derived from the constant's own exported value). All pre-existing tests in this file — including `outAndBackRoute.ts`'s offset fixture, the self-intersecting and closed-loop continuity tests, and the window-clipping tests — pass unmodified.
     - `src/navigation/rideNavigationCore.test.ts`: a full walk of the coincident fixture through `processFix()` proving `lastMatch`/`lastReliableMatch` both advance monotonically through approach/turnaround/return with no false reacquire, plus an explicit-step test distinguishing the raw off-route classification from the debounced `offRouteMachineState.level` through a beyond-turn excursion (three consecutive raw off-route fixes, per `CONSECUTIVE_TO_ESCALATE`, before the displayed level flips) and a genuine return that resumes both `lastMatch` and `lastReliableMatch` beyond the turnaround.
     - `src/ui/riding/useRideNavigation.test.ts`: one narrow storage-round-trip test — seeds `setActiveRideState` with progress exactly at the turnaround on the coincident fixture, restores via the hook, delivers a fix onto the return leg, and asserts `distanceRemainingMetres` decreases. No IndexedDB schema change: `lastMatchedPointIndex`/`matchedDistanceFromStartMetres`/`lastReliableMatchedPointIndex`/`lastReliableMatchedDistanceFromStartMetres` already round-trip via `src/storage/mapping.ts`'s existing fields.
     - `e2e/ridingOutAndBackTurnaround.spec.ts` (new): drives a real, exactly-coincident out-and-back GPX through `installLocalMapStyle` + real (mocked) `context.setGeolocation()` fixes. Outbound remaining distance decreases through several intermediate steps; three distinct fixes past the turnaround (each awaited via `waitForPersistedFixLongitude`, polling the persisted `lastFix` coordinate — since the debounced warning text itself does not change between the first two overshoot fixes, satisfying `CONSECUTIVE_TO_ESCALATE`, before `"Off route"` appears on the third); remaining distance confirmed frozen at the turnaround's own value throughout the excursion; a genuine return through two consecutive on-route fixes (satisfying `CONSECUTIVE_TO_DEESCALATE`) recovers `"On route"`; sustained, continuing advance over four further consecutive return-leg fixes — deliberately ending there, per the hand-off's own scope boundary, rather than driving to full route completion (left to item 46/29's own existing, unmodified coverage). - **Negative controls**, each mutation applied, the predicted failure confirmed, then reverted and the full suite re-verified green:
     1. Occurrence scan disabled (`windowed = primary` unconditionally): Trace A and Trace B failed exactly as they did against unmodified `main`, plus all three boundary tests (which depend on the mechanism existing at all).
     2. Directional/epsilon gates removed from `selectAmongOccurrences` (always attempt the override on gap size alone — the earlier, rejected rule): the sparse both-halves normal-approach test failed, along with the repeated-fix, jitter, and `PROGRESS_EPSILON_METRES` boundary tests — the dense normal-approach test alone did not discriminate this mutation, since in that specific construction the forward candidate already happens to be the default continuity-nearest choice regardless.
     3. `CONTINUITY_PREFERENCE_METRES` forced to `0`: Trace A, Trace B, Trace C and all three boundary tests failed (a broader failure than the idealised "only Trace B" prediction — see the constant's own entry above for why).
     4. `PROGRESS_EPSILON_METRES` forced to `0`: the jitter test failed (flapping onto the return leg on noise alone); the repeated-identical-fix test did not discriminate this specific mutation (a zero gap is never `> 0` regardless of epsilon).
     5. Each constant's own boundary test, mutated at its documented `±0.1 m` edge, flipped as predicted (see the constants section above).

    - **Contrast with item 46.** Item 46 (`docs/project/history/items-39-48.md#item-46`) fixed a windowed match wrongly rejected as clipped near a closed loop's finish (`isClippedAtEdge`'s two-edge independence) — a different failure mode from this item's, where both the outbound and return candidates are fully, legitimately inside the window and `isClippedAtEdge` cannot help either way. Item 46's own closed-loop coverage (`projection.test.ts`'s "closed-loop start/finish coincidence" tests, `rideNavigationCore.test.ts`'s "closed loop" describe block) was re-run unmodified and stays green — this item's occurrence scan never finds more than one occurrence near a closed loop's own finish, since the shared start coordinate (distance ≈ 0) is never within the ±400 m window of a fix approaching the finish (distance ≈ total).

    - **Files changed.** `src/navigation/projection.ts` (production); `src/test/fixtures/outAndBackCoincidentRoute.ts` (new fixture); `src/navigation/projection.test.ts`, `src/navigation/rideNavigationCore.test.ts`, `src/ui/riding/useRideNavigation.test.ts` (tests); `e2e/ridingOutAndBackTurnaround.spec.ts` (new e2e); `package.json`/`package-lock.json` (version `0.4.7` → `0.4.8`, hand-patched, no `npm install`); root `CLAUDE.md` (completed-items statement extended, item 98 remains selected next); `docs/project/backlog.md` (item 98 cross-references this item as its completed prerequisite); `docs/project/README.md` (stable item-number range extended to 6–104); this file; `docs/project/current-status.md` (item 104's own post-fix acceptance checklist added, item 97's narrow phone observation recorded).
    - **Verification.** `npm run format`, `npm run lint` (clean; the pre-existing root-owned `test-results-root-owned-leftover-DELETE/`/`_stale-root-owned-artifact-DELETE/` write errors during `format` are the same already-documented untracked, unrelated leftovers as prior items). `npm run typecheck` (`tsc -b --noEmit`) and `npm run build` — the already-documented root-owned `node_modules/.tmp` cache-write gap recurred; confirmed cache-write-only via the same documented `tsBuildInfoFile` scratch-path workaround used by prior items, run clean, then reverted (confirmed via `git diff` showing no residual change to either tsconfig file). `npm test` — **3118/3118** passed across 153 files (+16 tests from the `3102/153` baseline). `npm run e2e` — **273/273** passed across the `chromium` and `android-chrome` projects combined, confirmed on repeated full-suite runs including one at a reduced, CI-comparable worker count (`--workers=4`); `webkit-smoke` reproduced the exact same 7-test, same-missing-library (`libgtk-4.so.1`, `libevent-2.1.so.7`, `libgstcodecparsers-1.0.so.0`, `libavif.so.13`) pre-existing local-sandbox gap already documented by prior items — not a new regression. Under this sandbox's default (very high, 72-core-derived) parallelism, three separate full-suite runs each showed exactly one unrelated test fail once (`e2e/mapImageryRecovery.spec.ts`, `e2e/ridingImmersiveShell.spec.ts` twice, `e2e/rideSessionSwitchGuard.spec.ts`), none touching any file this item changed, each passing cleanly in isolation; the `ridingImmersiveShell.spec.ts` instance is the same test and assertion (`getByText("Route complete")`) already named as a confirmed instance of item 32's own documented contention-sensitive flake class — see that item's own "Further evidence" entries in `docs/project/current-status.md`, now including this item's own observation. The new e2e spec's own test was run 6 times total across isolated and full-suite runs with no flake. `npm run format:check` (run last) — the unrestricted repo-wide check surfaced only the same pre-existing, untracked, root-owned `*-DELETE` leftover directories; a scoped `prettier --check` against exactly the 6 files this item touched passed clean, and a further targeted pass, including a structural re-check after this documentation entry's own list nesting needed correcting (see below), confirmed all touched Markdown stable. `git diff --check` clean. `npm ci --dry-run` completed without error this run (`up to date`); `npm ls --depth=0` resolved cleanly. `npm audit --omit=dev` could not reach the registry from this sandbox (`self-signed certificate in certificate chain`) — a sandbox network-egress limitation, not a finding; no dependency changed in this slice (`package-lock.json`'s diff is exactly the two version-string lines), so this does not block. CI's own `npm ci`/audit steps are authoritative.
    - **A documentation-structure correction, recorded for honesty.** This entry's own first draft nested several top-level bullets (including "Real iPhone field symptom", "Deliberately reprioritised" and "Fail-first reproduction") without the indentation/spacing CommonMark needs to keep them distinct from the `104. **Title**` numbered wrapper's own paragraph, and a subsequent `prettier --write` pass — correctly, per strict CommonMark rules — collapsed the affected bullets onto single, very long lines rather than silently fixing the underlying structure. Caught by an explicit post-format structural check (an unusually long-line scan plus a check for multiple bold section headers sharing one line), not by visual inspection alone, and corrected by rewriting this entry with consistent 4-space-indented bullets throughout, matching item 97's own proven pattern exactly.
    - **The item-97 phone observation, recorded narrowly.** After deployment, the user reported of item 97's compact untrusted-GPX warning: "Okay, works great!" This confirms only that the principal compact-warning interaction was positively observed on the phone — it is not read as confirming every timer boundary, the Map/Profile transition, landscape/enlarged-text presentation, VoiceOver behaviour, or physical Android behaviour, all of which remain the open subchecks already listed in `docs/project/current-status.md`'s own item 97 entry.
    - **Limitations.** Automated (Vitest/Playwright Chromium) evidence only. No physical iPhone or Android verification is claimed for this item — see `docs/project/current-status.md` for the specific outstanding real-device acceptance checklist (approach, turnaround, a deliberate short beyond-turn excursion, the return leg, warning recovery, remaining distance, pause/resume). Playwright's `android-chrome` project is Chromium-emulated mobile viewport/UA/DPR, not real Android Chrome or WebView. The three new named constants (plus the fourth, implementation-level `OCCURRENCE_SEPARATION_METRES`) are pinned by TDD against this item's own required traces and boundary tests, not against a broader corpus of real-world GPX turnaround shapes; if a materially different real-world case is found, treat it as new evidence for a possible follow-up, not proof this fix is wrong.

    ---

    - **Follow-up (2026-09-04): the first physical acceptance failed, and why.** The correction above shipped as `0.4.8` on automated evidence alone. The user then walked a real, short out-and-back on the installed iPhone PWA and it failed in the field: after turning onto the exactly retraced return leg, committed progress ran **backwards**. Two screenshots one minute apart, both reading `On route` at `GPS ±14 m · Live`, showed the remaining/status line move `0.1 km` → `0.2 km` and the trusted finish cue move **150 m → 180 m**. The route's non-identifying structure: **eight track points, 374.519 m total**, an approach at 0/30.877/38.182/113.689/114.808 m, a **turnaround at 225.313 m**, and a **110.505 m mirrored leg traversed exactly in reverse** (its two shared endpoints byte-identical) before a 38.7 m finish exit. Because the finish manoeuvre sits at 374.519 m, the two cue readings pin committed progress to ≈ `T − 0.8 m` and then ≈ `T − 30.8 m` — i.e. the regression began with the anchor essentially **at** the turnaround, which is precisely where the correction above was expected to work.

    - **Diagnosis was gated on reproduction, not hypothesis.** No production file was edited until the private GPX had been run through the **production** importer (`importGpxFile`) and instrumented traces replayed through the **production** `processFix`/`projectFixOntoRoute`/`classifyFix`. A verbatim JS mirror of `projection.ts`'s private internals was used only to observe occurrences and vary the selection policy, and was validated fix-for-fix against production output first (**0 mismatches** across every trace). Production import confirmed the structure exactly, accepted the geometry digest, and confirmed the route as trusted with six manoeuvres.

    - **Confirmed root cause: rolling-anchor epsilon erosion.** `selectAmongOccurrences` returns the continuity-nearest occurrence immediately unless it is a regression **strictly greater than** `PROGRESS_EPSILON_METRES` (5 m) relative to `lastMatch`; below that the forward override is never even evaluated. That is correct for a single projection, but every accepted match becomes the next fix's own anchor, so the epsilon is applied **per fix and never cumulatively**. At walking cadence each backwards step is roughly 0.5–1.4 m of route distance, so `isGenuineRegression` is never true and the match walks smoothly down the outbound mirror for as long as the rider walks home. The replay reproduced the field numbers directly: from a sampled apex, committed progress and the cue read 150.6 m one fix later and **180.0 m at 30.8 m along the return** — the screenshots' own pair, at ≈ 0.5 m/s. The defect is therefore **cadence-dependent, not short-route-specific**: it reproduces on this project's own dense item-104 fixture at 1.4 m steps and disappears entirely at 7 m steps. Every test written for item 104 above steps 8, 25, 42, 45, 65 or 100 m at a time, which is exactly why none of them could reach this state.

    - **Two competing hypotheses refuted by measurement, not by precedent.** (a) *Tie not detected on diagonal geometry.* `LATERAL_TIE_TOLERANCE_METRES` had only ever been calibrated against constant-latitude fixtures, so it was plausible that two genuinely mirrored candidates on a real diagonal segment might differ by more than 1 cm. Measured on the real route at lateral offsets of 0/1/3/8/14/25 m and at 5/20/55/100 m from the apex, the two occurrences' lateral distances are **bit-identical** (delta exactly `0.000e+0`). Refuted. (b) *Clipped window / whole-route reacquire.* On a 374.5 m route, `WINDOW_RADIUS_METRES` (400 m) always spans the whole route, and `isClippedAtEdge` is structurally false at both edges. Confirmed by direct measurement rather than assumed from item 104's own earlier E2E observation. Refuted.

    - **The correction: an explicit disposition, and a hold in the navigation core.** `ProjectionResult` gains a required `disposition: ProjectionDisposition` (`"resolved" | "tied-sub-epsilon-regression"`, `src/navigation/types.ts`). `projectFixOntoRoute` does not change **what it returns** — the projection remains the honest current geometry, with its own `matchedCoordinate`, `lateralDistanceMetres` and the genuinely selected occurrence's own route distance; nothing is ever recombined from an earlier fix. It only labels the result, via a small private predicate `isTiedSubEpsilonRegression` reusing the existing `PROGRESS_EPSILON_METRES`, and only when more than one tied occurrence exists. `processFix` then declines to adopt a labelled projection, holding **both** `lastMatch` (the basis the next fix's regression is measured against) and `lastReliableMatch` (what the rider sees as remaining distance, remaining ascent and the trusted cue). Classification is computed **before** the hold decision and always from this fix's own lateral distance, so a rider drifting sideways while progress is held still escalates through the unchanged `offRoute.ts` debounce. `findWindowRange`, `nearestWithinRange`, `findTiedOccurrences`, `selectAmongOccurrences`, `isClippedAtEdge` and all four existing constants are otherwise untouched.

    - **The hold is bounded by cumulative regression, not by time or travel.** It ends once cumulative tied route-distance regression from the stable anchor exceeds `PROGRESS_EPSILON_METRES`, at which point the **unchanged** selector runs and resolves the fix either onto the return occurrence or onto genuine outbound backtracking. A stationary or sub-threshold-jittering rider therefore stays held indefinitely — correct, not a stall, because they are supplying no movement evidence at all. Measured: a full walking ride of the field route's structure from start to finish gave a **worst backwards step of 0.0 m** (against 1.4 m continuously on the unmodified tree) while reaching the identical final progress; a return leg walked with ±14 m lateral jitter finished on the correct branch, likewise with a worst backwards step of 0.0 m.

    - **No new constants, no new state, no storage change.** The scope gate is the existing tie itself. Measured across this repository's own fixtures, the hold **never engages** on non-coincident geometry: offset out-and-back (~11 m), self-intersecting and closed-loop routes produced **0 ties and 0 holds over roughly 7,000 fixes** at both 7 m and 1.4 m cadence, with byte-identical results either way; the dense coincident fixture at the existing tests' own 7 m cadence produced 57 ties and **0 holds**. Because the held anchor **is** `lastMatch`, and `lastMatch` already round-trips through `src/storage/mapping.ts`, a pause taken mid-hold persists exactly the right value — verified through the real `toStoredRideState`/`fromStoredRideState` at every hold position (restored anchor correct, post-resume worst backwards step 0.0 m in each). An earlier draft of this follow-up proposed one optional persisted scalar recording a proven-passed turnaround; the measurement above showed it was unnecessary, and it was dropped.

    - **Designs rejected on evidence, not preference.** An earlier plan proposed a bounded directional state machine: a sticky movement reference with accuracy- and timestamp-gated displacement, bearing comparison against candidate tangents, an apex-plausibility gate, a geometric/topological antiparallel-retrace proof, and five new constants. Every part of it was dropped once measured. The genuine-backtracking boundary is **exactly 15/16 m** — precisely `CONTINUITY_PREFERENCE_METRES / 2`, inherited unchanged from item 104 — and at ±14 m reported accuracy that lies **inside one accuracy radius**, so the proposed accuracy-scaled apex band (≈14 m) would not have improved on it. The antiparallel proof was dropped because the 1 cm tie gate is already exact in practice, and because a same-direction repeat that also ties is handled safely anyway (a ≤5 m hold that the unchanged selector then resolves forward). The rejected-shortcut list from item 104 above still stands in full: no monotonic clamp, no always-largest-distance rule, no fix history, no bearing-as-sole-authority, no weakening of off-route thresholds or debounce, no route-shape special cases.

    - **Fixtures added.** `OUT_AND_BACK_COINCIDENT_SHORT_ROUTE_POINTS` (a third export in `src/test/fixtures/outAndBackCoincidentRoute.ts`): eight points, ~372 m total, a **diagonal** ~110 m mirrored leg whose shared endpoint is used twice **by value** so indices 4 and 6 are byte-identical — the structural analogue of the field route at unrelated generic coordinates, and the case that proves the tie holds on genuinely two-dimensional geometry. `src/test/fixtures/repeatedPassRoute.ts` (new): a stem traversed **twice in the same direction** around a short loop, the parallel-rather-than-antiparallel coincidence that keeps the "no antiparallel proof" decision honest; its loop is deliberately short enough that both occurrences fall inside one ±400 m window. Both are built through a new `buildRoutePointsFromCoordinates` helper in `src/test/fixtures/routeGeometry.ts` — extracted from, and now reused by, `buildRoutePointsFromWaypoints` — because interpolating an endpoint as `from + 1 × (to − from)` is not guaranteed to reproduce `to` exactly in IEEE-754, which would have made "byte-identical" merely "nearly identical".

    - **Tests added, each failing first on the unmodified tree.** `projection.test.ts`: seven new tests covering diagonal tie detection, the exact `PROGRESS_EPSILON_METRES` boundary and the first value past it, a tied advance, an ordinary small regression on non-tied geometry, a whole-route reacquire, and both same-direction-repeat cases. `rideNavigationCore.test.ts`: six new tests — the decisive walking-cadence trace asserting that **neither committed progress nor the trusted cue regresses at any intermediate fix** (the cue driven through the real `selectNextManoeuvre` with its own `reachedIndex` threaded), the hold's exact reporting contract (projection describes the current candidate; both anchors are the *same objects* as before), the epsilon boundary, stationary/sub-threshold jitter, backtracking either side of the 15/16 m boundary, and classification continuing during a hold. `useRideNavigation.test.ts`: a resume-mid-hold test proving the already-persisted stable match is sufficient. `e2e/ridingShortTurnaroundWalkingPace.spec.ts` (new): an eight-point, sub-400 m GPX carrying **valid ACN navigation metadata** (its `geometrySha256` computed over the same `String(x)` values written into the trkpts), ridden across the turnaround in ~3 m steps with the persisted committed distance read after **every** fix, plus the rendered trusted cue asserted at visible checkpoints. `e2e/ridingOutAndBackTurnaround.spec.ts` is deliberately left **unchanged** as complementary long-route coverage.

    - **Fail-first values, recorded.** `rideNavigationCore.test.ts`'s decisive test failed on unmodified code with committed progress dropping from `223.726` to `222.326` on the very first return fix. `useRideNavigation.test.ts`'s resume test failed with remaining distance **rising** `148.744` → `150.144` — the field symptom exactly. The new E2E failed on its every-fix committed-progress assertion. The seven `projection.test.ts` tests failed only on the missing label, with their geometry assertions already passing — which is itself the evidence that the diagonal tie was being detected correctly all along.

    - **Negative controls**, each applied alone, the predicted failure confirmed, then reverted:
        1. Predicate disabled (`isTiedSubEpsilonRegression` always false): the new E2E fails on its committed-progress assertion, reproducing the original regression in a real browser. A first attempt to disable it by removing the call instead failed to **compile** (`noUnusedLocals`), which is incidental but useful evidence that the predicate is genuinely load-bearing.
        2. Tied gate (`occurrences.length > 1`) dropped: the non-tied small-regression test fails, i.e. ordinary backwards movement would start being held.
        3. Strictly-positive lower bound relaxed (`> 0` → `>= 0`): **did not discriminate** — all tests still pass. Recorded honestly rather than dropped. Analysis: a zero regression means the candidate equals the anchor, so holding and adopting produce equal-valued progress and differ only in object identity. `> 0` is retained because it states the intent exactly — only a genuinely *backwards* step is ever held, and a stationary fix stays on the ordinary path — not because a test distinguishes it.
        4. Holding `lastMatch` but not `lastReliableMatch`: four core tests fail. Holding `lastReliableMatch` but not `lastMatch`: six fail.
        5. Classification suppressed during a hold: the classification-during-hold test fails.
        6. Hold widened past `PROGRESS_EPSILON_METRES`: the same-direction-repeat boundary test flips.

    - **Files changed.** `src/navigation/types.ts`, `src/navigation/projection.ts`, `src/navigation/rideNavigationCore.ts` (production); `src/test/fixtures/routeGeometry.ts`, `src/test/fixtures/outAndBackCoincidentRoute.ts`, `src/test/fixtures/repeatedPassRoute.ts` (fixtures); `src/navigation/projection.test.ts`, `src/navigation/rideNavigationCore.test.ts`, `src/ui/riding/useRideNavigation.test.ts`, `e2e/ridingShortTurnaroundWalkingPace.spec.ts` (tests); `package.json`/`package-lock.json` (`0.4.8` → `0.4.9`, hand-patched, no `npm install`, the lockfile diff being exactly the two root version-string lines); root `CLAUDE.md`, `docs/project/backlog.md`, `docs/project/current-status.md` and this file (documentation). No storage, mapping, Dexie, `offRoute.ts`, `rideCompletion.ts`, map-layer or item-98 change.

    - **Privacy.** The private diagnostic GPX was materialised only outside the checkout, its SHA-256 verified against the one supplied, used solely for the ephemeral reproduction probe, and deleted. Its coordinates, route name, street names and geometry digest appear nowhere in this repository; the committed fixtures are unrelated generic geometry sharing only the structural properties that make the defect reproducible.

    - **Limitations.** Automated evidence only — no physical iPhone or Android confirmation of the correction is claimed; see `docs/project/current-status.md` for the outstanding walking-pace retest. The genuine-backtracking ambiguity boundary at `CONTINUITY_PREFERENCE_METRES / 2` (15/16 m) is **unchanged**, and inside one ±14 m accuracy radius it is irreducible from route-distance evidence alone; a rider turning back within roughly 15 m of a turnaround without reaching it is still read as having turned around. Separately, and left deliberately unfixed here: a single whole-route reacquire (raw classification `"untrusted"`) still moves committed progress for that one fix, because `lastReliableMatch` freezes only on raw `"off-route"` — observed in the probe as a one-fix jump of about 187 m before full recovery on the following fixes. That is a pre-existing behaviour independent of this follow-up, is not a regression it introduces, and has deliberately not been allocated a backlog number.

---

<a id="item-105"></a>

## Item 105 — Reveal newly relevant content in the Route Library tag interface — done

_Category: Route Library organisation_

105. **Reveal newly relevant content in the Route Library tag interface — done**
     - Origin: two observations from item 100 stage 4B's installed-iPhone field test (9 September 2026, deployed `0.4.19`) — see [`current-status.md`](current-status.md) for the exact reports. Neither is a regression against a shipped contract; both deliberately extend what `0.4.18` chose. Closing item 100's stage 4B acceptance depends on this item's outcome for the two clauses it left unaccepted.
     - **Cancelling a tag edit must bring the card's own top back into view**, as a successful save already does. Confirmed current state: `src/ui/library/RouteListItem.tsx`'s close/focus effect applies the top-reveal only on the successful-save path, gated on the `tagsSaveSucceeded` signal; Cancel and Escape keep a plain `.focus()` and the browser's own native focus-scroll, which guarantees only the focused tag button's visibility and has no notion of `.app-header--sticky`'s overlap or of a card grown tall from many tags.
     - Reuse `src/ui/library/routeCardTopReveal.ts`'s `computeTopRevealScrollDelta` and the existing `focus({ preventScroll: true })` plus single `window.scrollBy({ top, left: 0, behavior })` mechanism rather than introducing a second reveal path. `prefersReducedMotion()` must keep applying, and horizontal scroll position must stay untouched.
     - Existing component tests assert the opposite for Cancel and Escape ("keep plain `focus()` and never call `scrollBy`"). Those tests are the specification being changed: update them deliberately rather than deleting them, and keep the success path and the cancel path independently testable so a future regression in either is still caught separately.
     - **The Manage tags confirmation must be brought into view when it is armed.** Confirmed current state: `src/ui/library/RouteTagManager.tsx` renders the merge/delete confirmation inline beneath the action row and performs no scroll of its own, so on a long Route Library the confirmation can be armed off-screen while focus stays put.
     - Decide deliberately what "scrolled back again on cancel" should mean, and record the decision with its reasoning. Restoring an exact prior scroll offset is a different mechanism from a reveal delta and can feel wrong if the view moved in between; prefer the smallest behaviour that returns the previously focused control comfortably into view, and do not restore a stale offset blindly.
     - Preserve the existing focus contracts in both changes: `RouteLibrary.tsx`'s `useLayoutEffect` focus hand-off, the rule that a browser ignores `.focus()` on a disabled element (the confirmation disables Rename/Delete while open), and the one-at-a-time admission check between the manager and a card editor.
     - Do not change tag identity or normalisation, the storage lifecycle transaction, filter reconciliation, or any other stage 1–4A behaviour. This is a presentation slice.
     - Real-browser evidence must poll until `window.scrollY` has genuinely stopped changing before measuring, as the `0.4.18` reveal's own e2e coverage had to — a smooth-scroll animation sampled mid-flight reads indistinguishably from no scroll at all.
     - Physical acceptance on the installed iPhone Home Screen PWA is required, since both observations came from there.

     - **Scope correction (9 September 2026), before implementation.** The two specification bullets above about the **Manage tags confirmation** — bringing it into view when armed, and deciding what "scrolled back again on cancel" should mean — **mis-recorded what was actually asked for**, and are preserved above only as the historical record of that error. On review the user confirmed the confirmation's own arming and cancelling behaviour is acceptable exactly as `0.4.19` shipped it, and that the real request is a reveal of the **Manage tags panel's own top after a successful global operation**. The corrected contract implemented here is:
       - cancelling or escaping a per-route Edit tags editor reveals that card's top/title below the sticky header and returns focus to Edit tags, matching the accepted Save reveal (the first specification bullet above, unchanged);
       - after a successful global **Rename, Merge or non-final Delete**, the still-open Manage tags panel's top is revealed below the sticky header, preserving its existing post-success focus target;
       - a **final-tag deletion** keeps the existing Search focus hand-off — the panel is gone, so there is nothing to reveal;
       - a **cancelled confirmation**, a **failed operation** and a **manually closed panel** perform no deliberate scroll at all, leaving the viewport where it naturally lands.
         This is a transition-specific reveal, explicitly not a general scroll-on-focus rule.

     - **Implementation (9 September 2026, `0.4.20`).** Presentation only. No change to tag identity or normalisation, the storage lifecycle transaction, filter reconciliation, the one-at-a-time admission contract, or anything else from stages 1–4A.

       - **Fail-first evidence, run against unmodified `0.4.19` before any production edit.** `src/ui/library/RouteListItem.test.tsx`: **5 failed, 88 passed** — Cancel and Escape both reporting `expected undefined to deeply equal { preventScroll: true }` and `expected [] to have a length of 1`, plus the already-visible and reduced-motion cases and the post-rejection Cancel. `src/ui/library/RouteLibrary.tagManagement.test.tsx`: **4 failed, 30 passed** — the successful rename, merge and non-final delete reveals, and the already-framed negative control's focus assertion. The four "must not scroll" guard tests (final-tag deletion, cancelled confirmation, failed operation, manual close) **passed on the unmodified baseline**, as expected: they are leak guards for the new code, not fail-first evidence, and that is recorded here rather than presented as proof.

       - **Behaviour 1 — the card's Cancel/Escape reveal.** `RouteListItem.tsx`'s `tagsSaveSucceeded` signal is generalised to `revealCardOnClose`, and the close/focus effect keeps an explicit two-way branch on it rather than becoming unconditional: `revealCardOnClose` → `focus({ preventScroll: true })` plus one `applyTopRevealScroll`; otherwise the plain `focus()` it always had. The signal is set at two independent sites — the render-time success handshake, and `handleCancelTags` (which the Escape key handler also routes through) — so a regression in either path still fails on its own. `openTagEditor` resets it, exactly as before.

       - **The third close path is deliberately left alone.** A manager-driven dismissal (`dismissInlineEditorsToken`, when the global tag manager opens) closes the editor during render **without** setting the signal, so it keeps its plain `focus()` and scrolls nothing: the user's attention is moving to the manager panel, and the page must not jump to a card instead.

       - **Behaviour 2 — the Manage tags panel reveal.** `RouteLibrary.tsx` already routes every manager focus move through one `pendingFocusHandoff` marker consumed by a single `useLayoutEffect`, and that marker already distinguishes the paths by target: `"select"` from the render-time success reconciliation, `"search"` from the same block when the final tag has gone, and `"rename"`/`"delete"` from `requestManagerFocus` — which serves the **failed** operation (the `.catch` at what was line 863, not the success path) and the cancelled confirmation. The marker gains an explicit `reveal` flag, set **only** on the success hand-off and only when `focusTarget === "select"`. The effect branches on that flag, never on `target === "select"`: that the two currently coincide is a fact about today's call sites, not a contract. On that one branch the select is focused with `preventScroll: true` — so the browser's own native focus-scroll cannot override the deliberate one — and the panel's own top band (root top through heading bottom, mirroring the card's card-top/title-bottom band) is revealed. `closeTagManager` focuses directly and never uses the hand-off, so a manual Close is untouched by construction.

       - **One shared reveal path, not several.** `routeCardTopReveal.ts` gains `applyTopRevealScroll(band, headerBottomPx)`, which measures the usable visible band (the visual viewport when the on-screen keyboard or a pinch zoom has shrunk it, else the layout viewport), asks the existing pure `computeTopRevealScrollDelta` for the delta, and issues **at most one** `window.scrollBy({ top, left: 0, behavior: prefersReducedMotion() ? "auto" : "smooth" })`, returning the delta. This lifts measurement that was previously inline in `RouteListItem.tsx` and is now shared by all three callers, so there is provably one reveal mechanism rather than a second one growing beside it. The module keeps its name despite now also serving the tag manager, which is not a card: root `CLAUDE.md`, `backlog.md` and `current-status.md` all cite `routeCardTopReveal.ts` by name.

       - **Why the panel's action row was not the reveal anchor.** `computeTopRevealScrollDelta` is top-prioritising by design, so handing it a band taller than the visible area guarantees the band's **top** and lets its bottom fall away. Anchoring on the panel root through its heading is therefore safe; anchoring on the whole panel down to the action row would, on a short visible band, have been able to push the very control being focused below the fold.

       - **A superseded test, replaced rather than quietly loosened.** `RouteLibrary.tagManagement.test.tsx`'s `"never triggers the card-top reveal scroll for a global operation"` asserted no scroll at all after a successful global rename. That is now wrong by intent, and it was replaced — on the user's explicit direction — by the reveal matrix below rather than weakened. Its original point survives: the band asserted is the panel's, never a card's.

       - **Negative controls, and one that was rejected for not discriminating.** The proposed "re-open the editor after a save, then Cancel, and check it does not spuriously reveal" test was **discarded before being written**: Cancel now sets the reveal signal again, so a leaked signal and a correctly reset one produce byte-identical output, and the test could not fail for the reason it claimed to test. It is replaced by a genuinely discriminating sequence — save (signal set), re-open, then close through `dismissInlineEditorsToken` — where the only way a scroll can appear is a stale signal surviving `openTagEditor`'s reset. The other controls: the identical successful rename with the panel's top already framed must scroll **nothing** (the flow that differs only in measured geometry does scroll, so this cannot pass vacuously); Cancel on an already-visible card likewise; and in the browser, the e2e reveal tests assert an explicit **precondition** (the card's or panel's top really is occluded before the action) so they fail loudly rather than passing for the wrong reason.

       - **Real-browser evidence.** `e2e/routeLibraryTags.spec.ts`'s Cancel journey previously asserted the opposite contract in its own name — "leaves it comfortably visible without a forced reveal" — with a comment explaining why it deliberately made no `scrollY` assertion. It is rewritten: 40 tags are **saved first**, so the card stays genuinely tall after the later Cancel discards its next draft (a card that shrinks back to nothing would be framed by the browser's own scroll clamping, and the test would pass without the reveal running at all). A new sibling covers the already-visible no-scroll case. `e2e/routeLibraryTagManagement.spec.ts` gains a panel-reveal describe: the action is focused, the window is then scrolled so the panel's top sits above the sticky header's bottom, and the operation is activated **by keyboard** — a locator click would scroll the button back into view and silently undo the occlusion. Both specs poll until `window.scrollY` has genuinely stopped changing across ten consecutive animation frames before measuring, since a smooth-scroll sampled mid-flight reads indistinguishably from no scroll.

       - **A real defect the browser evidence found — in the test, not the code.** The first version of the manual-close e2e assertion compared raw `window.scrollY` before and after Close, and failed: 474 → 0. The cause is correct, unchanged `0.4.19` behaviour — `closeTagManager` returns focus to the "Manage tags" button, which sits well above the panel, and the browser's own focus-scroll brings it into view. That _is_ "where the viewport naturally ends after the panel closes". A raw `scrollY` comparison therefore cannot express this item's contract, which is about **deliberate** scrolls. The assertion was replaced by instrumenting `window.scrollBy` — the one call `applyTopRevealScroll` makes, and one that native focus-scroll never goes through — and asserting zero calls. The successful-rename test asserts exactly **one** call through the same instrumentation, which is the positive control that stops the zero-call assertion passing vacuously.

       - **Verification.** `corepack npm run format:check`, `lint`, `typecheck` all clean; the full Vitest suite **163 files / 3470 tests passed**; production build succeeded; Playwright **304 passed** across the `chromium` and `android-chrome` projects. `webkit-smoke` could **not** be run in this sandbox — WebKit fails to launch for missing host libraries (`libevent-2.1.so.7`, `libgstcodecparsers-1.0.so.0`, `libavif.so.13`), the same limitation recorded for earlier items — and was supplied by the CI end-to-end job instead. Toolchain, from versions actually run rather than assumed: `node --version` reported **v24.13.0** against `.nvmrc`'s pinned **24.18.0**, a genuine mismatch that cannot be fixed locally and for which CI is the authority; `corepack npm --version` reported **11.16.0**, which **matches** `package.json`'s `packageManager` pin and the workflow's `expected_npm`, so no npm mismatch is claimed. Separately, three pre-existing scratch git worktrees under `.claude/worktrees/` are collected by a bare `vitest run` because `test.exclude` does not cover that path; every Vitest run above was therefore scoped to the real tree with explicit `--exclude` flags. That is an environmental artefact of this sandbox, was not introduced here, and was deliberately not "fixed" by editing unrelated Vitest configuration.

       - **Files changed.** `src/ui/library/routeCardTopReveal.ts`, `src/ui/library/RouteListItem.tsx`, `src/ui/library/RouteTagManager.tsx`, `src/ui/library/RouteLibrary.tsx` (production); `src/ui/library/routeCardTopReveal.test.ts`, `src/ui/library/RouteListItem.test.tsx`, `src/ui/library/RouteLibrary.tagManagement.test.tsx`, `e2e/routeLibraryTags.spec.ts`, `e2e/routeLibraryTagManagement.spec.ts` (tests); `package.json`/`package-lock.json` (`0.4.19` → `0.4.20`, hand-patched, no `npm install`, the lockfile diff being exactly the two root version-string lines); root `CLAUDE.md`, `docs/project/backlog.md`, `docs/project/current-status.md`, `docs/project/history/README.md` and this file (documentation).

       - **Limitations.** Automated evidence only at the time of writing; installed-iPhone acceptance is tracked in [`current-status.md`](../current-status.md) and is what closes item 100 stage 4B. The existing 844×390 landscape tests in both Route Library specs were left exactly as they are — non-contractual regression coverage under the portrait-only orientation scope, neither deleted nor extended. Physical Android verification remains separately outstanding.
