# Completed backlog items 69–74

Bounded per [`README.md`](README.md)'s own instruction: `items-56-68.md` was already close to the ~150,000-character soft cap, so item 69 started a new range file rather than risk pushing it over. Items 70, 71, 72, 73 and 74 continue here; later items continue here too (or a further new range file) as the queue advances.

---

<a id="item-69"></a>

## Item 69 — Remaining positive ascent in the active route summary — done

_Category: Riding elevation enhancement_

69. **Remaining positive ascent in the active route summary — done**
    - A route-Riding information enhancement, distinct from the layout-only slice immediately above (item 68) because it introduces a new derived navigation metric rather than a presentation change to an existing one.
    - Confirmed (exhaustive grep of `src/navigation/` and `src/ui/riding/`): no "remaining ascent"/whole-route "ascent remaining" metric exists anywhere today. The only ascent-adjacent values that exist are the static, pre-ride `route.ascentMetres` total shown via `formatAscent()` in the idle route header and in `RidingLauncher.tsx` — a total, not live/remaining — and `ClimbProgressMetrics.elevationRemainingMetres` (`climbElevationView.ts`), explicitly scoped, per its own doc comment, to only the currently active climb ("not the same value as elevationGainMetres... not a new cumulative-ascent calculation") and shown only inside `RidingClimbProgressPanel` — never a whole-route figure.
    - Resolved architectural decision (settled in the backlog entry itself, not left to the implementer to choose): use the frozen/reliable `presentationDistanceFromStartMetres`/`lastReliableMatch` progress source for remaining ascent, matching every other presentation value in this app — climb progress, next-manoeuvre selection, the elevation marker, route completion. `RidingStatusStrip.tsx`'s existing "Remaining: X km" figure, previously computed from the live, raw `matchedDistanceFromStartMetres` (`coreState.lastMatch?.distanceFromStartMetres`, per `useRideNavigation.ts`) rather than that frozen/reliable value, was harmonised onto the same frozen/reliable progress source as an explicit, in-scope part of this same slice — not merely matched in wording — so the two figures on the compact status line can never disagree or jump independently under GPS jitter or off-route evidence.
    - Existing whole-route ascent/descent calculation reused, not reimplemented: `src/navigation/elevation.ts`'s `analyzeElevation` pipeline — filter known-elevation points, resample onto a fixed `RESAMPLE_STEP_METRES = 20` m grid, apply a centred `SMOOTHING_WINDOW_SAMPLES = 5`-sample moving average (~100 m window), then `computeAscentDescent`, a running-extremum/reversal-confirmation walk that only banks ascent once the series has reversed by at least `MIN_ASCENT_DELTA_METRES = 1` m from the tracked extremum.
    - Compact presentation shipped, verbatim:
      ```
      On route
      61.5 km · 993 m ascent
      GPS ±9 m · Live
      ```
      Both the distance and the ascent figure on the middle line are remaining values, not route totals. An unambiguous accessible label distinguishes this from the route's original total ascent, since the compact visible wording alone could be misread as a total.
    - Remaining ascent means the total future positive elevation gain from the rider's progress point onward — every future climb's gain summed, not merely the net elevation difference between the rider's position and the finish — with missing/gapped elevation handled honestly (never inventing 0 m when unknown). No ascent metric was added for route-less free roam, which has no route/elevation data at all.
    - **Calculation architecture (`src/navigation/elevation.ts`).** The shared "known points → resample → smooth" steps inside `analyzeElevation` were factored into a new private helper, `buildSmoothedElevationSeries(points)`, returning `{ sampleDistances, smoothed } | null` (`null` exactly when `!hasAnyElevation(points)`); `analyzeElevation` became a thin wrapper over that helper plus the still-private `computeAscentDescent`, with identical output for every existing caller (proven by the pre-existing `elevation.test.ts` suite passing unmodified). A single new exported function, `remainingAscentMetres(points, fromDistanceMetres): number | null`, builds the same series, clamps `fromDistanceMetres` to `[0, totalDistance]`, locates the seam value at that distance by calling the _existing_ `resampleElevations` with a one-element target array (re-expressing the smoothed grid as `KnownElevationPoint[]`, rather than writing a second interpolation routine), builds a tail array `[seamValue, ...smoothed-after-that-point]`, and returns `computeAscentDescent(tail, MIN_ASCENT_DELTA_METRES).ascentMetres`. Because `buildSampleDistances` always starts at exactly `0`, the seam lookup at `fromDistanceMetres = 0` lands on `resampleElevations`'s "at or before the first known point" branch and returns the first smoothed sample verbatim (no interpolation arithmetic at all) — making the tail array element-for-element identical to the whole-route smoothed array `analyzeElevation` itself uses. This gives exact (`toBe`, not approximate) agreement with `analyzeElevation(points).ascentMetres` at progress zero, proven directly by two unit tests. At and beyond the route finish the tail collapses to a single element, so `computeAscentDescent` returns `0` with no special-casing — the same construction that gives progress-zero exactness also gives the "known zero near the finish" behaviour for free. No new public type was introduced; `buildSmoothedElevationSeries`, `computeAscentDescent`, `resampleElevations` and `buildSampleDistances` all stayed module-private.
    - **`useRideNavigation.ts` harmonisation.** `distanceRemainingMetres` now derives from `presentationDistanceFromStartMetres` (previously `matchedDistanceFromStartMetres`); `matchedDistanceFromStartMetres` itself, and every other consumer of it (map position, off-route classification), was left untouched. A new `remainingAscentMetres: number | null` field was added to `RideNavigationState`, computed via `useMemo` keyed on `[route, presentationDistanceFromStartMetres]` — mirroring the file's own pre-existing `elevationDisplayPoints` memoization precedent — so the resample/smooth pass reruns only when the route changes or a new reliable fix genuinely moves progress, not on unrelated renders (`wakeLockDesired`, `elevationViewMode`, `isStale` flips, and so on). Both remaining figures now derive from the exact same `presentationDistanceFromStartMetres` value, so they are `null` together, non-`null` together, and freeze/resume together under off-route evidence, with no separate effect or synchronised state.
    - **Compact status presentation (`RidingStatusStrip.tsx`).** A new required `remainingAscentMetres` prop (required, not optional, so a forgotten call site is a compile error) drives a restructured three-line render: the unchanged route-status line (`role="alert"` only for "Off route"); a new remaining-metrics line (`"61.5 km · 993 m ascent"`, `"61.5 km · ascent unavailable"`, or `"61.5 km · 0 m ascent"` for a genuinely known zero — checked via `=== null`, never a truthy check, so a real `0` never renders as unavailable); and a simplified GPS line (`"GPS ±9 m · Live"` / `"GPS ±9 m · Stale (45s ago)"`, dropping the old `"GPS accuracy:"` label and em dash). The remaining-metrics line carries an `aria-label` (e.g. `"61.5 kilometres remaining, 993 metres ascent remaining"`) that fully overrides its accessible name, so the compact visible text and the spelled-out label announced to assistive technology can differ safely. A genuine, intentional behaviour change beyond copy: the fix-age parenthetical on the GPS line now shows only while `isStale` — previously it showed whenever `fixAgeMs !== null` regardless of freshness, so a fresh fix with a non-null age used to display a redundant age too. Neither the remaining-metrics nor the GPS line carries a live-region role, matching the pre-existing "only the off-route line is a live region" convention. `formatAscent()` (routeSummary.ts) was deliberately not reused for the new unavailable-ascent wording, since its `"ascent not available"` phrasing is tuned for, and remains unchanged at, the separate whole-route-total call sites (pre-ride header, `RidingLauncher.tsx`).
    - **CSS.** `.ride-status-strip` changed from a wrapping single-row flex (`flex-wrap: wrap; align-items: baseline`) to a three-line column stack (`flex-direction: column; gap: var(--space-4)`), each line reflowing via ordinary inline text wrapping rather than scrolling horizontally, at both normal and enlarged text sizes. `.ride-status-strip` and `.ride-status-strip-detail` are shared with `FreeRoamStatusStrip.tsx`, which renders only its own single GPS-only line inside the same container/detail classes — confirmed unaffected, since a lone flex child lays out identically under row or column direction and `FreeRoamStatusStrip.tsx` itself received zero changes. `.ride-status-strip-detail` was kept (still consumed verbatim there) and its colour rule merged into a shared selector group with two new classes, `.ride-status-strip-remaining` and `.ride-status-strip-gps`.
    - **`RidingScreen.tsx`.** One new prop threaded at the existing `<RidingStatusStrip>` call site: `remainingAscentMetres={nav.remainingAscentMetres}`. Free roam (`FreeRoamScreen.tsx`/`useFreeRoamNavigation.ts`) received zero changes, confirmed by inspection — it never imports from `elevation.ts` or `useRideNavigation.ts`.
    - **Deterministic coverage.** `elevation.test.ts` gained a `remainingAscentMetres` suite: exact (`toBe`) progress-zero agreement with `analyzeElevation` on two fixtures (a gentle sustained climb and a noisy signal); a four-segment climb/descent/climb/flat fixture proving the figure sums every future climb rather than a net delta to the finish (deliberately bounded to fail a net-delta implementation, not just a raw-noise-summing one — e.g. at the first climb's peak the correct answer is ~35 m against a net-delta implementation's ~20 m); the existing interior-null-bridging fixture, proving the gap policy is preserved and progress-zero still matches exactly; `null` (never `0`) when no elevation exists anywhere; numeric `0` (never `null`) for a genuinely flat known remainder; `0` at and beyond the finish when elevation is known, clamping past-the-end progress, versus `null` there when it is not; a plateau-padded linear-climb fixture proving genuine off-grid interpolation (an analytically exact expected value, `toBeCloseTo` at 6 decimal places) rather than snapping to the nearest resample-grid sample; and no mutation of frozen input points. `useRideNavigation.test.ts` gained a distinct elevation-bearing route fixture (the file's existing fixtures all carry `elevationMetres: null`) and four tests: both new/changed fields null before any fix and non-`null` together after one; both decreasing together across two on-route fixes; a strongly-off-route fix leaving both frozen while the raw match still moves (deliberately shallow, since `rideNavigationCore.test.ts`'s own `lastReliableMatch` suite already exhaustively covers the freezing mechanism's exact timing); and a subsequent on-route fix releasing both together. `RidingStatusStrip.test.tsx` was rewritten for the new three-line copy, the accessible label, the unavailable/known-zero ascent states, the new GPS wording, and an explicit case proving a fresh fix with a non-null `fixAgeMs` now shows no age parenthetical (falsifying, not just updating, the previous suite's assumption). Four sibling `RidingScreen*.test.tsx` files and two Playwright specs (`ridingFinishAndEnd.spec.ts`, `ridingImmersiveShell.spec.ts`) needed mechanical copy-only updates to the now-superseded `"Remaining:"`/`"GPS accuracy:"` text they asserted; none needed logic changes.
    - **Verification.** `npm run format` (no changes), `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2687/2687 across 137 files. `npm run e2e` — 175/175 across both the `chromium` (161) and `android-chrome` (14) projects. `npm run format:check` run last, immediately before committing — clean. Two unrelated, pre-existing flakes were encountered and confirmed not caused by this change by isolated reruns: `src/gpx/parseAcnExtension.test.ts`'s `"rejects when the manoeuvre count exceeds MAX_ACN_MANOEUVRES"` (the same CPU-contention timeout already documented against item 68's own reliability follow-up, reproducing regardless of this change) and `e2e/mapImageryRecovery.spec.ts`'s retry-loop banner test under full-suite parallel-worker contention (passed cleanly alone). Version bumped to 0.3.52 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed. Real-device acceptance remains outstanding: legibility of the three-line compact card while mounted and moving outdoors, and that the remaining-ascent figure updates sensibly over a real ride with genuine climbs and descents rather than only synthetic GPS sequences; the harmonised remaining-distance figure (now sourced from the frozen/reliable progress rather than the previous raw-match source) should also be re-confirmed sensible during a real off-route excursion and recovery. See `docs/project/current-status.md` for the tracked outstanding item.

---

<a id="item-70"></a>

## Item 70 — Simplify 2 km/10 km elevation distance guides — done

_Category: Riding elevation enhancement_

70. **Simplify 2 km/10 km elevation distance guides — done**
    - Refines only the presentation of item 54's already-shipped distance-guide feature; item 54's own text was left untouched, and neither `selectElevationDistanceGuides` (`src/navigation/upcomingElevation.ts`) nor its offset tables changed — the 2 km view still shows a single 1 km guide, the 10 km view still shows 2/4/6/8 km guides, and guides beyond a truncated window near the route finish are still omitted rather than clamped.
    - **Architecture decision.** Chose an SVG bottom gutter over moving labels into HTML, per the backlog's own stated preference — it keeps guide lines and their labels in the same coordinate system and reuses the existing `distanceToX`/edge-anchor logic unchanged. `ElevationChart.tsx`'s `height` prop keeps meaning exactly what it always meant, the plotting height, and every existing consumer of it (`buildElevationChartGeometry`, `buildElevationChartMarkerGeometry`, the tap-target `<rect>`, the position marker's `y2={height}`, the climb baseline, `areaPathFromRun`) is untouched. A new `outerHeight = height + (hasDistanceGuides ? DISTANCE_GUIDE_GUTTER_HEIGHT : 0)` (gutter constant `18`) drives only the outer `<svg>`'s own `viewBox`/`height` attributes, so the gutter is purely additive and only appears at all when at least one guide is present.
    - **Guide geometry.** Each guide line now spans `y1={0} y2={height}` — the full plotting height, mirroring the existing position-marker precedent — replacing the old restrained 14 px top-anchored tick (`DISTANCE_GUIDE_TICK_HEIGHT`, removed). Its label moved from `y=24` (inside the plot, near the top) to `y={height + DISTANCE_GUIDE_LABEL_OFFSET_Y}` (offset `13`) inside the new gutter, still using the pre-existing `distanceGuideTextAnchor`/`DISTANCE_GUIDE_EDGE_MARGIN_PIXELS` edge-flip logic unchanged. `distanceGuideLabel` dropped its leading `+` (`"1 km"`, not `"+1 km"`). Paint order, `pointerEvents="none"`, and the tap-target rect's own unchanged `height` (never `outerHeight`) were all preserved exactly.
    - **Accessible description, replacing the visible caption.** The visible `<p className="elevation-chart-distance-guides-caption">Distance guides ahead: …</p>` was removed entirely. In its place, a new `useId()`-generated id links the chart svg's `aria-describedby` to a `<p className="visually-hidden">` (this codebase's own established offscreen-clip utility, `src/index.css`, first used by item 68's wake-lock status) containing a natural-language description from a new pure helper, `formatDistanceGuideDescription` — e.g. `"Distance guides ahead at 1 kilometre"` or `"Distance guides ahead at 2, 4, 6 and 8 kilometres"` (comma-joined, `"and"` before the last value, singular/plural unit). This mirrors the `useId()` + `aria-describedby` + linked hidden-text pattern already established by `RouteListItem.tsx`, rather than introducing a new mechanism (e.g. an SVG `<desc>`, which had no precedent in this codebase). The chart's own `aria-label`-driven accessible _name_ (on both the `<figure>` and the `<svg role="img">`, including a caller-supplied `ariaLabel` override) is untouched — `aria-describedby` is additive only, and is omitted entirely when there are no guides.
    - **Files changed:** `src/ui/shared/ElevationChart.tsx` (the component itself); `src/ui/shared/ElevationChart.test.tsx` (the `describe("distance guides (backlog items 54, 70)", ...)` block extended in place: existing tests updated for the new geometry/label format, new tests added for gutter placement, the no-guide no-gutter case, the tap-target/marker's unchanged plotting height, and the accessible-description mechanism; the two old caption tests replaced); `src/ui/riding/RidingScreen.test.tsx` (six pre-existing integration tests asserting the old `"+N km"` format and the old caption — found only by running the full suite, not merely the component-level tests — updated for the new label format, and the caption test rewritten to assert the new `aria-describedby`-linked description instead); `e2e/ridingElevationWindows.spec.ts` (every `guideLabelLocator` expectation dropped its leading `+`; the phone-viewport test also gained an explicit vertical-fit assertion — `.ride-profile-pane--immersive`'s `scrollHeight` must not exceed its `clientHeight` at the worst case, the 10 km view with all four guides showing, proving the new gutter doesn't push the fixed Riding shell into needing its own internal scroll at ordinary text size); a doc-comment in `src/navigation/upcomingElevation.ts` updated from its now-stale `"+1 km"` example to `"1 km"`.
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2691/2691 across 137 files (one pre-existing, already-documented CPU-contention flake, `parseAcnExtension.test.ts`'s `"rejects when the manoeuvre count exceeds MAX_ACN_MANOEUVRES"`, was seen intermittently under full-suite load and confirmed passing cleanly in isolation — unrelated to this change, which touches neither GPX parsing nor that file). `npm run e2e` — 175/175 across both the `chromium` (161) and `android-chrome` (14) projects (one pre-existing, already-documented flake, item 32's `ridingFinishAndEnd.spec.ts` completion-detection test, was seen intermittently under full-suite parallel-worker contention and confirmed passing cleanly in isolation and on a clean full rerun — unrelated to this change, which touches neither route completion nor that spec). `npm run format:check` run last, immediately before committing — clean. Version bumped to 0.3.53 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed. Real-device acceptance remains outstanding: legibility of the full-height guide lines and gutter labels while mounted and moving outdoors in bright daylight and with gloves, and that the fixed Riding Profile pane genuinely does not need to scroll on a real device at normal text size with guides showing. See `docs/project/current-status.md` for the tracked outstanding item.

---

<a id="item-71"></a>

## Item 71 — Preview the next recognised climb during active Riding and improve climb-card hierarchy — done

_Category: Riding elevation enhancement_

71. **Preview the next recognised climb during active Riding and improve climb-card hierarchy — done**
    - Applies only to the active route-Riding Profile view (items 55/56); the pre-ride briefing's own `RidingClimbSelector` dropdown preview (item 13) is untouched and unduplicated.
    - **Architecture decision.** Extended the existing `EffectiveElevationView` union (`src/navigation/climbElevationView.ts`) with a third variant, `{ kind: "climb-preview"; featureId: string }`, alongside the pre-existing `ElevationViewMode` and `{ kind: "climb" }`. `selectEffectiveElevationView` gained two new parameters, `upcomingClimb: ClimbFeature | null` and `previewSelectedClimbId: string | null`, and now resolves a strict priority order — active climb wins unconditionally (unchanged logic, just restructured into an `if (activeClimb !== null) { ... }` branch that returns before the new parameters are even examined), then a matching manual preview selection, then the standard view. Because active always wins first, a previewed climb that actually begins riding transitions to the real `"climb"` presentation on the very next call with no special-casing. A new pure helper, `findNextClimbAfterDistance(climbs, distanceMetres)` (`src/navigation/routeFeatures.ts`, placed beside `findFeatureAtDistance`), returns the first route-ordered climb whose own start lies strictly after the given distance — a distance exactly at a climb's start is that climb's own active range, never double-counted as "upcoming".
    - **Transient preview selection.** `RidingScreen.tsx` gained one new local `useState`, `climbPreviewSelection: { routeId: string; climbId: string } | null`, mirroring the file's own pre-existing `explicitFeatureSelection` idiom exactly: an explicit choice tagged by route and climb id, honoured only while the tag still matches the currently-upcoming climb. This makes every required transition (leaving the previewed climb once entered, skipping past a previewed climb without ever entering it, a route change, an explicit standard-view tap) self-reset with no imperative clearing effect anywhere — the tag simply stops matching. `upcomingClimb` itself is derived unconditionally from `findNextClimbAfterDistance(climbs, presentationDistanceFromStartMetres)`, explicitly gated on `geolocationStatus !== "idle"` so a paused ride's restored idle position (which can already sit inside a climb) never leaks this active-Riding-only feature into the pre-ride screen.
    - **Settled button/state behaviour.** The `Climb` button now shows whenever `activeClimb !== null || upcomingClimb !== null`; its `aria-pressed`/selected state covers both the `"climb"` and `"climb-preview"` kinds. Tapping it while no climb is active sets `climbPreviewSelection`; tapping a standard view (Full/2 km/10 km) always clears it, in addition to the existing conditional `dismissedClimbFeatureId` write that only fires when a climb is genuinely active — so leaving a preview never touches active-climb dismissal state. The standard buttons' own `isSelected` check was widened to exclude both `"climb"` and `"climb-preview"`, so a standard button can never appear falsely selected while previewing.
    - **Preview presentation.** A new chart-model branch in `RidingScreen.tsx`'s existing if/else chain (inserted between the active-climb branch and the `elevationProfileDisplay.kind === "full"` branch) builds the preview chart via the existing `buildClimbChartViewModel({ kind: "pre-ride-selected-climb" }, ...)` — the same rebased-to-zero, no-marker mode the idle dropdown preview already uses, never `computeClimbProgressMetrics` (which clamps into the climb's own bounds and would fabricate progress at the climb's start). A new small component, `RidingClimbPreviewPanel.tsx` (mirrors `RidingClimbProgressPanel.tsx`'s shape), renders only a numbered heading (`"Climb N · Category"`) and a plain-text `"Starts in X km"` line — deliberately no current-gradient/elevation/completed-distance field, since none of those exist yet for a climb that hasn't begun. Length/ascent/average-gradient/max-gradient/climb-score are supplied by the existing, reused `RouteFeatureDetailsPanel` call site: its `feature` prop is overridden to the upcoming climb (not `microDetailFeature`) whenever `effectiveElevationView.kind === "climb-preview"`, which both supplies the static facts with zero new formatting code and guarantees a stale prior map/chart-tap selection elsewhere can never leak into the preview (there remains exactly one `RouteFeatureDetailsPanel` call site). `climbNumber` is deliberately withheld from that call while previewing, keeping the numbered "Climb N ·" identity solely on `RidingClimbPreviewPanel`'s own heading — the same accessible-name-collision avoidance the active-climb branch already relied on (`RidingClimbProgressPanel`'s numbered heading vs. `RouteFeatureDetailsPanel`'s unnumbered one).
    - **Item 57's Map cue untouched.** `RidingClimbCue`'s existing gate (`effectiveElevationView.kind === "climb"`) already excludes the new `"climb-preview"` kind with zero code changes — verified by new tests, not new logic.
    - **Active-progress card hierarchy (`RidingClimbProgressPanel.tsx`).** Split the old single combined "X completed · Y remaining" paragraph and four uniform `<p>` elements into a strongly-weighted primary pair (`<span>`-based tiles, not `<p>`, avoiding a CSS specificity clash with the existing `.riding-climb-progress p` rule) — "Distance to summit" (`distanceRemainingMetres`, always present) and "Elevation remaining" (`elevationRemainingMetres`, tile omitted when null, preserving the exact existing null-vs-zero convention) — plus a quieter secondary row (current gradient, current elevation, summit elevation, distance completed; each omitted when unavailable exactly as before). No climb-detection or elevation-mathematics changes; `elevationRemainingMetres` keeps its existing net-positive-difference-to-finish meaning. No percentage-complete value or progress bar anywhere, preserving the panel's own pre-existing product requirement.
    - **CSS.** `src/index.css`: the existing `.riding-climb-progress` card rule now also covers the new `.riding-climb-preview` card (shared card family); `.riding-climb-progress p` gained `color: var(--colour-text-muted)` (an existing, already-used token) — safe now that the two primary values are `<span>`s, not `<p>`, so the rule only ever matches genuinely-secondary content; new `.riding-climb-progress-primary`/`-metric`/`-metric-label`/`-metric-value`/`-secondary` rules, all flex-wrap based so neither tile forces horizontal scroll on a narrow phone or at enlarged text.
    - **Files changed:** `src/navigation/routeFeatures.ts` (+`findNextClimbAfterDistance`), `src/navigation/climbElevationView.ts` (`EffectiveElevationView` extended, `selectEffectiveElevationView` widened to five parameters), `src/ui/riding/RidingClimbPreviewPanel.tsx` (new), `src/ui/riding/RidingClimbProgressPanel.tsx` (hierarchy restructure), `src/ui/riding/RidingScreen.tsx` (state, derivations, button row, new chart branch, `RouteFeatureDetailsPanel` override), `src/index.css`. Test files: `src/navigation/routeFeatures.test.ts`, `src/navigation/climbElevationView.test.ts` (all ten pre-existing `selectEffectiveElevationView` call sites updated for the two new parameters, plus a new `describe("upcoming-climb preview (backlog item 71)")` block), `src/ui/riding/RidingClimbPreviewPanel.test.tsx` (new), `src/ui/riding/RidingClimbProgressPanel.test.tsx` (hierarchy assertions added, wording updated), `src/ui/riding/RidingScreen.test.tsx` (two pre-existing tests fixed — under item 71, a position between two climbs now legitimately shows `Climb` unpressed rather than hidden, since the second climb is upcoming there — plus a new nested `describe("upcoming-climb preview (backlog item 71)")` covering the full transition matrix), `e2e/ridingClimbView.spec.ts` (a stale in-file comment corrected, a preview flow inserted into the existing main test before the first climb begins, and a new phone-viewport/enlarged-text test for the preview and restructured active card).
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2723/2724 across 138 files (one pre-existing, already-documented CPU-contention flake, `parseAcnExtension.test.ts`'s `"rejects when the manoeuvre count exceeds MAX_ACN_MANOEUVRES"`, seen under full-suite load and confirmed passing cleanly in isolation and via a byte-identical diff against `HEAD` proving the file itself is untouched by this slice). `npm run e2e` — 175/176 across both the `chromium` and `android-chrome` projects (one pre-existing, already-documented flake pattern, `lowZoomLegibility.spec.ts`'s waypoint-marker zoom-scaling test — an unrelated Planning/camera-zoom test, untouched by this slice — seen once under full-suite parallel-worker contention and confirmed passing cleanly in isolation). `npm run format:check` run last, immediately before committing — clean. Version bumped to 0.3.54 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed. Real-device acceptance remains outstanding: preview readability while mounted and moving, the live transition at a real climb start, the restructured active card's glanceability, fixed-pane fit at normal and enlarged text on a real device, and confirmation the Map cue never appears early. See `docs/project/current-status.md` for the tracked outstanding item.

---

<a id="item-72"></a>

## Item 72 — One-tap route Pause/resume flow — done

_Category: Ride lifecycle_

72. **One-tap route Pause/resume flow — done**
    - Collapsed both remaining two-tap route-resume paths into one explicit action, matching free roam's own existing one-tap contract (item 42), while leaving that free-roam contract itself untouched.
    - **In-session Pause.** `App.tsx`'s `handleRidePaused` — previously an unconditional `resetRidingContentToLauncher()` for both route and free-roam sessions — now branches on `ridingContent.kind`: free roam still resets to the empty launcher (its own screen has no idle panel and always auto-starts on mount, so this remains the only way to gate its restart); a route session is left entirely untouched, so `RidingScreen` stays mounted and its own existing, unmodified idle/pre-ride branch — already rendering unconditionally once `nav.geolocationStatus` leaves `"watching"` — takes over immediately, with the global five-tab navigation restored via the existing, unmodified `onRidingActiveChange`/`isImmersiveRidingShell` machinery. The idle panel's resumable button (both this path and the ordinary Routes-card-reopen path) was renamed `"Resume riding"` → `"Resume ride"`; `RidingLauncher`'s own resumable button was renamed `"Resume route"` → `"Resume ride"`, for consistent terminology across both flows.
    - **Cold recovery.** `RidingLauncher`'s `Resume ride` button still only opens the route (`onResumeRoute(route)`, no geolocation call) — the one-tap behaviour is driven entirely by a new one-use resume intent App.tsx pairs with it: `RidingContent`'s route variant gained an optional `resumeIntentToken?: number`, generated by a plain monotonic ref (`nextResumeIntentTokenRef`, mirroring `useRideCamera.ts`'s own `nextCameraRequestIdRef` idiom) inside `handleResumeRoute` only — `handleOpenRoute`/`handleRouteSaved` (an ordinary Routes-card reopen or Planning-save-then-ride) never set it, so that path is entirely unaffected and keeps today's explicit-tap idle screen unchanged, exactly as the backlog item's own text scoped it.
    - **Restoration readiness (`useRideNavigation.ts`).** The existing mount-time restoration effect (unchanged in its own body) gained an explicit lifecycle around it: `restorationStatus: "loading" | "ready" | "error"` and `restoredForThisRoute: boolean` (true only once a persisted row was found **and** matched this exact `route.id` — distinct from `"ready"` alone, which is also reached when nothing matched at all), plus `retryRestoration()`. The previously fully-silent `.catch()` now calls `logError("ride-navigation-restore", error)` and sets `"error"` — a deliberate, minor behaviour widening scoped to this item. Mirroring `RidingLauncher`'s own existing `hydrationRetryToken` idiom, `retryRestoration` sets `"loading"` itself (an ordinary event-handler call, not a synchronous effect-body `setState`) before bumping a retry token that re-runs the same effect.
    - **Camera-ordering readiness (`useRideCamera.ts`).** A new `hasAppliedRestoredCamera` boolean flips `true` inside the same effect that dispatches the pre-existing `"restore"` action, immediately after that dispatch — batching both into one commit. No reducer change in `rideCamera.ts`: `"follow-requested"` never inspects the camera's prior mode (it always resolves to `"following"`, reusing `state.followZoomLevel`), so gating the resume-intent consumption on `hasAppliedRestoredCamera` (or on `restoredCameraState === null` when nothing needs restoring) guarantees `"restore"` is always processed before `"follow-requested"`, deterministically rather than by incidental hook-declaration ordering.
    - **Consuming the intent (`RidingScreen.tsx`).** `handleStart()` — the one authoritative start transition already shared by the ordinary button and the mid-ride "Try again" retry — was wrapped in `useCallback` (destructuring `nav.start`/`camera.requestFollow` into stable local bindings first, matching this file's own existing `cameraRequestZoom` idiom, since depending on the raw hook-result objects directly would have required them in the dependency array). A new effect gated on `resumeIntentToken`/`restorationStatus`/`restoredCameraState`/`hasAppliedRestoredCamera`/`restoredForThisRoute` calls it exactly once per token: `consumedResumeIntentTokenRef` (a ref, mutated synchronously, mirroring this codebase's `watchGenerationRef`/`hydrationGenerationRef` idioms) guards against double-consumption, structured as a single combined boolean (`shouldConsume`) rather than nested `if`s or an early return — the project's `react-hooks/set-state-in-effect`/`react-hooks/refs` lint rules (part of `reactHooks.configs.flat["recommended-latest"]`, backed by `enableAllowSetStateFromRefsInEffects`) specifically recognise a single ref-derived boolean controlling the block containing a setState-shaped call, but did not recognise the equivalent nested-`if`/early-return shapes tried first — confirmed empirically, not merely reasoned about, since the compiler's static "is this call ref-controlled" check is a real, narrow pattern match rather than a general dataflow proof. The ref is always updated to the latest token regardless of which branch runs, so a stale/missing/wrong-route row (`restoredForThisRoute` false) still marks the token consumed exactly once and falls through to the ordinary manual idle panel, never auto-starting a fresh ride and never retry-looping. While a token is pending-and-unconsumed, the idle-panel branch shows either a lightweight `role="status"` "Resuming your ride…" line (loading, or ready-but-awaiting-camera-readiness — no buttons, avoiding a flash of the ordinary panel) or, on a genuine restoration error, a retryable `role="alert"` panel with `Retry` (`nav.retryRestoration()`) and `Back to Ride options` as a safety-valve escape (a small, deliberate addition beyond the item's literal text, reusing the existing prop/handler unchanged).
    - **Adjacent flows confirmed unaffected, not merely assumed.** A never-started route still shows `Start riding` and never auto-starts (no `resumeIntentToken` reaches it). `Back to Ride options`, route End/Edit-copy behaviour, and free roam's existing one-tap Pause/resume contract are all unchanged — verified both by direct reading (`FreeRoamScreen.tsx`/`useFreeRoamNavigation.ts` received zero changes) and by the full existing test suite passing unmodified except for the mechanical button-label rename.
    - **Deterministic coverage.** New `describe("cold-recovery resume intent (backlog item 72)")` block in `RidingScreen.test.tsx`: no-token never auto-starts despite a matching persisted row; a deliberately deferred restoration promise blocks auto-start until it resolves, then starts exactly one watch and requests Follow once; a restoration rejection shows a retryable error, starts no watch, preserves the saved row, and completes the same resume intent once retried; `Back to Ride options` from the error state returns to the launcher without starting GPS; React Strict Mode's double-invoked effects still create exactly one watch; a stale/wrong-route stored row falls through to the ordinary idle panel without auto-starting; restored elevation-view selection and wake-lock preference survive the collapsed flow; a restored free-camera state cannot apply after and override the resumed Follow request (the final settled `setCamera` call always reflects the followed position); a never-started route is unaffected. New `describe("useRideNavigation restoration lifecycle (backlog item 72)")` block in `useRideNavigation.test.ts` covers the `loading`/`ready`/`error` transitions (matched row, no row, wrong-route row, rejected read with `logError` confirmed via `getRecentErrors()`), `retryRestoration` re-invoking the read and recovering from error, and a cancelled-by-unmount restoration applying no state without throwing. New `describe("restoration readiness (backlog item 72)")` block in `useRideCamera.test.ts` covers `hasAppliedRestoredCamera` flipping true in the same update the restore is applied, staying stable across an unrelated rerender with the same `restoredCameraState` reference, and `requestFollow` always landing on `"following"` afterward regardless of a restored `"free"` or `"following"` mode. `App.test.tsx`'s "Ride launcher session recovery" describe block was rewritten for the one-tap flow (including a new failed-Pause-stays-active-and-retryable case), and its "Back to Ride options" test redirected to reopen via the Routes card (unaffected by this item) rather than the launcher, matching its own sibling test's existing pattern.
    - **End-to-end coverage.** `e2e/ridingLauncher.spec.ts`'s cold-recovery test now proves one tap of the renamed `Resume ride` reaches active immersive tracking directly, with exactly one `watchPosition` call (via the existing `installGeolocationWatchCounter`/`readWatchPositionCallCount` helpers, whose counter resets on the intervening reload) and restored progress confirmed via `readActiveRideStateRow` polling; its `Back to Ride options` test was redirected to reopen via the Routes card, matching the same rationale as the component-level redirect above. `e2e/ridingImmersiveShell.spec.ts`'s main Pause test and its wake-lock-preservation test both collapsed their trailing two-click sequences into one. `e2e/stickyNavigation.spec.ts` and `e2e/ridingMapProfileViews.spec.ts`'s own Pause-then-resume tests were similarly collapsed to one tap. Confirmed via direct reading, not assumed: `e2e/ridingNextManoeuvre.spec.ts`, `e2e/androidPersistenceAndOffline.spec.ts`, `e2e/ridingCamera.spec.ts`'s zoom-persistence test, and `e2e/ridingFinishAndEnd.spec.ts` all reopen via the Routes card rather than the launcher's button, so each needed only a mechanical button-label rename, no flow change. Free roam's existing e2e Pause/resume coverage (`e2e/freeRoam.spec.ts`, and the free-roam Pause test within `ridingImmersiveShell.spec.ts` itself) was left unmodified and reconfirmed passing.
    - **Files changed:** `src/App.tsx`, `src/ui/riding/RidingLauncher.tsx`, `src/ui/riding/RidingScreen.tsx`, `src/ui/riding/useRideNavigation.ts`, `src/ui/riding/useRideCamera.ts`. Test files: `src/App.test.tsx`, `src/ui/riding/RidingScreen.test.tsx`, `src/ui/riding/useRideNavigation.test.ts`, `src/ui/riding/useRideCamera.test.ts`, `src/ui/riding/RidingLauncher.test.tsx`, `src/ui/riding/RidingScreen.closedLoopCompletion.test.tsx`, `src/ui/riding/RidingScreen.completionArming.test.tsx`, `src/ui/riding/RidingScreen.finishEndRide.test.tsx`, `src/ui/riding/RidingScreen.mapProfileViews.test.tsx`, `src/ui/shared/immersiveRidingShell.test.ts` (label-only). `e2e/ridingLauncher.spec.ts`, `e2e/ridingImmersiveShell.spec.ts`, `e2e/stickyNavigation.spec.ts`, `e2e/ridingMapProfileViews.spec.ts`, `e2e/ridingNextManoeuvre.spec.ts`, `e2e/androidPersistenceAndOffline.spec.ts`, `e2e/ridingCamera.spec.ts`, `e2e/ridingFinishAndEnd.spec.ts`.
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2743/2743 across 138 files. `npm run e2e` — 175/176 across both the `chromium` and `android-chrome` projects (one pre-existing, already-documented flake, item 32's `ridingFinishAndEnd.spec.ts` completion-detection test — that file received only a mechanical button-label rename elsewhere, no logic touching route completion — confirmed passing cleanly in isolation). `npm run format:check` run last, immediately before committing — clean. Version bumped to 0.3.55 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed. Real-device acceptance remains outstanding: a successful in-session Pause followed by one-tap route resume; a real cold relaunch followed by one launcher tap into active Map; the resulting single permission/watch lifecycle and correct Follow behaviour; preservation of progress, camera/elevation selection, wake lock, dismissed climb and completion state through the collapsed flow; recoverability when restoration or GPS start fails; and unchanged free-roam resume behaviour. See `docs/project/current-status.md` for the tracked outstanding item. Separately, the user has confirmed that the pre-existing intermittent fresh-Start Follow-at-overview symptom (item 66) could no longer be reproduced on the specific route that had previously been most reproduction-sensitive for it — already recorded in item 66's own closure text in `docs/project/current-status.md`, restated here only because this item's own prompt asked for it to be captured if not already present.

---

<a id="item-73"></a>

## Item 73 — Guard every unfinished-session switch against silent replacement — done

_Category: Ride lifecycle_

73. **Guard every unfinished-session switch against silent replacement — done**
    - Confirmed asymmetry, source-verified before this slice began: `App.tsx`'s `checkFreeRoamConflict()` only detected an unfinished **free-roam** session, returning `null` (no conflict) whenever the persisted singleton row was a **different unfinished route** — `handleOpenRoute`/`handleRouteSaved` then proceeded straight to `setRidingContent({kind:"route", route})`, silently discarding the in-memory pointer to the previous route; its own persisted row was then silently overwritten by the next write for the new session. `handleOpenFreeRoam` had no conflict check at all, "safe" only because `RidingLauncher`'s own mutually-exclusive UI branching never rendered `Start/Resume free roam` while a route row existed — a structural, not runtime, guard.
    - **Architecture: one storage-authoritative transition guard, used by all five entry points.** A new pure module, `src/ui/riding/rideSessionTransition.ts`, exports `RideSessionTarget` (`{kind:"route", route: PlannedRoute} | {kind:"free-roam"}`) and `classifyRideTransition(stored: StoredRideState | undefined, target): RideTransitionOutcome`, where `RideTransitionOutcome` is one of `{kind:"proceed"}` (no unfinished session), `{kind:"resume"}` (the exact same session — a matching `routeId`, or free-roam vs. free-roam, which is unconditionally the same session since free-roam rows carry no distinguishing id), or `{kind:"conflict", existing:"route"|"free-roam"|"unsupported"}`. Built entirely on the existing `resolveStoredRideSessionKind`/`isStoredRouteRideState`/`isStoredFreeRoamRideState` (`src/storage/mapping.ts`), so the legacy-`kind`-absent-means-route and present-but-unrecognised-means-`"unsupported"` rules stay in exactly one place; `"unsupported"` folds into `conflict`, not a separate bucket, since resolving it needs the identical confirm-then-clear treatment. A storage **read** failure is a distinct, one-layer-up concern the pure function can't itself observe: `App.tsx`'s own `checkRideTransition(target)` wraps `getActiveRideState()` in a try/catch, replacing `checkFreeRoamConflict` and adding a `{kind:"read-failed"}` outcome, fails closed exactly as before. `classifyRideTransition` is unit-tested directly (`rideSessionTransition.test.ts`, 11 cases covering every proceed/resume/conflict/unsupported/legacy-row combination) with no storage or React dependency at all.
    - **Race protection.** One monotonic `transitionRequestIdRef` (mirrors the existing `hydrationGenerationRef`/`nextResumeIntentTokenRef` idioms elsewhere in this codebase), incremented at the top of every entry-point flow; every async continuation compares `transitionRequestIdRef.current !== requestId` and bails if superseded. This is what makes a newer click discard an older pending check/dialog correct without any extra button-disabling — a stale launcher view, or two rapid clicks on different routes, can never open the wrong/older target.
    - **The five entry points, unified.** `handleOpenRoute`/`handleRouteSaved`/`handleResumeRoute` all became thin wrappers over a new `requestRouteTransition(route, {stampResumeIntent})`; `RidingLauncher`'s two free-roam buttons became `handleStartFreeRoam`/`handleResumeFreeRoam`, both thin wrappers over a new `requestFreeRoamTransition()` — the classification, not which button was pressed, decides whether it's a fresh write, a no-op resume of an already-existing row, or a genuine conflict, which incidentally closes a second latent bug the naive per-button design would have kept: a stale `Start free roam` click against an already-matching free-roam row now reuses that row rather than blindly overwriting it.
    - **Item 72's resume intent stays exactly as fast, with one added rule.** The one-use `resumeIntentToken` is stamped only by `handleResumeRoute`'s own **immediate, undialogued** check resolving to `resume` (the exact same route id) — never when the row vanished since hydration (a "never-started" open must still require an explicit `Start riding` tap) and never after any confirmed dialog switch (confirming a switch is not permission to auto-start GPS for the replacement). `RidingScreen.tsx` needed **zero changes**: its own restoration effect already independently re-validates the stored row against `route.id` and only calls `handleStart()` when `restoredForThisRoute` is true — a second, pre-existing safety net that makes the App-level stamping rule doubly safe even if it were ever gotten wrong.
    - **Free-roam initialisation moved entirely behind the guard.** `RidingLauncher.handleStartFreeRoam` previously wrote the fresh free-roam row itself, then called `onOpenFreeRoam()` only on success. That write now lives solely in `App.tsx` (`writeFreshFreeRoamState()`), called only after the guard has confirmed there's nothing to silently overwrite — `RidingLauncher` itself no longer imports `setActiveRideState`/`toStoredFreeRoamState`/`clock` at all, reduced to a presentational trigger driven by new `isFreeRoamPending`/`freeRoamError` props from App. `RidingLauncherProps.clock` relocated to a new, identically-defaulted `AppProps.clock?: Clock`.
    - **The pending-switch dialog: one state machine, one `ConfirmDialog` call site, six statuses.** A new `PendingRideSwitch` (`requestId`, `target`, `existing`, `status: "check-failed"|"conflict"|"clearing"|"clear-failed"|"starting-free-roam"|"start-free-roam-failed"`, `errorMessage`) drives a single `<ConfirmDialog>` rendered as a sibling of `<main>` in `App.tsx` — never gated by `screen`, so whichever screen is currently mounted (Routes, Planning, or the Ride launcher) stays exactly as-is underneath. This is what satisfies "preserve the originating screen... rather than redirecting merely to display an error": `screen` state is never touched by a conflict or check-failed outcome, superseding the old `blockedRouteOpenReason` behaviour, which redirected to the Ride screen just to show an inline banner via `RidingLauncher`. `blockedRouteOpenReason`/`describeBlockedRouteOpenReason`/`checkFreeRoamConflict` were removed entirely (confirmed by grep: used nowhere else). A private `describePendingRideSwitch(pending)` (mirroring `RidingLauncher.tsx`'s own `LAUNCHER_CLEAR_ACTION_COPY`/`describeUnresumableReason` pattern) supplies distinct copy per status — a read failure is never worded as a conflict, nor a conflict as a generic technical failure. Confirming dispatches through `confirmPendingSwitch` (clear → for a route target, open idle immediately; for a free-roam target, `starting-free-roam` → write → open or `start-free-roam-failed`), guarded by a synchronous `isPendingSwitchActionPendingRef` (mirrors `RidingLauncher`'s own `isClearActionPendingRef`) so a rapid double-click or a disabled-button-bypassing Escape can never issue two clears. Cancel/a failed clear restores focus to the originating trigger, captured generically via `document.activeElement` at the top of every `request*Transition` call — simpler than `RidingLauncher`'s own deferred-effect `pendingClearFocusRef` idiom, since App's dialog is a true sibling overlay that never unmounts the trigger's own screen the way that in-place confirmation morph does.
    - **`launcherSessionRefreshToken`.** A new `RidingLauncherProps.sessionRefreshToken?: number`, folded into the existing hydration effect's dependency array, bumped by App once immediately after any successful `clearActiveRideState()` inside the pending-switch flow — so if a launcher-originated switch (Resume-route/Start-free-roam/Resume-free-roam) then fails at a later step (e.g. the free-roam write) and the rider cancels, the launcher underneath has already re-hydrated to "no session" rather than continuing to show the just-cleared route/free-roam panel, satisfying "do not fabricate restoration of the deliberately ended old session."
    - **`RidingLauncher.tsx` reductions.** Removed: `startFreeRoamTriggerRef`/`isStartingFreeRoam`/`startFreeRoamError`/`isStartFreeRoamPendingRef`/`handleStartFreeRoam` and its `setActiveRideState`/`toStoredFreeRoamState`/`clock` imports; the `blockedRouteOpenReason` prop and its render block. Unchanged, deliberately: `performClearSession`/`renderClearAction`/`LAUNCHER_CLEAR_ACTION_COPY`/`describeUnresumableReason` and the whole End-ride/Discard `ConfirmDialog` machinery (items 29/41/50's own established mechanisms) — none of item 73's own required behaviour touches them.
    - **New e2e test-only seam.** `rideStateRepository.ts` gained `window.__acnE2eRideStateClearFailure?: () => Error | undefined`, mirroring the existing `__acnE2eRideStateWriteDelay` hook's exact contract (undefined for every real session, an armable one-shot override only under `page.addInitScript`) — used to construct a genuine, deterministic `clearActiveRideState()` rejection/retry in Playwright without faking the whole IndexedDB layer. `e2e/support/rideStateDb.ts` gained `writeActiveRideStateRow(page, row)`, mirroring `readActiveRideStateRow`'s own raw-IndexedDB-transaction structure, reusing its file-private `toIndexedDbError`/`INDEXED_DB_NAME` — needed to construct "storage changed after the launcher's own hydration already ran" scenarios deterministically within a single browser context, since the app itself never writes a conflicting row behind its own back through any honest UI path.
    - **Files changed:** `src/App.tsx` (central guard, dialog, five entry points), `src/ui/riding/RidingLauncher.tsx` (reduced to a presentational trigger for free roam), `src/ui/riding/rideSessionTransition.ts` (new), `src/storage/rideStateRepository.ts` (new e2e clear-failure seam). Test files: `src/ui/riding/rideSessionTransition.test.ts` (new), `src/App.test.tsx` (two existing tests rewritten for the new dialog lifecycle — the free-roam-blocks-route test and the failed-check test — one existing test extended with no-dialog/no-clear assertions, and a new `describe("App — Ride switch guard (item 73)")` block covering the remainder of the required matrix), `src/ui/riding/RidingLauncher.test.tsx` (the old `describe("Start free roam", ...)` write/failure/retry tests and the `describe("blockedRouteOpenReason", ...)` block replaced with prop-driven equivalents scoped to what the component still owns, plus a new `sessionRefreshToken` re-hydration test). `e2e/rideSessionSwitchGuard.spec.ts` (new — 7 tests), `e2e/freeRoam.spec.ts` (its own pre-existing free-roam-blocks-route test rewritten for the new stay-on-Routes dialog lifecycle), `e2e/support/rideStateDb.ts` (new `writeActiveRideStateRow` helper).
    - **Component/browser transition matrix covered.** Component level (`App.test.tsx`): no session → proceeds with no dialog/clear; same-route resume → no dialog, resume-intent consumed once, one watch; route A → route B → dialog before any replacement, Cancel preserves A's exact row and restores focus, confirm clears once then opens B idle with no watch; free roam → route (rewritten existing test) → identical lifecycle; a stale launcher render exposing `Start free roam` while a route became active after hydration → guard re-reads storage at click time and is not bypassed, clear-before-write-before-watch proven via `invocationCallOrder`; a rejected clear → preserves the original row/target, retry succeeds with no duplicate clear; a rejected free-roam write after a successful old-session clear → no watch, honest retryable state, old row genuinely absent (not fabricated back); rapid double route clicks with a deferred first read → only the newer target's outcome is ever applied; an unsupported/corrupt stored `kind` → conflict requiring "Discard and continue", Cancel preserves it exactly; the full pre-existing suite (End/Finish/Pause/"Back to Ride options"/item 72 restoration-error tests) passes with only mechanical prop-rename updates. `RidingLauncher.test.tsx` covers the component's own narrowed responsibility: both free-roam callbacks fire correctly, `isFreeRoamPending`/`freeRoamError` render correctly, and a bumped `sessionRefreshToken` re-triggers hydration. Real-browser (`e2e/rideSessionSwitchGuard.spec.ts` plus `freeRoam.spec.ts`'s updated test): route A → route B full lifecycle with IndexedDB-row polling before/after each step; a stale-hydration `Start free roam` construction via the new `writeActiveRideStateRow` helper; free roam → route; same-route and same-free-roam resume each proving exactly one `watchPosition` call and no `alertdialog`; a Planning save while a different route is unfinished, proving Cancel preserves both the old session and the newly-saved route; a genuine storage-clear failure (via the new `__acnE2eRideStateClearFailure` seam) followed by a successful retry with no duplicate clear.
    - **Planning-save entry point — component-level constraint discovered and worked around, not silently dropped.** `App.tsx`'s own `<PlanningScreen>` call site has never threaded a mock `mapFactory` through to it (only `RidingScreen`/`FreeRoamScreen` receive one) — confirmed empirically, not assumed, by a throwaway spike test: mounting `<App>` and navigating to "Plan" without a map-factory override throws inside jsdom (`MapLibreAdapter.remove`'s `this.map.remove()`, since MapLibre never receives a working WebGL2 context there), exactly matching this file's own pre-existing top-of-file comment about why "Plan" is excluded from its "switching every navigation destination" test. Since `handleOpenRoute` and `handleRouteSaved` are literally identical one-line wrappers over `requestRouteTransition(route, {stampResumeIntent: false})`, the full component-level transition matrix already exercised via Routes-card clicks proves `handleRouteSaved`'s guard behaviour by direct code-reading equivalence; the genuine, real-browser Planning-save-vs-conflict scenario is covered instead by `e2e/rideSessionSwitchGuard.spec.ts`'s own dedicated test, which drives an actual Planning save (mocked ORS response) against a pre-seeded unfinished route session.
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2761/2761 across 139 files. `npm run e2e` — 183/183 across both the `chromium` (169) and `android-chrome` (14) projects; one flake was seen during development (`ridingFinishAndEnd.spec.ts`'s completion-detection test, already documented as item 32's unconfirmed CPU-contention flake) and confirmed unrelated on a clean full rerun, which passed 183/183 with zero failures. `npm run format:check` run last, immediately before committing — clean. Version bumped to 0.3.56 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - This slice ships with automated evidence only (Vitest and Playwright's Chromium/Chromium-emulated Android projects) — no physical-device verification is claimed. Real-device acceptance remains outstanding: same-route/same-free-roam recovery genuinely showing no destructive prompt and starting exactly one GPS/Follow lifecycle on a real phone; a hand-performed route→different-route, route→free-roam and free-roam→route switch, confirming the old session's progress is genuinely gone after confirming and the replacement behaves as an ordinary fresh/idle open (never auto-starting GPS merely because a switch was confirmed); Cancel and a genuine on-device storage-read/clear failure preserving the original session with a working retry; a Planning save's own conflict handling; unchanged missing-route Discard, free-roam resume and item 72 Pause/cold-recovery flows; and the new confirmation dialog's readability, focus behaviour and touch targets at real phone viewports and enlarged text. See `docs/project/current-status.md` for the tracked outstanding item.

---

<a id="item-74"></a>

## Item 74 — Investigate intermittent free-roam initial Follow zoom — done

_Category: Riding camera reliability investigation_

74. **Investigate intermittent free-roam initial Follow zoom — done**
    - Field evidence, as recorded: on one non-reproducible occasion in free roam (not route Riding), a live GPS fix was showing and Follow was visibly selected, but the map camera sat at an approximately whole-world zoom. Pressing Follow again did not fix it (correct — Follow preserves the chosen zoom). Manual zoom-in and the `+` control both restored a normal view. The abnormal zoom then survived Pause and Resume. Kept separate from item 66 throughout, per that item's own instruction: nothing here proves the two share a cause, and item 65's follow-anchor zoom fix is not credited with fixing this either.
    - **Root cause confirmed by direct source reading, then proven with a failing test against unmodified code before any production change.** `src/ui/riding/rideCamera.ts`'s shared `rideCameraReducer` (used by both `useFreeRoamCamera.ts` and `useRideCamera.ts`) reconciles `followZoomLevel` from a `"follow-zoom-settled"` event whenever `mode === "following" && !awaitingFreshFix`. `awaitingFreshFix` only tracks "has a fresh-fix event been dispatched" — it says nothing about whether `MapView.tsx`'s own `cameraTarget` effect has actually called `setCamera` for the resulting command yet, which is separately gated on `styleStructurallyReady`. On a fresh Start, `FreeRoamScreen`'s synchronous mount-time `requestFollow()` fires before the map style is ready; once the first GPS fix arrives, `awaitingFreshFix` flips `false` and a real follow command is built (carrying `NAVIGATION_ZOOM`), but that command cannot reach MapLibre until the style loads. `MapView.tsx`'s `onCameraSettled` registration already carried a first-party comment (from backlog item 67's own investigation) confirming MapLibre genuinely fires its own internal pre-style-ready `moveend` at its raw default transform (`center:[0,0], zoom:0`), forwarded unconditionally (deliberately, since Planning's crosshair-enable logic depends on that same early settle). That spurious settle satisfied every existing no-op condition and silently corrupted `followZoomLevel` to `0` — and the _next_ fresh fix then reused the corrupted value for a real, live command, visibly easing the on-screen camera to whole-world zoom. `rideCamera.ts`'s `"restore"` case accepted any finite zoom verbatim (`Number.isFinite(0) === true`), so the corruption survived Pause/Resume exactly as reported. A new `rideCamera.test.ts` test (`describe("backlog item 74: ...")`) replayed this exact four-event sequence against the unmodified reducer and reproduced both the corrupted `followZoomLevel` and the corrupted next command — this is what elevated the mechanism from a plausible race to a confirmed defect before any fix was written. Confirmed genuinely shared with route Riding, not assumed: `useRideCamera.ts` independently but identically declares its own `CameraSettledEvent` and routes into the same `rideCameraReducer`, satisfying this item's own "only touch Riding if a shared defect is proven and a shared fix is the smallest safe one" condition.
    - **The fix: a settle-provenance latch, not a settle-timing gate or a value clamp.** `awaitingFreshFix` answers "have we dispatched a fresh-fix event"; the missing fact is "has MapView actually applied a real `setCamera` call yet." `MapView.tsx` gained `appliedCameraCommandGenerationRef`, set to the current `attachGenerationRef` value immediately after the `cameraTarget` effect's `setCamera` call (which only ever runs once `styleStructurallyReady` is true) — so the latch is provably still stale for the entire pre-style-ready window the corruption requires, and self-invalidates across a map recreation (retry/fallback swap) exactly like the existing `cameraRestorePendingGenerationRef` pattern it mirrors. The internal `onCameraSettled` handler forwards a new `hasAppliedCameraCommand: boolean` field alongside the existing settle payload — additive, so the deliberately-ungated general forwarding Planning depends on (backlog item 67) is untouched. `rideCamera.ts`'s `"follow-zoom-settled"` event and reducer case gained the same field as a fourth, required no-op condition. `useFreeRoamCamera.ts` and `useRideCamera.ts` both thread it through their own `CameraSettledEvent` type and `reportCameraSettled` signature (a 5th required parameter); `FreeRoamScreen.tsx` and `RidingScreen.tsx`'s identical `onCameraSettled` call sites both pass it through. No change to `mapAdapter.ts` — the field is computed entirely inside `MapView.tsx`'s own handler, so the adapter-level payload and its test mock's input shape are untouched. Deliberately not a per-command id/generation correlation (a coarse one-shot latch is sufficient, since it can only ever become true from inside the very effect that calls the real `setCamera`), and deliberately not built on `CameraTarget.requestId` — that field is a frozen, value-dedup mechanism for outgoing explicit-press commands only, explicitly excluding automatic fresh-fix commands (see this file's own root `CLAUDE.md` Riding-behaviour bullet); reusing it here would have risked reintroducing the stationary-fix jitter bug it was built to prevent.
    - **Deterministic coverage, prove-first.** `rideCamera.test.ts`: the reducer-level reproduction test (kept as a permanent regression, updated post-fix to dispatch `hasAppliedCameraCommand: false` for the spurious settle and assert no corruption, plus a sibling proving genuine reconciliation still works with `hasAppliedCameraCommand: true`); all 5 pre-existing `follow-zoom-settled` tests updated for the new required field; one new test for the fourth no-op condition itself. `MapView.test.tsx`: a pre-fix-style test proving a spurious `triggerCameraSettled` reaches `onCameraSettled` while a real `cameraTarget` sits unapplied (`setCameraSpy` never called), extended to assert `hasAppliedCameraCommand:false` then `:true` once the command genuinely applies; a new generation-reset test proving the latch does not survive a post-load tile-error-then-manual-retry map recreation as stale `true`; the one pre-existing exact-payload assertion updated for the new field. `useFreeRoamCamera.test.ts`/`useRideCamera.test.ts`: existing `reportCameraSettled` call sites updated, plus a new test per file proving `hasAppliedCameraCommand: false` does not corrupt `persistableCameraState.zoom`. `e2e/freeRoam.spec.ts` gained a new real-browser test mirroring `ridingCamera.spec.ts`'s own `installLocalMapStyle(page, {styleDelayMs: 2_000})` race test — free roam had no equivalent before this item, despite being the screen the field symptom was observed on; it proves convergence to `data-camera-zoom:"16"` when the style loads well after the first fix, honestly noting in its own comment that the exact spurious-settle timing cannot be forced deterministically in a real browser. `e2e/ridingCamera.spec.ts`'s own existing style-delay race test (item 66's) re-run unmodified as a non-regression check, since `rideCamera.ts` is shared.
    - **Files changed.** Production: `src/map/MapView.tsx`, `src/ui/riding/rideCamera.ts`, `src/ui/riding/useFreeRoamCamera.ts`, `src/ui/riding/useRideCamera.ts`, `src/ui/riding/FreeRoamScreen.tsx`, `src/ui/riding/RidingScreen.tsx`. Test: `src/ui/riding/rideCamera.test.ts`, `src/map/MapView.test.tsx`, `src/ui/riding/useFreeRoamCamera.test.ts`, `src/ui/riding/useRideCamera.test.ts`, `e2e/freeRoam.spec.ts`.
    - **Verification.** `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build` — all clean. `npm test` — 2768/2768 across 139 files. `npm run e2e` — 184/184 across both the `chromium` and `android-chrome` projects, no flakes. `npm run format:check` run last, immediately before committing — clean. Version bumped to 0.3.57 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent) — a genuine production camera-reconciliation defect was fixed, not merely tests/docs.
    - This slice ships with automated evidence only — no physical-device verification is claimed. Real-device re-acceptance remains explicitly outstanding, because automated Chromium cannot prove the original intermittent iPhone PWA timing: repeated fresh free-roam Starts entering normal Follow framing across several GPS-start/map-readiness timings; a deliberately-chosen zoom surviving Pause and Resume on a real phone; pressing Follow still preserving that zoom. See `docs/project/current-status.md` for item 66's own updated cross-reference to this item.
