# Amazing Cycling Navigation

## Purpose

Build a private-use, iPhone-first progressive web app for exercise and leisure rides on a road bike, with two modes:

1. **Planning**: place waypoints on a map, route them along road-bike-suitable roads, inspect distance, elevation and surface suitability, save the route locally, and export a GPX containing elevation.
2. **Riding**: import or open a saved GPX, see the route and current location, stay on track, and inspect the upcoming elevation profile.

Reliability and a simple interface matter more than feature breadth. This is a visual route follower, not a replacement for a native turn-by-turn navigation app.

## Product priorities

In order of importance:

1. A saved or imported route must remain usable without the routing provider.
2. Riding mode must make it immediately clear whether the rider is on the route.
3. The active route and UI state must recover cleanly after iOS suspends or reloads the PWA.
4. Planning must produce a self-contained GPX with dense track geometry and elevation.
5. Distance to the next planner-generated manoeuvre and elevation segmentation are enhancements, not prerequisites for the first usable release.
6. Planning must strongly prefer reliably paved road-bike-suitable routes while treating missing OpenStreetMap surface data as uncertainty to expose, not proof that a road is unsuitable.

## Explicit non-goals

Do not add these unless the user changes the scope:

- spoken instructions
- automatic rerouting
- ride recording or location history
- Bluetooth sensors
- haptic alerts
- background location tracking
- accounts, cloud synchronisation, analytics, advertising, or telemetry
- automatic loop generation by target distance
- full offline map downloads
- weather, traffic, social, sharing, or route-popularity features

Sharing, above, currently means: no inbound file-association, Web Share Target or universal route-link work. Sharing a GPX through Messages, Mail or another application, followed by manual GPX import, is sufficient for the current product; iOS cannot currently provide the cross-platform installed-PWA "Open with GPX" experience that would be desired here, so do not promise it. This can be reconsidered later without blocking ordinary GPX export/import — do not remove the existing outward GPX export functionality, or any already-supported use of the Web Share API, on the strength of this note.

## Platform facts and lifecycle

- The primary device is an iPhone Home Screen PWA.
- Web geolocation updates are not reliable while the document is hidden or the phone is locked. Never imply otherwise.
- Persist the active route and navigation state locally before suspension.
- On `visibilitychange` and `pageshow`, restore state immediately, display any old location as stale, restart the location watch, and replace it with a fresh fix.
- Request high-accuracy geolocation only while Riding mode is visible and active.
- An optional screen wake-lock may be added later, off by default.
- The essential route line, current position, progress, off-route state, and elevation data must not depend on the routing service during a ride.

## Hosting and privacy constraints

- The frontend is a static GitHub Pages site served over HTTPS.
- The repository is public and intentionally has no licence for now. Do not add a `LICENSE` file or claim an open-source licence.
- Never commit secrets, personal GPX files, real home coordinates, generated route histories, or API keys.
- Do not add analytics or external error reporting.
- Imported and planned routes stay in IndexedDB unless the user explicitly exports them.
- A routing-provider API key must be entered by the user and stored locally. Never bake it into the JavaScript bundle, source, examples, tests, or GitHub Actions configuration.
- Planning coordinates may be sent to the configured routing provider. Riding locations must not be sent to it.

## Preferred technical stack

Respect an established stack if the repository already has one. If it is empty, use:

- Vite
- React and TypeScript with strict type checking
- MapLibre GL JS for map rendering
- IndexedDB through Dexie for local persistence
- a service worker generated with `vite-plugin-pwa`
- Vitest for unit and component tests
- ESLint and Prettier

Use small focused packages rather than broad frameworks. Add a dependency only when it materially reduces risk or complexity. Keep geospatial and GPX logic behind project-owned typed interfaces.

## Architecture

Keep these concerns separate:

- `domain`: provider-independent route, elevation, manoeuvre, waypoint, and navigation types
- `gpx`: secure GPX parsing, validation, normalisation, and export
- `routing`: a `RoutingProvider` interface and provider adapters
- `navigation`: distance arrays, GPS-to-route projection, progress continuity, off-route classification, and upcoming-elevation selection
- `storage`: versioned IndexedDB schema and migrations
- `pwa`: manifest, service-worker policy, update lifecycle, visibility recovery, and install behaviour
- `map`: presentation of route, progress, current position, accuracy, and waypoints
- `ui`: Planning, Riding, route library, settings, and diagnostics

The UI must depend on the project's canonical domain model, never directly on a provider response.

Suggested core types:

```ts
type Coordinate = readonly [longitude: number, latitude: number];

interface RoutePoint {
  coordinate: Coordinate;
  elevationMetres: number | null;
  distanceFromStartMetres: number;
}

interface Manoeuvre {
  distanceFromStartMetres: number;
  type: string;
  instruction?: string;
}

interface RouteWarning {
  kind:
    | "unknown-surface"
    | "questionable-surface"
    | "unsuitable-surface"
    | "access"
    | "steps"
    | "ford"
    | "ferry"
    | "other";
  startDistanceMetres: number;
  endDistanceMetres: number;
  message: string;
}

interface SurfaceSummary {
  pavedMetres: number;
  questionableMetres: number;
  unsuitableMetres: number;
  unknownMetres: number;
}

interface PlannedRoute {
  id: string;
  name: string;
  createdAt: string;
  points: RoutePoint[];
  manoeuvres: Manoeuvre[];
  distanceMetres: number;
  ascentMetres: number | null;
  descentMetres: number | null;
  surfaceSummary?: SurfaceSummary;
  warnings: RouteWarning[];
  source: {
    kind: "gpx-import" | "planner";
    provider?: string;
    profile?: string;
  };
}

interface RoutingProvider {
  calculateRoute(
    waypoints: Coordinate[],
    options: RoutingOptions,
    signal?: AbortSignal,
  ): Promise<PlannedRoute>;
}
```

Improve these types as implementation knowledge grows, but preserve provider independence.

## GPX behaviour

- Accept GPX tracks and routes, with tracks preferred when both exist.
- Parse locally with `DOMParser`; never upload imported files.
- Validate file type, XML parsing errors, coordinate ranges, finite numeric values, and reasonable input size.
- Handle missing elevation explicitly. Do not invent elevation silently.
- Preserve dense geometry and elevation when exporting.
- Export standards-compatible GPX. Project-specific manoeuvre metadata may use an optional namespaced extension that other readers can ignore.
- Document and test the chosen distance, smoothing, ascent, and descent calculations.
- Do not sum raw positive elevation noise. Resample or smooth before ascent calculation and retain the raw imported elevations separately if useful.

## Riding behaviour

- Design for road-bike speeds and brief glances: show a high-contrast route, current location, GPS accuracy, completed portion, remaining portion, distance remaining, off-route state, and upcoming elevation with minimal interaction.
- Offer upcoming-elevation windows of 2 km, 5 km and 10 km, defaulting to 5 km.
- Always distinguish a stale fix from a fresh one and show fix age when relevant.
- After a location error, Try again must reactivate or replace the location watch and request camera follow. Once a fresh fix is accepted, the error clears, geolocation status returns to watching, and the Follow-location and north-up controls reappear automatically, without reopening or restarting the route. A location error preserves the last known fix as stale rather than discarding route progress or resetting the camera to overview.
- A second Northwards press after an intervening manual rotation, and a second Follow-location press with an unchanged GPS fix after an intervening manual gesture, must both genuinely re-apply the camera rather than being silently swallowed. This was a confirmed field bug for Northwards (real-device testing showed a repeat press could do nothing after a manual rotation/tilt) and, on investigation, an equally reproducible defect for Follow-location: both explicit commands previously shared MapView's single value-based `cameraTarget` deduplication with automatic GPS-driven updates, so a second press producing byte-identical target values as the first (the north-up reset is always the same fixed values; a stationary rider's resumed-follow bearing resolves identically once a manual gesture clears the rotation dead band) was indistinguishable from an unrelated rerender and discarded. Fixed by giving `CameraTarget`/`RideCameraCommand` an optional `requestId`, generated by a plain monotonically-increasing counter in `useRideCamera.ts` (never a timestamp or `crypto.randomUUID()`) for these two explicit commands only: when present and different from the last-applied one, MapView reapplies the camera even though the values are unchanged; when absent — every automatic fresh-fix follow update and the one-time restore jump — deduplication stays exactly as value-based as before, so unrelated rerenders and repeated stationary fixes still never restart the camera or jitter.
- Restoring a suspended ride into `following` camera mode with only a stale fix (before Resume riding is pressed, or while it's still awaiting the first fresh fix afterwards) must show the pre-ride full-route overview, never MapLibre's raw default world view. `useRideCamera` tracks a sticky, monotonic `hasActionableCameraTarget` latch (true only once a real camera command — a live follow ease or a restore jump — has actually been produced this route-open session; reset only when the camera genuinely returns to `overview`, via a new route or an overview-mode restore) and `RidingScreen` passes that, not a raw `camera.mode !== "overview"` check, as `MapView`'s `suppressInitialOverviewFit`. This makes the pre-ride framing independent of whether restoration or the map's own style-readiness wins the startup race, and the latch's monotonicity also stops a later mid-ride manual pan (following → free) from spuriously re-triggering the overview fit.
- Project each accepted GPS fix onto plausible route segments.
- Preserve progress continuity at self-intersections and out-and-back sections. Do not simply choose the globally nearest segment when that would jump implausibly along the route.
- Base off-route classification on both lateral distance and reported GPS accuracy. Require repeated evidence before showing a strong off-route warning.
- A saved route must work without map tiles; use a neutral fallback background when necessary.
- Imported GPX files generally do not contain reliable manoeuvres. Do not infer or promise turns from geometry alone. Show next-turn information only when trusted manoeuvre metadata exists.
- When trusted manoeuvres are added, show them early enough for road-bike speeds and make the remaining distance increasingly prominent inside 500 m.
- Current GPS speed may inform plausibility or presentation, but do not retain speed history or turn the feature into ride recording.

## Planning behaviour

- Add, insert, drag, reorder, and delete waypoints with undo/redo.
- Tapping the currently selected waypoint again deselects it, leaving its coordinate, order and the routed result unchanged and triggering no recalculation or undo/redo entry; tapping a different waypoint transfers selection instead. While an explicit Move or Insert-after relocation is active for that waypoint, re-tapping it leaves the relocation active — only the existing Move/Insert-after toggle-off or the placement/confirmation action ends it.
- Include an explicit “return to start” action for closing a loop. Do not generate a loop automatically.
- Recalculate only changed route legs and debounce drag completion.
- Retain the last successful route when a provider request fails.
- Never silently substitute straight lines for failed routed legs.
- Show distance, ascent, descent, and elevation profile before export.
- Offer two road-cycling profiles, selectable in Planning: `cycling-road` (Road bike, default for every new draft) and `cycling-regular` (General cycling, may use more cycling infrastructure but is not a guarantee of paved surfaces or road-bike suitability). One profile drives every leg of a given route; no automatic fallback between profiles. For openrouteservice, use each profile's own recommended routing preference rather than merely the shortest route.
- Strongly prefer asphalt and other reliably paved surfaces, including suitable paved cycleways.
- Strongly discourage or avoid steps, fords, foot-only paths, dismount sections, sand, grass, ground and rough tracks.
- Treat fine gravel, compacted surfaces, paving stones and similar surfaces as configurable or questionable rather than universally suitable.
- Do not hard-reject an otherwise valid road solely because its surface tag is missing. Record and display the unknown distance instead.
- Request provider surface, way-type and access metadata where available, normalise it into provider-independent warnings and retain provenance.
- Show route distance, ascent, descent, paved distance or proportion, questionable distance, and unknown-surface distance before export. Estimated duration is secondary for exercise and leisure rides.
- Make questionable, unsuitable and unknown segments inspectable on the map. Never present incomplete map data as a guarantee that a route is paved or legally accessible.
- Keep ferry avoidance configurable instead of silently rejecting all ferries.
- A genuinely fresh Planning session (no restored draft, no waypoints yet) frames an approximately 50 × 50 km area around the rider's approximate location. A restored draft or any existing waypoint always takes precedence, and this automatic framing is skipped entirely in that case. On the first successful Planning geolocation in a fresh session, the map performs this existing one-time regional framing around the position, approximately a 50 × 50 km box. Subsequent Locate-me actions only recentre on the latest valid position, preserving the current zoom, bearing and pitch. They do not repeat the regional box fit or enable follow mode. If Locate me/Retry produces the session's first successful location before the user has established another camera view, it may use the initial regional framing; once initial framing has occurred, Locate me always uses recentre-only behaviour. Updating the location marker never retriggers the box fit. The control has its own visible locating/failure/retry state and never overrides in-progress waypoint editing; once resolved, the rider's approximate current location is also shown on the map as a plain dot, preserved across a failed retry rather than cleared. A north-up/top-down control mirrors Riding's semantics (resets bearing and pitch to 0° without recentring, changing zoom, or introducing following mode or camera tilt).

The first provider adapter should target openrouteservice, offering its `cycling-road` and `cycling-regular` profiles, but provider-specific code must remain isolated. The API key is user-supplied and local. Handle `401`, `403`, `429`, network failures, cancellation, malformed responses, and quota headers where available. Do not call the provider continuously while a waypoint is being dragged.

## Maps and data attribution

- MapLibre is only the renderer; keep the tile source configurable.
- Display visible attribution required by OpenStreetMap and the selected tile provider.
- Do not bulk-download or prefetch from OpenStreetMap community tile servers.
- Do not store regional map tiles or routing graphs in the GitHub Pages deployment.
- The core ride display must degrade usefully if tiles or connectivity fail.

## GitHub Pages and PWA requirements

- Support deployment beneath `https://<user>.github.io/<repository>/`, not only at `/`.
- Derive or configure Vite's `base` correctly and keep manifest `start_url`, `scope`, icons, service-worker URLs, and asset URLs within that base.
- Prefer no URL router. If routes become necessary, use hash routing so direct loads do not return a GitHub Pages 404.
- Supply a GitHub Actions Pages workflow that runs install, checks, tests, build, and deployment.
- Pin the exact Node.js version (via `.nvmrc`, read by CI through `actions/setup-node`'s `node-version-file`) and its bundled npm version; fail CI immediately if the resolved versions don't match, and use `npm ci`.
- Cache the application shell, not personal data or arbitrary routing responses.
- Never force a service-worker update or page reload during an active ride. Surface a deferred update prompt.
- Keep IndexedDB data compatible across ordinary application updates through explicit schema versions and migrations.

## Interface and accessibility

- Optimise Riding mode for a small phone mounted on a bicycle, bright daylight, gloves, vibration, and brief glances.
- Assume road-bike speeds: prioritise advance visibility, low interaction and rapid comprehension over dense information.
- Use large touch targets, high contrast, restrained motion, and few controls.
- Do not rely on colour alone for route or warning status.
- Use metric units throughout.
- Use British spelling in user-facing text and documentation.
- Make empty, loading, stale, offline, permission-denied, and provider-failure states explicit.
- `src/index.css` carries a small shared visual foundation: spacing (`--space-4` through `--space-32`), two radius tokens and one restrained shadow token, and colour roles including a new `--colour-info`/`--colour-info-soft` for informational/location cues, independent of any map colour (map/route/gradient/warning colours are unchanged). A small button vocabulary (`.btn-primary`/`.btn-secondary`/`.btn-danger`, the latter deliberately quiet/outlined rather than a large solid red) and layout classes (`.screen`, `.stack`, `.row`) sit on top. Routes (`RouteLibrary`/`RouteListItem`) is the first screen migrated to this foundation, presented as route cards rather than a bullet list; other screens keep their existing internal layout for now and are expected to migrate incrementally in later slices, per this file's own smallest-coherent-vertical-slice discipline. A second slice migrates Settings to this same foundation and completes Future-backlog item 15 (Avoid ferries by default). Settings is restructured beneath its single `<h1>` into two `.panel` sections — a new **Route planning** panel and the existing **OpenRouteService** panel (both `h2`, unchanged heading level) — plus a native, collapsed-by-default `<details className="settings-disclosure">` ("How the key and route data are used") inside the OpenRouteService panel, holding the two longer explanatory paragraphs (the HeiGIT data-flow sentence, and the "not encrypted"/clearing-site-data sentence); the sign-up sentence and link, the verification-status line, the offline notice, and any rejected/quota-limited/unavailable status all stay outside it, always visible, never concealed inside a closed disclosure. The Route planning panel's only control is a new **Avoid ferries by default** setting: a real `<input type="checkbox">` (`.setting-row`, wrapped in a `<label>` so the whole row activates it, 44×44 px minimum via `--touch-target-min`, with an explicit `aria-label` so its announced name stays just the concise title rather than the row's hint sentence too) whose checked state is bound directly to a live-query-derived persisted value from a new `getPlanningPreferences()` — never a separate optimistic local flag — so a save in flight, or one that fails, can never make the control appear to have already succeeded; on failure the checkbox simply keeps showing the last-persisted value (nothing was ever changed locally) alongside a small `role="alert"` message, and a `role="status"` "Saving…" note mounts only while a write is genuinely in flight. Backing storage is a new singleton `planningPreferences` table (schema `version(3)`, id `"planning"`, purely additive like v1→v2, no `.upgrade()` needed): `src/storage/mapping.ts`'s `fromStoredPlanningPreferences` defaults a missing row's `avoidFerriesByDefault` to `true`, and `src/storage/planningPreferencesRepository.ts` exposes `getPlanningPreferences()`/`savePlanningPreferences(...)`. `PlanningScreen.tsx`'s existing one-time draft-hydration effect is unchanged for a restored draft (one or more waypoints already present); along its existing "genuinely fresh" branch only (no draft row at all, or a draft row with zero waypoints — the same boundary the debounced autosave effect already used), it now additionally fetches this default and seeds `avoidFerries` from it exactly once. From a draft's first autosave onward it is fully self-contained: changing the Settings default afterwards never rewrites it, never recalculates its route, and never touches an already-open draft or a saved route. Planning's previous directly editable "Avoid ferries" checkbox is removed and replaced with concise read-only text ("Ferries: avoided for this plan" / "Ferries: allowed for this plan") plus a "Change default in Settings" button reusing the existing `onNavigateToSettings` prop — no new navigation plumbing. New shared classes: `.panel` (a neutral bordered/elevated surface, mirroring `.route-card`), `.status-row`/`.status-row--info` (the latter the first real use of `--colour-info`/`--colour-info-soft`, applied only to the offline notice), `.setting-row`/`.setting-row-checkbox`/`.setting-row-text`/`.setting-row-title`, `.field-hint`/`.field-error` (also reused by Planning's read-only ferry-policy text), and `.settings-disclosure`; `ConfirmDialog.tsx` (still Settings' only consumer) picks up the existing `.route-delete-confirm`/`.route-delete-confirm-actions`/`.btn-secondary`/`.btn-danger` classes rather than new ones, since its visual need is identical to the existing route-deletion confirmation. A third slice ("Active Riding glance hierarchy") migrates `RidingScreen` itself onto the shared foundation (`.screen`/`.screen-title`, the latter gaining `overflow-wrap: anywhere` so a long route name always wraps rather than overflowing) and restyles only its active-riding states (watching, error, stale, off-route, active-climb) — the pre-ride/idle prompt, the offline banner, and `RidingClimbSelector` are deliberately left untouched, matching this slice's presentation-only scope. `RidingWakeLockControl` keeps its existing DOM-order-before-the-title/non-pushing-popover behaviour and just gains a small, muted `.ride-wake-lock-control` treatment. A route's distance/ascent moves out of a standalone paragraph into a compact `.route-card-meta`-styled line directly under `<h1 className="screen-title">`, deliberately reusing Route Library's own meta-line class rather than a near-duplicate. The "On route"/GPS-accuracy/"Remaining" paragraphs combine into one new `src/ui/riding/RidingStatusStrip.tsx` — status, then remaining distance, then GPS accuracy/staleness (a genuine reorder from before) — a small presentational component receiving only the already-derived `offRouteLevel`/`distanceRemainingMetres`/`accuracyMetres`/`isStale`/`fixAgeMs`, never touching off-route/stale computation; "Off route" keeps `role="alert"`, "On route"/"Possibly off route" stay `role="status"`, and the latter two additionally get a non-exclusive `--colour-warning`/`--colour-danger` text-colour cue (the app's first real use of `--colour-warning`) — the unchanged wording alone still carries the distinction. The waiting-for-fix message and the geolocation-error block (same retry button) stay inline in `RidingScreen.tsx`, gaining `.status-row` and a new `.ride-alert-panel` class respectively, rather than folding into `RidingStatusStrip`, since neither carries the same already-derived-value shape. `RidingNextManoeuvrePanel` moves from fully inline styling to `.ride-manoeuvre-card`/`.ride-manoeuvre-icon`/`.ride-manoeuvre-text`/`.ride-manoeuvre-instruction` classes, while its urgency-driven distance paragraph deliberately keeps font-size/font-weight as inline style (same `URGENCY_FONT_SIZE_REM`/`URGENCY_FONT_WEIGHT` values as before) since Vitest's `css:false` test environment cannot verify a class-driven computed style; its urgency-escalation test now locates that paragraph via a new `.ride-manoeuvre-distance` class instead of a fragile `querySelectorAll("p")[1]` index — a selector-only test change, its "never `role=alert`" and "role=status text stays byte-identical across same-urgency rerenders" behaviour is unchanged. Its two "unavailable" messages gain `.status-row` rather than becoming an empty card. The map's previous inline `{height:320, position:"relative"}` wrapper becomes `.ride-map-container` (still exactly 320px pre-ride) plus a `.ride-map-container--active` modifier applied whenever `geolocationStatus !== "idle"`; `MapView`'s existing `ResizeObserver` picks this class-driven height change up with no logic change, proven by a new `e2e/layout.spec.ts` test comparing the map container's and its canvas's bounding boxes before and after Start riding. Real-browser investigation during this slice found that a raw `clamp(280px, 46dvh, 420px)` — the originally intended rule — reproducibly breaks the Follow-location repeat-press-after-a-manual-gesture regression test: certain resulting container heights (unrelated to whether the value is fractional; a whole-pixel 331px was just as broken as 331.1875px) leave MapLibre's own drag-rotate/drag-pitch handler permanently "active" for this route/camera combination, so it never emits `moveend`/`rotateend` and the camera visibly never finishes rotating even though its internal bearing/pitch did change — a genuine MapLibre/CSS interaction, not a test artefact, since the same fixed-pixel drag gesture a rider could plausibly make is what triggers it. The shipped rule therefore snaps to a coarse 20px grid instead of a continuous value — `@supports (height: round(nearest, 46dvh, 20px)) { .ride-map-container--active { height: clamp(280px, round(nearest, 46dvh, 20px), 420px); } }`, behind the plain-`dvh` `@supports` fallback (`clamp(280px, 46dvh, 420px)`) which itself sits behind a flat `340px` fallback, mirroring `.app-shell`'s own documented minifier-safe pattern of separate `@supports`-gated rules — verified directly, via the real drag gesture, against both the default desktop e2e viewport (1280×720 → 340px) and the project's 390×844 mobile viewport (→ 380px); no test currently pins the exact height value itself, only that it differs from the pre-ride height and that the canvas reflows to match it. The North-up/Follow-location buttons move from large inline style objects to `.ride-map-control`/`.ride-map-control--north-up`/`.ride-map-control--follow`/`.is-pressed`, mirroring (not reusing) `.planning-map-control`'s shape/pressed-swap at Riding's own bottom-right position, keeping the same aria-labels/aria-pressed wiring, the same literal `#ffffff` pressed-state text colour, and the Follow button's pre-existing unbolded weight. `RidingClimbProgressPanel`'s long-orphan `className="riding-climb-progress"` finally gets matching CSS (a `.panel`-like bordered card) with no JSX change at all, and now renders immediately before its own detailed chart rather than after it — a presentation-only reorder with no test dependency either way. The Full/2km/5km/10km/Climb buttons keep their exact DOM order and `aria-pressed` source of truth but gain a real `.elevation-window-button` class plus an explicit `.is-selected` toggle (mirroring `.cycling-profile-button.is-selected`/`.route-warning-button.is-selected`, never an `[aria-pressed="true"]` attribute selector), and `.elevation-window-group` gains `display:flex;flex-wrap:wrap` so five buttons never force horizontal scroll on a narrow phone. The chart, climb-progress panel, `GradientColoursDisclosure`, `RouteFeatureDetailsPanel` and `GradientSegmentDetailsPanel` are grouped, together with the elevation-window-group, inside one new unlabelled `.ride-elevation-section` wrapper — outer spacing only, no internal prop/markup changes to any of them. A fourth slice ("Planning workflow organisation") migrates `PlanningScreen` onto the same foundation (`className="screen planning-screen"`), the last of the four screens to move — no routing, map, waypoint, draft, warning, elevation or Save/Export _behaviour_ changed anywhere in this slice, only markup/classes around it. The previous inline `{height:320, position:"relative"}` map wrapper becomes `.planning-map-container`: a flat 340px fallback, then `@supports (height: 44dvh) { height: clamp(280px, 44dvh, 460px) }`, deliberately without a second `--active` modifier — unlike Riding, Planning's map is one continuous workspace with no idle/active transition to key a second height off. Real drag-rotate/drag-pitch gesture verification at this height range, mirroring the third slice's own method, did not reproduce the stuck-gesture MapLibre bug that motivated Riding's `round()`-snapped 20px-grid tier at the time, so that extra tier was not shipped in this slice — see the fifth slice below, where a subsequent CI run proved that omission wrong. The three locate-failed/warning-selected/route-feature-selected blocking messages, previously flow-positioned inside the fixed-height wrapper (where they could overflow below the visible map, the same class of bug `.map-attribution`'s own comment already guards against), move into a new absolutely-positioned `.planning-map-status-overlay`/`.planning-map-status-message`, with byte-identical conditional logic and `role="status"` text. Undo/Redo/Return to start/Add to end become one `.row` toolbar; Return to start alone gains `.btn-secondary` as its sole visual distinction from the other three (never `.btn-danger`, which stays reserved for genuinely destructive actions). `WaypointList` migrates from a per-button inline `TOUCH_TARGET_STYLE` object to real classes (`.waypoint-list`/`.waypoint-row`+`.is-selected`/`.waypoint-row-select`/`-ordinal`/`-label`/`-reorder`/`-icon-button`/`-relocate`, `.waypoint-relocate-button.is-pending`); a row's selected state uses `.main-nav-button[aria-current="page"]`'s richer inset-ring-plus-accent-text treatment rather than the plainer neutral-fill pattern, deliberately distinct from Move/Insert-after's own pending state (the plain neutral variant, matching `.cycling-profile-button.is-selected`) so the two never look identical. Each row gains a purely decorative, `aria-hidden` numbered ordinal badge; the select button's accessible name stays exactly the pre-existing "Start"/"Waypoint N" text, so no query semantics changed. DOM order within a row changes deliberately (select → reorder → delete → conditional Move/Insert-after sub-row, was select → conditional group → reorder → delete) to match the section's own prose ordering; Move/Insert-after stay in their own `role="group"`, continuing the file's original relocate-vs-reorder separation rationale. The empty state changes from "No waypoints placed yet." to "No waypoints yet. Tap the map or use the crosshair button below to add one." Road bike/General cycling becomes an equal-width segmented selector (`.cycling-profile-group` gains `display:flex`, a new base `.cycling-profile-button{flex:1 1 0}` rule added alongside the existing `.is-selected` modifier) inside a new **Route options** `.panel`, alongside the unchanged 0.3.1 read-only ferry-policy readout and a **Calculate route** section restyled as `.btn-primary` with its key-status/error/stale messages gaining `.status-row`/`.field-error` and its own "calculated in sections" explanation moved into a `<details className="settings-disclosure">How recalculation works</details>` — the provider-key status, provider error and stale-result message all stay outside that disclosure, always visible, per this file's own "never hide the current provider error or stale-result message" rule. `RouteSummaryPanel` gains a visible `<h2>Route overview</h2>` and a `.panel`-styled root, while its `aria-label="Route summary"` landmark is deliberately left unchanged — a landmark name and a visible heading serve different navigation purposes, and roughly twenty existing tests query the landmark by that name. Distance/ascent/descent/waypoint-count move into a `.route-summary-metrics` row; the elevation chart, `GradientColoursDisclosure`, `RouteFeatureDetailsPanel` and `GradientSegmentDetailsPanel` are grouped inside a new `.route-overview-elevation-section` wrapper, mirroring `.ride-elevation-section`'s own identical rationale but kept as a separate class, continuing this codebase's convention of not sharing Planning/Riding presentational classes for similar concepts; the surface breakdown becomes a `.surface-summary-grid` (all four categories, including zero/unknown, still always shown); the warnings list gains a conditional `<h3>Route warnings</h3>` (only when `warnings.length > 0`) at the same heading depth as the two shared detail panels' own existing `<h3>`s. None of the warning-selection, map-tap-reveal-and-scroll, `aria-expanded`/`aria-controls`, or surface-detail logic changed. Route name/Save route/Export GPX become a **Save or export** `.panel`, the input gains `.field-input`, and Save/Export gain `.btn-primary`/`.btn-secondary` — `canSaveOrExportPlan`'s gate itself is untouched. `NoApiKeyNotice` gains `.status-row status-row--info` (the second real use of `--colour-info`/`--colour-info-soft`, after Settings' offline notice) and a `.btn-secondary` Open Settings action. A new `.planning-section` class (`max-width: 720px` centred via auto inline margins, gated behind a `min-width: 900px` media query) constrains every non-map section's text measure on a wide desktop browser while the map itself stays full width — a single well-spaced column, not a two-column grid, per this slice's own explicit design-decision record. A fifth, corrective slice reinstates the `round()`-snapped 20px-grid tier the fourth slice's own real-browser verification had concluded was unnecessary: a subsequent GitHub Actions CI run of `e2e/planning.spec.ts`'s "pressing Northwards twice" test failed exactly the way Riding's own investigation predicted — a genuine right-button drag left `data-camera-bearing` stuck at `"0"` because MapLibre's drag-rotate handler never fired `moveend` — at the unsnapped `44dvh` height (≈316.8px on the project's default 1280×720 e2e viewport). Extensive local reproduction attempts (10 unthrottled repeats and 15 repeats under 3× CDP CPU throttling, at the exact CI-matching measured height) did not reproduce the stuck gesture, consistent with the repository's own existing account of this MapLibre behaviour as narrow, environment-sensitive and not reliably reproducible from height alone — so this fix is applied on the strength of the CI evidence and Riding's own already-proven mitigation, not a repeated local reproduction. `.planning-map-container` now mirrors `.ride-map-container--active`'s exact three-`@supports`-tier structure, keeping Planning's own `44dvh`/`460px` numbers. `e2e/planning.spec.ts`'s "pressing Northwards twice" test gains a phone-viewport (390×844) counterpart proving the same manual-drag-then-Northwards contract plus no horizontal overflow and contained map controls at that width, without duplicating the desktop test's full setup. A sixth slice ("Pre-ride route briefing") migrates `RidingScreen`'s idle state onto the same foundation — the one pre-ride path the third slice's own scope note explicitly left untouched. The Start/Resume prompt becomes a `.panel`/`.stack`-based `.ride-start-panel` with a `.btn-primary` `Start riding`/`Resume riding` button; `.stack`'s own default `align-items: stretch` already makes it full width on a narrow phone for free, so the only new CSS is a `.ride-start-panel-button` cap (`align-self: flex-start`, `min-width: 220px`/`max-width: 320px`) gated behind `.planning-section`'s own existing `min-width: 900px` desktop threshold rather than a second breakpoint; `handleStart` itself, its idle-only clear-on-start guard, and the button's accessible text are untouched. The pre-ride map gains a `.ride-map-container--overview` modifier (applied instead of `--active` while idle, never combined with it), a restrained 1px border/`--radius-md`/`--shadow-sm` mirroring `.panel`, deliberately without `overflow: hidden` since `.map-attribution` and any status/alert banner sit absolutely positioned inside the same container; the border's own ~2px content-box shrinkage (global `* { box-sizing: border-box }`) is negligible against the unchanged 320px pre-ride height and leaves `e2e/layout.spec.ts`'s pre-vs-active height comparison unaffected, since `--active` itself gains no border. `RidingClimbSelector` moves onto `.panel`/`.stack` and its own heading moves from `h3` to `h2` — same `id`/`aria-labelledby` wiring, same wording/order/default-to-"All route" behaviour — now that it is one of two top-level pre-ride sections beneath the route's own sole `h1`, per Future-backlog item 6's still-holding "exactly one `<h1>`" invariant; `.recognised-climb-select` gains an explicit `width: 100%` it never previously needed outside a flex/stack ancestor. A new `.ride-profile-panel` wraps the existing, byte-identical `.ride-elevation-section` (chart, `GradientColoursDisclosure`, `RouteFeatureDetailsPanel`, `GradientSegmentDetailsPanel`) beneath a visible `<h2>Route profile</h2>`, but only while idle — implemented as an always-present outer `<div>` whose class and heading child alone are conditional, rather than a ternary between a wrapped and a bare render of the section: the latter would change the rendered tree's depth at the exact moment riding starts, and React's own unkeyed positional reconciliation would then remount the whole chart/legend/detail-panel subtree, silently resetting `GradientColoursDisclosure`'s own uncontrolled `<details>` open state. The stable wrapper keeps `.ride-elevation-section` and everything inside it mounted in place across the Start-riding transition, at the cost of one always-present, unstyled, class-less `<div>` remaining once active — invisible to any class- or role-based query, and not observable in the rendered layout, proven by a new test asserting the section's own DOM node reference is unchanged across Start riding. No geolocation, camera, map-layer, or elevation-analysis behaviour changed anywhere in this slice. A seventh, final slice ("Diagnostics screen readability") migrates `DiagnosticsScreen` onto the same foundation, the last screen to move and completing this visual-foundation migration series — presentation-only, with no diagnostic-capture, redaction, storage-schema, or connection-test logic changed anywhere in this slice. Its System status `<dl>` becomes a new `.diagnostics-definition-grid` (each `dt`/`dd` pair wrapped in its own child `<div className="diagnostics-definition-item">` — valid `<dl>` content per the HTML spec's own div-grouped-terms model, and still adjacent siblings so the existing `nextElementSibling`-based test lookup is unaffected), one column on a phone and two columns behind a new `.diagnostics-section`'s `min-width: 900px` breakpoint — reusing `.planning-section`'s own threshold value but as its own class, since `.planning-section` is documented as Planning-specific and this codebase keeps separate class families per screen for the same visual idea. Build and Active route are the only two values given a restrained `.diagnostics-value--mono` monospace treatment, being the only build-ID/route-UUID-shaped values on the screen. The four top-level sections (System status, Recent errors, Routing diagnostics, Recent map imagery attempts) each become their own `<h2>`-headed `.panel stack diagnostics-section`, mirroring `SettingsScreen.tsx`'s own `aria-labelledby`-per-panel pattern. Recent errors/routing attempts/map attempts move from bare `<ul>` bullet lists to a new `.diagnostics-log-list`/`.diagnostics-log-row` treatment — hairline-separated rows in one bordered/elevated container, mirroring `.waypoint-list`/`.waypoint-row`'s own rationale (a continuous chronological log, not independent items) rather than `.route-list`/`.route-card`'s. The previously bare CORS/502-ambiguity paragraph moves into a collapsed-by-default `<details className="settings-disclosure">Why a fetch can fail before an HTTP response</details>` inside Routing diagnostics, reusing Settings' own disclosure class verbatim; the connection-test result keeps one nested `.diagnostics-definition-grid` (never split into per-value cards), its own and Copy diagnostic report's buttons both gain `.btn-secondary`, and the copy-failure fallback `<textarea>` gains a new `.diagnostics-report-textarea` (composed with `.field-input`; `resize: vertical` and `overflow-wrap: anywhere` only, so it can never be dragged or forced wider than the viewport) plus the app's first monospace font stack. A restrained system-status colour cue was considered and deliberately not shipped: every affected value's own wording ("Offline", "Unavailable", "Denied") already fully carries the distinction, mirroring `.status-row`'s own existing rationale, so this slice adds no new colour role and computes no new health classification.

## Engineering standards

- Keep TypeScript strict; do not suppress errors with broad `any`, `@ts-ignore`, or unchecked casts.
- Use deterministic pure functions for distance, elevation, projection, and off-route calculations, with fixture-based tests.
- Ordinary e2e tests must not depend on a live map tile provider: tests expecting the map's normal ready state serve a locally fulfilled minimal style (`e2e/support/localMapStyle.ts`'s `installLocalMapStyle`), and tests exercising the fallback style deliberately fail that request instead (`forceMapStyleFailure`). Live-provider reachability is a separate manual/optional check, not a required automated test.
- Treat GPS, GPX, provider, IndexedDB, service-worker, and map failures as expected conditions.
- Abort obsolete network requests.
- Avoid logging coordinates or API keys. Redact sensitive values in diagnostics.
- Include a local diagnostics screen showing app version, online state, service-worker state, storage health, geolocation status, fix accuracy/age, active route ID, and recent redacted errors.
- Prefer incremental, reviewable commits when asked to commit. Never commit generated personal data.
- Before finishing a task, run the relevant formatter, type checker, tests, and production build. Report anything not run.

## Change discipline

- Work in the smallest coherent vertical slices that leave the application runnable and easier to verify.
- An initial scaffold is acceptable in an empty repository. After that, prefer targeted edits over broad rewrites.
- Before editing, inspect the relevant files, tests, configuration, Git status, and nearby conventions. Do not assume the repository is clean.
- Preserve user changes and unrelated work. Never discard, reset, overwrite, or reformat unrelated files.
- Do not refactor unrelated code, rename broad APIs, upgrade dependencies, or change tooling merely while passing through an area.
- When repairing a broken lockfile or toolchain pin, reconstruct the smallest deterministic diff against the last known-good commit (e.g. a literal version-string patch) rather than regenerating via `npm install`; a full re-resolution can silently add, drop, or re-nest transitive or optional dependencies well beyond the intended fix.
- Avoid repository-wide mechanical changes unless they are explicitly required. Keep formatting churn out of behavioural changes.
- For each slice: define the observable outcome, implement it, add or update focused tests, run the narrowest useful checks, and only then continue.
- Keep temporary compatibility shims small and documented. Do not leave two competing architectures behind.
- Treat changes to domain types, IndexedDB schemas, GPX output, service-worker caching, manifest scope, and provider interfaces as compatibility-sensitive. Add migrations or compatibility tests where required.
- Justify new production dependencies. Prefer an existing dependency or a small project-owned implementation when the maintenance and security trade-off is better.
- Do not hide incomplete behaviour behind successful-looking UI. Use an explicit disabled, unavailable, or not-yet-supported state.
- If a request is too large for one safe pass, complete the highest-priority end-to-end slice, keep the repository green, and report the precise remaining slices. Do not leave a half-migrated codebase.
- Do not create commits, amend history, push, force-push, open pull requests, or modify remote state unless explicitly asked.
- Never use destructive Git commands to resolve local problems. Report conflicts or ambiguous user changes instead.

## Delivery order

Implement in milestones and keep each milestone deployable:

1. **Foundation**: static GitHub Pages deployment, installable PWA shell, IndexedDB, route domain model, diagnostics.
2. **GPX Riding core**: import, validation, map route, elevation profile, live visible-page location, projection, progress, off-route state, persistence, suspension recovery.
3. **Planning**: waypoint editing, `cycling-road` provider adapter, paved/unknown-surface analysis, route statistics, elevation, local save, GPX export. A first slice of Planning-map usability is implemented: a genuinely fresh session frames an approximately 50 × 50 km area around the rider's approximate location instead of the earlier fixed zoom-6 point jump, an explicit "Locate me" control re-centres on demand with its own loading/failure/retry state, and a north-up/top-down control mirrors Riding's own semantics. A second slice fixes the crosshair placement point and adds identifiable waypoint markers: the visual crosshair ring now shares exactly one centring mechanism with the coordinate placement actually uses (a previous redundant CSS offset made the two disagree by a few pixels); the "Add waypoint here"/"Move waypoint here"/"Insert waypoint here" action is disabled while a genuine pan/pinch/rotate gesture (including momentum) is still in flight, rather than risking a stale pre-gesture centre; completing a debounced route recalculation after an edit (append/move/insert/delete/reorder/undo/redo) no longer re-fits the camera to the whole route — a route is only fitted once per draft, on the first successful calculation (tracked explicitly via `usePlanningRoute`'s `isFirstRouteForDraft`, not by comparing coordinate-array references), and every later recalculation preserves whatever camera the rider already has; the placement callout's bottom-centre position is unchanged; and Planning waypoints render as numbered DOM markers (ordinal, start/finish/combined-loop-"`1/n`"/selected treatment, all shape/border-based, never colour alone) that have no glyph/sprite dependency and so remain visible under the local fallback style, with the generic routed-line start/finish markers suppressed whenever Planning's own waypoint markers are present. A third slice adds small, restrained direction arrows along routed geometry in both Planning and Riding: a project-owned RGBA icon, baked directly as pixel data with no external glyph, sprite or network request, is registered once per map instance and repeated along the existing remaining-route line via a single MapLibre symbol layer — so it needs no new GeoJSON source, remains available under the local fallback style, covers Planning's routed geometry but never its dashed unrouted preview (a separate source), and automatically prioritises Riding's remaining portion as the rider's live progress updates the same source the route line itself already uses. A fourth slice adds small, restrained kilometre distance-badge markers along routed geometry, in Planning, in Riding's pre-start/full-route overview, and in active Riding: each badge's label is the absolute cumulative distance from the route's original start (`RoutePoint.distanceFromStartMetres`), which never resets or renumbers as the rider progresses, zooms, or switches camera mode. A pure placement algorithm (`src/map/distanceBadgeLayer.ts`) walks the route's points once, forward only, interpolating the exact coordinate at each positive multiple of an adaptively-chosen interval (1/5/10/20 km, chosen from the map's own settled zoom — quantised to the nearest whole level and only re-read once the camera stops moving, never per animation frame — and the route's total length, escalating to a coarser interval to respect a conservative marker cap and de-escalating so a short route still gets at least one useful marker), stopping short of the finish by a small named clearance so a badge never sits on top of the finish marker. Badges are plain DOM markers, mirroring Planning's own waypoint markers via an entirely independent `setDistanceBadges`/`badgeMarkersById` collection on the map adapter, so the two marker groups can never delete each other; they need no glyph/sprite/network dependency and stay upright under rotation and tilt for free via MapLibre's default marker alignment. In active Riding, a badge is omitted once the rider's frozen/reliable matched progress — `presentationDistanceFromStartMetres`, the same value the elevation marker already uses, deliberately not the live value driving the route line/arrows — has passed it, so only the next absolute marker(s) ahead remain visible, never renumbered. Two badges that would land on the same or near-identical coordinate (a loop or an out-and-back) combine into one truthful multi-value label (e.g. "10 / 30 km") rather than overlapping unreadably. This completes the route-orientation overlay area: both direction arrows and distance markers are now implemented. A fifth slice completes Future-backlog item 14: a selectable cycling routing profile, exposing openrouteservice's `cycling-regular` profile ("General cycling") alongside the existing default `cycling-road` ("Road bike"), one profile for a whole route's every leg, with no automatic fallback between them. `RoutingProfile` itself (now `"cycling-road" | "cycling-regular"`) moved from `routing/provider.ts` into `domain/types.ts` — mirroring `ManoeuvreType`'s own domain ownership — because `routing/normalizeOpenRouteServiceRoute.ts` already depends on `gpx/` for point normalisation, and this slice's own GPX profile round-trip (below) would otherwise have created a `gpx/`↔`routing/` circular dependency; `provider.ts` now only re-exports the type, so no existing import site needed to change. `domain/routingProfile.ts` holds the validating `isRoutingProfile` guard and `DEFAULT_ROUTING_PROFILE` (`"cycling-road"`), consumed directly by `gpx/` and `storage/` without depending on `routing/`; a new `routing/routingProfiles.ts` holds the UI-facing metadata table (`ROUTING_PROFILES`: label, description, `isDefault`, backed by a `Record<RoutingProfile, …>` so a third profile value would fail to compile without matching metadata) and the `formatRoutingProfileLabel`/`describeRoutingProfile` formatters, consumed only by `ui/`. The endpoint path (`openRouteServiceAdapter.ts`'s `/directions/${options.profile}/geojson`), the request body, and `routeLegs.ts`'s leg-cache key (`buildRouteLegKey` already included `options.profile`, added originally for `avoidFerries`'s own sake) were all already profile-generic, so no adapter or cache code changed at all — only the type widened, and a parallel `cycling-regular` test was added alongside each existing `cycling-road` one. `PlanningScreen.tsx` replaced its hardcoded `profile: "cycling-road"` literal with React state, defaulted to `DEFAULT_ROUTING_PROFILE` for every new draft, and a new selector — a `role="group"`/`aria-pressed` button row mirroring Riding's own elevation-view selector exactly, placed above the existing "Avoid ferries" control, with each profile's own explanatory sentence shown beneath it (never a guaranteed claim of cycle-path preference or paved surfaces for General cycling, per this file's own routing-profile constraint) — feeds `usePlanningRoute`'s already-reactive `profile` parameter, so the existing debounced recalculation machinery needed no new coordination logic. The one genuine gap this slice closed: `usePlanningRoute` had no concept of whether its current routed result still matched the live inputs that would produce it, so toggling `avoidFerries` or (now) `profile` left Save/Export enabled throughout a recalculation, risking silently saving the previous profile's route under the newly selected one. A new private `computeRouteCalculationFingerprint` (coordinate-based, never waypoint id, mirroring `buildRouteLegKey`'s own id-exclusion) is stamped against the exact waypoints/profile/avoidFerries a successful calculation used, and a new `isStale` on `UsePlanningRouteResult` compares it against the live inputs every render; `canSaveOrExportPlan` folded `isStale` into its existing single-source-of-truth gate (`(state, isStale)`). Because the fingerprint necessarily covers waypoints alongside profile/avoidFerries (excluding them would need more code, not less), this incidentally closed a related, more serious pre-existing gap where saving immediately after a waypoint edit, within the recalculation debounce window, could silently save the pre-edit route under the intended post-edit name — not something this slice set out to fix, but a direct, documented consequence of the same mechanism. A new `describeStaleRouteStatus` (`src/ui/planning/`) supplies the explanatory text shown while stale, distinguishing a genuine profile change ("Recalculating for General cycling; showing the previous Road bike result below.", naming the previous profile from the retained route's own `source.profile`, never from live selector state) from an ordinary waypoint/avoid-ferries-triggered recalculation (generic wording, no profile named). Switching back to a previously used profile with unchanged waypoints resolves via the existing per-leg cache alone — `resolveRouteLegsInOrder` already returns without any adapter call when every leg key is already cached — so `isStale` clears near-instantly with no new short-circuit mechanism added, consistent with this file's own preference for recalculating over guessing where the existing architecture doesn't cleanly support a shortcut; proven end-to-end through `PlanningScreen` itself, not just the underlying cache. `StoredPlanningDraft` gained an optional, non-indexed `profile` field, following the exact `routeName`/`avoidFerries` precedent (no schema version bump); `mapping.ts`'s `fromStoredPlanningDraft` validates it via `isRoutingProfile` rather than trusting an arbitrary stored string, defaulting to `cycling-road`. `PlannedRouteSource`'s existing `profile` field (present since this project's original type sketch) is now typed `RoutingProfile` rather than a bare string, and is available on the `gpx-import` arm as well as `planner` — a purely informational fact, unlike the ninth slice's `ManoeuvreProvenance`, since it never gates behaviour (`RouteSummaryPanel`, its only display site, only ever shows a route's own recorded provenance, never the live selector); this was judged not to warrant a second parallel provenance field. `exportGpx.ts`'s existing `<acn:source>` writer (already written for the ninth slice's manoeuvre work) now considers `profile` independently of `provider`/`kind`, so a re-exported `gpx-import` route that itself recovered a profile from an earlier ACN reimport keeps it on a second export, not just the first; a new `parseAcnExtension.ts` export, `readAcnSourceProfile`, reads it back — deliberately synchronous and independent of `<acn:navigation>`'s geometry-digest gate, since `<acn:source>` carries no digest binding of its own and the two are siblings, not nested — tolerating an absent element, absent attribute or unrecognised value identically as "no known profile", never rejecting the file or inventing a profile for a third-party GPX with no ACN extension at all. Two existing `importGpx.test.ts` assertions were deliberately updated to reflect this, not just one: both the "becomes trusted for navigation" case and the "discards a corrupted ACN extension" case now expect the recovered profile to survive, since `<acn:source>` is untouched by a corrupted `geometrySha256`. `RouteSummaryPanel`'s "Routed via" line now shows a friendly label (`formatRoutingProfileLabel`) alongside the raw provider/profile value, e.g. "Routed via openrouteservice · General cycling (cycling-regular)", always read from the successful route's own recorded provenance, never the live profile selector. Total cycleway distance in the route summary (this backlog item's own explicitly optional extension) was deliberately left out of this slice: `normalizeOpenRouteServiceRoute.ts`'s existing waytype/waycategory parsing only decodes steps/ferry/construction codes and the waycategory bit field today, never waytype code 6 (cycleway), so showing it truthfully would need genuinely new mapping logic rather than reuse of already-normalised data. A sixth slice adds two small Planning UI polish items, reusing existing mechanisms rather than introducing new ones. Planning's map now shows the rider's own current-location dot, reusing Riding's existing generic `currentPosition`/`acn-position` MapView mechanism (previously wired only from Riding's continuous high-accuracy watch) fed instead from Planning's own existing one-shot `getApproximateLocationOnce` calls (the initial auto-frame and the explicit Locate me control) — no new geolocation watcher, permission prompt, heading, accuracy circle, follow mode, or persistence; a resolved coordinate is validated via `gpx/validateGpx.ts`'s existing `isValidLongitude`/`isValidLatitude` before being shown, and a failed or invalid Locate me retry preserves whatever marker last successfully resolved rather than clearing it, mirroring this file's own "last known fix" philosophy elsewhere. Separately, the Road bike/General cycling selector's selected button now gets a darker neutral fill plus a non-colour box-shadow ring (`.cycling-profile-button.is-selected`, mirroring the existing `.route-warning-button.is-selected` treatment) so the selection reads as visibly distinct without relying on colour alone or touching `aria-pressed` semantics; Riding's own mirrored elevation-view selector was deliberately left unstyled in this slice — a small, known visual inconsistency between the two.
4. **Riding enhancements**: trusted next manoeuvre with road-speed-appropriate advance display, distance to turn, gradient colouring, simple elevation/climb segments, optional wake-lock. A first slice of this milestone is implemented: a selectable Full/2 km/5 km/10 km elevation view, with the rolling 2/5/10 km windows correctly rebased so the rider's position is the exact left edge of the chart (previously compressed towards the right edge late in a route), a Full-profile view showing the whole route with a route-progress marker that distinguishes fresh from stale (restored) fixes and freezes at the last reliable position while strongly off-route, and persistence of the selected view across suspension/reload. A second slice fixes a location-watch lifecycle bug where, after an initial geolocation error, tapping Try again could leave the Follow-location and north-up controls permanently hidden even once a valid GPS fix arrived: the watch lifecycle now uses an explicit generation token, a retry reliably disposes any obsolete watch and creates a working one, a fresh accepted fix always restores geolocation status to watching and clears the error, and callbacks from a superseded watch are structurally ignored. A third slice adds noise-resistant gradient (uphill/downhill) colouring, shared by one provider-independent analysis module (`src/navigation/gradient.ts`): known-elevation points are split into runs separated by gaps over 500 m (left `unknown`), each run is resampled at the existing 20 m step and lightly smoothed, grade is measured over a centred ~100 m baseline window (clamped to whatever's actually available, never shifted away from the target distance, near a run's edges), classified into seven bands from steep descent to very steep climb plus `unknown`, and short (<80 m) flicker segments are absorbed into whichever neighbour is closer in severity across repeated reassign-then-merge passes until stable. One authoritative palette (`src/navigation/gradientPalette.ts`, colour-distance-tested against every existing warning/route colour and the fallback map background) drives both the elevation chart — now shown in Planning too, not just Riding — and the map: `ElevationChart` plots the shared smoothed elevation series (never the raw imported samples) and colours it per class, a shared `GradientLegend` lists only the classes actually present with a text label, exact grade range and non-colour glyph, and Full/2/5/10 km views all clip the same whole-route analysis (`clipGradientSegments`) rather than re-analysing a window, so classification always agrees at a shared distance. On the map, `src/map/gradientRouteLayer.ts` slices the route into one GeoJSON feature per gradient-class range and a new data-driven `match`-expression line layer (`mapAdapter.ts`'s `DataDrivenLineColor`, MapLibre's categorical expression support, no `lineMetrics`) colours it; gradient and surface/access/ferry warnings stay independent visual dimensions by nesting three nested-ring layers — the selected-warning halo (widened to 13 px), the six warning-category casings (widened to 8–10 px), then the narrower gradient centre (5 px, matching the existing route-line width) — with direction arrows repainted above the whole stack so they stay visible regardless of what colours the line beneath them. Active Riding clips the gradient overlay using the same live `matchedDistanceFromStartMetres` that already drives the completed/remaining route split and the arrows (not the frozen `presentationDistanceFromStartMetres` badges/the Full marker use — a known, already-documented three-way progress-value divergence, not unified here). A fourth slice unifies the elevation-chart line and the gradient classification into one shared pass, `analyzeRouteElevationProfile` (`src/navigation/gradient.ts`), so the same underlying smoothed values drive both, rather than two independently-tuned smoothing stages: the ~100 m smoothing window reuses `elevation.ts`'s own `SMOOTHING_WINDOW_SAMPLES` constant directly (not a second, separately-tuned window as before), and every gradient produced this way is fitted by least-squares linear regression over each baseline window's _raw_ resampled elevations, deliberately not the smoothed display series — using the smoothed series' own already edge-shrunk values as grade endpoints was found to bias the measured slope for roughly the first/last baseline window's worth of a run, which regression avoids since it's unbiased for a linear signal regardless of window symmetry. `ElevationChart` (Riding and Planning both) now renders this smoothed series — never the raw imported samples — as its prominent line, with raw `RoutePoint.elevationMetres` left completely untouched for storage, GPX export and diagnostics; the chart's vertical scale additionally adds ~10% padding and enforces an ~20 m minimum displayed range so a near-flat route no longer reads as an exaggerated series of hills, while the figcaption continues to report the true (unpadded) elevation range. A fifth slice replaces the continuous local-gradient "rainbow" with a two-level, Garmin-ClimbPro-inspired presentation, completing Future-backlog item 9. **Macro**: `src/navigation/routeFeatures.ts`'s `detectRouteFeatures(profile)` derives recognised complete climbs and descents from the exact same shared `RouteElevationProfile` — extended with a new `runs: ElevationRun[]` field exposing each run's already-computed resampled distances/smoothed elevations/per-point local-gradient percentages (`gradesPercent`, the same regression-fitted values gradient classification already used, previously discarded), so feature detection is never a second resample/smooth pass. Boundaries are found by a reversal/hysteresis walk per run (never bridging the existing >500 m gap-based run split): a confirmed boundary requires either the elevation to reverse by `REVERSAL_BRIDGE_ELEVATION_METRES` (10 m) or the distance since the tracked running extremum to reach `REVERSAL_BRIDGE_DISTANCE_METRES` (200 m); a point within `FLAT_EPSILON_METRES` (0.01 m) of the current extremum is treated as neither extending nor reversing — discovered, during this slice's own verification, to be essential: without it, a flat lead-in or trailing plateau adjacent to a climb (common on real routes, and never itself a "reversal") was silently absorbed into the climb's own boundary, pulling its reported start/end all the way to the run's own edge. A candidate climb is recognised at length ≥ 500 m, average gradient ≥ 3%, and Garmin's own published `climbScore = lengthMetres × averageGradientPercent` ≥ 1,500 (mathematically implied by the other two thresholds together, but kept explicit and independently tested), classified by Garmin's own published score bands (Uncategorised/Cat 4/Cat 3/Cat 2/Cat 1/HC at 8,000/16,000/32,000/64,000/80,000) — the score formula and category thresholds are Garmin's; only the boundary-detection heuristic above is an app-specific approximation, since Garmin's own boundary algorithm is proprietary and undisclosed. A candidate descent (length ≥ 500 m, average gradient ≤ −3%) has no Garmin equivalent at all, so its three severity bands (gentle/steep/very steep at −6%/−9%) and colours are entirely app-specific, and its details panel heading always reads "Recognised descent", never "Category N". One authoritative palette, `src/navigation/routeFeaturePalette.ts` (colour-distance-tested the same way as `gradientPalette.ts`, against every warning/route/marker colour — deliberately not required to be distinguishable from local-gradient colours, since macro and micro are never shown at the same route point simultaneously), drives both a new sparse macro map layer (`src/map/routeFeatureLayer.ts`, one GeoJSON feature per recognised climb/descent, clipped to the remaining portion exactly like the existing gradient layer) and a new `RouteFeatureLegend`. **Micro**: the existing local-gradient layer, legend and chart overlay are unchanged in their own analysis, but are now fed only the selected-or-currently-active feature's own clipped range (via the existing `clipGradientSegments`, reused unchanged) — empty otherwise — so an ordinary (non-feature) route section now shows only its plain base colour, never a rainbow. **Layering**: the whole climb/descent group (a new selected-feature halo, the macro layer, then the micro layer, each narrower than the last, mirroring the existing nested-ring technique) is added, as a group, before the whole warning group, so a wider, later-added warning always visually wins wherever it overlaps a climb/descent, per this file's own surface-data priority; tap-hit-testing is unaffected by paint order (a new `queryTopRouteFeatureAt`, mirroring `queryTopWarningFeatureAt` exactly, is queried only after warnings miss). **Selection**: `RouteFeatureDetailsPanel` (feature-level: category or "Recognised descent" heading, position, length, gain/loss, average and maximum/steepest gradient, climb score, explanatory sentence) and `GradientSegmentDetailsPanel` (a finer-grained drill-down into one local-gradient segment within the shown detail feature, with elevation at each end interpolated from the shared smoothed series via the existing `interpolateRoutePointAt`) are shared by Riding and Planning; a route-feature selection and a warning selection are mutually exclusive (selecting one clears the other, mirroring the existing "editing a waypoint clears warning selection" precedent); in Riding, the feature shown in detail is `selectedFeature ?? activeFeature`, where `activeFeature` is looked up via a new `findFeatureAtDistance` against the existing frozen-while-off-route `presentationDistanceFromStartMetres` — never live/raw progress — and no camera auto-fit is triggered by a route-feature selection, deliberately, since an unexpected camera jump while actively riding would be unsafe. **Chart interaction**: `ElevationChart` gained a single `onTapDistance` prop, converting a tap's pixel position to a route distance via a new `xPixelToDistanceMetres` (the exact inverse of the chart's existing `distanceToX`) rather than SVG element hit-testing — every coloured path and the marker are explicitly `pointerEvents="none"` so one transparent, full-chart `<rect>` is always what actually receives the tap (a stroked SVG path's default `pointer-events: visiblePainted` was found, during this slice's own e2e verification, to otherwise intercept clicks meant for an element beneath it). A shared, pure `resolveElevationChartTap` then resolves the tapped distance to whichever boundary the route analysis already produced — the containing local-gradient segment if the tap falls inside the currently-shown detail feature, otherwise the containing macro feature — never inventing a new boundary from the tap coordinate itself; map-tap selection remains macro-feature-only (chart interaction is where micro-segment selection lives). A single, first-of-its-kind `<details>` disclosure in this codebase, `GradientColoursDisclosure` (collapsed by default, not persisted), replaces the previously always-visible `GradientLegend` call site in both Riding and Planning, with two sections — "Recognised route features" (the new `RouteFeatureLegend`) and "Detailed local gradient" (the existing, unmodified `GradientLegend`) — each with its own required explanatory sentence. Performance: `detectRouteFeatures` is memoised alongside the existing `analyzeRouteElevationProfile` call, both keyed on route identity and computed once per route change, never per GPS fix; a fix only changes which feature is "active" via a plain distance lookup. A sixth slice fixes a real legend bug and adds a pre-ride climb selector. **Legend visibility fix**: every legend swatch (`GradientLegend.tsx`, `RouteFeatureLegend.tsx`) was an empty inline `<span>` with `backgroundColor` set correctly but no width or height anywhere — not in a CSS rule (none existed) and not inline — so it painted onto a 0×0 box and was invisible despite the colour being "correct" in the DOM; no test caught this because Vitest's jsdom environment never loads `index.css` (`test: { css: false }` in `vite.config.ts`). Fixed by a new shared `GradientColourSwatch` (`src/ui/shared/GradientColourSwatch.tsx`) whose width, height, background and border are all set via **inline style**, not a CSS class, so correctness never depends on a stylesheet being loaded — reused by both legends and by `RouteFeatureDetailsPanel`'s heading; a border is applied to every swatch unconditionally rather than only to visually light ones, avoiding per-colour luminance detection. **One authoritative metadata source**: `gradientPalette.ts`'s previous single `GRADIENT_CLASS_LABELS` (combining name and range into one string, e.g. `"Moderate climb (4% to 7%)"`) is split into three separate maps — `GRADIENT_CLASS_NAMES`, `GRADIENT_CLASS_RANGE_LABELS`, `GRADIENT_CLASS_COLOUR_NAMES` — so a legend row can show name, exact range and human colour name as three distinct pieces; range wording was also rewritten to remove a real ambiguity where adjacent bands both stated the same boundary number with no inequality cue (e.g. "Descent (−6% to −2%)" beside "Steep descent (< −6%)" never told a reader which band owned −6%) — every boundary is now phrased so exactly one entry owns it (e.g. "−6% to just below −2%"), and the same fix was applied to the three descent-severity entries in `routeFeaturePalette.ts`'s `ROUTE_FEATURE_LABELS` (described by steepness magnitude, not signed value, since a "just below" reading against negative numbers that grow more negative as they steepen was confusing). `RouteFeatureLegend` also gained one always-present, UI-only "ordinary route" row (a route section that is neither a recognised climb nor descent, including one with missing or insufficient elevation data — today genuinely indistinguishable, since there is no separate macro "unknown" treatment) using a local colour constant that deliberately mirrors, but is not part of, `ROUTE_FEATURE_COLOURS` — that map feeds `MapView.tsx`'s real MapLibre paint expression, so adding a fake feature key there would both fail the colour-distance test (identical to the base route colour) and pollute the map's real lookup table. **Pre-ride climb selector**: in Riding, before `Start riding` (`nav.geolocationStatus === "idle"`), a new `RidingClimbSelector` (`src/ui/riding/RidingClimbSelector.tsx`) shows a native `<select>` listing the route's recognised climbs in order (`listClimbsInRouteOrder`, a thin filter over `detectRouteFeatures`'s own already-ascending, non-overlapping result), numbered "Climb 1", "Climb 2", …, each option reading e.g. "Climb 2 · Category 3 · starts at 18.4 km", plus an "All route" option; a route with none shows an explanatory empty state instead of an empty dropdown. It drives the exact same `selectedRouteFeatureId`/`routeFeatureOverlay`/chart-`selectedRangeMetres` machinery already used by map- and chart-tap selection — no `MapView.tsx` changes were needed, and the existing "no camera auto-fit on feature selection" decision already satisfies "must not unexpectedly zoom or pan." The shared `RouteFeatureDetailsPanel` gained an optional `climbNumber` prop (numbering the heading "Climb 2 · Category 3" instead of "Category 3 climb", and a category-colour swatch) passed only from this pre-ride context, so there remains exactly one climb-information card rendered at any time rather than a second, competing panel. The route's first climb was originally selected by default (later reverted by a fourteenth slice below, after real-device use showed it was surprising): rather than resetting stored selection state imperatively (an effect trips this project's `react-hooks/set-state-in-effect` lint rule; a during-render ref-comparison, React's documented alternative and already used by `PlanningScreen.tsx`'s own routed-route-change invalidation, unexpectedly tripped `react-hooks/refs` for this specific component and wasn't worth chasing further), the displayed selection is a pure derivation: an explicit choice is stored tagged with the route id it was made for, and only counts while that id still matches the current route, otherwise falling back to a default (originally the first climb, now no selection) — needing no effect, no ref, and correctly resetting even if this screen were ever not remounted on a route change (today it always is, since `App.tsx` never swaps `selectedRoute` while Riding is shown). Starting the ride (`handleStart`, guarded on `nav.geolocationStatus === "idle"` specifically, since the same handler also backs the mid-ride "Try again" retry button and CLAUDE.md requires that retry to preserve ride progress) explicitly records "no selection" for the route, so a climb merely previewed pre-ride never continues to override the rider's actual active climb once riding begins. A seventh slice retires the whole-route seven-class `GradientClass` scheme (`classifyGrade`, `gradientPalette.ts`, `GradientLegend.tsx`, and their dedicated tests) entirely — confirmed unused elsewhere by a repo-wide search before deletion — and replaces it with two purpose-built detail schemes, both reusing the exact same smoothed ~100 m regression grades (`ElevationRun.gradesPercent`) already computed for the fifth slice's macro/micro split, with no second gradient calculation. **Climbs** get a genuine Garmin-ClimbPro-style five-band local scale, `classifyClimbGradientBand` (`src/navigation/routeFeatures.ts`): `< 3%` green ("Gentle, flat or brief descent"), `3–6%` yellow/gold ("Moderate climb"), `6–9%` orange ("Hard climb"), `9–12%` red ("Very hard climb"), `>= 12%` dark red ("Extremely steep climb") — deliberately worded to describe only the local gradient at one point, never "Category N", since a climb's overall category depends on length and average gradient while a local band does not. Where practical the five local bands share the exact same colour token as their corresponding macro category (`routeFeaturePalette.ts`'s `CLIMB_CATEGORY_TIER`/`CLIMB_GRADIENT_BAND_TIER`), which also means Uncategorised and Category 4 climbs — the two least-severe of six categories mapped onto five bands — now render with one identical shared green, collapsing what were previously two distinct shades; the underlying six-value `ClimbCategory` data and its separate text labels are unaffected, only the colour merges, and the legend shows one combined "Uncategorised or Category 4 climb" row rather than two identically-coloured ones. Detailed climb bands render only within the climb selected via the pre-ride dropdown, a map/chart tap selection, or (during active Riding) the climb currently occupied — exactly the same selection precedence the fifth slice already established — never across the whole route. **Descents** keep three blues rather than collapsing to one: a renamed `DescentBand` (`"moderate" | "steep" | "very-steep"`, thresholds unchanged at −6%/−9% from the retired `DescentSeverity`) is now used identically at both the macro level (the complete descent's own average gradient) and, newly, at the local level (`classifyDescentLocalKey`, the same smoothed local gradient a selected/active descent is drawn from) — unlike climbs, a descent's macro and local classification are literally the same scheme, so the legend shows the three descent rows only once, in the macro section, with the "Detailed local gradient" section explaining that a selected descent reuses those same three colours locally. A local stretch shallower than the −3% descent-eligibility threshold (a flat or brief rise inside an otherwise-recognised descent) resolves to `"neutral"` and renders as the plain ordinary-route colour rather than any blue — a deliberate asymmetry from climbs, whose shallow/flat/descending local sections still render green. `RouteFeatureDetailsPanel` gained one fixed disclaimer for descents: "Blue intensity reflects gradient steepness only, not surface, bends, traffic or other conditions." A new `src/navigation/routeFeatureDetail.ts` (`buildFeatureDetailSegments`) builds these detail segments per selected/active feature by generalising `gradient.ts`'s own classify→merge-adjacent→suppress-flicker pipeline (`ClassifiedSegment<Class>`, `classifyRunGrades`, `clipClassifiedSegments`, `findClassifiedSegmentAtDistance`) over an arbitrary classification, so climbs and descents share one tested algorithm rather than two copies; because this does real work over one run (unlike the cheap clip-only approach it replaces), both screens memoise the call, keyed on the selected/active feature and the route's analysed runs. This slice also fixes a real, narrow defect found in the code it was rewriting: `elevationChartGradient.ts`'s local-gradient chart overlay coerced any run outside the currently-narrowed detail range to a synthetic grey "unknown" colour instead of falling through to plain `currentColor` like its sibling macro-feature overlay already did, which — since both screens have narrowed this range to the selected/active feature ever since the fifth slice — painted a solid grey stroke over every other climb/descent's own macro colour; the replacement `buildFeatureDetailChartRuns` preserves `visualKey: null` for an out-of-range run, and `ElevationChart.tsx`'s overlay now omits `null` runs entirely rather than rendering a coercion. `MicroDetailVisualKey` (`routeFeaturePalette.ts`) is the shared nine-value key space (five climb bands, three descent bands, `neutral`) driving one authoritative `MICRO_DETAIL_COLOURS`/`MICRO_DETAIL_LABELS` pair consumed identically by the map's micro layer, the chart's detail overlay, the new `ClimbGradientBandLegend` (replacing `GradientLegend`), and `GradientSegmentDetailsPanel` — no second copy of thresholds or colours anywhere. An eighth slice completes Future-backlog item 8: the openrouteservice adapter already requested `instructions: true` and `normalizeOpenRouteServiceRoute.ts`'s `buildManoeuvres` already built `PlannedRoute.manoeuvres` from ORS's `segments[].steps[]`, but nothing consumed that data anywhere in Riding, and the `type` field was a bare stringified ORS numeric code, not a canonical value. `Manoeuvre.type` (`src/domain/types.ts`) is now the canonical, provider-independent `ManoeuvreType` union (`start`/`continue`/six directional variants/`u-turn`/`roundabout`/`waypoint`/`finish`/`unknown`); `src/routing/manoeuvreTypes.ts`'s `decodeOrsManoeuvreType` decodes ORS's raw codes into it, mirroring `surfaceCodes.ts`'s own never-throws decode style, and `buildManoeuvres` now also drops a step whose `way_points[0]` is not a genuinely valid in-bounds index (rather than defaulting it to distance 0), caps instruction length, and sorts the result by distance. This surfaced and fixed a real, previously-latent bug in `stitchPlannedRouteLegs.ts`: its leg-boundary manoeuvre dedup only ever matched an exact type-and-instruction equality, so it could never recognise ORS's own real per-leg Arrive/Depart step pair (distinct types, distinct instruction text) — every internal waypoint of a multi-leg Planning route was silently getting both a spurious "arrived" and a spurious "departed" manoeuvre. Since every leg's own trailing step now decodes context-free to canonical `finish` and its own leading step to `start`, the dedup rule was replaced with a narrower, correct one: a leg boundary's `finish`+`start` pair (within the existing seam tolerance) now collapses into one `waypoint` manoeuvre, dropping its instruction (neither leg's own arrive/depart text is correct mid-route); the very first leg's `start` and the very last leg's `finish` are never touched. A route's `manoeuvres.length > 0` is already sufficient, existing proof of trusted (openrouteservice-derived) data — GPX import (`normalizeGpx.ts`) always sets it to `[]` and no other code path writes to it — so no new provenance flag was added anywhere (superseded by the ninth slice below, which adds an explicit `ManoeuvreProvenance` field once GPX import can legitimately carry manoeuvres too). A new pure `src/navigation/nextManoeuvre.ts` selects the next manoeuvre from the existing frozen/reliable `presentationDistanceFromStartMetres` (never the live matched distance), giving the off-route-freeze and stale-restore behaviour for free; a named `MANOEUVRE_REACHED_TOLERANCE_METRES` (15 m, order-of-magnitude comparable to `offRoute.ts`'s own thresholds) must be passed by at least this much, not merely approached within it, before a manoeuvre counts as reliably passed, and advancement is monotonic against a caller-held `previousReachedIndex` so GPS jitter can never regress the shown manoeuvre. `RidingNextManoeuvrePanel` (Riding-only, shown once riding is active, mirroring `RidingClimbSelector`'s own idle-only visibility inverted) shows a small project-owned SVG `ManoeuvreIcon` (no external asset, inline-styled like `GradientColourSwatch`), the provider's own instruction text (or a generic per-type label when absent), and a distance whose rounding tier and font-size/weight band (this slice's own judgement calls, not literal CLAUDE.md figures) become more prominent inside 500 m and again inside 100 m — driven by inline style, not a CSS class, for the same "Vitest never loads index.css" reason `GradientColourSwatch` already documents. Only the instruction text carries `role="status"`, so it announces a genuine manoeuvre or urgency-band change but never the continuously-updating numeric distance beside it. A planner route with no usable manoeuvres shows an explanatory "Turn information is unavailable for this route" message; an ordinary imported GPX shows its own explanatory message and never an inferred turn, since `normalizeGpx.ts` never produces manoeuvres from geometry. No IndexedDB schema/migration was needed: `manoeuvres` already round-trips through `routesRepository.ts`'s raw Dexie `put`/`get` untouched. A ninth slice closes the GPX round-trip gap this left behind: exporting a planned route already wrote a flat, unbound `<acn:manoeuvre>`/`<acn:source>` extension, but nothing on import ever read it back, so a planned-then-exported-then-reimported route silently lost its turns. The exporter's existing namespace (`https://adf-md.github.io/amazing-cycling-navigation/gpx-extensions/v1`, unchanged — never yet consumed by anything, so restructuring its contents cost nothing) now writes a geometry-bound envelope, `<acn:navigation version="1" pointCount="N" geometrySha256="<64-char lower-case hex>">`, nested (as before) inside the track's own shared `<extensions>` alongside the unrelated `<acn:source>` provenance element; each `<acn:manoeuvre trackPointIndex="N" distanceMetres="X" type="...">` inside it carries an `<acn:instruction>` child (previously an attribute). `trackPointIndex` is the nearest of the route's own exported track points to that manoeuvre's distance (`navigation/distance.ts`'s new `nearestPointIndexForDistance`, a binary search — needed because a stitched multi-leg route's manoeuvre distance can legitimately fall between two points, not exactly on one) and is the round trip's primary anchor; `distanceMetres` is the canonical route distance, kept as a secondary validation/forward-compatibility value. `geometrySha256` binds the envelope to the exact exported track: `src/gpx/geometryDigest.ts`'s `canonicalizeTrackGeometry` joins each point's `lon,lat` (via the exact same bare `String(x)` the exporter already writes into `<trkpt>`, so export-time and import-time canonicalisation provably agree; elevation excluded, since anchoring is index/distance-based) with `\n`, and `computeGeometryDigestHex` hashes it with `crypto.subtle.digest("SHA-256", …)` — an integrity/binding check, not a digital signature. This makes `exportRouteToGpx` genuinely `async` for the first time (both UI export call sites and every test call site updated accordingly); if trusted manoeuvres exist but Web Crypto is unavailable, export throws a typed `GpxExportError` rather than silently dropping the turn data, while a route with no trusted manoeuvres is unaffected either way. Trust is now an explicit, provider-independent `ManoeuvreProvenance` field on `PlannedRoute` (`{ kind: "routing-provider"; provider: string }` or `{ kind: "acn-gpx-extension"; version: 1 }`, `src/domain/types.ts`), read by one shared predicate, `src/domain/manoeuvreTrust.ts`'s `hasTrustedManoeuvres` — replacing the old `sourceKind === "gpx-import"` short-circuit `RidingNextManoeuvrePanel` and `RidingScreen` used to gate on. A route saved before this field existed (or an ordinary GPX import that never carried a validated ACN extension) has no `manoeuvreProvenance` at all and falls back to exactly the previous implicit rule (`source.kind === "planner" && manoeuvres.length > 0`), so every pre-existing stored route and every ordinary GPX import behaves identically to before, with no IndexedDB migration (a plain new optional field on the already-blob-stored `PlannedRoute`). `RidingScreen` gates both `selectNextManoeuvre`'s own input (an empty list when untrusted) and the panel's messaging on `hasTrustedManoeuvres`, restoring the invariant "a non-null selection is always trustworthy" even though a `gpx-import`-sourced route can now legitimately carry manoeuvres. Import-side reading (`src/gpx/parseAcnExtension.ts`'s `readAcnNavigationExtension`) is namespace-aware throughout (the `acn:` prefix itself is never trusted, only the namespace URI) and scoped to exactly the track `parseGpx.ts`'s `extractRoutePoints` actually selected (a new `selectedTrackElement` result field) — never a second, non-selected `<trk>`, and never an extension nested inside a per-point `<extensions>`, since only the track's own direct-child `<extensions>` is ever consulted. Validation is strictly all-or-nothing: an unsupported `version`, a `pointCount`/digest mismatch, an out-of-range or non-integer `trackPointIndex`, a non-finite/negative/out-of-order `distanceMetres`, or a manoeuvre count above the defensive `MAX_ACN_MANOEUVRES` (10,000 — a sanity cap, not this file's primary size defence, which remains `validateGpxFile`'s whole-file `MAX_GPX_FILE_SIZE_BYTES`) discards the entire envelope, never a partial manoeuvre list, while the ordinary track/elevation import always still succeeds; a rejected-but-present extension surfaces one non-blocking `GpxImportNotice` ("This GPX contained turn information, but it did not match the route geometry and was ignored."), reusing the same notice mechanism as the existing multiple-track/-route warnings. An unrecognised `type` string coerces to `"unknown"` rather than rejecting the envelope (matching `decodeOrsManoeuvreType`'s existing never-throws policy), and each manoeuvre's `distanceMetres` is sanity-checked against its `trackPointIndex`'s own recomputed distance using a tolerance that adapts to the local point spacing at that index (never a flat constant, which would either be too tight for sparse provider geometry or too loose for dense geometry) — a structural guard layered on top of the digest, not a replacement for it. Re-exporting a re-imported ACN route reproduces the same manoeuvres losslessly, since the reader stores each manoeuvre's canonical, recomputed point-distance rather than the raw attribute text, making the next export's own nearest-point lookup exact. Interoperability is unchanged from what the namespace mechanism always implied: this remains a standards-compatible GPX 1.1 track that any reader can open, ignoring the unknown `acn:` extension; another application may strip unknown extensions entirely when rewriting the file; round-trip turn preservation is guaranteed only for direct export-then-import through this app; and none of this implies Garmin or FIT course-point compatibility, which remains a distinct, unimplemented feature. A tenth slice completes the milestone: an optional, off-by-default **Keep screen awake** control in active Riding, implemented entirely through platform-neutral capability detection (`"wakeLock" in navigator && typeof navigator.wakeLock?.request === "function"`, `src/platform/wakeLock.ts`) behind the same small typed DI boundary already used for geolocation and clocks — no user-agent or browser-specific branching anywhere. An unsupported browser renders no control at all, not a disabled one. `src/ui/riding/useScreenWakeLock.ts` owns the live sentinel using the same generation-token discipline `useRideNavigation`'s geolocation watch already established, extended with an explicit mounted flag and a status state machine (`inactive`/`acquiring`/`active`/`unavailable`) so a stale request, an unmount, or a superseded ride can never leak a sentinel or a state update; the control is mounted only while a ride is active (the same `nav.geolocationStatus !== "idle"` gate `RidingNextManoeuvrePanel` uses), so React's own unmount already provides "release when riding stops/the route changes/the rider leaves the screen." The rider's desired on/off preference (never a global setting) is a new optional `wakeLockDesired` field on `StoredRideState`, restored/persisted alongside the existing camera and elevation-view fields with no schema version bump; an unrelated new ride always starts with the option off. A held lock releases defensively while the document is hidden and reacquires automatically once visible again if still desired; a rejected request or an unexpected release while visible surfaces a concise, non-technical retry state instead of looping. This provides no background location and does not replace the existing suspension/reload recovery — it is a pure display-on convenience layered on top of it. An eleventh slice completes backlog item 13: an off-by-default-until-entered **Climb** elevation view, offered in active Riding only while the reliable presentation distance lies inside an already-recognised climb. One pure derivation, `selectEffectiveElevationView` (`src/navigation/climbElevationView.ts`), decides the displayed view from the rider's standard Full/2/5/10 km preference, the currently active climb, and one new persisted scalar, `dismissedClimbFeatureId` (`StoredRideState`, no schema bump) — auto-opening once per distinct climb, respecting a manual dismissal for the rest of that climb, and auto-reopening on a different climb, with no effect and no second "already shown" flag needed. Climb-relative metrics (`computeClimbProgressMetrics`) reuse the existing shared smoothed profile and already-classified local-gradient segments (no new elevation or gradient calculation), reporting distance completed/remaining, current/summit elevation, elevation remaining (the net smoothed-elevation difference to the climb's own detected finish, not a new cumulative-ascent sum) and current local gradient, with no percentage-complete value anywhere. `ElevationChart` gained an opt-in `areaFill` prop (geometry in `src/ui/shared/climbFillGeometry.ts`) that fills the profile down to the chart's own existing padded lower bound using the same Garmin-inspired climb local-gradient band colours, splitting the ridden portion (dimmed) from the remaining portion at the rider's live progress — Full/2/5/10 km views and Planning's own chart usage are unaffected. A new `RidingClimbProgressPanel` shows these live metrics with its own numbered heading, alongside the unchanged, selection-driven `RouteFeatureDetailsPanel`. A twelfth slice compacts the wake-lock control's presentation without touching `useScreenWakeLock`'s own lifecycle, state machine or generation-token discipline: the control now renders as a single compact row immediately above the route title while a ride is active (the same `isWakeLockSupported()`-and-ride-active gate, unchanged), and the always-visible battery/behaviour explanation is replaced by a tap/keyboard-accessible information popover — the first anchored, non-modal disclosure in this codebase (deliberately not the existing native `<details>` pattern, since that expands in document flow rather than overlaying). Acquisition success and the retry-on-failure state are shown compactly inline rather than as separate paragraphs; an unsupported browser still renders no control at all. A thirteenth slice extends the pre-ride climb selector with a detailed, read-only preview of the selected climb's own elevation profile, reusing the active-Riding current-climb view's rendering path rather than a second implementation: one shared pure function, `buildClimbChartViewModel` (`src/navigation/climbElevationView.ts`), takes an explicit `ClimbChartMode` (`"active-current-climb"`, carrying the rider's own progress marker, or `"pre-ride-selected-climb"`, carrying none) and builds whatever `ElevationChart` needs — points, domain, gradient segments, marker, area-fill — from the same precomputed climb/gradient-segment data the existing overview and active view already use, with no second climb-detection or grade-classification pass. The active mode reproduces today's route-global points/domain unchanged (a pure refactor of `RidingScreen.tsx`'s existing inline construction, proven behaviour-identical by its own unchanged test suite); the pre-ride mode rebases every point and gradient-segment boundary so the climb's own start is distance 0, satisfying "local distance from climb start" as a genuine, unit-tested property of the pure model itself (rendering is pixel-identical either way today, since `ElevationChart` draws no distance tick labels), and supplies no marker. This exposed one real gap in `ElevationChart`'s existing `areaFill`: its filled area previously required a marker to render at all (`buildClimbFillRuns` needed a numeric pixel split point); both now accept `markerX: number | null`, rendering one uniform-opacity fill with no completed/remaining distinction when there's no rider progress to split against — the one deliberate, spec-required behaviour change, with its own existing test rewritten (not just relaxed) to assert the new behaviour. `ElevationChart` also gained an optional `ariaLabel` prop, applied identically to both the figure and the chart's own `role="img"` name, so the new chart's accessible name identifies which climb it shows (e.g. "Elevation profile for Climb 2") without a new dependency. The chart itself slots into the existing shared `RouteFeatureDetailsPanel` via a new optional `detailChart` prop, rendered directly between the heading and the fact list — populated only by Riding's pre-ride call site (gated identically to that panel's own existing `climbNumber`, so heading/chart/facts always describe the same climb in the same render), leaving Planning's own use of the same panel, and Riding's own active-climb view (which continues to show its own chart and `RidingClimbProgressPanel` above this panel, unchanged), untouched. One accepted, documented (not fixed) pre-existing edge case: the new chart's idle-gate matches the panel's own existing gate rather than the top-of-screen chart's separate `matchedDistanceFromStartMetres` gate, so a reload that restores in-progress ride progress while the rider hasn't yet tapped "Resume riding" can show both this pre-ride preview and the active Climb view/progress panel simultaneously — an already-existing quirk of that one specific, rare state, not introduced or worsened by this slice. Manually verified on the user's iPhone across multiple climbs: the detailed chart's colours match the active current-climb view, no GPS marker appears pre-ride, and the preview still renders after a reload. A fourteenth slice reverts a design choice from the sixth slice above, after this same real-device check showed it was surprising rather than helpful: the pre-ride "Recognised climbs" dropdown no longer auto-selects the route's first recognised climb, and instead starts on "All route" until the rider explicitly picks a climb, matching Planning's own screen (which never auto-selects). The mechanism the sixth slice introduced is unchanged — `RidingScreen.tsx`'s `selectedRouteFeatureId` is still "an explicit choice, tagged with the route id it was made for, otherwise falls back"; only the fallback value changes, from `climbs[0]?.id ?? null` to a plain `null`, so a fresh pre-ride session and a route change while already viewing Riding both now start on "All route" rather than the first recognised climb. This has no effect on active-ride behaviour: `activeFeature`/`activeClimb` (GPS-progress-driven, not explicit-selection-driven) and the Climb elevation view's own separate auto-open/dismiss state machine are entirely untouched, since neither ever read `selectedRouteFeatureId`'s fallback value.

Do not start a later milestone by weakening or bypassing earlier reliability requirements.

## Manual acceptance status (6 August 2026)

This is the authoritative manual-test ledger. It records verification evidence only; the behaviour contracts remain defined by the sections above and are not restated here.

### Manually verified on the deployed iPhone PWA

- **Routes:** long route names wrap and remain usable; GPX export works; inline route deletion and its confirmation work. Route renaming now happens inside the existing route card, preserves the card and route summary while editing, and persists the changed name correctly.
- **Planning:** waypoint placement/editing, waypoint identification and map controls work; both Road bike (`cycling-road`) and General cycling (`cycling-regular`) profiles work and produce viable, meaningfully different routes; route warnings work; saving and GPX export work.
- **Pre-ride:** a newly opened route is framed correctly; recognised-climb selection works; the selected climb's detailed elevation profile works; Start riding and Resume riding have suitable prominence. The restored **Resume riding** state now reliably shows the route overview instead of occasionally remaining at MapLibre's whole-world default while restored follow mode awaits a fresh GPS fix.
- **Current-climb interaction:** automatic entry into Climb view, manual exit, and manually returning to Climb view work.
- **Diagnostics and Settings:** layout, wrapping, disclosures and touch targets are satisfactory.
- **PWA recovery:** ordinary reload and suspension recovery work.
- **Wake lock:** previously verified separately on the user's iPhone and working as intended.
- **Waypoint-row marker colour parity:** the list badges' start/ordinary/finish/loop role colours and shapes match the map's own waypoint markers as intended.
- **Ride screen scroll reset and library scroll restoration:** opening a route from Routes and saving a newly planned route from Planning both work as intended.
- **Navigation chrome and waypoint rows:** the sticky navigation safe-area background and its lower buffer look correct; `Delete` stays on the same waypoint row at double-digit waypoint counts; the centred fixed-width ordinal badge slot and aligned labels look correct; waypoint labels stay single-line through waypoint 106, including endpoint and closed-loop start/finish badge roles; the three-digit layout remains acceptably readable (item 34, checked at v0.3.25 / commit `7fae104`).
- **Planning camera framing for restored/seeded waypoints:** the restored/seeded-waypoint Planning view no longer opens at MapLibre's whole-world default zoom and is now framed appropriately (item 35, checked at v0.3.26 / commit `b8cec2c`, confirmed 13 August 2026).

### Preliminarily checked, bicycle field test still pending

- **Active Riding:** location/map readability, the next-manoeuvre panel, camera controls, wake lock and elevation-window selection look correct during walking and car tests, but still require a normal road-bike field test.
- **Current-climb progress:** entering/leaving the view is verified, but the live distance/elevation progress while actually cycling through a climb still needs a field test.

Do not treat either preliminarily-checked item above as fully verified until it has been tested on a moving road bicycle.

## Future backlog

The following items are approved directions or confirmed bugs for future work. They are recorded here for continuity across sessions and must not be implemented until a future slice explicitly scopes them in.

### Navigation and library interface

6. **Header hierarchy — done**
   - The persistent visible "Amazing Cycling Navigation" heading was removed from the app shell; the product name remains, unchanged, in the document title, PWA manifest and Home Screen identity. Every screen now renders exactly one primary `<h1>` (`.screen-title`): `Routes`, `Ride` when no route is selected, the route's own name once one is, `Plan a route`, `Diagnostics`, or `Settings` — with Settings' nested "OpenRouteService" subsection bumped from `h3` to `h2` to keep a valid descending hierarchy beneath its new `h1`.
   - The previous five unclassed `<nav>` buttons in `App.tsx` became `MainNavigation` (`src/ui/shared/MainNavigation.tsx`, with `src/ui/shared/NavIcon.tsx` supplying five small project-owned inline SVG glyphs, mirroring `ManoeuvreIcon.tsx`'s existing conventions exactly — `aria-hidden`, `fill="currentColor"`, inline `style` only, never a CSS class). It renders a compact, non-sticky, equal-width row of five icon-and-label destinations (`Routes`/`Ride`/`Plan`/`Diagnostics`/`Settings`, unchanged labels and order); the active destination is never colour alone — `aria-current="page"` is the single source of truth, paired with a soft accent surface plus an inset ring (`.main-nav-button[aria-current="page"]`), mirroring the existing `.cycling-profile-button.is-selected` convention. `Screen` (the app's own navigation-state union) now lives in `MainNavigation.tsx` and is exported from there rather than declared locally in `App.tsx`.
   - Riding's own previously-untested Ride-with-no-selected-route state is now a proper empty screen (`Ride` heading, a short explanation, and a primary "Choose a route" action back to Routes) rather than a bare paragraph.

7. **Inline route-deletion confirmation — done**
   - Pressing a route's Delete button now opens a non-modal, per-row confirmation (`role="alertdialog"`, no `aria-modal`) rendered inside that route's own `<li>`, directly beneath its Rename/Export/Delete row, replacing the previous screen-detached `ConfirmDialog`. `RouteLibrary` keeps single-source-of-truth ownership of `pendingDeleteId`/`isDeleting`/`deleteError` and the actual `deleteRoute` call; `RouteListItem` only ever renders its own row's confirmation, so at most one is open at a time and opening a second row's replaces the first.
   - Opening the confirmation moves focus to Cancel; Cancel and Escape-within-the-confirmation both close it without touching storage and return focus to that row's own Delete button — both actions are disabled while a deletion is in flight so Cancel can never appear to undo a delete that storage is still carrying out. Clicking Rename while a delete confirmation is open on the same row cancels the pending delete first, rather than leaving it silently orphaned.
   - On success the reactive `useLiveQuery`-backed list removes the row automatically; a small pure helper (`routeDeleteFocus.ts`) then moves focus to the next, else previous, else (empty library) the Routes heading — with no global selectors or route-name matching, since route names aren't guaranteed unique. A failed deletion keeps the route, shows an inline error, and leaves the confirmation open for cancel or retry; the destructive button reads "Deleting…" while in flight, to prevent double submission.
   - `ConfirmDialog.tsx` itself is unchanged and still backs Settings' "Delete OpenRouteService key" confirmation — only `RouteLibrary`'s own usage of it was removed.
   - Renaming a route (the "Rename" button) is likewise an in-card edit state: `RouteListItem` renders exactly one `<li className="route-card stack">` per route across its normal, renaming and delete-pending states — entering rename mode no longer swaps in a separate, unstyled `<li>` above the list. Distance/ascent stay visible while renaming; Export/Delete (and any open delete confirmation) are fully unrendered, not merely disabled, while renaming; the input autofocuses with its existing name selected; Save and Cancel both return focus to the row's own Rename button, and Escape performs the same discard-and-restore as Cancel, mirroring the delete confirmation's own Cancel/Escape-returns-focus precedent above.

### Remaining Milestone 4 features

8. **Trusted next manoeuvre and distance — done**
   - Milestone 4's eighth slice completes this item: `PlannedRoute.manoeuvres`, originally populated only from openrouteservice-planned routes, drives a Riding-only "next manoeuvre" panel with a road-bike-speed-appropriate advance display and a distance that becomes increasingly prominent inside 500 m. The ninth slice extends trusted manoeuvres to a validated ACN GPX re-import too, via the explicit `ManoeuvreProvenance` field and `hasTrustedManoeuvres` predicate described there. See Milestone 4's own eighth- and ninth-slice paragraphs above for full detail.

9. **Climb detection and summary segments — done**
   - Gradient colouring itself was implemented in Milestone 4's third slice (`src/navigation/gradient.ts`, `gradientPalette.ts`, `src/map/gradientRouteLayer.ts`) — noise-resistant analysis, accessible non-colour legend cues, and shared classification between the elevation chart and the map.
   - Milestone 4's fifth slice completes this item: `src/navigation/routeFeatures.ts`'s `detectRouteFeatures` groups the same shared analysis into named, Garmin-scored climbs and app-specific-severity descents; `RouteFeatureDetailsPanel` is the climb/descent "summary card", showing position, length, gain/loss, average and maximum gradient, and climb score, reusing the shared analysis rather than re-analysing. See Milestone 4's own fifth-slice paragraph above for full detail.

10. **Optional wake lock — done**
    - Milestone 4's tenth slice completes this item: an off-by-default **Keep screen awake** control, shown only in active Riding and only when the Screen Wake Lock API is genuinely available. See Milestone 4's own tenth-slice paragraph above for full detail.
    - Manually verified on the user's iPhone: the first real-device test worked as intended.

### Optional external-data feature

11. **Weather**
    - Candidate provider: Open-Meteo free non-commercial API, no API key.
    - Current conditions plus approximately the next three hours.
    - Temperature, precipitation, wind speed, gusts and direction.
    - Manual or restrained refresh, never a dependency of Planning/Riding.
    - Required attribution and privacy disclosure because location is sent to a weather provider.
    - Must fail independently and gracefully.

### Separate feasibility project

12. **Offline map storage**
    - Do not implement until the active tile provider explicitly permits deliberate offline prefetching.
    - Investigate route-corridor/selected-area storage, style/sprite/glyph dependencies, iOS eviction, size estimates and available-storage checks.
    - Preferred eventual architecture: global tile cache keyed by URL, per-route references, deduplication across routes, deletion only when no route references a tile.
    - Offer bounded detail presets and estimate storage before download.
    - Never bulk-prefetch from an OSMF community tile endpoint that prohibits offline download.

### Riding elevation enhancement

13. **Current-climb elevation view (Riding) — done**
    - Milestone 4's eleventh slice completes this item exactly as scoped: Climb view is offered only while the rider is inside an already-recognised climb, auto-opens once per distinct climb, respects a manual dismissal for the remainder of that climb, and reopens automatically on a different climb — see Milestone 4's own eleventh-slice paragraph above for full detail (state machine, metrics, chart fill).
    - One accepted deviation from the original brief's own open question: the heading's "announce meaningful transitions" requirement is implemented via `aria-live="polite"` rather than `role="status"`, since an explicit `role` would have overridden the heading's implicit accessible role.
    - Manual real-device phone acceptance: automatic entry into Climb view, manual exit, and manually returning to it are now manually verified on the user's iPhone (see "Manual acceptance status" above), but the live distance/elevation progress while actually cycling through a climb is still outstanding — flag this to the user before treating that part as fully verified in the field.
    - Milestone 4's thirteenth slice extends this item: the pre-ride climb selector now also shows a detailed, read-only preview of the selected climb's whole elevation profile (same colours, fill and ground-line as the active view above, no rider marker), reusing one shared pure view-model rather than a second chart implementation — see Milestone 4's own thirteenth-slice paragraph above for full detail. Manually verified on the user's iPhone across multiple climbs (colours, no GPS marker, survives a reload). A fourteenth slice changed the dropdown's own default to "All route" (see the fourteenth-slice paragraph above) after this same real-device check.

### Planning routing profiles

14. **Selectable Planning cycling profiles — done**
    - Milestone 3's fifth slice completes this item: an explicit Road bike (`cycling-road`, default)/General cycling (`cycling-regular`) profile selector, profile-aware leg caching (already supported by the existing cache-key design), draft and saved-route persistence, a stale-result indicator that gates Save/Export until a matching recalculation lands, and GPX round-tripping of the recorded profile via the existing `<acn:source>` extension. See Milestone 3's own fifth-slice paragraph above for full detail.
    - Total cycleway-distance or cycleway-percentage reporting is intentionally not planned. The user does not want this additional statistic, and a provider's cycleway classification is not equivalent to paved surface quality or road-bike suitability; the existing surface analysis remains the relevant measure for this app. Do not add this reporting unless the user explicitly changes the scope.

### Settings defaults

15. **Avoid ferries by default — done**
    - Implemented in the second Settings-visual-migration slice — see this file's own Interface and accessibility section (the `src/index.css`/Settings paragraph) for full detail: a locally persisted `Route planning` setting, backed by a new singleton `planningPreferences` table (schema v3), resolving to `avoidFerriesByDefault: true` whenever no row exists.
    - A genuinely fresh Planning draft (no draft row, or a draft row with zero waypoints — the pre-existing draft-lifecycle boundary) copies this setting into the draft's own `avoidFerries` field exactly once; a restored draft always keeps its own already-stored value, and changing the Settings default afterwards never rewrites, recalculates, or otherwise touches an already-open draft.
    - Planning's previous directly editable "Avoid ferries" checkbox is removed; Planning now shows the current draft's own stored ferry policy as concise read-only text ("Ferries: avoided/allowed for this plan") next to a "Change default in Settings" action, reusing the existing `onNavigateToSettings` path.

### Planning visual-organisation follow-ups

16. **Desktop two-column Planning layout**
    - The fourth visual-migration slice (Interface and accessibility section) deliberately kept Planning as a single, width-constrained column (`.planning-section`, max-width 720px) rather than a two-column grid, since the brief explicitly permitted either and a single column carries no DOM/tab-order risk.
    - If pursued later, use CSS Grid with explicit `grid-template-areas` placement while keeping JSX/DOM order linear (Waypoints → Route options → Route overview → Save/export) — never reorder the markup itself to achieve a visual pairing, so keyboard and reading order stay sequential regardless of visual column position.

17. **Waypoint-row marker colour parity — done**
    - `src/map/planningLayer.ts`'s new `deriveWaypointRoles(coordinates)` derives each waypoint's ordinary/start/finish/combined-start-finish role purely from position, and is now the single source both `buildWaypointMarkerSpecs` (the map marker) and `WaypointList` (via `PlanningScreen.tsx`'s new `waypointRoles` prop) are built from — never two independently-guessed `index === 0`/`index === last` checks. Unlike the map's own collapsed "1/n" combined marker for a closed loop, the list keeps both the first and final row with their own individual ordinal number, sharing only the visual role — see `deriveWaypointRoles`'s own doc comment. `MapMarkerRole` (`src/map/mapAdapter.ts`) is renamed `WaypointRole`, its single other reference site being `MapMarkerSpec.role` itself.
    - `.waypoint-row-ordinal`'s base rule and three new `--start`/`--finish`/`--start-finish` modifiers (`src/index.css`) mirror `.planning-waypoint-marker`'s own colours/border-widths/shape literally, using the same hardcoded hex values — deliberately not `--colour-accent`/`--colour-border-strong`, since the map marker's own colours never respond to `prefers-color-scheme` while those tokens do; a variable-driven badge would only match the map in light mode. The badge stays smaller than the map marker (24px vs 26px, a decorative list element, not a map marker) and omits the map's extra `--start-finish` padding, since the list badge's label is always a single ordinal number, never the map's combined "1/n".
    - The ordinal badge remains purely decorative (`aria-hidden`) and its row's own accessible name/text ("Start"/"Waypoint N") is unchanged; no routing, selection, persistence or map-marker-lifecycle behaviour changed. Automated tests (unit, component and a real-browser Playwright check comparing computed badge/marker styles at the phone viewport) pass, and the visual result is manually verified on the deployed iPhone PWA (see "Manual acceptance status" above).

### Navigation and library interface

18. **Ride screen opens from the top; library scroll position remembered for the session — done**
    - Opening a route from Routes, or saving a newly planned route from Planning, previously kept whatever document scroll offset Routes (or Planning) happened to be at, so a route opened from far down a long library — or saved after scrolling deep into a long Planning form — could land on Riding already scrolled away from its own heading. A new hook, `useResetScrollForNewRideContent` (`src/ui/shared/`), scrolls the document to `(0, 0)` with `behavior: "auto"` (never animated) the instant `App.tsx`'s `screen` becomes `"riding"` and a token it owns internally has advanced past whatever it last actually applied, bumped only by `handleOpenRoute` and `handleRouteSaved` — App's only two genuine "a new route is now the selected ride" call sites. It never fires on a plain nav-tab click back to an already-open ride: `RidingScreen` always fully unmounts/remounts on every screen switch (no `key` anywhere in `App.tsx`'s conditional screen rendering), so only `App` itself — which never unmounts — can tell "genuinely new" apart from "already open."
    - `RouteLibrary` gained one new optional prop, `restoreScrollYRef` (a plain `RefObject<number | null>`, never a dereferenced value — reading a ref's `.current` during render would both trip this project's `react-hooks/refs` lint rule and not actually work, since a ref mutation alone never triggers the re-render needed to pick up a changed prop). `App.tsx`'s `handleOpenRoute` captures `window.scrollY` into this ref synchronously, as the very first statement of the click handler, strictly before any state setter — the only point guaranteed to run before React unmounts the library's tall content (a browser's own scroll clamping only ever bounds `scrollY` to the new, shorter document's max, never zeroes it, so an explicit reset is required regardless). `RouteLibrary` restores it, via `window.scrollTo`, inside a `useLayoutEffect` gated on real route cards having rendered (never the "Loading routes…" placeholder), applies it at most once per mount, and always nulls the ref out afterwards, so neither a later reactive `useLiveQuery` update from the same mount (rename/delete) nor an unrelated later remount (e.g. Routes → Diagnostics → Routes with no intervening route-open) ever reapplies a stale offset. Both refs live only in `App`'s own in-memory state — nothing is persisted to IndexedDB or any storage, so a full reload always starts Routes at the top, and this has no effect on the restored-ride map camera contract, GPS state, progress restoration, or the Resume-riding route framing.
    - Automated coverage: `App.test.tsx`, `src/ui/library/RouteLibrary.test.tsx`, a new `src/ui/shared/useResetScrollForNewRideContent.test.ts` (needed because `App.test.tsx` cannot mount a real `PlanningScreen` in jsdom — see that file's own "Deliberately excludes 'Plan'" comment — so the Planning-save leg of this contract is proven at the unit level), plus a new `e2e/routeLibraryScroll.spec.ts` and one new assertion added to `e2e/planning.spec.ts`'s existing save-and-reopen test. `App` also gained a new optional `mapFactory` prop, forwarded only to `RidingScreen`, purely so these tests can inject a stub map and avoid mounting a real, unmocked MapView in jsdom — production's `main.tsx` passes no props and is unaffected. Manually verified on the deployed iPhone PWA (see "Manual acceptance status" above).

19. **Route library search and sort — done**
    - `RouteLibrary` gained a session-only name search (a labelled `<input type="search">`, "Search routes") and a persistent sort control (a labelled `<select>`, "Sort by": `Most recent`/`Name A–Z`). Search matches a trimmed, case- and diacritic-insensitive substring anywhere in a route's name only — never fuzzy, never tags/folders/route-content — via Unicode NFD normalisation (e.g. a query of "hutte" matches "Hütte"). Both are computed by a new pure `src/ui/library/routeLibraryView.ts` (`normalizeSearchText`/`filterRoutesByName`/`sortRoutesForLibrary`/`selectRouteLibraryView`), applied to whatever `listRoutes()`'s own liveQuery returns, never assumed to already match — the pipeline is always normalise query → filter → sort → render, and neither function mutates its input array. `Most recent` preserves `PlannedRoute.createdAt` descending exactly as before (opening/exporting/renaming a route never makes it "recent"; no `updatedAt`/last-opened field was added); `Name A–Z` uses a module-level `Intl.Collator("en-GB", { sensitivity: "base", numeric: true })` (a pinned locale, matching `providerKeyStatus.ts`'s existing `DATE_TIME_FORMATTER` convention, so ordering is deterministic across machines) so e.g. "Route 2" sorts before "Route 10". Both orders are tie-broken deterministically by route id.
    - Sort choice is the first Route-library-specific durable preference: a new singleton `routeLibraryPreferences` table (`id: "route-library"`, schema `version(4)`, purely additive like v1→v2→v3, no `.upgrade()` needed) holds a plain `sortOrder` string; `src/storage/mapping.ts`'s `RouteLibrarySortOrder`/`isRouteLibrarySortOrder`/`fromStoredRouteLibraryPreferences` guard-validate it on read (mirroring `fromStoredPlanningDraft`'s profile guard, not `fromStoredPlanningPreferences`'s bare `??`, since this is a closed enum) and default an absent or corrupt value to `most-recent`. `src/storage/routeLibraryPreferencesRepository.ts` mirrors `planningPreferencesRepository.ts` exactly. `RouteLibrary` waits for both the routes liveQuery and this preference to resolve before rendering any list or the toolbar, avoiding a "Most recent, then jump to Name A–Z" flash; the `<select>`'s value is always bound directly to the live-query-resolved preference, never optimistic local state, so a failed or in-flight save can never look like it succeeded — on failure it keeps showing the last-persisted value alongside a `role="alert"` message, with a `role="status"` "Saving…" note only while a write is genuinely in flight.
    - The search query is deliberately not persisted to IndexedDB — it's transient view state that may clear on a full reload/relaunch. Within the current app session it survives navigating away from Routes to any other screen and back, via a new `routesSearchQueryRef` owned by `App.tsx` (a `RefObject<string>`, continuously synced on every keystroke rather than captured once like `restoreScrollYRef`, since — unlike the Riding-open path — there is no single "about to navigate away from Routes" call site covering every destination; `MainNavigation`'s `onNavigate` goes straight to `setScreen`). `RouteLibrary` hydrates its local `searchQuery` state from this ref exactly once per mount via a `useLayoutEffect` with an empty-ish dependency array (never a lazy `useState` initializer, for the same `react-hooks/refs` reason `restoreScrollYRef` documents), and never nulls the ref afterwards.
    - The existing scroll-restoration effect and `computeFocusRouteIdAfterDelete` (`src/ui/library/routeDeleteFocus.ts`, itself unchanged — it was already order-agnostic) now both operate on the filtered/sorted array actually on screen rather than the raw liveQuery result. Deleting the last visible route under an active filter focuses the search field rather than the Routes heading. A rename that causes the renamed route to drop out of the active filtered view (and so unmount its own row, stranding any focus inside it) is handled by a small, explicitly-scoped effect — tracking the just-renamed id and comparing the previous rendered view to the new one — rather than a generic "was focus just here" check, since by the time any effect can observe a removed row the browser has already moved focus to `<body>`, making rename-caused and delete-caused disappearance indistinguishable at that point; a `stillExists` guard defers to the delete flow's own focus handling if the same route was deleted before the rename's write round-tripped. Reordering under `Name A–Z` needs no equivalent code: `RouteListItem` stays keyed by `route.id`, so React's key-based reconciliation moves the existing DOM node (and any focus within it) rather than unmounting it.
    - No fuzzy search, tags, folders, route-content search, remote search, analytics or new dependency; `Intl.Collator`/Unicode `normalize("NFD")` are Web-platform-native. No change to import, export, inline rename/delete, route selection or GPX/Riding behaviour. Automated tests (storage mapping/repository/migration, pure view-logic unit tests, `RouteLibrary`/`App` component tests, and a phone-viewport `e2e/routeLibrarySearchSort.spec.ts`) pass; real-device iPhone verification of search, reload persistence, narrow-viewport layout and returning from a searched/sorted route is still outstanding — do not treat this item as manually verified until that check has actually been done.

20. **Route pinning — done**
    - Persistent Route Library pinning, exactly as recorded for continuity above: pinned routes form one group, `Pinned`, always shown above unpinned routes; within `Pinned` the most recently pinned route is always first, ordered purely by pin recency; the existing persisted `Most recent`/`Name A–Z` preference applies only within the unpinned group (`Other routes`) and never reorders `Pinned`. `PlannedRoute` gained one new optional field, `pinnedAt?: string | null` (`src/domain/types.ts`) — an ISO timestamp when pinned, `null`/absent when not, documented as local Route Library metadata rather than route geometry or provenance, never read by `gpx/`, `routing/` or `navigation/`. Because it is optional and never indexed, it needed no Dexie schema version bump — the exact same precedent already established for `manoeuvreProvenance`/`surfaceSummary` on `PlannedRoute` and the camera/wake-lock fields on `StoredRideState`: Dexie's `version().stores()` only lists indexes, so a plain new field on an existing row is handled entirely by explicit defaulting in application code, not a schema migration.
    - `src/storage/routesRepository.ts` gained `pinRoute(id, clock: Clock = systemClock)` (writes `pinnedAt: new Date(clock.now()).toISOString()`) and `unpinRoute(id)` (writes `pinnedAt: null`, not `undefined`, the correct member of the `string | null` union for Dexie's `update()`), mirroring `renameRoute`'s own `db.routes.update(id, {...})` shape and `src/gpx/importGpx.ts`'s existing `(file, clock: Clock = systemClock)` convention so pin timestamps are testable without `vi.setSystemTime`. Deleting a route naturally deletes its pin metadata with it — there is no separate pin table to leave an orphan row in. GPX export/import were confirmed structurally incapable of touching `pinnedAt` (`exportGpx.ts`/`normalizeGpx.ts` both name every field explicitly, with no `{...route}` spread anywhere in either file) and needed no code change, only regression tests: a pinned route exports with no pin metadata in the GPX, and any reimported route — including a re-export/reimport of a previously pinned one — is unpinned. Pin state is specific to this device's local library, exactly like sort order and search.
    - `src/ui/library/routeLibraryView.ts` gained `isPinnedRoute` (a route counts as pinned only when `pinnedAt` is a string that parses to a finite timestamp — missing, `null`, or malformed local data is treated as unpinned rather than destabilising ordering) and `selectRouteLibraryGroups(routes, query, sortOrder): { pinned, unpinned }`, superseding the previous flat `selectRouteLibraryView`: normalise the query → filter by name (the existing, unchanged `filterRoutesByName`) → partition into pinned/unpinned by `isPinnedRoute` → sort pinned by `pinnedAt` descending (id-tiebroken, never consulting `sortOrder` at all, so a sort-order change can never touch `Pinned` even in principle) → sort unpinned via the existing, unchanged `sortRoutesForLibrary`. The partition is exhaustive and disjoint by construction (a plain filter/predicate split), proven directly by a dedicated unit test rather than left implicit.
    - `RouteLibrary.tsx` derives `groups = selectRouteLibraryGroups(...)` and a flattened `viewRoutes = [...groups.pinned, ...groups.unpinned]` kept feeding exactly what it already fed before this slice — `computeFocusRouteIdAfterDelete`, the rename-orphan-focus effect, and scroll-restoration gating — so deletion focus continues to use the full displayed pinned-then-unpinned order with no change to `routeDeleteFocus.ts` itself. Rendering splits into up to two `<h2>`-headed `<ul className="route-list">` groups — `Pinned` shown only when non-empty, `Other routes` shown only when both groups are non-empty — so with zero pinned routes the DOM is byte-for-byte identical to before this slice. A new optional `clock?: Clock` prop (default `systemClock`), mirroring `PlanningScreen`'s own existing convention, lets tests assert deterministic newest-pinned-first ordering without depending on real clicks landing in different milliseconds.
    - Pin/unpin writes are never optimistic: because `routes` is a Dexie live query, a card only ever visually moves once the write has actually committed and the query re-fires, so "never move a card before the write succeeds" and "keep the previous group/order on a failed write" both fall out of the existing architecture with no extra bookkeeping — a rejected write simply never touches `routes`. A per-route `pinPendingIds` set disables that route's own toggle and blocks a duplicate submission while its write is in flight; a per-route `pinErrors` map shows a concise `role="alert"` message on failure. Because pinned and unpinned render as two separate `.map()`-produced arrays under two separate `<ul>` parents, a route moving between them is an unmount-then-mount pair even though its `key` is unchanged — React does not carry a component's DOM identity, and therefore focus, across that move the way it would for a plain reorder within one array. A `pendingPinFocusIdRef` set on a successful write, consumed by an effect keyed on `groups`, explicitly refocuses the route's own toggle at its new position (an accepted, narrow race — pinning a second route before the first one's effect has fired overwrites the pending target — documented the same way as the pre-existing `lastRenamedIdRef` rename-orphan-focus race, and equally sub-perceptible in practice).
    - `RouteListItem.tsx` gained one compact, icon-only pin toggle in the title row's upper-right, via a new `.route-card-title-row` (`display: grid; grid-template-columns: minmax(0, 1fr) auto`, plus `min-width: 0` added to the existing `.route-card-title` rule) so a long/wrapping route name never collides with or displaces the fixed-size toggle. A new project-owned, dependency-free `src/ui/library/PinIcon.tsx` (mirroring `ManoeuvreIcon.tsx`'s exact convention — `aria-hidden`, all styling inline since Vitest's `test: { css: false }` never loads `index.css`) renders an outline pushpin when unpinned and a filled one when pinned, never rotated; `.route-pin-toggle`/`.route-pin-toggle.is-pinned` add only the real CSS deltas on top of the global `button` rule's own inherited 44px touch target, mirroring `.waypoint-row-icon-button`'s override-only-the-delta convention — the pinned state additionally gets a pale `--colour-accent-soft` surface and a stronger accent ring, mirroring `.main-nav-button[aria-current="page"]`'s soft-surface-plus-ring convention, so pinned state is never colour alone. `aria-pressed`/a full route-scoped accessible label (`Pin Evening loop`/`Unpin Evening loop`) carry the state explicitly; no `Pin`/`Unpin` text is ever shown inside the button itself. The toggle is deliberately **not** disabled merely because that row's delete confirmation is open (only while a pin write is genuinely in flight, or while deleting) — clicking it while a confirmation is open cancels that confirmation first via a local `handlePinClick`, mirroring exactly where the equivalent Rename-cancels-delete logic already lived in this same file (`openRename`), rather than lifting that per-row concern into `RouteLibrary`. The toggle is unavailable during inline rename with no extra logic at all, since it lives inside the same "not renaming" fragment as the rest of the card's normal-state markup. The per-card delete-confirmation heading was downgraded from `h2` to `h3` (and its CSS selector updated to match) so it sits one level beneath the Route Library's own new `Pinned`/`Other routes` `h2` group headings rather than as a same-level sibling.
    - No drag-and-drop or manual ordering, folders, tags, favourites separate from pins, cloud synchronisation, route duplication or new search modes were added, matching this item's own original scope note. Automated tests (pure grouping/partition unit tests including an explicit disjointness check, repository tests for `pinRoute`/`unpinRoute` with a literal fixed `Clock`, a migration-test assertion that a legacy route survives with `pinnedAt` absent, GPX export/reimport regression tests, `RouteListItem`/`RouteLibrary` component tests covering the toggle's states/labels/focus/error/duplicate-click/delete-cancel-first behaviour, and a phone-viewport `e2e/routeLibraryPinning.spec.ts`) pass; real-device iPhone verification of pin order, reload persistence, search/sort interaction, long route names, touch targets and returning from an opened pinned route is still outstanding — do not treat this item as manually verified until that check has actually been done.

21. **`e2e/planning.spec.ts`'s "pressing Northwards twice" test's manual-rotation precondition — done**
    - Following the v0.3.10 Route Library search/sort push, a GitHub Actions run of this desktop-viewport test failed at its own precondition — `data-camera-bearing` never left `"0"` after the synthetic right-button drag, so the second Northwards press was never actually exercised — the exact `moveend`/`rotateend`-never-fires signature already documented for the fractional-height MapLibre drag-rotate bug elsewhere in this file. A same-commit re-run of the identical job passed, and nothing in that push touched Planning, its map, or camera code (confirmed by diff inspection before the re-run), so this is treated as a further occurrence of that same known, narrow, CI-environment-sensitive MapLibre quirk — not a Northwards regression — and no production camera-logic change was made on the strength of a single failure.
    - Hardened in both the desktop test and its phone-viewport twin (`e2e/planning.spec.ts`, "pressing Northwards twice on a phone viewport…"): the synthetic right-button pointer drag through MapLibre's `DragRotateHandler` — the genuinely non-deterministic step, since a drag/inertia gesture can in principle finish without the handler ever firing `moveend`/`rotateend` — is replaced by MapLibre's own built-in `KeyboardHandler` (confirmed by reading the installed v6.0.0 source directly: `keyboard.ts`, `handler_manager.ts`, `camera.ts`, `map.ts`). Both tests now focus the actual `<canvas class="maplibregl-canvas">` inside `[data-testid="map-container"]` — MapLibre sets `tabindex="0"` on the canvas itself, not the container div (`map.ts`'s `_setupContainer`) — and send `page.keyboard.press("Shift+ArrowRight")` then, once that settles, `"Shift+ArrowUp"`: each is one discrete trusted keydown+keyup that `KeyboardHandler.keydown()` turns into a single fixed +15° bearing or +10° pitch `map.easeTo()` call through MapLibre's own ordinary camera-animation pipeline (`_prepareEase` → `movestart`/`rotatestart` → per-frame `move`/`rotate` → `_afterEase` → `rotateend` → `moveend`) — the exact pipeline a real drag, or this app's own North-up reset (`mapAdapter.ts`'s `setCamera`), already uses, so `MapView.tsx`'s `onCameraSettled`-driven `data-camera-*` attributes are republished through the real production code path, never set directly. Because there is no drag/inertia state machine in this path at all, the mechanism cannot reproduce the stuck-gesture failure mode by construction, not merely retry past it faster; the two key presses are sent sequentially, only once the first has fully settled, since both share MapLibre's fixed `easeId: "keyboardHandler"` and an in-flight same-id ease would otherwise suppress the earlier one's own end events. Both tests also gained an explicit `aria-pressed` assertion of the North-up control around the rotation and each press — real-browser coverage of the same contract `PlanningScreen.test.tsx`'s "a manual rotation unpresses the control" already proves at the mock level — and the desktop test alone gained new coverage this test never had before: a numeric-tolerance check (matching the existing "Locate me recentres…" test's own `CENTRE_CHANGE_TOLERANCE_DEGREES`-style pattern) that centre and zoom, captured once settled after the first press, are still preserved after the second, proving `setCamera(null, null, 0, 0, …)`'s contract across both presses at runtime rather than only by reading the source. Independent real pointer-drag-rotate coverage of MapLibre's `DragRotateHandler` itself remains intact and untouched: `e2e/directionArrows.spec.ts`'s "arrows remain attached and correctly oriented after the map is rotated" test still performs a real right-button drag and asserts, via decoded canvas pixel angle detection, that the rendered route line's on-screen angle genuinely changed — this is test hardening only, since no production code changed at all (`mapAdapter.ts`/`MapView.tsx`/`PlanningScreen.tsx` are untouched, and the `requestId`-dedup fix itself already has full unit coverage in `MapView.test.tsx`'s "Northwards-pressed-twice regression" test and `PlanningScreen.test.tsx`'s north-up `aria-pressed` suite) — this slice only changes how the e2e test establishes its precondition. Verified via `npx playwright test e2e/planning.spec.ts --grep "pressing Northwards twice" --repeat-each=20 --workers=1`, both locally (40/40 passed) and again with `CI=1` set, which forces a fresh preview-server boot each run (40/40 passed), plus a full `npm run e2e` run (62/62 passed, including the independent `directionArrows.spec.ts` drag-rotate test). This is not a claim that every MapLibre pointer gesture is universally deterministic in CI generally — only that this specific regression test no longer depends on one.

### Planning waypoint interaction

22. **Deselect a waypoint on a repeat tap — done**
    - Amends the existing waypoint-selection and relocation behaviour rather than introducing a new movement architecture: placing, dragging, moving, inserting, reordering and deleting waypoints (see "## Planning behaviour" above and Future-backlog item 17's `WaypointList` work) are already implemented and unchanged by this item. Before this item, `waypointHistory.ts`'s `"select"` action always set `selectedWaypointId` to the tapped waypoint, with no toggle-off case.
    - In the normal selected state, tapping the already-selected waypoint again deselects it (`selectedWaypointId` returns to `null`). Selecting a different waypoint continues to transfer selection to that waypoint, exactly as today.
    - Once an active relocation (Move/Insert-after) operation has begun, tapping the waypoint again must not silently cancel or commit the move — the explicit Cancel action remains the only way to cancel a relocation in progress.
    - Preserve the existing route recalculation, undo/redo and stale-result (`isStale`/`canSaveOrExportPlan`) protections unchanged.
    - Implemented as a single-call-site change: `PlanningScreen.tsx`'s `onSelect` handler passed to `WaypointList` now dispatches `{ type: "select", waypointId: null }` instead of the tapped id when the tapped waypoint is already `state.selectedWaypointId` **and** no Move/Insert-after relocation is currently pending for it (`effectivePendingAction === null`) — otherwise it dispatches the tapped id unchanged, exactly as before. `waypointHistory.ts`'s `"select"` reducer case already accepted a `null` id, already excluded selection from undo/redo, and `usePlanningRoute` already never read `selectedWaypointId`, so no reducer, staleness, `WaypointList`, `MapView` or map-marker code changed — list-row `aria-pressed`/`.is-selected` and the map marker's `--selected` ring already reacted correctly to a cleared selection via existing plumbing. Real-device iPhone verification is still outstanding — do not treat this item as manually verified until that check has actually been done.

### Map zoom-dependent route and waypoint presentation

23. **Low-zoom route-colour and waypoint-marker legibility — done**
    - Confirmed visual bug, evidenced on the user's phone: at low zoom, the geographically drawn route in Planning, the pre-ride overview and active Riding can appear predominantly blue even though the underlying recognised climb/descent classification (`src/navigation/routeFeatures.ts`, `src/map/routeFeatureLayer.ts`, `src/map/gradientRouteLayer.ts`) has not changed. The elevation chart is unaffected by map zoom and is out of scope for this item. Planning is additionally obscured by the existing fixed-size numbered waypoint markers (Future-backlog item 17) once many waypoints are visible together.
    - Required outcome: preserve the existing climb/descent detection, category boundaries and colour meanings exactly as they are; make the map's route presentation responsive to zoom so a full-route overview stays legible and no single colour/layer visually overwhelms nearby route sections purely because the view has been zoomed out; restore the existing close-range widths as the rider zooms in so active Riding stays as easy to read as it is today; apply the resulting styling consistently across Planning, the pre-ride overview and active Riding.
    - Implementation direction to validate, not yet a proven fix: investigate the existing MapLibre sources, layers, draw order, and line caps/joins before changing anything; the likely direction is a progressively thinner route at lower zoom, with a thin neutral route casing/base beneath the coloured climb/descent/gradient/warning overlays, the overlays themselves staying narrower at overview zoom than at close range. Do not record a fixed line width as an already-proven sole root cause. Separately verify that layer draw order does not systematically paint descent segments over climb segments where the route geographically overlaps itself.
    - For Planning's waypoint markers: reduce their _visual_ size at low zoom while keeping at least a 44 × 44 px effective touch target; keep the start marker, the selected waypoint and waypoint ordinal numbers individually identifiable; do not cluster waypoints or discard their individual identities.
    - Add focused unit/integration coverage of the zoom-derived presentation logic, plus visual/e2e coverage at representative close, regional and full-route zoom levels, proving the underlying route data and classifications are unchanged — only their presentation is.
    - **Evidence-backed root cause**, confirmed by direct source inspection: `src/map/mapAdapter.ts`'s `LineLayerPaint.lineWidth`/`CircleLayerPaint.circleRadius` were plain numbers, and MapLibre renders `line-width` in fixed _screen_ pixels regardless of zoom — no zoom expression existed anywhere in the codebase, so the same pixel width covers ever more geographic distance as the map zooms out, and nearby/parallel/self-crossing route sections visually merge. This was compounded, not caused, by a second fact: `MapView.tsx`'s `addRouteAndPositionLayers` gave the neutral base route (`acn-route-remaining-line`/`acn-route-completed-line`, width 5) and the always-on macro climb/descent overlay (`acn-route-feature-line`, width 5, populated for _every_ recognised climb/descent along the whole route, not just a selection) the exact same width at every zoom, so the coloured overlay always fully covered the neutral base with no visible casing — a route with a long recognised descent therefore read as solid blue regardless of zoom. Separately verified, via a new geographic self-overlap fixture in `routeFeatureLayer.test.ts` (an out-and-back where a climb and a coincident, reversed-direction descent occupy identical geometry): `detectRouteFeatures` (`routeFeatures.ts`) pushes features in plain route-distance order with no kind-based sort, and `buildRouteFeatureFeatureCollection` preserves that order into the one shared GeoJSON layer — there is **no** category-based "descents always drawn over climbs" bug; whichever feature is later by route-distance simply happens to be later in paint order, which is a property of route shape, not category. No feature-ordering change was made.
    - **Route-width policy**: a new pure, MapLibre-free module, `src/map/routeWidthPolicy.ts`, exports three shared zoom bands (`ROUTE_WIDTH_OVERVIEW_ZOOM = 6`, `ROUTE_WIDTH_REGIONAL_ZOOM = 11`, `ROUTE_WIDTH_CLOSE_ZOOM = 15` — the last deliberately equal to `distanceBadgeLayer.ts`'s own `DISTANCE_BADGE_STREET_ZOOM_MIN`, one shared "close navigation" boundary) and two pure functions, `recedingWidthStops(closeWidthPx)`/`legibleWidthStops(closeWidthPx)`, each building a `{ stops: [{zoom, width}, …] }` value from a layer's own unchanged close-zoom width via one multiplier family (recede-faster for the always-on macro/micro colour overlays; recede-gentler for the neutral base/casing, selection halos and warning casings). Both families resolve to exactly the input width at `ROUTE_WIDTH_CLOSE_ZOOM` — today's appearance is unchanged at close zoom, by construction — and the gentler "legible" family stays strictly wider than the "receding" family below that, so the neutral base begins to peek through as a visible casing ring around the narrower coloured overlay once zoomed out, without adding a second route-colour pipeline or a new layer. These zoom bands and multipliers are first-pass, reasoned starting points, explicitly **not yet verified on a real iPhone viewport** — mirroring `distanceBadgeLayer.ts`'s own documented humility for its zoom bands.
    - **MapLibre plumbing**: `mapAdapter.ts` gained `ZoomWidthStop`/`ZoomInterpolatedLineWidth` types (mirroring `DataDrivenLineColor`'s existing narrow, project-owned-abstraction convention rather than exposing a raw MapLibre expression), widening `LineLayerPaint.lineWidth` to `number | ZoomInterpolatedLineWidth`; `addLineLayer` translates a non-number width into a native `["interpolate", ["linear"], ["zoom"], …]` expression at the same single-field-cast site its existing `lineColor` handling already uses. `CircleLayerPaint.circleRadius` (position/start/finish circles) was deliberately left untouched — out of this item's own scope. Because MapLibre itself evaluates the expression per render frame, `addLineLayer` is still called exactly once per layer at style-load — **no new React state or effect drives route width at all**, satisfying the "must not cause a React state update on every zoom frame" constraint by construction. `MapView.tsx`'s `addRouteAndPositionLayers` now wraps every route/warning/preview layer's width in the appropriate policy call, with its existing "nested-ring" draw-order comment left unchanged (draw order still governs which layer wins where they overlap; width now separately governs how visible that relationship is at a given zoom) plus one added paragraph recording this. `addRouteAndPositionLayers` has no Planning/Riding mode branch and none was added, so the two screens resolve identical widths for the same zoom by construction, proven by a dedicated regression test rather than left implicit.
    - **Waypoint-marker zoom scaling**: implemented with zero changes to `MapMarkerSpec`, `setMarkers`, or `waypointMarkerElement.ts` (so the tested "never `element.className =`" hazard in that file is never touched). The map's own container div (`data-testid="map-container"`, an ancestor of every MapLibre `Marker` DOM node) gained a stable `map-canvas-host` class and a new `data-marker-zoom-band` attribute (`"close" | "regional" | "overview"`), populated from the existing settle-quantised `distanceBadgeZoom` state (reused unrenamed, not duplicated into a second zoom-tracking state) via a new pure `deriveMarkerZoomBand(zoom)` in `planningLayer.ts` (mirroring `distanceBadgeLayer.ts`'s own `zoomToBandIndex` band-lookup shape; a non-finite zoom, i.e. before the first camera settle, resolves to `"close"`, matching the marker's current unchanged full-size appearance). New CSS-only descendant rules in `src/index.css` (`.map-canvas-host[data-marker-zoom-band="…"] .planning-waypoint-marker`) cascade the size reduction (~26px close → ~20px regional → ~16px overview, plus a narrower selection ring at the smallest band) to every marker with zero per-marker JS work and no marker rebuild — `setMarkers`'s own effect is untouched and never re-runs on a zoom-only camera settle, proven by a dedicated test.
    - **Touch-target decision** (confirmed with the user before implementation): `.planning-waypoint-marker` was, and remains, `pointer-events: none` — it is not, and has never been, an interactive tap target; waypoint selection is exclusively `WaypointList`'s own rows, which already satisfy the app's global `min-width/height: var(--touch-target-min)` (44px) at every zoom, unaffected by any of this. Rather than restructuring the marker's DOM into a fixed 44×44 invisible hit-box around a smaller visual glyph (which would have no functional effect today, since the element still wouldn't be clickable), this slice does a **visual-only CSS shrink** and documents, in both `index.css`'s own comment and here, that `WaypointList` remains the sole ≥44×44px waypoint-selection surface at every zoom — satisfying the requirement's substance without an interaction-model change. Selection, deselection-on-repeat-tap (item 22) and active-relocation behaviour are all unaffected; this was proven, not merely asserted, by e2e coverage that selects then deselects a waypoint at overview zoom.
    - **Tests**: new `src/map/routeWidthPolicy.test.ts` (monotonicity, close-zoom-unchanged, casing-wider-than-overlay); new cases in `mapAdapter.test.ts` (the generated `interpolate` expression shape) and `MapView.test.tsx` (every layer's real policy-derived paint, Planning/Riding parity, zoom-invariant classified-feature source data, a climb+descent fixture retaining both classes at overview zoom, the `data-marker-zoom-band` transition, no marker rebuild on a zoom-only settle, and fallback-style regression); new `deriveMarkerZoomBand` boundary tests in `planningLayer.test.ts`; the out-and-back self-overlap fixture in `routeFeatureLayer.test.ts`. A new e2e spec, `e2e/lowZoomLegibility.spec.ts`, uses MapLibre's own real, deterministic `KeyboardHandler` (`Shift+=`/`Shift+-`, the same class of mechanism `planning.spec.ts`'s "pressing Northwards twice" test already uses for bearing/pitch, chosen for the same reason — no drag/inertia state machine to reproduce CI's documented `DragRotateHandler` flakiness) rather than a synthetic pointer/wheel gesture, and proves: a dense ~14-waypoint Planning fixture's markers visibly shrink at overview zoom and restore exactly at close zoom, while staying individually identifiable (start/ordinary/selected) and the `WaypointList` touch target stays ≥44×44px throughout; the same holds under the local fallback style; and a calculated route keeps rendering (`data-route-loaded` stays `"true"`, the canvas stays visible) across a full zoom-out-and-back cycle in both Planning and the saved route's pre-ride Riding overview. This exposed one real, narrow regression in the _pre-existing_ `planning.spec.ts` "waypoint list badges visually match…" test (item 17): its implicit assumption that a badge and its map marker match at _any_ zoom no longer holds, since the list badge is deliberately not zoom-responsive while the map marker's size now is — fixed by a small local `zoomToCloseRange` helper (mirroring the new spec's own `zoomToBand`, duplicated per this file's established no-shared-e2e-helpers-across-specs convention) that deterministically zooms to the close band before that test's own style comparisons, preserving its original claim exactly rather than loosening it. Real-device iPhone verification is still outstanding — do not treat this item as manually verified until that check has actually been done.

### Navigation and library interface

24. **Always-sticky compact primary navigation, except during active Riding — done**
    - `MainNavigation` (Future-backlog item 6) is sticky on every screen — Routes, Planning, Diagnostics, Settings, the pre-ride briefing and paused/awaiting-Resume riding — except while a ride is genuinely being GPS-tracked, where it stays in normal document flow to maximise dashboard space; it never auto-hides. The state predicate is `nav.geolocationStatus !== "idle"` (`useRideNavigation.ts`) — the app's own authoritative ride-tracking state, never inferred from button text, GPS freshness, route presence or CSS, and identical to the predicate `RidingScreen.tsx` already used for the map's active/overview class, the wake-lock gate and the next-manoeuvre panel. A transient GPS error mid-ride (`"error"`) deliberately still counts as active, since the underlying watch is never torn down for it, matching this file's own existing "Try again preserves progress" philosophy.
    - A new pure module, `src/ui/shared/navPositionMode.ts`, exports `NavPositionMode` (`"sticky" | "static"`) and `deriveNavPositionMode(screen, isRidingActive): NavPositionMode` — returning `"static"` only for `screen === "riding" && isRidingActive`. `App.tsx` computes `positionMode` from this and applies it as a state-dependent modifier class directly to its own `<header>` element (`className={positionMode === "sticky" ? "app-header--sticky" : undefined}`) — `MainNavigation`/`<nav>` itself carries no positioning class and has no opinion about sticky/static at all.
    - **A real, confirmed field bug, found via the deployed iPhone PWA at build 0.3.14**: the first implementation of this item put `position: sticky` directly on `.main-nav` (`<nav>`), nested inside a bare `<header>` that only ever wraps that one nav and so is barely taller than it. A sticky element's stuck range is bounded by its own containing block — with `<header>` as `<nav>`'s containing block and almost no height beyond the nav itself, the nav had essentially no room to remain stuck before scrolling away with that too-short header regardless of the sticky declaration being otherwise correct. Real iOS Safari confirmed the nav was not sticky on Diagnostics (and everywhere else). This was root-caused precisely (not merely worked around) by moving the sticky declaration one level up, from `.main-nav` to `<header>` itself: `<header>`'s own containing block is `.app-shell`, which spans the full page height, giving it the room a sticky element actually needs. `App.tsx`'s `<header>` now carries the `positionMode`-derived `app-header--sticky` class described above; `MainNavigation`/`<nav>` reverted to a plain, unconditional `main-nav` class with no `positionMode` prop at all. An earlier, incorrect diagnosis briefly attributed the automated test failures this bug produced to a headless-Chromium/root-scroller rendering limitation rather than this real containing-block defect — that diagnosis was wrong and has been removed from both this file and `e2e/stickyNavigation.spec.ts`; genuine bounding-box scroll geometry against the real app renders correctly here once the sticky declaration sits on the correct element.
    - `RidingScreen` gained a new optional `onRidingActiveChange?: (active: boolean) => void` prop, synced by a small effect keyed on `[nav.geolocationStatus, onRidingActiveChange]` that calls it on every genuine `idle ⇄ watching ⇄ error` transition, and again via its own cleanup on unmount — the latter is what resets `App`'s copy to `false` the instant the rider navigates away from Riding entirely, since this screen always fully unmounts on every screen switch (no `key` anywhere in `App.tsx`'s conditional rendering), needing no separate reset logic. This is the first instance in this codebase of a screen reporting state back up to `App` via a callback prop — mirrors the existing `onOpenRoute`/`onRouteSaved`/`onNavigateToSettings` convention rather than introducing React context, which remains unused anywhere in `src/`. `App.tsx` wires it with a bare `onRidingActiveChange={setIsRidingActive}`, mirroring its own existing `onNavigate={setScreen}` convention of passing a raw `useState` setter directly.
    - `.app-header--sticky` (`src/index.css`, directly after the existing `header` padding rule): `position: sticky`, `top: env(safe-area-inset-top, 0px)` (the exact same safe-area idiom `.app-shell`'s own top padding already establishes, so the resting and stuck positions agree exactly and the transition is continuous by construction — no jump); `z-index: 10` (comfortably above every other z-index in this file, none of which exceeds 5, and none of which ever overlaps the header's own screen region under normal use); an opaque `background: var(--colour-bg)` (`header` had no background before, needed so scrolling content doesn't show through once pinned); and `border-bottom: 1px solid var(--colour-border)` (the same token `.main-nav`'s own `border-top` already uses) to separate the pinned bar from content beneath it. No `@supports` gate needed for `position: sticky` itself, unlike this file's `dvh`/`round()` fallbacks elsewhere. `header`'s own top padding/wake-lock-popover dead zone, `.main-nav`, `.main-nav-button`, and `.main-nav-button[aria-current="page"]` are all unchanged.
    - Automated e2e coverage (`e2e/stickyNavigation.spec.ts`) asserts genuine bounding-box geometry against the real, running app for Routes, Diagnostics and the pre-ride/Resume screen (scrolled `<header>` stays within ~2px of its unscrolled position), plus real geometry proving active Riding's `<header>` scrolls fully out of view and returns once scrolled back to the top, and computed-style checks that every other top-level screen renders `<header>` sticky and that navigating away from an active ride restores it. `App.test.tsx`/`MainNavigation.test.tsx`/`navPositionMode.test.ts`/`RidingScreen.test.tsx` cover the state-derivation and callback contract at the unit level; `App.test.tsx` asserts the sticky class on the `<header>` ancestor of the rendered `<nav>`, since `MainNavigation` itself no longer carries any positioning class.
    - Files changed: `src/ui/shared/MainNavigation.tsx`, `src/ui/shared/navPositionMode.ts`, `src/ui/riding/RidingScreen.tsx`, `src/App.tsx`, `src/index.css`; tests: `src/ui/shared/MainNavigation.test.tsx`, `src/ui/shared/navPositionMode.test.ts`, `src/ui/riding/RidingScreen.test.tsx`, `src/App.test.tsx`, `e2e/stickyNavigation.spec.ts`.
    - Real-device iPhone verification of the corrected, `<header>`-based fix has confirmed that navigation remains sticky in Planning while scrolling. Routes, Diagnostics, Settings, the pre-ride briefing, paused/Resume Riding, and the active-Riding scroll-away/return transition have not yet been separately confirmed on a real device — do not treat any of those as manually verified until each has actually been checked. Android Chrome verification is outstanding entirely.

### Navigation and library interface

28. **Optional adaptive compact navigation while scrolling**
    - A possible later visual refinement to the always-sticky primary navigation delivered in item 24 above. The user is currently unsure whether they want this at all — it is a candidate direction only, not approved or scheduled work, and must not be started without explicit confirmation.
    - Motivation: the full sticky icon-and-label row occupies noticeable vertical space on long screens such as Planning.
    - Candidate behaviour, all subject to later confirmation and none of it settled: at the top of a sticky screen, show the current full icons-and-labels navigation; after a meaningful downward scroll, collapse it into a shorter icon-only sticky row; after a meaningful upward scroll, expand it again before the user reaches the top; at the top of the page, always use the expanded presentation; retain accessible names for every destination while text labels are visually hidden; preserve the current non-sticky behaviour during active Riding rather than adding any compact/sticky scroll handling there.
    - Any implementation would need scroll-direction state plus hysteresis/thresholds so small touch movements and scroll bounce do not cause rapid toggling; would need to expand when keyboard focus enters the navigation; would need to respect `prefers-reduced-motion` if a size transition is used; and must not create a second navigation element, change current-page semantics, or reduce effective touch targets below the existing minimum.
    - Decisions that remain open and must be settled with the user before any implementation prompt is written, not invented here: whether to build this at all; which sticky screens should use it, or whether all sticky screens should behave consistently; the downward-collapse and upward-expand thresholds; whether any upward movement expands it immediately or a separate upward hysteresis distance is required; the compact bar's exact height, icon size, padding and visual separation from scrolling content; whether expansion should also occur on navigation focus, destination change or orientation change; whether the compact-to-expanded transition should animate, and its reduced-motion behaviour; and real iOS Safari and Android Chrome acceptance criteria.
    - Keep this item outside the immediate ordered implementation sequence. It must not displace Android compatibility acceptance (item 25), editable GPX conversion (item 26), route reversal (item 27), weather (item 11), offline-map feasibility (item 12), the optional desktop Planning layout (item 16), or the still-pending bicycle field acceptance items recorded under "Manual acceptance status" above. It must not be merged into active Riding's navigation work — active Riding's non-sticky contract (item 24 above) is settled and unaffected by this idea. Implementing this item does not reopen or mark incomplete the already-delivered item 24.

### Platform compatibility

25. **Explicit Android/Chrome PWA compatibility — done**
    - A compatibility and acceptance-testing commitment, not a native-app project: ACN remains one PWA codebase. No APK, Android Studio project, Play Store packaging or Android-specific application architecture was introduced.
    - Target: Android 10 or later running the current stable release of Chrome for Android. No hard-coded Chrome version exists anywhere in product behaviour. Other Android browsers are not formally supported at this stage.
    - An audit of the manifest/service-worker configuration, GPX import/export, and geolocation/Wake Lock/persistence/map-fallback code (`vite.config.ts`, `vite.pwa.workbox.ts`, `src/platform/wakeLock.ts`, `src/ui/riding/useRideNavigation.ts`, `src/ui/riding/useScreenWakeLock.ts`, `src/map/MapView.tsx`, `src/storage/`) found **no production-code defect**: `grep -rniE "useragent|isAndroid|navigator\.platform" src/` returns nothing, confirming capability detection is used throughout with no UA-sniffing anywhere; diffing the built `dist/index.html`/`dist/manifest.webmanifest` against source confirmed Vite's `base` rewriting already correctly prefixes every asset reference, the manifest's `start_url`/`scope`/`display: standalone` are already correct, and `vite.pwa.workbox.ts` already precaches only the app shell with no `runtimeCaching` (independently proven by the pre-existing `src/routing/serviceWorkerExclusion.test.ts`). This slice is therefore test-coverage-and-documentation only — no application logic changed.
    - A new second Playwright project, `android-chrome` (`playwright.config.ts`), uses `devices["Pixel 7"]` — a current, mainstream Android reference device (Android 14, satisfying the "10+" target) — split from the existing `chromium` project via `testMatch`/`testIgnore` on an `android*.spec.ts` filename convention, so each spec runs exactly once, never duplicated across both projects. This is still Chromium underneath (mobile viewport/UA/touch/device-scale-factor emulation only), never real Android Chrome or WebView — `docs/android-chrome-acceptance.md` states this plainly and tags every checklist item as automatically verified, real-device-only, or already-verified-on-iPhone-needs-Android-recheck, per this file's own "never mark real-device-only behaviour as verified" discipline.
    - New coverage: `e2e/pwaManifestAndScope.spec.ts` (manifest/base-path/service-worker-scope against the real built/served artefacts, run under the existing desktop `chromium` project since it doesn't depend on device emulation); `e2e/androidMobileLayout.spec.ts` (no horizontal overflow, ≥44px touch targets, the sticky-header-except-active-riding contract); `e2e/androidGpxImportExport.spec.ts` (real file-input import, a real download export with exact-filename and ACN-manoeuvre-content assertions, then an entirely offline re-import); `e2e/androidPlanning.spec.ts` (mocked-provider calculate/save/reopen on the default Road bike profile only — General cycling is already proven by `e2e/planning.spec.ts` and PlanningScreen's own unit/component tests, and is not duplicated here); `e2e/androidRiding.spec.ts` (mocked-fix Riding, camera controls, the non-sticky-during-riding contract, and three lighter-touch Wake Lock checks — visible-and-requests-when-supported, absent-when-unsupported, a rejecting request surfaces the existing retry state without crashing Riding — since the full Wake Lock state machine is already proven at the hook level and in `e2e/ridingWakeLock.spec.ts`); `e2e/androidPersistenceAndOffline.spec.ts` (three scenarios: a genuine `page.reload()`-then-reopen recovery flow, tile/style-blocked fallback rendering combined with Riding for the first time, and a real `context.setOffline(true)` proving the app's existing "Offline —" banner and continued route/progress/elevation usability); and `e2e/androidOfflineAppShell.spec.ts`, the one spec in the whole suite that deliberately does not block service workers, proving the real installed service worker serves the app shell after a reload with the network fully down. This last one was the slice's one identified flake risk (real service-worker activation/control timing); it passed 8/8 in the full suite run plus a further 8/8 under `--repeat-each=8`, so it shipped rather than being dropped to the real-device-only checklist.
    - A genuine, previously-undocumented architectural finding surfaced while writing the reload-recovery test, confirmed by reading `src/App.tsx` directly: `screen`/`selectedRoute` are plain, unpersisted `useState`, with no mount-time read of the stored active-ride row anywhere in `App.tsx`. A real page reload during a ride therefore returns the rider to the Routes screen, not back into Riding automatically — recovery happens only once the rider reopens the _same_ route (matched by `routeId` in `useRideNavigation.ts`), at which point the persisted fix/progress restore and the pre-ride panel offers "Resume riding" rather than "Start riding". `e2e/androidPersistenceAndOffline.spec.ts`'s first test asserts this precisely, through an actual reload — no test at any level (hook, component or e2e) previously did this.
    - Version bumped to 0.3.16 (`package.json`, and `package-lock.json`'s two matching `version` fields, hand-patched rather than regenerated). Full verification (format, lint, typecheck, the complete unit/component suite, production build, and the complete e2e suite across both Playwright projects — 84 tests, zero failures) passed; a single unrelated `RouteLibrary.test.tsx` focus-timing flake was observed under full-suite parallel load and confirmed pre-existing (44/44 pass in isolation; that file was untouched by this slice).
    - Real-device Android verification has not happened in this slice at all — every check above is Chromium-emulated. See `docs/android-chrome-acceptance.md` for the full, tagged checklist and an ordered manual test procedure for the user's next Android-phone session.
    - A follow-up fixes a genuinely flaky instance of this file's own reload-recovery test (the first test above), found via a real CI failure, not a hypothetical: the test gated its `page.reload()` on a fixed `page.waitForTimeout(300)`, mirroring the same fixed-sleep idiom used at 4 other reload-precondition sites across `e2e/ridingNextManoeuvre.spec.ts`/`e2e/ridingFinishAndEnd.spec.ts` (left unchanged by this fix — a known, still-open instance of the identical pattern, out of this fix's own scope). Root cause, confirmed by direct reading of `useRideNavigation.ts`'s persistence effect: the `rideState` write (`setActiveRideState` → a plain, un-throttled `db.rideState.put(...)`) fires once per accepted GPS fix with no debounce or timer, so its actual IndexedDB commit time depends on the browser's transaction queue and machine load — a fixed delay can never be provably sufficient. Directly reproduced locally, not just inferred: under calibrated CPU contention (`taskset -c 0-3`, 4-6 concurrent workers), the unmodified test failed with the exact reported symptom — `getByRole('button', { name: 'Resume riding' })` resolving to `undefined` after reload — confirming the row genuinely hadn't committed in time, not a Playwright auto-retry gap (CDP `Emulation.setCPUThrottlingRate` was also tried and rejected for this diagnosis: it throttles only the renderer's own JS execution, not the browser's storage-service write path, so it failed to reproduce the delay at all).
    - Fixed by replacing the sleep with two small local helpers, `readSavedRouteId`/`readActiveRideStateRow` — the repo's first e2e use of raw `indexedDB.open(...)` from inside `page.evaluate`, kept in the spec file itself rather than `e2e/support/`, per this file's own established no-shared-e2e-helpers-across-specs convention (see item 23's `zoomToCloseRange`). The test now waits for the on-route/off-route status text (proves a fix was accepted and classified) and then `expect.poll`s the real committed `rideState` row — `routeId` matching the just-saved route (looked up from the `routes` store), a non-null `lastFix`, numeric matched-progress fields, and `completionArmed: false` (provably correct here, since the injected fix never leaves the route's own start coordinate and so can never satisfy `rideCompletion.ts`'s arming departure-radius condition) — before reloading. Re-run under the same calibrated contention that reproduced the original failure (60+ repeats total across light, moderate-contention and `CI=1` fresh-server-boot conditions): zero occurrences of the original failure signature.
    - No production code changed — the persistence and restoration paths were already correct and already unit-tested (`useRideNavigation.test.ts`'s own `finish()` suite already flushes a microtask before asserting against `getActiveRideState()`); the gap was purely in this one e2e test's own precondition. No version bump, matching item 21's own precedent for a test-only hardening fix.
    - A second follow-up hardens the 4 remaining `page.waitForTimeout(300)` reload-precondition sites the paragraph above explicitly left open (2 in `e2e/ridingNextManoeuvre.spec.ts`, 2 in `e2e/ridingFinishAndEnd.spec.ts`) — all four are now removed. The two `ridingNextManoeuvre.spec.ts` sites (each already preceded by an existing UI assertion proving the intended fix was accepted — `"50 m"` visible, or `"Arrive at your destination"` visible) poll the committed `rideState` row's own `lastReliableMatchedDistanceFromStartMetres` — the exact frozen/presentation field `nextManoeuvre.ts`'s `selectNextManoeuvre` reads, confirmed by direct reading of its signature, never the live `matchedDistanceFromStartMetres` — reaching a threshold with a wide (300m+) margin against the fixture's own earlier fixes, before reloading; `matchedDistanceFromStartMetres` was deliberately left unchecked here (unlike the paragraph above's own precedent), since `rideNavigationCore.ts` shows the reliable and raw fields only ever diverge on an `"off-route"` classification, which these dead-straight, always-on-route fixtures never produce — checking the live field would add no real coverage and isn't the field that's actually load-bearing for the manoeuvre panel's restored content. The two `ridingFinishAndEnd.spec.ts` sites instead poll for the row's outright absence: `finish()` (`useRideNavigation.ts`, the sole caller of `clearActiveRideState()`, shared by both End ride and Finish ride) awaits the storage delete first, before any in-memory reset, making absence a race-free, strictly-stronger signal than the clean-pre-ride-UI assertions already present just above each site. The earlier "kept in the spec file itself rather than `e2e/support/`... per this file's own established no-shared-e2e-helpers-across-specs convention" decision two paragraphs above, for these same two reader functions, is now **explicitly superseded**: with two more spec files needing the identical raw-database-read mechanism (a third and fourth consumer beyond the original one), `readSavedRouteId`/`readActiveRideStateRow`/`toIndexedDbError`/`INDEXED_DB_NAME` were extracted into a new `e2e/support/rideStateDb.ts`, mirroring `localMapStyle.ts`'s own precedent for a genuinely shared, `Page`-coupled e2e support module. The extracted module stays deliberately narrow — IndexedDB reads only, no assertions, no polling policy, no scenario-specific expectations — so each spec still states and polls its own exact postcondition locally via a small per-file helper (`waitForCommittedRideProgress` in `ridingNextManoeuvre.spec.ts`; `waitForClearedRideState` in `ridingFinishAndEnd.spec.ts`), never a shared assertion. `androidPersistenceAndOffline.spec.ts` now imports the same two functions rather than defining them, with its own test bodies and `expect.poll(...).toMatchObject({...})` assertion byte-identical to before. No production persistence, restoration, ride-completion, End/Finish-ride, route-matching, manoeuvre-selection or PWA/service-worker behaviour changed; direct re-reading of `useRideNavigation.ts`/`rideStateRepository.ts`/`mapping.ts` for this slice found no defect making any of the four postconditions unreachable. No retries, enlarged timeouts or weakened assertions were introduced; no version bump, matching this same entry's own established precedent for test-only hardening.

### Editable route copies and GPX round-trip

26. **Edit an existing route as a new Planning draft — done**
    - A route opened in the pre-ride overview (Riding's `.ride-start-panel`, idle state only — never every Routes card) shows an **Edit copy in Planning** button whenever `domain/editableWaypoints.ts`'s `canDeriveEditableWaypoints` finds at least two points to work from; otherwise the button is disabled with an inline explanation. Pressing it never mutates the source route: it seeds the app's single Planning draft row with independent waypoint objects (fresh `createWaypointId()`s), the source route's own name as the suggested route name, and either the source route's recovered routing profile/avoid-ferries or (when nothing is recoverable) `DEFAULT_ROUTING_PROFILE` plus the current Settings avoid-ferries default — then hands off navigation to `App.tsx` via a new `onNavigateToPlanning` prop, mirroring the existing `onNavigateToSettings` convention. If the singleton draft already holds a meaningful (non-empty) unrelated plan, the shared `ConfirmDialog` (see below) interrupts first; Cancel leaves that draft untouched. Opening the copy issues zero routing requests — Planning's own existing "no auto-calculate, only `calculateNow()`" behaviour already guarantees this, since a freshly seeded draft never has a `"routed"` result.
    - Waypoint recovery follows a two-tier priority, implemented as a single new domain type, `PlanningProvenance` (`domain/types.ts`, mirroring `ManoeuvreProvenance`'s exact two-kind shape): `{kind: "planning-session", waypoints, profile, avoidFerries}` (stamped by `PlanningScreen.tsx` on **every** successful `handleSave`/`handleExport`, from live `state.present`/`profile`/`avoidFerries` — not scoped to edit-copies only, since any future Planning save should retain its own authored waypoints) or `{kind: "acn-gpx-extension", version: 1, ...}` (recovered from a validated GPX reimport). Both resolve to `origin: "exact"` in `resolveEditableWaypoints`; when neither is present, `navigation/deriveWaypointsFromRoute.ts` derives waypoints from the route's own geometry instead (`origin: "derived"`). PlanningScreen shows a small, persistent informational notice matching the origin ("Editable copy created from the route's original planning waypoints..." vs "Editable waypoints were estimated from this route. Recalculation may follow different roads..."), threaded through the existing draft-hydration effect and — critically — through _every_ run of the debounced autosave effect (not just hydration), since `saveDraft` fully replaces the stored row and would otherwise silently drop the notice's backing fields on the first post-open edit.
    - `deriveWaypointsFromRoute` is a pure, dependency-free, hand-rolled adaptive Ramer-Douglas-Peucker simplification (local equirectangular metric projection, not raw lat/lon degrees) over the route's `RoutePoint[]`: an epsilon-doubling search (starting at 0m) guarantees convergence to at most `MAX_DERIVED_WAYPOINTS = 20` waypoints (RDP's own point-count monotonicity in epsilon makes a separate truncation step both unnecessary and unsafe — it could otherwise drop the finish waypoint), followed by gap-filling any remaining capacity with real route points nearest the midpoint of oversized (>8km, the documented midpoint of the spec's "5–10km" guidance) gaps, then near-duplicate removal (<20m, never touching the first or last waypoint). A detected closed loop (start/finish within 50m) has its final waypoint forced to _exactly_ the first waypoint's coordinate, mirroring Planning's own "return to start" action. Always preserves route order and (subject to the above) the original endpoints; never shares coordinate tuple references with the source route.
    - The GPX side extends the existing `<acn:navigation>`/`<acn:source>` namespace with a new, geometry-digest-bound `<acn:planning version="1" profile="..." avoidFerries="..." waypointCount="N" geometrySha256="...">` element (`acnNamespace.ts`'s `ACN_PLANNING_EXTENSION_VERSION`), containing one `<acn:waypoint lon="" lat=""/>` per waypoint — written by `exportGpx.ts` whenever `route.planningProvenance` is present (independent of, but sharing one already-computed geometry digest with, the existing `<acn:navigation>` block when both apply), and read by a new `parseAcnExtension.ts` function, `readAcnPlanningExtension`, using the _same_ strict, all-or-nothing, digest-verified rejection tier as `readAcnNavigationExtension` (not the weaker best-effort tier `readAcnSourceProfile` uses) — version/profile/avoidFerries/waypoint-count/coordinate-range/digest all validated, any single failure discards the whole envelope. A rejected (but present) envelope surfaces a new non-blocking `GpxImportNotice` (`"acn-planning-extension-rejected"`) and the import still succeeds, falling back to geometry-derived waypoints. `MAX_ACN_PLANNING_WAYPOINTS = 300` bounds a validated import — independent of, and much larger than, the unrelated 20-waypoint derivation cap, which only bounds the geometry-fallback path.
    - `StoredPlanningDraft`/`PlanningDraftContent` gained two new optional fields, `editCopySourceRouteId`/`editCopyWaypointsOrigin` (`storage/db.ts`/`storage/mapping.ts`), following the exact `profile`/`avoidFerries`/`routeName` precedent — plain, non-indexed fields needing no Dexie version bump (schema stays at v4), guarded on read via a real `"exact" | "derived"` membership check rather than a bare `??`, since a corrupt value should suppress the notice, not show a wrong one.
    - The shared `src/ui/shared/ConfirmDialog.tsx` (previously bare, with no focus/keyboard handling at all — its only prior caller was Settings' key-deletion confirmation) gained `autoFocus` on Cancel and an Escape-anywhere-inside-cancels `onKeyDown`, mirroring `RouteListItem.tsx`'s own already-proven inline delete-confirmation pattern exactly — purely additive, so Settings' existing call site needed no changes and gained the same accessibility improvement for free. `RidingScreen.tsx` is the dialog's second caller, owning its own trigger-button ref for focus-restore on Cancel (the dialog itself has no notion of what opened it).
    - Saving the edited copy always creates a new route: `usePlanningRoute.ts` already mints a fresh `createRouteId()` on every calculation, so Save/Export's existing copy semantics needed no change at all — proven by e2e coverage that the source route remains present, unchanged, and independently reopenable after saving an edited copy under a different name.
    - Automated coverage: `deriveWaypointsFromRoute.test.ts` (straight/bends/dense-thousands/loop/very-short/near-duplicate/deterministic/order-preserving/20-cap fixtures), `editableWaypoints.test.ts`, GPX export/import/round-trip tests (including a tampered-digest-falls-back-to-derivation case), storage mapping/migration tests, `PlanningScreen.test.tsx` (notice text per origin, the notice surviving an unrelated waypoint edit — the autosave-threading fix above — and provenance stamping on both Save and Export), `ConfirmDialog.test.tsx` (autoFocus/Escape), `RidingScreen.test.tsx` (button state, both no-existing-draft and meaningful-draft-confirmation paths, storage-failure handling), and a new `e2e/editRouteAsPlanningCopy.spec.ts` covering exact-provenance recovery with zero routing requests until Calculate, a capped derived-waypoint arbitrary GPX, the meaningful-draft confirmation (Cancel/Confirm), a full export→offline-reimport→edit-copy-again round trip proving exact (not re-derived) recovery, and a tampered-digest-falls-back-to-derivation scenario. Real-device iPhone verification has not happened — do not treat this item as manually verified until that check has actually been done.
    - This item's `PlanningProvenance`/`resolveEditableWaypoints`/`deriveWaypointsFromRoute` foundation was built so item 27 below could reuse the same editable-waypoint representation, as originally scoped; item 27 is now also done.

27. **Reverse a route by rerouting reversed waypoints — done**
    - A **Reverse route** button sits directly beside **Edit copy in Planning** in the pre-ride overview (`.ride-start-panel`, idle state only), sharing its exact eligibility rule (`canDeriveEditableWaypoints`) and disabled-hint text verbatim — the two buttons are always simultaneously enabled or disabled, so a single shared `field-hint` paragraph covers both rather than duplicating identical text. Pressing it never mutates the source route: it resolves editable waypoints via the unchanged v0.3.17 `resolveEditableWaypoints` (exact `planningProvenance` first, `deriveWaypointsFromRoute` fallback otherwise), reverses their order, and seeds the singleton Planning draft with a suggested `<source name> (reversed)` name. Opening the reverse copy issues zero OpenRouteService requests — the draft has no `"routed"` result until the rider explicitly presses Calculate route.
    - **Shared architecture**: `RidingScreen.tsx`'s previous single-purpose `performEditCopy`/`handleEditCopyClick`/`handleEditCopyCancel`/`handleEditCopyConfirm` became one parameterised `performCopyOperation`/`handleCopyOperationClick`/`handleCopyOperationCancel`, driven by a small `CopyOperationConfig` object (button/dialog copy, error messages, log tags, a `transformWaypoints` function and a `suggestedName` function) with two module-level instances, `EDIT_COPY_CONFIG` (identity transform, unchanged copy) and `REVERSE_ROUTE_CONFIG` (`reverseEditableWaypoints`, `suggestReversedRouteName`). Both actions read and write the _same_ singleton `planningDrafts` row, so they deliberately share one guard pair (`pendingConfirmKind`/`activeOperationKind`/`isActionPendingRef`) rather than independent per-action state: `handleCopyOperationClick`'s existing `pendingConfirmKind !== null || isActionPendingRef.current` guard is what serialises each action against itself (a rapid double click) _and_ against the other action (clicking Reverse route while Edit copy's own confirmation is open is a no-op) with no extra coupling code, since both conditions collapse to the identical check. `ConfirmDialog` is reused unchanged with reverse-specific title/message/confirm-label text; Cancel and a persistence failure both restore focus to whichever button (`editCopyButtonRef`/`reverseButtonRef`) triggered the pending operation, mirroring the v0.3.17 behaviour exactly.
    - **Reversal semantics**: `domain/editableWaypoints.ts`'s new `reverseEditableWaypoints(waypoints)` is `waypoints.map(clone).reverse()` — a plain positional reversal with no loop-aware special-casing, which is already exactly correct for a closed loop: `deriveWaypointsFromRoute`'s own loop-closure step produces a _value_-equal (not reference-equal) final waypoint when it detects one, so for `[A, B, C, A]` a positional reversal (swap index 0↔3, 1↔2) yields `[A, C, B, A]` exactly, understood the only way any code in this app ever compares a coordinate — by value, never by object identity. Every output tuple is freshly constructed via `.map`, so the input array and its coordinate tuples are never mutated or shared. `domain/routeNaming.ts` (new file, the first naming helper anywhere in this codebase) holds `suggestReversedRouteName(sourceName) => \`${sourceName} (reversed)\``: preserves the complete source name unmutated, no trimming or language-aware suffix logic (Planning's own existing `routeName.trim() || "Planned route"` already covers whitespace at Save/Export time), and no dedupe against existing Route Library names, since duplicate names are already an accepted, pre-existing condition.
    - **Draft metadata compatibility**: one new additive, non-indexed field, `editCopyOperation?: string` on `StoredPlanningDraft` (`storage/db.ts`) and `EditCopyOperation = "forward" | "reverse"` on `PlanningDraftContent` (`storage/mapping.ts`, guarded by a real `isEditCopyOperation` membership check, mirroring `isEditCopyWaypointsOrigin` exactly) — no Dexie version bump needed, following the identical precedent the two v0.3.17 fields already established. Unlike `editCopyWaypointsOrigin` (which stays `undefined` when absent, since that `undefined` state is what lets the unchanged two-field hydration gate suppress the notice for an ordinary draft), `fromStoredPlanningDraft` resolves `editCopyOperation` to a **concrete default of `"forward"`** whenever absent or corrupt: this field is only ever consulted once the two-field gate has already passed, at which point "forward" is the objectively correct reading of "no marker recorded" for any pre-Reverse-route draft — so a v0.3.17-written draft (which has `editCopySourceRouteId`/`editCopyWaypointsOrigin` but no `editCopyOperation` at all) continues to hydrate as an ordinary forward edit copy, showing the unchanged forward notice, with no special-casing in `PlanningScreen.tsx`. `PlanningScreen`'s `editCopyMeta` state gained one field, `operation: EditCopyOperation`, threaded through the existing hydration effect (gate condition unchanged) and the existing debounced-autosave effect's conditional spread (a third line alongside the two v0.3.17 fields, required for the same "saveDraft fully replaces the row" reason). A new pure `describeEditCopyNotice(meta)` replaces the previous inline ternary with the full 2×2 (operation × origin) matrix: the two forward strings are byte-identical to v0.3.17; the two new reverse strings read "Reversed editable copy created. Recalculate before saving; one-way restrictions may make the new route differ from the original. The saved route remains unchanged." (exact) and "Reversed waypoints were estimated from this route. Recalculation may follow different roads, especially around one-way restrictions. The saved route remains unchanged." (derived).
    - **Zero changes needed, confirmed by direct reading rather than assumed**: the GPX `<acn:planning>` export/import layer (`exportGpx.ts`/`parseAcnExtension.ts`/`importGpx.ts`) never branches on how `planningProvenance.waypoints` were produced, so a reversed-then-saved route gets a correct, fresh `planningProvenance` for free via the unchanged `buildPlanningProvenance` (which always stamps from live editor state at Save/Export time); `usePlanningRoute.ts` already mints a fresh `createRouteId()` on every calculation, so Save always creates a new route regardless of whether the draft originated as a reverse copy; `canSaveOrExportPlan`'s existing `state.kind === "routed"` gate already keeps Save/Export unavailable for any freshly-seeded, uncalculated draft. None of `resolveEditableWaypoints`, `canDeriveEditableWaypoints`, `deriveWaypointsFromRoute` or `PlanningProvenance` itself changed at all.
    - CLAUDE.md backlog item 30 (the pre-existing Planning autosave race) was explicitly checked, not fixed: `editCopyOperation` is threaded through the autosave effect using the exact same conditional-spread pattern as the two v0.3.17 fields, so it inherits the already-documented race identically without making it worse or different in kind; no evidence was found that this slice's metadata cannot persist correctly without touching item 30, so it was left alone per this file's own change-discipline.
    - Automated coverage: `editableWaypoints.test.ts` (two-point/multi-point/closed-loop reversal, no input mutation, no tuple reference-sharing, permutation-only length preservation including an already-20-capped list), `routeNaming.test.ts` (suffix behaviour, unmutated source name, determinism), `mapping.test.ts`/`planningDraftRepository.test.ts` (round-trip, v0.3.17-legacy-row defaults to `"forward"`, corrupt-value defaults to `"forward"`, an ordinary draft's resolved default never leaks a false notice), `RidingScreen.test.tsx` (a full "Reverse route" suite mirroring every "Edit copy in Planning" case plus a new cross-action guard suite proving the two actions' shared in-flight state), `PlanningScreen.test.tsx` (reverse+exact/derived notices, legacy-draft compatibility, autosave survival, no-notice-for-ordinary-draft), and a new `e2e/reverseRoute.spec.ts` covering: zero requests until Calculate, exact-provenance reversal with reversed leg coordinates sent to the mocked provider, Save creating an independent second route with the source route left unchanged and reopenable, export→offline-reimport→Edit-copy-in-Planning recovering the reversed waypoints exactly, a capped derived-waypoint arbitrary GPX reversal, a closed-loop reversal (proved both at the UI level — the combined start/finish marker persists — and at the routing-request level, where the three reversed legs are shown to be the three original legs in reverse order with each leg's own endpoints swapped), the meaningful-draft confirmation (Cancel/Confirm, focus restored to Reverse route specifically), and reload recovery with no automatic routing request. Real-device iPhone verification, and manual verification once the currently-observed live OpenRouteService outage clears, have not happened — do not treat this item as manually verified until each has actually been done.
    - Version bumped to 0.3.18 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched rather than regenerated, following the 0.3.16→0.3.17 precedent). Files changed: `src/domain/editableWaypoints.ts`, `src/domain/routeNaming.ts` (new), `src/storage/db.ts`, `src/storage/mapping.ts`, `src/ui/riding/RidingScreen.tsx`, `src/ui/planning/PlanningScreen.tsx`; tests: `src/domain/editableWaypoints.test.ts`, `src/domain/routeNaming.test.ts` (new), `src/storage/mapping.test.ts`, `src/storage/planningDraftRepository.test.ts`, `src/ui/riding/RidingScreen.test.tsx`, `src/ui/planning/PlanningScreen.test.tsx`, `e2e/reverseRoute.spec.ts` (new).

### Ride lifecycle

29. **A finished ride's persisted state is never cleared — done**
    - Found during the Android/Chrome compatibility audit (item 25 above), not fixed there: `src/storage/rideStateRepository.ts`'s `clearActiveRideState()` is implemented and unit-tested but never called from any application code — no "finished ride" flow exists anywhere in the UI, so a completed or abandoned ride's persisted `rideState` row lingers in IndexedDB indefinitely rather than being cleared. This is a general product question (what should "finishing a ride" mean — an explicit action, reaching the route's end, a time-based expiry?), not an Android-compatibility defect, and was deliberately left unimplemented in that slice per its own non-goal of unrelated backlog work.
    - Before implementing, decide the intended trigger and UX for clearing ride state, and confirm it should not interfere with the existing stale-fix/"Resume riding" restoration contract for a ride that was merely paused or interrupted, not genuinely finished.
    - This slice answers the questions the entry above left open with two entry points converging on one authoritative path: a manual, always-available **End ride** action, and a conservatively-detected, rider-confirmed **Finish ride** action. Neither records a ride — ACN still has no ride-history feature. `useRideNavigation.ts` gained a new exported `finish(): Promise<void>` — the sole caller of `clearActiveRideState()` anywhere in the app (previously zero call sites, per the entry above). It calls `clearActiveRideState()` first, before touching anything else in memory; only on success does it reuse the existing `stop()` (bumps `watchGenerationRef`, disposes the watch, `setStatus("idle")`) and reset `startedAtRef`/`currentFix`/`coreState`/`isStale`/`geolocationError`/`elevationViewMode`/`wakeLockDesired`/`dismissedClimbFeatureId`/`restoredCameraState` to fresh-ride defaults. `currentFix` returning to `null` is what makes `RidingScreen.tsx`'s pre-existing "Resume riding"-vs-"Start riding" label (already keyed off `currentFix`, not a new flag) correct for free. No IndexedDB schema change was needed: deleting the singleton `active` row outright is sufficient, confirmed by direct inspection of `storage/mapping.ts`/`storage/db.ts` — nothing about a finished ride needs remembering once its progress is exactly what must not persist.
    - **Storage-write ordering and obsolete-callback protection.** A new `isFinalizingRef` closes the one genuine race this entry's own text warned about: a persistence write racing `clearActiveRideState()` and completing after it, recreating the just-cleared row. `finish()` sets this ref synchronously, in the same synchronous block as its own call to `clearActiveRideState()` (before any `await`); the existing per-fix persistence effect (previously unconditional) now checks it as its literal first line and skips writing while a finalisation is in flight — closing the window where a genuine GPS fix arriving while the delete's own IndexedDB transaction is still pending would otherwise `put()` the row again after the `delete()` transaction, recreating it (same-object-store Dexie transactions commit in creation order, per the mechanism item 31 above already characterised and proved against this repository's real Dexie/fake-indexeddb stack — a new `useRideNavigation.test.ts` case proves this exact race directly, with no mock needed: `finish()`'s own real `await` already creates the window a synchronous test can drive a late fix into deterministically). The ref is deliberately **not** checked inside `handleFix`/`handleError`: a genuine fix arriving mid-finalisation still updates in-memory ride state normally — harmless on success (overwritten by `finish()`'s own reset) and exactly the correct live state to show on failure, since a real GPS fix doesn't become invalid merely because a background storage clear is in flight; only the persistence _write_ itself is suppressed. On failure the ref is cleared and the original error re-thrown untouched, leaving every other piece of state exactly as it was — the UI never claims success before the persisted state is actually proven cleared, and no rollback logic is needed anywhere since nothing else was ever touched. `finish()` also guards its own re-entrancy at the hook level (a silent no-op on a redundant call), a belt-and-braces layer beneath `RidingScreen.tsx`'s own primary `isFinalizeActionPendingRef` guard, mirroring the existing `isActionPendingRef` idiom `performCopyOperation` already established. A late callback from an already-disposed watch is rejected by the pre-existing `watchGenerationRef` discipline unchanged.
    - **Wake lock and camera** needed no new release logic and one small addition respectively. `RidingWakeLockControl`/`useScreenWakeLock` are already mounted only while `nav.geolocationStatus !== "idle"`, so `finish()` flipping status to `"idle"` unmounts that subtree and its own existing unmount-cleanup effect releases the sentinel, exactly per `useScreenWakeLock.ts`'s own doc comment — zero new wake-lock code. The camera needed one addition: `useRideCamera.ts` already had exactly the right reset transition (`rideCameraReducer`'s `"route-opened"` case, previously only ever dispatched automatically on a genuine route-id change) — a new `resetCamera(): void` on `UseRideCameraResult` just dispatches that same event, reusing the existing reducer branch rather than adding a second reset path; `RidingScreen.tsx`'s finalisation callback calls it explicitly, since ending a ride never changes `routeId` and so would never trigger the existing automatic route-change reset on its own. One accepted, documented, unfixed residual: `showPausedToast` (a separate `useState`, not part of the reducer's own `HookState`) isn't touched by `"route-opened"`, so a "Map follow paused" toast already showing at the exact instant of finalize lingers until its own pre-existing 3 s timeout — negligible, self-heals, and the message isn't wrong, just momentarily stale after a screen transition that already shows an entirely different panel.
    - **Route-completion detection** (Finish ride's trigger) is a new, deliberately conservative pure module, `src/navigation/rideCompletion.ts`. `isRouteCompletionCandidateEligible` requires, together: an actively-tracking ride; a fresh (non-stale) fix; an off-route level below the strong `"off-route"` threshold; a trusted fix accuracy (reusing `offRoute.ts`'s own `MAX_TRUSTED_ACCURACY_METRES`, 100 m — a fix already too poor to trust for route-matching is too poor to trust for completion either); reliable progress — the same frozen/reliable `presentationDistanceFromStartMetres` every other Riding presentation value already uses, per this file's own stated preference, never the live matched distance — within `ROUTE_COMPLETION_REMAINING_DISTANCE_THRESHOLD_METRES` (50 m, matching `offRoute.ts`'s own `OFF_ROUTE_BASE_METRES` order of magnitude) of the route's total distance; and the raw GPS coordinate within an accuracy-inflated radius of the route's own final point (`route.points.at(-1)`) — `ROUTE_COMPLETION_ENDPOINT_BASE_RADIUS_METRES` (25 m, comparable to `offRoute.ts`'s `POSSIBLY_OFF_ROUTE_BASE_METRES`) plus the fix's own accuracy times `ROUTE_COMPLETION_ENDPOINT_ACCURACY_MULTIPLIER` (1.0, mirroring `offRoute.ts`'s identical multiplier), capped at `ROUTE_COMPLETION_ENDPOINT_MAX_ACCURACY_ALLOWANCE_METRES` (40 m) so one very inaccurate fix can never qualify from an unreasonable distance. A closed loop or an out-and-back/self-crossing route needs no special-casing: the remaining-progress gate alone already rejects an early pass near the shared start/finish, or a geographically-close-but-early crossing, since reliable progress there is far from the route total regardless of raw proximity — proved directly against this project's existing `outAndBackRoute.ts`/`selfIntersectingRoute.ts` test fixtures. These are first-pass, reasoned starting values, compared against this codebase's own existing accuracy/off-route/route-matching thresholds (`offRoute.ts`; `projection.ts`'s `WINDOW_RADIUS_METRES`/`MAX_ACCEPTABLE_LATERAL_METRES`, which bound map-matching search rather than completion but confirm the chosen ~65 m worst-case endpoint radius is comfortably tighter than the app's existing matching tolerance), not yet field-verified on a moving bicycle — matching this file's own established humility about exact metre-scale tuning elsewhere (`routeWidthPolicy.ts`, `distanceBadgeLayer.ts`). A narrow `routeCompletionTrackerReducer` state machine (`consecutiveEligibleCount`/`isDismissed`) requires `ROUTE_COMPLETION_CONSECUTIVE_FIXES_REQUIRED` (2, matching `offRoute.ts`'s own `CONSECUTIVE_TO_DEESCALATE`) consecutive eligible fixes before `isRouteCompletionConfirmed` reports true, deliberately non-latching: an ineligible fix resets the count, per this item's own required test list. Completion detection never clears anything automatically — it only ever surfaces `RidingRouteCompletionPanel`; the rider must press Finish ride. `src/ui/riding/useRouteCompletionCandidate.ts` wires this into React using the same "conditional dispatch during render" idiom `RidingScreen.tsx` already established for `reachedManoeuvreIndex` (not a `useEffect`), tagging each evaluated fix by object identity so neither an unrelated re-render nor React 18 StrictMode's double-invoked render can double-count a fix — proved directly by a dedicated `{ wrapper: StrictMode }` test mirroring `useScreenWakeLock.test.ts`'s own existing convention. An explicit `reset()` (called from the finalisation callback, alongside the hook's own automatic route-id-keyed reset) is needed because Finish/End ride never changes `routeId`, so starting the same route fresh again does not inherit a stale dismissal or count.
    - **UI.** `RidingScreen.tsx` gained an `End ride` `.btn-danger` trigger — shown at the bottom of the pre-ride panel's button stack whenever a Resume-riding state exists (`geolocationStatus === "idle" && currentFix !== null`), and, in the same DOM slot `RidingClimbSelector` occupies (mutually exclusive with it, since that panel is idle-only), whenever a ride is genuinely active — never in a clean, never-started pre-ride state. It opens the shared `ConfirmDialog` ("End this ride?" / "Navigation progress for this ride will be cleared. The saved route will remain in your library." / Cancel / End ride) as a single top-level element reachable from either trigger site, not `window.confirm()`. A new `RidingRouteCompletionPanel` ("Route complete" status paragraph / Finish ride primary / Keep riding secondary) renders directly after `RidingNextManoeuvrePanel`, so it never competes with the map or manoeuvre panel — shown only once `useRouteCompletionCandidate` reports confirmed, and never auto-clearing anything itself. Both End ride and Finish ride funnel into one `performFinalizeRide(source)`, guarded by a single `isFinalizeActionPendingRef`/`activeFinalizeSource` pair mirroring `performCopyOperation`'s own existing catch-block shape (`logError`, a source-tagged accessible `.field-error role="alert"` message) — a storage-clear failure therefore always leaves the ride active/resumable with a concrete retry path, never silently discards in-memory state. `ConfirmDialog.tsx` gained a small, additive `confirmDisabled`/`cancelDisabled` prop pair (unused by its two existing callers, so their behaviour is unchanged), used by End ride's dialog to disable both buttons while a finalisation is in flight — mirroring `RouteListItem.tsx`'s own already-documented "both actions disabled while a deletion is in flight, so Cancel can never appear to undo work storage is still carrying out" precedent (item 7 above). A genuine focus-restoration bug was found and fixed during this slice's own test-writing, not merely theorised: calling `.focus()` directly inside `performFinalizeRide`'s `catch` block targets a button that is _still_ disabled in the DOM at that exact point (`activeFinalizeSource` only resets to `null` in the `finally` block immediately after, and React does not commit that until this whole synchronous catch/finally sequence finishes — a disabled control cannot receive focus, so the call silently no-ops and focus falls to `<body>` once the confirmation dialog unmounts). Fixed by moving the failure-path focus-restoration into a `useEffect` keyed on `finalizeError`'s own object identity (fresh only for a genuinely new error), which React guarantees runs only after the DOM has actually committed the button's re-enabled state — proved directly by this slice's own `RidingScreen.finishEndRide.test.tsx`, and confirmed empirically against jsdom's real disabled/focus behaviour before settling on this fix. A second, smaller gap found by reading `RidingScreen.tsx` directly: `reachedManoeuvreIndex` (the monotonic furthest-reached-manoeuvre tracker) was never reset by the existing `handleStart` — correct for Resume riding, but not for a fresh ride starting after a finalisation. `performFinalizeRide`'s success path now resets it to 0 explicitly, alongside `camera.resetCamera()` and the completion tracker's `reset()`. `explicitFeatureSelection`/`selectedGradientSegment` needed no new reset of their own: `handleStart`'s existing `nav.geolocationStatus === "idle"` branch already clears both, and `finish()` always leaves status `"idle"`, so pressing Start again after a finalisation re-triggers that existing clear for free.
    - No routing endpoint/payload/profile/diagnostics, GPX behaviour, route-reversal/editable-copy behaviour, Planning draft hydration/autosave, camera follow/north-up behaviour beyond the one new explicit reset, elevation/climb detection, manoeuvre selection beyond the one `reachedManoeuvreIndex` fix above, map-layer styling, service-worker caching, or deployment configuration changed. Automated coverage: `src/navigation/rideCompletion.test.ts` (all required pure-eligibility and tracker-state cases, including closed-loop/out-and-back/self-intersecting fixtures reused from `src/test/fixtures/`); `src/ui/riding/useRouteCompletionCandidate.test.ts` (dispatch-during-render correctness, including the StrictMode case above); `src/ui/riding/RidingRouteCompletionPanel.test.tsx` (new, isolated); `src/ui/riding/useRideNavigation.test.ts` gained a `finish()` suite (success clears storage and resets every field; failure re-throws, leaves state untouched, and correctly re-arms persistence; a duplicate concurrent call is a no-op; and the late-write race itself, proved against this repository's real Dexie/fake-indexeddb stack); `src/ui/shared/ConfirmDialog.test.tsx` gained `confirmDisabled`/`cancelDisabled` cases; a new `src/ui/riding/RidingScreen.finishEndRide.test.tsx` (deliberately separate from the existing, much larger `RidingScreen.test.tsx` — pure file-size/organisation hygiene, using the exact same real-Dexie-backend/targeted-`vi.spyOn` approach that file already uses) covering all twelve required "Riding UI and finalisation" scenarios; and a new `e2e/ridingFinishAndEnd.spec.ts` covering the required End-ride reload-recovery proof and a deterministic-geolocation Finish-ride proof (both the remaining-distance and the two-consecutive-fix requirements exercised explicitly against a locally-imported GPX fixture, no OpenRouteService/OpenFreeMap dependency), mirroring `e2e/ridingNextManoeuvre.spec.ts`'s own established geolocation-injection/local-map-style conventions. Version bumped to 0.3.20 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent). Real-device iPhone/bicycle verification of the completion-detection distance/accuracy constants above has not happened — do not treat that part as field-verified until it has actually been ridden.
    - **A follow-up slice (v0.3.21) corrects the closed-loop claim above and adds a mandatory completion-arming gate.** The previous paragraph's "a closed loop or an out-and-back/self-crossing route needs no special-casing... the remaining-progress gate alone already rejects" claim is **too strong** and is superseded by this entry. Direct inspection of `src/navigation/projection.ts`'s `projectFixOntoRoute` found a real, reachable hole: the very first fix of a ride has `lastMatch === null`, so it runs a **whole-route** nearest-point search rather than a windowed one; `classifyFix`'s "reacquired ⇒ untrusted" handling leaves `offRouteMachineState` at its _initial_ `"on-route"` value (an untrusted raw classification never changes the debounced level, and the initial level is already on-route). A first fix that geometrically ties — or nearly ties — between a closed loop's shared start/finish coordinate is therefore never blocked by the off-route gate, and if that same fix's whole-route search happens to snap onto the route's _final_ segment (plausible whenever start/finish geometry overlaps or nearly overlaps), `reliableDistanceFromStartMetres` can misreport near-total progress before the rider has gone anywhere — confirmed directly, not assumed: `src/navigation/rideCompletion.test.ts`'s "a hostile closed-loop start projection" test constructs exactly this input and asserts `isRouteCompletionCandidateEligible` alone _would_ have returned `true` for it, the precise pre-existing hole this slice closes.
    - **The arming invariant**: every genuinely new ride starts `completionArmed = false`. No fix counts towards the existing completion counter, and `RidingRouteCompletionPanel` cannot show, until reliable accepted fixes have first demonstrated both genuine geographic departure from the finish and credible interior route progress, across `ROUTE_COMPLETION_ARMING_CONSECUTIVE_FIXES_REQUIRED` (2, matching the completion module's own consecutive requirement) consecutive arming-eligible fixes. `src/navigation/rideCompletion.ts` gained a new pure `isRouteArmingFixEligible`, requiring both, together: the raw GPS coordinate strictly outside `ROUTE_COMPLETION_ARMING_DEPARTURE_BASE_RADIUS_METRES` (75 m — deliberately much larger than completion's own 25 m endpoint radius, a ~2× hysteresis margin against completion's ~65 m worst case) plus an accuracy allowance capped at `ROUTE_COMPLETION_ARMING_DEPARTURE_MAX_ACCURACY_ALLOWANCE_METRES` (50 m) — this cap works in the _opposite_ sense from completion's own accuracy cap: completion's cap stops a poor-accuracy fix being wrongly _admitted_ from too far away, whereas this cap stops a poor-accuracy fix facing an unreasonably _inflated_ exclusion zone it could never practically clear; and reliable route progress within `[ROUTE_COMPLETION_ARMING_INTERIOR_MIN_FRACTION, ROUTE_COMPLETION_ARMING_INTERIOR_MAX_FRACTION]` (10%–80% of the route's own total distance, route-relative so both short and long routes stay practical) — deliberately well short of 100%, so a fix that misreports near-total progress (the exact hostile scenario above) can never itself count as interior progress. `RouteCompletionTrackerState` gained `isArmed`/`consecutiveArmingEligibleCount`; its `"fix-evaluated"` event now carries both `armingEligible` and `completionEligible` in one dispatch and branches on the _current_ `isArmed`: while unarmed, only arming evidence progresses and `completionEligible` is ignored outright regardless of its value (not merely an incidental consequence of the two ranges never overlapping — they mathematically can, on a route short enough that its remaining-distance completion threshold falls inside the interior fraction; the ignore-while-unarmed rule holds regardless); once armed, only the completion streak progresses, and arming fields are frozen, one-way, for the rest of that ride. `isRouteCompletionConfirmed` gained `state.isArmed &&` as a first, defensive condition.
    - **Ownership and persistence**: `completionArmed` is owned and persisted by `useRideNavigation.ts`, mirroring `wakeLockDesired`'s exact established pattern (a plain `useState`, restored from `getActiveRideState()` in the mount effect, included in the persistence effect's write call and dependency array, reset to `false` inside `finish()`'s success branch, exposed as `completionArmed`/`setCompletionArmed`) — `storage/db.ts`'s `StoredRideState` gained an optional, non-indexed `completionArmed?: boolean` (no Dexie `version()` bump; a legacy row without it, or a legacy row whose _other_ stored fields happen to describe near-total progress, both restore as `false` — never inferred `true` from stored progress, proved directly by `mapping.test.ts`). `src/ui/riding/useRouteCompletionCandidate.ts` receives this as a new `armed: boolean` option (the current, possibly-still-restoring value) and reports its own one-way `false → true` transition back as `isArmed`; an idempotent `"external-armed"` event lets it adopt a persisted `true` value that arrives asynchronously after mount (the restore effect resolving later) without needing a fresh fix. `RidingScreen.tsx` is the single integration point that observes `completion.isArmed` and persists it: `if (completion.isArmed && !nav.completionArmed) { nav.setCompletionArmed(true); }`, called conditionally during render — the same "adjust state during render" idiom already used for `reachedManoeuvreIndex`, just operating on a setter returned from a _different_ custom hook. This was verified safe, not merely assumed: `useRideNavigation(route, ...)` executes synchronously inside `RidingScreen`'s own render body, so the `setCompletionArmed` closure it returns is bound to `RidingScreen`'s own fiber — indistinguishable, at the React-internals level, from a `useState` called directly there. Explicitly _not_ justified by "it lints clean": `eslint-plugin-react-hooks`'s `set-state-in-render` rule only traces setters back to a `useState`/`useReducer` call in the _same_ function body, so it is structurally blind to a setter reached through a custom hook's return object, in both directions — its silence here is not evidence either way, only the fiber-identity argument is. No second completion detector, no second finalisation path, no competing copies of the flag: `rideCompletion.ts`'s pure reducer remains the sole authority on whether fresh evidence is sufficient; `useRideNavigation` remains the sole authority on what's durable.
    - Preserved unchanged: the manual End-ride action (available regardless of armed state, proved by a dedicated before-and-after-arming test pair), Finish ride's own confirmation-free single-click UX, `Keep riding`'s dismissal semantics, the shared `performFinalizeRide`/`useRideNavigation.finish()` path, and every existing off-route/route-matching/progress-projection calculation this slice only ever _reads_ the output of. Automated coverage: `src/navigation/rideCompletion.test.ts` gained a full `isRouteArmingFixEligible` unit suite (mirroring completion's own, including the inverted-direction accuracy-cap case) plus tracker-level integration tests for the hostile closed-loop start, stationary endpoint jitter, a single false-departure fix resetting the streak, legitimate arming that never un-arms, every existing completion gate still holding once armed, a legitimate closed-loop arm-then-complete sequence, and overlapping out-and-back/self-intersecting geometry (reusing the existing fixtures); `src/ui/riding/useRouteCompletionCandidate.test.ts` gained arming-seeded-from-mount, arming-adopted-via-rerender, hostile-input-via-hook-options, legitimate-arming-via-hook-options, route-change-resets-armed and arming-specific fix-dedup cases; a new `src/ui/riding/RidingScreen.completionArming.test.tsx` (mirroring `RidingScreen.finishEndRide.test.tsx`'s own split-for-file-size precedent) proves the hostile scenario end-to-end through real `processFix` matching (a fix placed exactly at the route's final coordinate as the _first ever_ fix, reproducing the whole-route-reacquire code path directly rather than a synthetic stand-in), a full legitimate arm-then-complete sequence, End ride working identically before and after arming, in-memory (not just storage) arming reset after a finalise-then-restart within the same mount, a resumed `completionArmed: true` row skipping re-arming, and a legacy resumed row with no `completionArmed` field restoring unarmed; `mapping.test.ts` gained round-trip and legacy-default cases mirroring `wakeLockDesired`'s own block. The existing `e2e/ridingFinishAndEnd.spec.ts`'s deterministic-geolocation test needed no structural changes: its existing progression (two consecutive interior fixes well outside the arming departure radius, before the finish-approach fixes) already happens to satisfy arming, so the same real geolocation-driven sequence now proves arming and completion together end-to-end — confirmed by re-running it against this slice's build, not assumed. Version bumped to 0.3.21 (`package.json`/`package-lock.json`, hand-patched). Real-device iPhone/bicycle verification remains outstanding for both the original completion thresholds and these new arming thresholds — do not treat either as field-verified until actually ridden.

30. **Planning's draft-autosave can race a just-completed Save, briefly resurrecting a cleared draft — done**
    - Found while hardening `e2e/editRouteAsPlanningCopy.spec.ts`'s own coverage (item 26 above): `PlanningScreen.tsx`'s debounced draft-autosave effect (900ms, rescheduled on every waypoint/routeName/avoidFerries/profile/editCopyMeta change) and `handleSave`'s own `saveRoute()` → `clearDraft()` → waypoint-reset promise chain both wrote to the same singleton `planningDrafts` row independently, with no explicit cancellation between them beyond the effect's ordinary dependency-array cleanup. If an autosave timer already pending from an earlier edit (e.g. typing the route name just before pressing Save) had not yet fired by the time `handleSave` ran, and it fired after `clearDraft()` resolved but before the post-save waypoint-reset re-render had caused React to run the effect's own cleanup, the stale timer's callback could write the pre-save waypoints back into the just-cleared draft row. Observed directly under heavy parallel e2e load — rare, never reproduced in an ordinary interactive session — and previously worked around, not fixed, by an explicit `page.waitForTimeout(1_200)` in `e2e/editRouteAsPlanningCopy.spec.ts` before every Save. Distinct from item 31 below, which this slice leaves entirely unmodified: item 31 protects autosave against a still-loading/failed hydration read; this item coordinates autosave against a concurrent explicit Save.
    - Fixed by four small coordination primitives added to `PlanningScreen.tsx`, declared together immediately before the autosave effect: `saveTimeoutRef` (`useRef<number | undefined>`, mirroring `usePlanningRoute.ts`'s own `debounceTimeoutRef` shape) is the one synchronously-reachable handle for whichever autosave timer is currently scheduled, written by the effect and read/cleared by `handleSave`; `saveGenerationRef` (`useRef(0)`, mirroring `hydrationGenerationRef`'s existing idiom) is bumped at the start of every `handleSave` attempt and in a new unmount-only cleanup effect, and is checked by both the autosave timer callback and `handleSave`'s own `.then`/`.catch` continuations before they touch any state; `isSavingRef` (`useRef(false)`) is a synchronous re-entrancy guard checked+set as the first lines of `handleSave`, mirroring this same file's existing `isLocatingRef` rationale that a React state check alone isn't synchronous enough to block a rapid second click; `isSaving` (`useState(false)`) is the user-visible counterpart, added to the autosave effect's own gate and dependency array (`if (hydrationStatus !== "ready" || isSaving) return;`) and used to disable/relabel the Save button.
    - `handleSave` now synchronously cancels the pending timer (`window.clearTimeout(saveTimeoutRef.current)`) and bumps+captures the generation before any `await`/`.then` begins — closing the race entirely rather than narrowing it, since the only timer that could ever race a Save is the one that existed at the exact moment Save was clicked (the autosave effect's own dependencies provably don't change during the async gap, and effects only run at commit time). On failure — either `saveRoute` or the explicit `clearDraft` rejects, a single shared `.catch()` covering both and preserving existing partial-failure semantics with no cross-store rollback attempted — nothing was reset, so `isSaving` flipping back to `false` re-arms the autosave effect against current, live render state with no snapshotting. On success, the post-reset render re-arms the effect with `state.present = []`, so any further "autosave" is a harmless, idempotent `clearDraft()` call, never a resurrection. The Save button gained `disabled={!canSaveOrExport || isSaving}` and an in-flight `"Saving…"` label, matching this codebase's existing "Saving…"/"Deleting…" button vocabulary (`RouteLibrary.tsx`, `SettingsScreen.tsx`, `RouteListItem.tsx`).
    - Automated coverage: a new, dedicated `src/ui/planning/PlanningScreen.saveAutosaveRace.test.tsx` (mirroring `PlanningScreen.draftHydration.test.tsx`'s own fake-timer/controlled-promise conventions, plus a new mock of `routesRepository.ts`'s `saveRoute`) proves, deterministically: a pending autosave timer is cancelled before it can fire when Save is clicked just ahead of it; no autosave write occurs for the whole successful in-flight window and the draft is never resurrected afterward; a rapid double Save click issues exactly one `saveRoute`/`clearDraft` pair; the button shows `"Saving…"` and disables while in flight; a rejected `saveRoute` resumes autosave with the current (unreset) in-memory plan; a rejected explicit `clearDraft` (with `saveRoute` resolved) does the same, documented as accepted partial-failure semantics; a Save attempt's continuation discards itself if it resolves after unmount; a pending autosave timer never fires after unmount; ordinary post-hydration autosave is unchanged; and item 31's failed-hydration no-write invariant still holds. `PlanningScreen.test.tsx`'s two existing real-Dexie Save-click tests (`"saving clears the draft…"` and `"clears the edit-copy notice once the copy is saved"`) were extended with an edit-the-route-name-then-immediately-click-Save sequence followed by a real 1,100ms wait, re-asserting `getDraft()` is still `undefined` after the wait — an integration-level proof alongside the mocked unit-level one.
    - `e2e/editRouteAsPlanningCopy.spec.ts`'s `planAndSaveTwoWaypointRoute()` no longer waits 1,200ms before Save; it clicks Save immediately after filling the route name and asserts, via a new `readPlanningDraftRow` export added to `e2e/support/rideStateDb.ts` (mirroring `readActiveRideStateRow` exactly, extended for a third store per this file's own established precedent — see item 25), that the singleton draft row stays `null` after saving. Passed 15/15 under `--repeat-each=3` against the same heavy-parallel-load conditions that originally surfaced the race. The unrelated second `page.waitForTimeout(1_200)` in that same spec file (protecting an intentionally-unsaved, unrelated draft before navigating away to test the replace-draft confirmation dialog) was deliberately left untouched.
    - Version bumped to 0.3.22 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent). Real-device iPhone verification of the new `"Saving…"` button state is still outstanding — do not treat it as manually verified until that check has actually been done.
    - A follow-up, test-only slice removes the four `1_200`ms pre-Save workaround waits `e2e/reverseRoute.spec.ts` had carried since before this fix (in `planAndSaveTwoWaypointRoute`/`planAndSaveClosedLoopRoute` and the two direct Save clicks on a reversed draft), superseding the "left untouched, out of this slice's scope" note above. Each site now clicks Save immediately and awaits a new shared `assertPlanningDraftStaysCleared(page)` helper. This is deliberately stronger than `editRouteAsPlanningCopy.spec.ts`'s own `expect.poll(() => readPlanningDraftRow(page), {...}).toBeNull()` proof: inspection found that pattern's poll runs after an assertion that the "route saved" heading is visible, and `handleSave`'s own promise chain resolves `clearDraft()` strictly before the callback that makes that heading visible fires — so that poll's first check already observes `null` and succeeds immediately, without ever waiting through the ~900ms window in which a _regressed_ (non-synchronous) timer cancellation would let a stale autosave fire and resurrect the row later. `assertPlanningDraftStaysCleared` therefore polls to `null` once and then keeps re-sampling the real row every 150ms across a 1,200ms margin (900ms debounce + 300ms headroom), so a resurrection landing anywhere in that window is still caught; `editRouteAsPlanningCopy.spec.ts` itself was left unmodified (out of this slice's own scope) and still carries the weaker, first-success-only form. A fifth, differently-purposed wait — before `page.reload()` in the "reloading after creating the reverse draft…" test, confirming the seeded draft's fields are genuinely persisted before reloading — was replaced too, since it was never actually a Save-race guard (no Save button is pressed near it): it now polls the real `planningDrafts` row directly (`expect.poll(() => readPlanningDraftRow(page), {...}).toMatchObject({ routeName: ... })`), a strictly stronger, equally small postcondition than the fixed wait it replaced. The one remaining `1_200`ms wait in `reverseRoute.spec.ts` (protecting an intentionally-unsaved, unrelated draft before navigating away to test the reverse-specific replace-confirmation dialog) is unrelated to the Save race and was left unchanged, mirroring `editRouteAsPlanningCopy.spec.ts`'s own identical, still-untouched wait. No production code changed and no version bump was needed, matching this same entry's own established precedent for test-only hardening.
    - A second follow-up, also test-only, supersedes the "still carries the weaker, first-success-only form" note above: `e2e/editRouteAsPlanningCopy.spec.ts`'s own `planAndSaveTwoWaypointRoute` helper now also awaits a locally-copied `assertPlanningDraftStaysCleared(page)` (identical mechanism to `reverseRoute.spec.ts`'s own — poll to `null`, then re-sample every 150ms across the same 1,200ms margin), so it too samples across the complete danger window rather than stopping at the first successful poll. This file's other two Save clicks — saving an edited three-waypoint copy, and saving a GPX-derived copy — were deliberately left unasserted: direct reading of `PlanningScreen.tsx` confirms `handleSave`'s cancellation sequence never branches on `editCopyMeta`, waypoint count or origin, so the one strengthened site already proves the same generic mechanism these other saves also exercise; duplicating full-window sampling there would add ~1.2s per site for no additional coverage. The unrelated `page.waitForTimeout(1_200)` protecting an intentionally-unsaved draft before the replace-confirmation-dialog test remains untouched, since no Save button is involved there. No production code changed and no version bump was needed, matching this same entry's own established precedent for test-only hardening.

31. **Planning draft hydration/autosave race — done**
    - A dedicated reliability slice, distinct from item 30 above. Evidence-backed root cause, confirmed by direct reading of the pre-fix `PlanningScreen.tsx`: the hydration effect's `getDraft().then(...).catch((error) => { logError(...); setIsDraftHydrated(true); })` set the single boolean coordination signal `isDraftHydrated` to `true` on a **rejected** read — without ever having applied anything, `state.present` was still the pristine empty array. The debounced autosave effect, gated only on `isDraftHydrated`, then saw "hydration complete, zero waypoints" and after 900ms called `clearDraft()` — silently deleting whatever real draft row existed in storage, which had never actually been read. Particularly dangerous immediately after item 26/27's "Edit copy in Planning"/"Reverse route" flows seed that same singleton row before navigating in. Separately confirmed safe and unchanged: `RidingScreen.tsx`'s `performCopyOperation` already `await`s `saveDraft(...)` before calling `onNavigateToPlanning?.()`, and `App.tsx` never mounts `PlanningScreen` before that navigation, so the _seeding_ write itself was never part of the race — the bug was entirely inside `PlanningScreen.tsx`'s own hydration/autosave coordination.
    - New invariant: _Planning must not autosave until the initial draft read has completed successfully and the resolved draft, or the deliberate fresh-plan defaults when no draft exists, has been applied as the authoritative in-memory state._ Implemented as an explicit `PlanningDraftHydrationStatus = "loading" | "ready" | "failed"` state machine (extending this file's own existing `locateStatus` tri-state convention, rather than importing a cross-file pattern) plus a numeric `hydrationGenerationRef` (mirroring `useRideNavigation.ts`'s `watchGenerationRef` idiom) as the sole authority on whether a given `getDraft()`/`getPlanningPreferences()` attempt is still relevant — replacing the old per-effect `cancelled` closure boolean entirely, including the _nested_ fresh-session `getPlanningPreferences()` continuation, not just the outer `getDraft()` one. Both the debounced autosave effect and the existing fresh-session location-framing effect now gate on `hydrationStatus === "ready"` instead of the removed `isDraftHydrated` boolean. A rejected `getDraft()` now sets `hydrationStatus` to `"failed"`, never `"ready"` — nothing was read or applied, so autosave stays genuinely blocked, and no `clearDraft()`/`saveDraft()` call can ever fire from that state. The restore batch (waypoints, route name, avoid-ferries, profile, and — set together, never separately — edit-copy/reversal provenance) is still applied as one atomic React state flush inside a single `.then()` callback, exactly as before; only the completion condition changed.
    - Obsolete/superseded results: the generation ref is bumped both at the top of every effect run (initial mount and every retry, via a `hydrationRetryToken` state bumped by the Retry button) and in the effect's own cleanup, so a result from a prior attempt can never apply after unmount, a retry, or React StrictMode's mount→cleanup→mount double-invocation (`getDraft()` is a read, so a harmless duplicate call; only the winning generation's result is ever applied).
    - "Which state wins" while hydration is loading or has failed: rather than blocking Planning's controls before hydration completes (the map and every interactive control already render unconditionally today, and gating them behind a loading screen would have needed a much larger change, breaking a large fraction of the pre-existing, non-async-aware `PlanningScreen.test.tsx` in the process, for no additional data-integrity benefit over the approach below), a new `hasUserModifiedDraftFieldsRef` — set by a new `noteHydrationOverriddenByUserEdit()` helper called at the top of `dispatchWaypointAction` (the one wrapper every waypoint mutation already goes through) and at the cycling-profile buttons' `onClick` and the route-name input's `onChange` — tells the hydration effect's _restore_ branch specifically to skip re-applying its fields once the rider has already directly changed one of the three restorable fields (waypoints, route name, profile) themselves, rather than silently overwriting their edit; `hydrationStatus` still transitions to `"ready"` either way, so autosave is unblocked for whichever state — restored or rider-edited — is now authoritative. Separately, the same helper unblocks a permanently `"failed"` status directly (`setHydrationStatus((current) => current === "failed" ? "ready" : current)`), since a rejected read has no pending promise left to ever resolve it any other way, and the rider has now started fresh work that deserves saving.
    - **A real regression found and fixed during this slice's own verification, not merely theorised**: the first implementation made `noteHydrationOverriddenByUserEdit()` unconditionally force `hydrationStatus` to `"ready"` and bump the generation on _any_ pre-ready edit — which broke three genuine, pre-existing tests (`"a genuinely fresh draft (no prior draft row) seeds avoidFerries from the current Settings default"` and its two siblings), all of which place a waypoint via a bare map tap immediately after mount, with no `await` in between, before the fresh-session `getPlanningPreferences()` read has any chance to resolve. The fix: the rider-edit override now only gates the **restore** branch's field-application (which genuinely conflicts with a waypoint/name/profile edit), never the **fresh-session** branch's `getPlanningPreferences()` seeding (which touches only `avoidFerries` and so can never conflict with any of the three fields the override tracks) — the fresh branch is unconditional, exactly as before this slice. This is a stronger, more precise statement of "which state wins" than the slice's own first draft, verified against real, previously-passing product behaviour rather than assumed.
    - Hydration failure shows a concise `.field-error`/`role="alert"` message ("Your saved plan could not be loaded. Nothing in storage has been changed.") plus a `.btn-secondary` "Retry" button (reusing this screen's own existing error/button vocabulary, not Riding's), positioned directly beneath the existing `NoApiKeyNotice`, above the `editCopyMeta` notice. Nothing is ever auto-cleared from storage on failure. The Retry button itself sets `hydrationStatus` back to `"loading"` directly in its own `onClick` before bumping `hydrationRetryToken` — mirroring `handleLocateMe`'s own existing convention of setting status in the triggering event handler rather than inside the effect it kicks off, which also avoids a `react-hooks/set-state-in-effect` lint violation a synchronous `setHydrationStatus("loading")` at the top of the effect body would otherwise trip. A separate, non-blocking `role="status"` "Loading your plan…" message shows only while `hydrationStatus === "loading"` — additive, and in production typically imperceptible, since IndexedDB reads normally resolve in low single-digit milliseconds.
    - Out-of-order autosave writes (this slice's own required audit): two new characterization tests in `planningDraftRepository.test.ts` prove `saveDraft`/`clearDraft` calls issued back-to-back with zero awaited gap between them still commit strictly in call order against this repository's real Dexie/fake-indexeddb stack — same-object-store readwrite transactions are queued and committed in request order by the IndexedDB spec itself. This closes the audit for the mechanism this slice touches (a single debounced timer per mount, cancelled before a new one is scheduled, so only one `saveDraft`/`clearDraft` call is ever in flight from the autosave effect itself) — **not a fix**, positive regression coverage for already-safe behaviour. It does **not** address item 30 above, which remains a separate, open, unfixed race.
    - Automated coverage: a new `src/ui/planning/PlanningScreen.draftHydration.test.tsx` (deliberately separate from the existing, much larger `PlanningScreen.test.tsx`, which never mocks the draft repository and never uses fake timers — this new file mocks `planningDraftRepository.ts`/`planningPreferencesRepository.ts` and uses `vi.useFakeTimers({ toFake: ["setTimeout", "clearTimeout"] })`, deliberately narrowed to avoid interfering with React's own scheduler, plus manually-controlled promises, to hold a draft read open indefinitely and assert no premature write occurs) covers: no write while an existing-draft read is pending, full restoration of every field (including edit-copy/reversal provenance) once it resolves; fresh-session defaults applied once, then normal autosave; an editable-copy and a reversed-copy draft both surviving a delayed read with zero premature writes and (for the reversed case) zero routing-provider requests; hydration failure blocking autosave and showing the accessible failure state, with a successful retry recovering; a hydration result that resolves after unmount doing nothing; a rider edit landing before a slow restore-branch read resolves taking precedence over that read's own content; the Settings avoid-ferries default still applying even when a waypoint edit lands before the fresh-session preferences read resolves (the regression above, now pinned as an explicit test); and a plain post-hydration edit continuing to debounce-save normally. `e2e/planning.spec.ts` gained one new reload-recovery test for a plain, hand-built (non-edit-copy) Planning draft — the one reload-recovery path not already covered; `e2e/reverseRoute.spec.ts`'s existing `"reloading after creating the reverse draft recovers the draft and notice with no automatic routing request"` test (around line 521) already proves the edit-copy/reverse-seeded reload path and needed no changes, and passes unchanged against this slice's rewritten hydration effect.
    - No routing endpoint/headers/payload/profile, leg caching, GPX behaviour, route-reversal semantics, map camera behaviour, gradient/warning/elevation calculation, saved-route data, service-worker caching, or CI workflow changed. Version bumped to 0.3.19 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established 0.3.17→0.3.18 precedent). This is a pure logic/state-integrity fix with a small, additive accessible-status/error UI; real-device iPhone verification of the new loading/failure/retry UI is still outstanding — do not treat it as manually verified until that check has actually been done.

32. **`ridingFinishAndEnd.spec.ts`'s completion-detection test: an unconfirmed CPU-contention flake**
    - Found 2026-08-12 while hardening the four `page.waitForTimeout(300)` sites (commit `c243100`, atop `9372991`). Under heavy artificial contention (`taskset -c 0-3`, 4-6 Playwright workers pinned to 4 cores), `e2e/ridingFinishAndEnd.spec.ts`'s "conservatively confirms route completion only after consecutive fixes, and Finish ride clears recovery state" test occasionally failed at `await expect(page.getByText("Route complete")).toBeVisible()` (a 5000ms timeout, element not found), after the test's second consecutive finish-line geolocation fix.
    - Reproduced identically on the pristine pre-`c243100` baseline (`git stash`, rebuilt, re-run) under the same contention — 2/30 failures, same assertion, same line — confirming this predates and is unrelated to that commit's own persistence-polling changes. Never reproduced under ordinary, non-contended concurrency across 150+ runs of the same test.
    - Not investigated further: the reproduction depended on artificial contention deliberately introduced to stress-test unrelated code, not a condition either normal local runs or CI would ordinarily hit. Investigate only if this exact assertion is seen failing in an ordinary local run or in a real GitHub Actions run.

### Navigation and library interface

33. **Compact Route Library grouping — done**
    - Follow-up to item 20 (Route pinning). Remove the `Pinned`/`Other routes` `<h2>` headings (`RouteLibrary.tsx`) — the pin toggle itself (`.route-pin-toggle`/`.is-pinned`, item 20) already communicates pin state, and the headings cost vertical space without adding information. Preserve the existing pinned-first ordering, pin-recency-within-pinned, `Most recent`/`Name A–Z` applied only within the unpinned group, and all existing search/sort/rename/export/delete behaviour (`selectRouteLibraryGroups`, `routeLibraryView.ts`) exactly as item 20 built it.
    - Required outcome: no visible `Pinned`/`Other routes` headings; pinned-first ordering, pin-recency-within-pinned, and existing sort within unpinned all unchanged; pin/unpin toggle, its `aria-pressed`/accessible label, and focus behaviour after pin/unpin preserved, with no regression even if the underlying DOM structure changes; search, sort, rename, export, delete, scroll restoration and duplicate-name handling unchanged.
    - **Implementation (supersedes the two-`<ul>` architecture and open `pendingPinFocusIdRef` question this item originally recorded above):** `RouteLibrary.tsx`'s render block became a single `<ul className="route-list">{viewRoutes.map(renderCard)}</ul>` — `viewRoutes` (`[...groups.pinned, ...groups.unpinned]`) was already the correctly-ordered flattened array feeding scroll restoration, rename-orphan focus and `computeFocusRouteIdAfterDelete`, so none of those needed to change. `groups`/`selectRouteLibraryGroups` are unchanged: pinned and unpinned still need their own sort policy (pin recency vs. the rider's chosen order) before the caller flattens them into one render order. `.route-list`'s existing `gap: var(--space-12)` now governs the pinned/unpinned boundary automatically, with no special-cased extra gap — confirmed both by a real-browser `getBoundingClientRect()` gap measurement (12px uniformly, including across the boundary) and a manual screenshot at the 390×844 phone viewport with a pinned/unpinned mix.
    - **`pendingPinFocusIdRef` was kept, not removed — but for a different, previously-undocumented reason than the "two separate `<ul>` parents" rationale this item originally suspected.** The single-list restructure was tried first: removing the ref/effect/`pinButtonRefs` Map/`pinButtonRef` prop and re-running the full pinning suite. Every jsdom/RTL test passed (108/108, including two new tests added specifically to exercise a pin and an unpin crossing a non-empty-pinned/non-empty-unpinned boundary) — jsdom does not reproduce a real browser's native rule that a focused control is blurred automatically the instant it becomes `disabled`, so it couldn't have caught the actual defect. A real-browser Playwright run of the same scenario (`e2e/routeLibraryPinning.spec.ts`) failed immediately (`toBeFocused()` — "Received: inactive"): the pin toggle is `disabled` for the duration of the write, so the browser blurs it on click regardless of DOM structure, and nothing re-focuses it once the mechanism is removed. The mechanism was restored. A second, genuinely pre-existing defect (present before this slice, just never caught by any prior real-browser focus assertion) was found and fixed at the same time, confirmed via direct DOM-state instrumentation rather than guessed: the routes live-query update (which reruns the focus effect, keyed on `groups`) and the `pinPendingIds` state clear (which re-enables the button) are two independent async updates with no guaranteed order, and the live query consistently landed first in this build — so the effect fired and called `.focus()` while the button was _still_ disabled, silently no-opped, and consumed the pending marker with nothing left to retry it. Fixed by adding `pinPendingIds` to the effect's own dependency array and checking `!button.disabled` before consuming the marker, so it retries on the next relevant render instead of giving up — verified via 5 repeated real-Chromium runs of both pinning e2e tests (10/10 passed) after the fix, against a reliable, repeatable failure before it. Both doc comments (`RouteLibrary.tsx`'s effect, `RouteListItem.tsx`'s `pinButtonRef` prop) were rewritten to state this evidence-backed cause, not the original "two separate list parents" premise, which the single-list restructure now makes moot.
    - The delete-confirmation heading (`RouteListItem.tsx`) changed from `<h3>` to `<h2>`, since it previously sat one level beneath the now-removed group `<h2>`s and would otherwise skip a level beneath the page's own single `<h1>` ("Routes"). `index.css`'s `.route-delete-confirm h3` selector and its comment were updated to `.route-delete-confirm h2` accordingly; `role="alertdialog"`/`aria-labelledby`/`aria-describedby`/focus/Escape/Cancel/disabled-while-deleting behaviour is unchanged, since all of it is id- or text-based, not tag-based.
    - Files changed: `src/ui/library/RouteLibrary.tsx`, `src/ui/library/RouteListItem.tsx`, `src/index.css`, `src/ui/library/routeLibraryView.ts` (light doc-comment edit only, no functional change), `src/ui/library/RouteLibrary.test.tsx`, `e2e/routeLibraryPinning.spec.ts`.
    - Verification: focused suites (`RouteLibrary.test.tsx`, `RouteListItem.test.tsx`, `routeLibraryView.test.ts` — 108/108) and the full repository suite (`npm run lint`, `npm run typecheck`, `npm run build`, `npm test` — 2194/2194 across 120 files, `npm run e2e` — 98/98 across both the `chromium` and `android-chrome` projects including the two updated pinning tests, `npm run format:check`) all pass. Real-device iPhone verification of this slice's visual result has not happened — do not treat it as manually verified until that check has actually been done.

34. **iPhone navigation chrome opacity and waypoint-row layout correction — done**
    - Presentation-only correction, evidenced on the installed iPhone Home Screen PWA in portrait orientation (screenshots reviewed 2026-08-12). Two related but independently testable defects, both now fixed.
    - **Sticky-header safe-area opacity — evidence-backed root cause.** Follow-up to item 24 (Always-sticky compact primary navigation). `index.css`'s `.app-header--sticky` rule pinned itself at `top: env(safe-area-inset-top, 0px)`, so its own opaque `background` began _below_ the status-bar strip, not at it. `.app-shell`'s own `padding-top: env(safe-area-inset-top, 0px)` only reserves that strip once, at the very start of the document flow; once the page is scrolled past that one-time reservation, nothing continued to protect `viewport-y ∈ [0, inset)` — whatever page content had scrolled to that screen position painted there instead, exactly matching the observed "scrolled content visible through the strip" symptom. `.app-shell` itself has no `background` of its own, so there was no ancestor backstop either.
    - **First attempt, tried and empirically disproven — recorded for anyone revisiting this.** The first fix attempted was a negative-margin trick: give `.app-header--sticky` `margin-top: calc(-1 * inset)` to algebraically cancel `.app-shell`'s own padding-top contribution, keeping `top` unchanged. A minimal, framework-free repro (`position: sticky`, first child, ancestor `padding-top: 59px`, `margin-top: -59px`, `top: 59px`) disproved this directly: the rendered box stayed pinned at `y = 59` at every scroll position, never `y = 0` — Chromium does not fold a sticky element's own negative margin into the "flow position" its stickiness offset is computed from, unlike an ordinary static/relative box (confirmed by the identical repro with `position: static`, which correctly rendered at `y = 0`). This is a genuine browser behaviour, not a spec detail this file got wrong — do not reintroduce a negative-margin cancellation for a sticky element without re-testing it directly first.
    - **Shipped fix.** A new project-owned custom property, `--safe-area-inset-top: env(safe-area-inset-top, 0px)` (`:root`), is shared by `.app-shell` and `.app-header--sticky` so the two always agree and the value is independently testable. `.app-shell` gained `.app-shell:has(> .app-header--sticky) { padding-top: 0; }` — removing the ancestor's top padding entirely whenever a sticky header is present (`:has()` is supported since Chrome 105/Safari 15.4, comfortably below this project's target platforms), rather than trying to algebraically cancel it. `.app-header--sticky` folds that same inset directly into its own `padding-top: calc(var(--safe-area-inset-top) + var(--space-8))` and sets `top: 0` — with no ancestor padding left to overcome, the header's natural flow position already sits at the true viewport top, so `top: 0` needs no offsetting trick and is continuous by construction (re-verified with the same minimal-repro method: zero ancestor padding, sticky `top: 0`, the inset folded into the sticky element's own padding-top, stayed pinned at `y = 0` across every scroll position tested, both at rest and once genuinely stuck). `.main-nav`'s own visual position is unchanged (the same total offset as before, `inset + space-8`, just sourced entirely from the header's own box instead of split between the ancestor and header). A new `padding-bottom: var(--space-8)` on `.app-header--sticky` only is the required lower buffer — the base `header` rule (used by active Riding's static header) is untouched, so that header keeps its original zero bottom padding and normal document-flow behaviour exactly as item 24 specifies. On a non-notched device/browser, `--safe-area-inset-top` resolves to `0px`, so every new declaration is a complete no-op there.
    - **Making it testable.** Desktop Chromium and the `android-chrome` Playwright project both always resolve `env(safe-area-inset-top)` to `0px` and cannot emulate iOS standalone status-bar compositing, so `e2e/stickyNavigation.spec.ts` gained a new `"synthetic safe-area inset (iOS status-bar strip coverage)"` group that overrides `--safe-area-inset-top` with a representative non-zero value (59px) via an inline style set through `page.addInitScript`. This surfaced a second genuine, empirically-confirmed finding: `document.documentElement` is not always present yet when an `addInitScript` callback first runs (a bare `document.documentElement.style.setProperty(...)` there threw `Cannot read properties of null`, silently making the whole override a no-op with no visible test failure until the geometry assertions caught it) — fixed with a `MutationObserver` on `document` itself (which always exists) that applies the property the instant `<html>` is inserted, well before this app's own deferred module script can run. The new tests prove: the header's opaque box starts at the true viewport top under the synthetic inset; `.main-nav` still sits below `inset + --space-8`; a `--space-8`-scale gap separates `.main-nav` from the header's own border; the header's background is genuinely opaque; the box stays pinned after a real document scroll; ordinary screens stay sticky under the synthetic inset; and active Riding stays static, with its `padding-bottom` unaffected by the sticky-only buffer. Both new tests were confirmed, by temporarily reverting only the CSS fix and rebuilding, to fail against the pre-fix rules with the exact predicted symptom (`.main-nav`'s bounding box landing at the old, unpatched offset) — genuine regression detectors, not vacuous assertions. This is structural, Chromium-verified evidence of the CSS geometry only, not proof of the real iOS Safari/WKWebView status-bar compositor result, which remains an outstanding real-device check.
    - **Waypoint-row wrapping — confirmed root cause.** Follow-up to Milestone 3's fourth slice (`WaypointList` markup) and item 17. `.waypoint-row-main` was `display: flex; flex-wrap: wrap` over three children — the shrinkable `.waypoint-row-select`, the fixed `.waypoint-row-reorder`, and a standalone Delete button with no `flex-shrink`/`flex-basis` reservation of its own. Once the row's available width fell short of all three at their natural sizes, `flex-wrap` pushed Delete (the last flex item) onto its own line. The previously-recorded bold-selected-label width increase (`.waypoint-row.is-selected .waypoint-row-label { font-weight: 700 }`) is confirmed as a genuine contributor (it narrows the margin before the threshold is crossed) but not the sole cause: the structural defect is that `flex-wrap: wrap` permitted wrapping at all for a row that must always stay single-line.
    - **Fix.** `.waypoint-row-main` changed from `display: flex; flex-wrap: wrap` to `display: grid; grid-template-columns: minmax(0, 1fr) auto auto;` — the entire code change. With exactly three fixed grid children mapped 1:1 onto three explicit columns, there is no `flex-wrap` mechanism left to trigger and no implicit second row, so Delete cannot wrap regardless of waypoint count or selection state. Column 1 absorbs available width via the unchanged `.waypoint-row-select { min-width: 0 }` (which does the same job for a grid item that it previously did for a flex item); columns 2/3 size to the reorder group's and Delete's own natural (≥44px touch-target) content width. No markup changed in `WaypointList.tsx`; `.waypoint-row-relocate` (the selected row's Move/Insert-after sub-row) is a sibling of `.waypoint-row-main`, not a grid child, and is completely unaffected.
    - **Testing.** `WaypointList.test.tsx` gained a separate 12-waypoint fixture (independent of the existing 3-item `WAYPOINTS`, which is untouched) with accessible-name and endpoint-disabled coverage at double-digit ordinals, unselected and selected — Vitest's `css: false` environment cannot observe the grid layout itself, so this only extends existing accessible-name coverage, not a no-wrap proof. The actual no-wrap proof is a new `e2e/planning.spec.ts` test at the 390×844 phone viewport: 12 waypoints placed via direct map taps (no OpenRouteService contact — Calculate route is a separate, explicit action), with genuine `getBoundingClientRect()`-style geometry proving `.waypoint-row-main`'s Select/Move-up/Move-down/Delete controls occupy the same vertical band and the wrapper is no taller than its tallest child, for both an unselected double-digit row and the selected final row, plus that the relocate sub-row sits below without disturbing the main row's own geometry, plus 44×44 touch-target and no-horizontal-overflow checks. Confirmed, by the same temporarily-reverted-CSS method used for Scope A, to fail against the pre-fix rules (the wrapped row rendered at roughly double a single control's height) — a genuine regression detector.
    - Files changed: `src/index.css`, `e2e/stickyNavigation.spec.ts`, `e2e/planning.spec.ts`, `src/ui/planning/WaypointList.test.tsx`, plus this entry and the version bump below. No changes to `App.tsx`, `MainNavigation.tsx`, `navPositionMode.ts`, `WaypointList.tsx`, `PlanningScreen.tsx`, or `e2e/androidMobileLayout.spec.ts` (re-run unmodified as a regression check, since the CSS is shared across both Playwright projects).
    - Verification: focused suites (`App.test.tsx`, `MainNavigation.test.tsx`, `navPositionMode.test.ts`, `WaypointList.test.tsx`, `e2e/stickyNavigation.spec.ts`, the new `e2e/planning.spec.ts` waypoint test, `e2e/androidMobileLayout.spec.ts`) and the full repository suite (`npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` — 2197/2197 across 120 files, `npm run e2e` — 102/102 across both the `chromium` and `android-chrome` projects) all pass. Version bumped to 0.3.24 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched).
    - Real-device iPhone verification of the corrected safe-area strip, including the real iOS Safari/WKWebView status-bar compositor, and of the waypoint-row layout at double-digit counts, is now confirmed — checked on the installed iPhone Home Screen PWA in portrait orientation, most recently at v0.3.25 (see "Manual acceptance status" above). Real Android Chrome verification (distinct from Playwright's `android-chrome` Chromium-emulated project) remains outstanding for both — do not treat either as Android-verified until that check has actually been done.
    - **Corrective follow-up (ordinal 10's endpoint badge still wrapped) — done.** Real iPhone testing after the fix above found one remaining defect the 12-waypoint e2e fixture above never covered: inside column 1 of `.waypoint-row-main` (`.waypoint-row-select`, holding the ordinal badge and the label), "Waypoint 10" wrapped onto two lines specifically when ordinal 10 was an endpoint (white `--finish` or green `--start-finish` badge, `border: 3px solid`) but not when it was an ordinary intermediate waypoint (`border: 2px solid`) — confirmed root cause: the endpoint badge's extra 1px of border per side (+2px total width at a two-digit ordinal) was just enough to push the label past its available shrink space, given `.waypoint-row-select` still inherited the global `button` rule's full `var(--space-8) var(--space-16)` padding and had no internal column structure of its own beyond plain flex. **Fix.** `.waypoint-row-select` became a fixed 2-column grid (`grid-template-columns: 32px minmax(0, 1fr)`, the 32px slot measured directly against the widest real badge — `.waypoint-row-ordinal--finish`/`--start-finish` at a two-digit ordinal, ~27–31px across the font stacks measured — with a small safety margin) — a constant badge-slot width immune to badge-role/border-width variance, so the label always starts at the same x position and gets the same available width regardless of which of the four roles renders; `.waypoint-row-ordinal` gained one added declaration, `justify-self: center`, to keep the badge centred instead of Grid's default stretch-to-fill distorting its circular/rounded-square shape. A real-browser measurement (this button's own font-size, 16px regular weight) found "Waypoint 10" needs ~100px on one line, more than an 8px-per-side padding plus an 8px badge-label gap would leave inside this button's own fixed ~153px grid-track width (dictated externally by `.waypoint-row-main`'s other two columns, untouched) — no combination of gap and badge-slot width closes that shortfall while padding stays at 8px per side, so both the horizontal padding and the internal badge-label gap were reduced to `var(--space-4)` (4px) instead of the originally-proposed `var(--space-8)` (8px), a measured deviation from the initial estimate, chosen because the single-line requirement is the actual outcome that matters; 4px already has a direct precedent in this same file (`.waypoint-row-reorder`'s own inter-button gap). Vertical padding was left entirely unstated (not the shorthand, and not `.waypoint-row-icon-button`'s zero-vertical-padding pattern), so it stays genuinely inherited and the existing row/touch-target vertical geometry is provably unchanged. The dead `flex: 1 1 auto` (a no-op since the fix above made the parent `display: grid`) was removed. **Testing.** A new `e2e/planning.spec.ts` phone-viewport test proves all three ordinal-10 roles (`start-finish` via Return to start, `finish` on a genuinely new open-route 10th waypoint after Undo, `ordinary` once an 11th waypoint is added) stay single-line — via a label-height-vs-known-single-line-label structural comparison, not a hardcoded pixel constant — stay within their own button, stay in the same vertical band as Move/Delete, keep 44×44 touch targets, and that the label's own left edge lands at the same x position across all three roles, proving the fixed badge slot. Confirmed, by the same temporarily-reverted-CSS method used above, to fail against the pre-fix rules with the exact predicted symptom (the label's rendered height roughly doubling, from a genuine text wrap at the space in "Waypoint 10") — a genuine regression detector, not a vacuous assertion, in this same Chromium build. `.waypoint-row-main`'s own 3-column grid and every `.waypoint-row-ordinal`/role-modifier rule's existing visual declaration are unchanged; `WaypointList.tsx` was not touched. Files changed: `src/index.css`, `e2e/planning.spec.ts`, plus this entry and the version bump below. Verification: focused suites (`WaypointList.test.tsx`, `PlanningScreen.test.tsx` — 118/118 unchanged) and the full repository suite (`npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test`, `npm run e2e` across both the `chromium` and `android-chrome` projects) all pass. Version bumped to 0.3.25. **Real-device iPhone verification of this corrective fix is now confirmed.** Checked on the installed iPhone Home Screen PWA in portrait orientation at the deployed v0.3.25 (commit `7fae104`): the centred fixed-width badge slot and aligned labels look correct, waypoint labels stay single-line through waypoint 106 — including endpoint and closed-loop start/finish badge roles, well beyond this slice's own three-state automated proof — and the three-digit layout remains acceptably readable (see "Manual acceptance status" above). Real Android Chrome verification (distinct from Playwright's `android-chrome` Chromium-emulated project, re-run here only as an unrelated regression check) remains separately outstanding — do not treat this corrective slice as Android-verified until that check has actually been done.

### Planning draft workflow

35. **One-time Planning camera fit for restored or seeded waypoints — done**
    - Follow-up to Milestone 3's second slice (the existing fit-once-per-draft-on-first-calculated-route behaviour, `usePlanningRoute.ts`'s `isFirstRouteForDraft`) and to item 26 (Edit an existing route as a new Planning draft). Confirmed root cause matched this item's own original description: no "fit to waypoints" logic existed anywhere in Planning; the only pre-calculation camera framing was the existing one-time ~50×50 km regional box (`localAreaBounds.ts`), explicitly skipped whenever `waypointsRef.current.length > 0` — true for every restored draft and every edit-copy/reverse-copy seed. A restored/seeded draft genuinely fell through to MapLibre's raw whole-world default camera until a route was calculated.
    - **Mechanism.** `PlanningScreen.tsx`'s draft-hydration effect's restore branch now computes, alongside its existing `dispatch`/`setRouteName`/`setAvoidFerries`/`setProfile`/`setEditCopyMeta` calls (still gated by the same `!hasUserModifiedDraftFieldsRef.current` check), the candidate bounds for these restored/seeded waypoints via a new pure helper, `computeWaypointHydrationBounds(waypoints)`: a single waypoint reuses `computeLocalAreaBounds` (the same ~50 km "frame around one coordinate" abstraction the fresh-session effect and Locate-me already use — chosen deliberately over `computeBoundingBox` on one coordinate, which returns a degenerate zero-area box that `fitBounds`'s hardcoded `maxZoom: 16` would resolve into an unhelpfully tight, marginless zoom-in); two or more waypoints use `computeBoundingBox` (`map/routeLayer.ts`), the same coordinate-envelope helper the calculated-route fit already uses. Since calculated route geometry is never persisted in the draft, this candidate is stored in a new `pendingWaypointHydrationBounds` state (`useState<BoundingBox | null>`, never a ref, specifically so setting it triggers React to re-run the effect that consults it) rather than applied directly from the hydration effect.
    - **Where the guard actually lives, and why.** The straightforward design — checking `hasManualCameraActionRef`/`hasAppliedInitialFramingRef` directly inside the hydration effect, right where the candidate bounds are computed — was implemented first and discovered, empirically, to trip `eslint-plugin-react-hooks@7`'s `react-hooks/immutability` rule (part of `recommended-latest`, the React Compiler's own static soundness analysis): reading a ref's `.current` inside **two separate** `useEffect` bodies, when that ref is also mutated from plain callbacks (`handleLocateMe`, `MapView`'s `onUserCameraInteraction` prop), is flagged as unsound, because the analysis can no longer verify the two mutation sites stay consistent across independently-scheduled effects — confirmed by isolated reproduction (a minimal two-effect/one-callback-write probe did **not** reproduce it in isolation, but reverting to a single check-site in the real component made the flagged errors disappear and reappear deterministically as each ref's second read site was added/removed). The existing fresh-session regional-fit effect was already the _sole_ reader of `hasManualCameraActionRef`/`hasAppliedInitialFramingRef` in production code; rather than duplicating either ref (which would create two objects meant to represent the same fact, an anti-pattern this codebase explicitly avoids elsewhere — see `editCopyMeta`'s own "held as one nullable object... so the two fields can never drift out of sync" precedent), the actual guard-and-apply logic was moved into that same effect, extended with a new early branch: when `waypointsRef.current.length > 0` (previously an unconditional early return), it now checks `pendingWaypointHydrationBounds && !hasManualCameraActionRef.current && !hasAppliedInitialFramingRef.current` before calling `setBoundsTarget`, reusing the effect's own pre-existing `hasRequestedInitialLocationRef` "run my meaningful body once per mount" guard for both concerns rather than adding a second one. This keeps `hasManualCameraActionRef`/`hasAppliedInitialFramingRef` read from exactly one effect, satisfying the linter honestly rather than suppressing it, and is a smaller, more centralised diff than the original design — the fresh-session effect is now the single place all three initial-framing paths (regional geolocation fit, Locate-me, restored/seeded-waypoint fit) are decided.
    - **Guard correctness.** `hasManualCameraActionRef` is set synchronously at the very top of `handleLocateMe` (before its own `await`) and inside `onUserCameraInteraction` for any real gesture — so a rider who gestures or presses Locate-me before a slower hydration read resolves is already correctly excluded, with no new ref needed; this covers a real (not hypothetical) race, since `hydrationStatus === "loading"` never disables the map or the Locate-me button. `hasAppliedInitialFramingRef` is both checked (defensive — it cannot already be true when this branch first runs, since the restore branch and the fresh-session geolocation path are mutually exclusive within one mount) and _set_ here, which is what makes a later Locate-me press correctly recentre (`handleLocateMe`'s own `shouldApplyInitialFraming` check) rather than re-doing a fresh regional box-fit — the literal "integrate with `hasAppliedInitialFramingRef`" requirement. `hasUserModifiedDraftFieldsRef` (the pre-existing enclosing guard) already covers "the rider placed their own waypoint(s) before a discarded restore lands": in that case `pendingWaypointHydrationBounds` is never set, so the fresh-session effect's new branch sees `waypointsRef.current.length > 0` (the rider's own waypoint) but a null `pendingWaypointHydrationBounds`, and correctly applies no fit — the rider's own camera at the moment of placement is retained, exactly as a fresh session's manually-placed first waypoint already was. No coordination was needed with the existing calculated-route fit (`usePlanningRoute`'s `isFirstRouteForDraft` → `suppressInitialOverviewFit` → `MapView`'s own `computeBoundingBox(points...)` effect): that mechanism only ever fires once `routing.state.kind === "routed"`, which requires an explicit Calculate press and so is structurally always strictly later than hydration-resolve time — the two fits are temporally and architecturally disjoint, confirmed by a dedicated test proving exactly two `fitBounds` calls (waypoint envelope, then calculated-route geometry, with different bounds) and no third on a later edit-triggered recalculation.
    - **No `MapView.tsx` runtime change needed.** All four camera-command dedup refs (including `lastAppliedBoundsRequestIdRef`) are reset to `null` at the top of the map-creation effect on every `retryToken`-driven recreation, and the caller's own `boundsTarget` React state is untouched by a remount — so the still-current waypoint fit is automatically, correctly reapplied once a replacement map instance's style becomes ready, with zero new code, a generic property of the existing mechanism already relied on by the pre-existing fresh-session/Locate-me fits. Only two doc-comment updates were made there (`BoundsCameraTarget`'s interface comment and its consuming effect's comment), enumerating the new third caller, matching this file's own "enumerate every caller" documentation convention (see `CameraTarget`'s own comment) — no logic changed.
    - **One pre-existing, documented-not-fixed edge case.** Because `boundsTarget` is never cleared after being applied, if the rider manually pans _after_ any of the three fits (fresh-session, Locate-me, or this new restored-waypoint one) and _then_ a map-instance recreation happens (an imagery retry or fallback-style swap), `MapView` re-applies the stale old `boundsTarget`, overriding the manual pan. This is already true today for the two pre-existing fits — not a regression introduced by this slice — and was deliberately left unfixed, matching the item's own explicit "do not casually change MapView's established retry/fallback behaviour... do not expand this slice into a general camera-state rewrite" instruction.
    - **Testing.** `src/ui/planning/PlanningScreen.draftHydration.test.tsx` (extended with a `fitBoundsSpy` on its previously all-stub mock map): fresh-empty-draft and first-waypoint-placed-manually cases show no fit; the rider's-own-edit-wins case shows no fit for the discarded stored waypoints; the pre-existing unmount-discards-a-late-result case now also asserts no fit; a failed-first-read-then-successful-retry case asserts exactly one fit with the correct bounds (a genuinely stale _second_ generation from mid-flight retry-after-retry isn't constructible in this architecture, since Retry is UI-reachable only after a real rejection and a rejected promise can never also later resolve — the unmount case, sharing the identical generation-guard code path, stands in for "stale/superseded never frames"). `src/ui/planning/PlanningScreen.test.tsx` (real Dexie-backed, real-timer `waitFor`): an ordinary two-waypoint restored draft fits once to `computeBoundingBox`; a single restored waypoint fits to `computeLocalAreaBounds`; all four edit-copy/reverse-copy notice tests (exact/derived origin, forward/reverse operation — one reverse variant, since the mechanism doesn't discriminate on `operation`) now also assert the fit; a dedicated test proves no refit across append/select/deselect/undo/redo/delete after the initial restore; a dedicated test proves the sequencing (one waypoint fit, then one calculated-route fit with different bounds, then no third fit on an edit-triggered recalculation); a rider-gesture-before-hydration-resolves test and a rider-presses-Locate-me-before-hydration-resolves test (both landing synchronously, before the real fake-indexeddb-backed `getDraft()` read has any chance to settle) prove the restored-waypoint fit never fires and, for the Locate-me case, that Locate-me's own bounds apply instead — deterministic regardless of which of the two real promises (draft read vs. mocked geolocation) happens to resolve first, since the synchronous ref write always precedes both. **Regression proof**: the new restore-branch `setPendingWaypointHydrationBounds` call was temporarily removed (production code only, tests untouched); the "ordinary restored draft fits once" test then failed with exactly the predicted symptom (no `fitBounds` call — camera stays at MapLibre's default); the implementation was restored and the full suite re-verified green before committing.
    - **Real-browser proof**, using the local deterministic map style (never live map/routing services): `e2e/planning.spec.ts`'s existing reload-recovery test now also asserts, after reload, that both waypoint markers' real DOM bounding boxes lie fully inside the map container, that `data-camera-center`/`data-camera-zoom` are populated (not MapLibre's un-settled default), and — via a genuine manual pan through MapLibre's own deterministic `KeyboardHandler` (an unmodified `ArrowRight`, mirroring the file's existing "Locate me recentres..." test's technique) followed by placing a third waypoint — that the camera is unaffected by that later, unrelated edit; a new single-waypoint variant proves the local-area-box framing (asserting `data-camera-zoom` stays comfortably below `fitBounds`'s `maxZoom: 16`, ruling out a degenerate fit) and marker containment. `e2e/editRouteAsPlanningCopy.spec.ts` and `e2e/reverseRoute.spec.ts` each gained a local `isFullyWithin` helper (duplicated per this project's established no-shared-e2e-helpers-across-specs convention) and the identical marker-containment/camera-settled/manual-pan-preservation proof, inserted into their own main "Edit copy in Planning"/"Reverse route" flow tests — previously, both files navigated straight into a waypoint-populated `PlanningScreen` with zero camera assertions at all. One genuine, harmless behaviour change was confirmed rather than assumed: `editRouteAsPlanningCopy.spec.ts`'s existing raw pixel `mapContainer.click({x:260,y:190})` (appending a third waypoint) now lands against the newly-framed camera instead of MapLibre's previous unframed default — re-run explicitly as part of verification and confirmed still passing, since no assertion in that test pins an absolute coordinate.
    - Files changed: `src/ui/planning/PlanningScreen.tsx`, `src/map/MapView.tsx` (doc comments only), `src/ui/planning/PlanningScreen.draftHydration.test.tsx`, `src/ui/planning/PlanningScreen.test.tsx`, `e2e/planning.spec.ts`, `e2e/editRouteAsPlanningCopy.spec.ts`, `e2e/reverseRoute.spec.ts`. `usePlanningRoute.ts`, `mapAdapter.ts` and `localAreaBounds.ts` were read but not modified — no genuine need was found.
    - Verification: focused suites (`PlanningScreen.draftHydration.test.tsx`, `PlanningScreen.test.tsx`, `usePlanningRoute.test.ts`, `MapView.test.tsx`) and the full repository suite (`npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` — 2205/2205 across 120 files, `npm run e2e` — 104/104 across both the `chromium` (92) and `android-chrome` (12) projects) all pass. `planning.spec.ts`/`editRouteAsPlanningCopy.spec.ts`/`reverseRoute.spec.ts` are structurally excluded from the `android-chrome` project by this repo's own `android*.spec.ts` filename-matching convention (`playwright.config.ts`), so this item's new browser coverage — like the rest of these three files — only ever runs under `chromium`; this is a pre-existing project-selection limitation, not a gap introduced here. Version bumped to 0.3.26 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched).
    - Real-device iPhone verification is now confirmed (13 August 2026): checked on the installed iPhone Home Screen PWA once commit `b8cec2c`'s deploy reached production — the first deploy that actually carried this item's changes live, since `0665d00`'s own deploy attempt was blocked by the unrelated E2E precondition failure item 44 records — the restored/seeded-waypoint Planning view no longer opens at MapLibre's whole-world default zoom and is now framed appropriately. Physical Android verification remains outstanding — per item 43's ledger, do not treat this as Android-verified until that check has actually been done. See also "Manual acceptance status" above.

36. **Compact per-draft routing preferences and reordered Calculate action — done**
    - Follow-up to item 14 (Selectable Planning cycling profiles), item 15 (Avoid ferries by default) and Milestone 4's fourth visual-migration slice (the previous `Route options` panel). Supersedes that panel's _prominence_ and location, not its underlying data model — `RoutingProfile`, `avoidFerries`, `computeRouteCalculationFingerprint`/`isStale`/`canSaveOrExportPlan` (`usePlanningRoute.ts`) are byte-identical to before; this was a presentation and Settings-defaults change only, plus one hydration-coordination redesign it made necessary (below).
    - **Storage/defaulting.** `StoredPlanningPreferences` (`storage/db.ts`) gained one plain, optional, non-indexed field, `profileByDefault?: string`, alongside the existing `avoidFerriesByDefault` — no Dexie version bump, following the exact precedent every other additive field on this table and on `StoredPlanningDraft`/`StoredRideState` already established. `PlanningPreferences`/`fromStoredPlanningPreferences`/`toStoredPlanningPreferences` (`storage/mapping.ts`) resolve it via a real `isRoutingProfile` validity check (mirroring `fromStoredPlanningDraft`'s own `profile` handling), never a bare `??` — a missing, corrupt or future-unknown stored value defaults to `DEFAULT_ROUTING_PROFILE` (`cycling-road`). `planningPreferencesRepository.ts` needed no code change at all, since `getPlanningPreferences`/`savePlanningPreferences` were already generic over the whole `PlanningPreferences` shape.
    - **Settings** (`Route planning` panel, `SettingsScreen.tsx`) gained a `Default cycling profile` control — a `role="group"` pair of buttons (`ROUTING_PROFILES`, `aria-pressed`, `.cycling-profile-group`/`.cycling-profile-button`/`.is-selected`, all reused verbatim from Planning's own existing pattern, no new CSS) with a visible `<p className="setting-row-title">` label (`aria-labelledby`), placed above the existing `Avoid ferries by default` checkbox. Both controls share one writer, `savePreferences(next: PlanningPreferences)`, so every save carries the _complete_ resolved object (never a partial one) — the concrete mechanism behind "a profile change must never reset the ferry default, and vice versa": each handler reads the other field's current resolved value into the write. A synchronous `isSavingPreferencesRef` guard (mirroring `isSavingRef`/`isLocatingRef` elsewhere in this codebase) plus `disabled={isSavingPreferences}` on both controls together prevent two overlapping writes from clobbering one another — proven by a dedicated test that toggles ferries then immediately changes profile and asserts neither write reverted the other. Non-optimistic/failure/Saving-status behaviour is unchanged from the pre-existing ferries checkbox, just generalised to cover both fields with one shared error/status pair. `routing/routingProfiles.ts`'s `cycling-road` description lost its now-false "The default profile for every new plan." claim (the road-bike routing description itself, and `DEFAULT_ROUTING_PROFILE` as the fallback when nothing is stored, are unchanged) — `isDefault`/`ROUTING_PROFILES` still key off `DEFAULT_ROUTING_PROFILE`, unrelated to the new Settings-configurable default.
    - **Fresh-vs-restored draft boundary** (unchanged from items 30/31): the same "no draft row, or a draft row with zero waypoints" boundary. A genuinely fresh draft copies both Settings defaults once, from a single `getPlanningPreferences()` read inside the existing hydration effect; a restored draft (one or more waypoints) always keeps its own stored `profile`/`avoidFerries` exactly as before, never the Settings defaults; an already-open or already-persisted draft is never rewritten, recalculated or invalidated by a later Settings change.
    - **Field-specific hydration coordination.** Before this slice, `avoidFerries` was never directly editable inside Planning, so the single boolean `hasUserModifiedDraftFieldsRef` only ever needed to gate the restore branch atomically. Now that `avoidFerries` is directly editable (like `profile` already was) and both can be touched before the fresh-session `getPlanningPreferences()` read resolves, a coarse boolean could no longer serve both consumers without either overwriting a rider's in-flight choice or suppressing an unrelated Settings default. `hasUserModifiedDraftFieldsRef` is now `Record<"waypoints" | "routeName" | "profile" | "avoidFerries", boolean>`: the restore branch still gates atomically (`Object.values(...).some(Boolean)` — any one of the four fields touched skips the _entire_ restore, preserving "never partially apply stale stored fields"); the fresh-session branch gates `profile`/`avoidFerries` independently against their own flag, so editing one never suppresses the untouched other's Settings default, and never reads the `waypoints`/`routeName` flags at all, so an early waypoint or route-name edit still can't suppress either default (the pre-existing invariant, now proven for `profile` too, not just `avoidFerries`). A `getPlanningPreferences()` rejection on a fresh session leaves `profile`/`avoidFerries` at their safe `useState` fallback values (`cycling-road`/avoided) and still reaches `hydrationStatus: "ready"`, identically to the pre-existing ferries-only behaviour.
    - **Current-draft controls reuse existing staleness gating unchanged.** The profile buttons (now inside the new disclosure) and the new avoid-ferries checkbox both call `setProfile`/`setAvoidFerries` directly — the same live state `usePlanningRoute`'s `computeRouteCalculationFingerprint`/`isStale` already reads every render, so a change marks an existing routed result stale immediately (before the debounce even starts), disables Save/Export, retains the previous result visibly, and clears once the matching recalculation resolves — no new gating logic was written, only new UI entry points into state that already existed. Both controls make zero routing-provider requests before Calculate is pressed explicitly, and switching back to a previously-used profile/ferries combination remains compatible with the existing per-leg cache.
    - **Interaction design.** A native `<details className="settings-disclosure">` (collapsed summary text exactly `Change`, reusing the exact same disclosure class as `How recalculation works` — no new focus-management state, no modal) sits beside the always-visible compact summary (`describeCurrentDraftRoutingSummary`, e.g. "Routing: Road bike · Ferries avoided"). Inside it: a `role="group" aria-label="Cycling profile for this draft"` button pair (mirroring Planning's pre-existing, purely-`aria-label`-driven convention, no separate visible heading needed since the button labels speak for themselves) and a real `<input type="checkbox">` wrapped in `.setting-row`/`.setting-row-checkbox`/`.setting-row-text`/`.setting-row-title` (accessible name "Avoid ferries for this draft", reusing Settings' own checkbox pattern). Nothing about the `<details>` element's mount identity changes when `profile`/`avoidFerries` change, so it never unexpectedly closes on selection; whether it's open is never persisted. `NoApiKeyNotice`'s `onOpenSettings={onNavigateToSettings}` remains the only Settings-navigation affordance left in Planning — no second one was added for the current-draft controls.
    - **Action-card and heading-order result.** `PlanningScreen.tsx`'s previous standalone `role="group" aria-label="Waypoint actions"` row and the entire `Route options` panel (heading included) were replaced by one `.panel.stack.planning-section` action card, positioned directly after the map and before `Waypoints`, containing in order: the unchanged waypoint-actions row (Undo/Redo/Return to start/conditional Add to end); the unchanged `.planning-calculate-actions` block (Calculate button, key status, error, stale-result message, the `How recalculation works` disclosure — none of these ever sit inside a closed disclosure, per this file's own established rule); then the compact routing summary plus the `Change` disclosure. DOM/heading order is now `Plan a route` (h1) → action card (no heading) → `Waypoints` (h2) → conditional `Route overview` (h2) → `Save or export` (h2) — no `Route options` heading exists anywhere.
    - **Testing.** Storage: no-row/legacy-row/corrupt-value defaults, full round-trip of every profile/ferry combination, and a dedicated `db.migration.test.ts` no-schema-bump block for `profileByDefault`, mirroring the existing `editCopySourceRouteId`/`editCopyWaypointsOrigin` template. Settings: default-pressed state, persistence, cross-field preservation, non-optimistic failure, and the disabled-while-saving guard on both controls together. Planning hydration (`PlanningScreen.draftHydration.test.tsx`): all four pending-hydration field-specific scenarios (profile-only edit, ferries-only edit, an early waypoint/route-name edit still letting both defaults seed, and a slow restore still blocked atomically by touching only the new ferries checkbox), plus the preferences-rejection fallback case. `PlanningScreen.test.tsx`: heading-order assertion rewritten (no `Route options`, plus a `compareDocumentPosition` proof that Calculate precedes `Waypoints`), the two tests documenting the old read-only ferries UI removed, the `cycling profile selector` block and every restore/persistence test updated to open `Change` first, and new tests for the avoid-ferries checkbox mirroring the profile selector's own coverage (staleness on change, zero requests before Calculate, leg-cache compatibility, persistence across reload). `e2e/planning.spec.ts`: the existing General-cycling routing test extended to also toggle ferries through the `Change` disclosure and assert the ORS request body omits `avoid_features: ["ferries"]`; two new tests (a fresh draft picking up a General-cycling Settings default with zero requests before Calculate; Calculate's real visual/DOM position above `Waypoints`); the phone-viewport layout test extended to prove the compact summary row and open `Change` disclosure introduce no horizontal overflow and every revealed control keeps a real ≥44×44px touch target.
    - **Resolved tooling note (TypeScript 6.0.3 closure narrowing).** While writing the `e2e/planning.spec.ts` ferries/profile request-body test above, a bare `let` variable (`capturedUrl`/`capturedBody`) reassigned only inside an asynchronous Playwright `page.route` callback and later read, via optional chaining, outside that callback was incorrectly narrowed to `never` at the read site — `expect(capturedBody?.options?.avoid_features ?? [])` failed to typecheck with `Property 'options' does not exist on type 'never'`, even though the value was genuinely assignable. Reproduced in isolation with a minimal file outside this project (`tsc --strict`), confirming it is a real TypeScript 6.0.3 (`~6.0.2` per `package.json`) narrowing defect in this exact shape, not a mistake specific to this file. Resolved, with no loss of coverage, by storing the callback's captured state as properties of one plain object (`const captured: { url: string | null; body: CapturedOrsRequestBody | null } = { url: null, body: null }`, mutated as `captured.url =`/`captured.body =` inside the route callback) instead of two separate `let` bindings — this pattern does not trigger the narrowing bug. See `e2e/planning.spec.ts`'s `captured` object (commit `91bedd4`) for the exact code; this is a test-tooling workaround only; no production code was affected.
    - **Verification.** Focused: storage (`mapping.test.ts`, `planningPreferencesRepository.test.ts`, `db.migration.test.ts`, `routingProfiles.test.ts` — 69 tests), `SettingsScreen.test.tsx` (23), `PlanningScreen.draftHydration.test.tsx` (13), `PlanningScreen.saveAutosaveRace.test.tsx` (10), `PlanningScreen.test.tsx` (108) — all pass. Full: `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` — 2220/2220 across 120 files, `npm run e2e` — 106/106 across both the `chromium` (94) and `android-chrome` (12) projects, `npm run format:check` run last, immediately before committing — all pass. Version bumped to 0.3.27 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - Real-device iPhone and physical Android verification of this slice's visual result have not happened — do not treat it as manually verified until each check under item 43 has actually been done.

37. **Clear the current draft and standardise draft terminology — done**
    - **Terminology.** A fresh, direct-source audit (not a reuse of this item's own earlier example list, which had gone stale — see below) found 8 user-visible strings meaning the mutable, locally autosaved Planning workspace, all renamed "plan"→"draft": `RidingScreen.tsx`'s `EDIT_COPY_CONFIG`/`REVERSE_ROUTE_CONFIG` `dialogTitle`/`dialogMessage` (`"Replace your current plan?"`→`"...draft?"`, `"Replace your current plan to reverse this route?"`→`"...draft..."`, both `dialogMessage`s' `"...unsaved plan in Planning..."`→`"...unsaved draft in Planning..."`), `handleCopyOperationClick`'s catch-path message (`"Your existing plan could not be checked. Try again."`→`"...draft..."`), `PlanningScreen.tsx`'s hydration loading/failure text (`"Loading your plan…"`→`"...draft…"`, `"Your saved plan could not be loaded..."`→`"...draft..."`), and `SettingsScreen.tsx`'s avoid-ferries-by-default hint (`"Used when a new plan is created."`→`"...draft..."`, found fresh — not on this item's own prior example list at all). This item's own previous text cited `PlanningScreen.tsx`'s avoid-ferries hint reading `"Set when this plan was created"` as a confirmed target; that exact string no longer exists anywhere in source (confirmed via `git log -S` — deleted by item 36's own "Compact per-draft routing preferences" refactor, which replaced the whole panel it lived in) — this entry replaces that stale reference with the real, current 8-item list above rather than perpetuating it. `grep -rniE` across every `*.test.ts(x)`/`*.spec.ts` in `src/`/`e2e/` for all 8 exact strings found genuinely zero existing assertions on any of them going in — except one the same sweep initially missed by being too specific (`"unsaved plan in Planning"`, case-sensitive on the wrong casing): `e2e/reverseRoute.spec.ts`'s own `/reversing this route will replace your unsaved plan/i` regex, a real pre-existing test that would have broken, caught only once the full e2e suite was actually run — fixed alongside two harmless, non-assertion comment updates in `reverseRoute.spec.ts`/`editRouteAsPlanningCopy.spec.ts` ("Start an unrelated, unsaved plan" → "...unsaved draft"). Deliberately **not** renamed: `<h1>Plan a route</h1>`/`aria-label="Planning"` (screen/domain name), `RidingScreen.tsx`'s `"Edit copy in Planning"` button label (item 38's own scope), `MainNavigation.tsx`'s `"Plan"` nav label, `SettingsScreen.tsx`'s `"Route planning"` heading, and the `"Planned route"` default _saved-route_ name (a different, larger-blast-radius concept — the default display name of a saved route, not the draft workspace label — with ~30+ existing test assertions across 6 files; out of this item's own scope). `RidingScreen.test.tsx`'s `"Unsaved plan"` string, used only as an arbitrary fixture value for a seeded draft's `routeName` in several tests, is left as-is (test data, not app source).
    - **Clear draft action.** A `.btn-danger` `Clear draft` trigger sits in its own new row at the end of the existing `.panel.stack.planning-section` action card (`PlanningScreen.tsx`, after the routing-summary/`Change`-disclosure row, before the card's own closing tag) — not inside `role="group" aria-label="Waypoint actions"` (a narrower, waypoint-list-specific group) and not folded into "Save or export" (the semantic opposite of a wipe). The shared `ConfirmDialog` is rendered as a top-level sibling immediately after the action card (mirroring `RidingScreen.tsx`'s own End-ride dialog placement), with the exact required copy: title `Clear this draft?`; message "This removes all waypoints, the calculated route and other unsaved draft details. Saved routes are not affected."; confirm label `Clear draft` (`"Clearing…"` while in flight, matching the established `"Saving…"`/`"Ending ride…"`/`"Deleting…"` in-flight-label vocabulary); cancel label `Cancel`.
    - **Race coordination — reuse, not a third mechanism.** `handleClearDraftConfirm` reuses `saveGenerationRef`/`saveTimeoutRef` literally (the same ref objects `handleSave` already declares) to cancel the pending autosave timer and invalidate stale continuations, exactly as `handleSave` itself does — satisfying this item's own "not a third, independently-invented mechanism" requirement to the letter. Clear gets its own re-entrancy/visibility state (`isClearingRef`/`isClearing`/`clearDraftError`/`isClearDraftConfirmOpen`/`clearDraftTriggerRef`), since it must work in states Save cannot (`routing.state.kind !== "routed"`) and the two are made mutually exclusive both directions: `handleSave`'s existing guard gained `|| isClearingRef.current`; the Save button's `disabled` gained `|| isClearing`; the Clear trigger's `disabled` is `isSaving || isClearing`. `clearDraft()` (writes `planningDrafts`) and a fresh `getPlanningPreferences()` read (reads `planningPreferences`) run via `Promise.all` — independent tables, no ordering dependency; the preferences read swallows its own rejection and falls back to the same safe hardcoded defaults `getPlanningPreferences()` itself resolves to when nothing has ever been saved (mirroring the existing fresh-session hydration branch's identical precedent), so a Settings-read hiccup is never reported as "the draft could not be cleared" when the destructive clear itself succeeded. `dispatchWaypointAction({type:"reset", waypoints:[]})` (not raw `dispatch`) both clears the full `waypointHistoryReducer` `past`/`present`/`future`/`selectedWaypointId` history in one action and calls `noteHydrationOverriddenByUserEdit("waypoints")` as a side effect — which is what protects against a still-in-flight _original_ mount-time `getDraft()` read resolving with stale pre-clear content afterward, reusing the existing per-field gate on `hasUserModifiedDraftFieldsRef` with no new protection code (proved directly, not assumed — see Testing).
    - **A real bug found and fixed during this slice's own e2e testing, not merely theorised.** The first implementation gated the profile/ferries reseed the same way the hydration effect's own fresh-session branch does — `if (!hasUserModifiedDraftFieldsRef.current.profile) setProfile(preferences.profileByDefault)` — reasoning by analogy from that existing branch. This was wrong: `hasUserModifiedDraftFieldsRef`'s flags mean "modified since this component _mounted_" and are never reset by an ordinary edit, so a rider who customises routing via the "Change" disclosure at any point before pressing Clear draft — an entirely normal flow, e.g. immediately after arriving via "Edit copy in Planning" — already has one or both flags permanently `true`, silently skipping Clear's own reseed exactly when it matters most: `e2e/clearPlanningDraft.spec.ts`'s main test (which drives the real "Change" disclosure buttons, not a mocked draft) failed with `aria-pressed="false"` on "Road bike" after Clear, revealing the old draft's just-cleared "General cycling" choice had survived. The equivalent `PlanningScreen.clearDraft.test.tsx` case did **not** catch this on its own first pass, because it seeded the draft's non-default profile directly via the mocked `getDraft()` resolution rather than clicking through the real UI controls — `hasUserModifiedDraftFieldsRef` was never actually touched by that path, a concrete demonstration of why this item's own "browser-level proof, not just mocked-call assertions" testing requirement mattered here specifically, not just in the abstract. Fixed by making the reseed **unconditional** — `setProfile(preferences.profileByDefault); setAvoidFerries(preferences.avoidFerriesByDefault);`, no gate at all — since Clear draft is itself the explicit "start over" request and always wins over whatever was set before it; `hasUserModifiedDraftFieldsRef` itself is otherwise left completely untouched by Clear (every other consumer — the original hydration effect's own restore/fresh-session branches — was traced and found to have no scenario where a stale flag value causes incorrect behaviour, since that effect never re-runs after Clear, which doesn't touch `hydrationRetryToken`). The regression was then reproduced deterministically at the unit/component level too (reverting only the fix, re-running, confirming the exact same `aria-pressed="false"` failure, then restoring the fix) and the strengthened `PlanningScreen.clearDraft.test.tsx` case now genuinely clicks the real "Change" disclosure controls before clearing, closing the gap the first pass left.
    - **`usePlanningRoute` extension — closes an in-flight-calculation gap.** The existing render-time `isInsufficientWaypoints` branch only ever cleared `routedResult` when waypoints dropped below 2 — it never aborted `abortControllerRef.current`, bumped `requestSeqRef.current`, or reset `isCalculating`/`updatingLegCount`/`lastErrorMessage`, so a calculation genuinely in flight when Clear draft ran (Calculate pressed, then Clear pressed before the response landed) was not aborted, and its `.then()`/`.catch()` — which only check `requestSeq !== requestSeqRef.current`, unchanged by that branch — could still have applied a stale routed result afterward. A new `reset(): void` (`usePlanningRoute.ts`, added to `UsePlanningRouteResult`, inserted immediately after `calculateNow`) aborts the controller, bumps `requestSeqRef.current`, clears the debounce timer, and resets `routedResult`/`isCalculating`/`updatingLegCount`/`lastErrorMessage` — called explicitly from Clear draft's own success handler, never automatically by the hook itself. Proved directly at the narrowest layer (`usePlanningRoute.test.ts`'s new `describe("reset", ...)` block: a queued request is genuinely aborted, and resolving it _after_ `reset()` neither revives `state.kind === "routed"` nor surfaces the abort as a user-facing error) and again through the real component (`PlanningScreen.clearDraft.test.tsx`: Calculate pressed, Clear draft confirmed while the controllable provider's promise is still pending, the promise is _then_ resolved with a route, and no Route summary region ever appears).
    - **Camera/geolocation fresh-session re-eligibility.** A new `freshSessionFramingToken` state (declared beside `hydrationRetryToken`, the same "just a counter, whose only job is to appear in a dependency array" idiom) was added to the existing fresh-session location-framing effect's dependency array — the only change to that effect; its body is untouched. Clear draft's success handler resets, in one synchronous batch, `hasRequestedInitialLocationRef.current = false` (re-arms the "run my meaningful body once" guard), `hasAppliedInitialFramingRef.current = false`, `hasManualCameraActionRef.current = false`, `hasManualGestureRef.current = false` (needed, not optional — a stale-`true` value would permanently downgrade a post-clear Locate-me press to recentre-only instead of the one-time regional fit a genuinely fresh mount would allow, reachable whenever the automatic post-clear geolocation attempt itself fails or returns null), and `pendingWaypointHydrationBounds = null`, then bumps `freshSessionFramingToken`. Writing (never reading) `hasManualCameraActionRef`/`hasAppliedInitialFramingRef` from this plain callback does not reproduce item 35's own documented `react-hooks/immutability` lint constraint, which is specifically about a _read_ from two separate effect bodies — the location-framing effect remains the sole reader, exactly like `handleLocateMe` and `onUserCameraInteraction` already are for the same two refs (confirmed clean, not merely argued: `npm run lint` passes with zero new suppressions). Ordering safety (the location effect's `waypointsRef.current.length > 0` check must see the post-clear empty array, not a stale one) is a direct consequence of React's documented effect-declaration-order execution per commit, since the `waypointsRef`-mirroring effect is declared textually earlier in the file and both effects' dependencies change in the same batched update. Proved end-to-end in both `PlanningScreen.clearDraft.test.tsx` (a `fitBoundsSpy` on the mock map records exactly one additional call directly attributable to the clear, and none from a later ordinary edit) and `e2e/clearPlanningDraft.spec.ts` (a genuine manual pan via MapLibre's `KeyboardHandler`, then Clear draft, asserts the real `data-camera-center` attribute changes away from the panned position and stays put across a later edit) — the e2e test deliberately does not assert the re-fit lands on a _specific new_ geolocation fix, having discovered along the way that `getApproximateLocationOnce`'s own deliberate `maximumAge: 5 * 60 * 1000` (a correct, pre-existing production choice, left untouched) means the real Geolocation API is permitted to return a recently-cached position rather than a brand-new reading even after `context.setGeolocation()` changes the mocked fix mid-test — a genuinely different `requestApproximateLocation` result being what gets applied is proved deterministically instead at the component level, where the mock has no such caching.
    - Cancel and a storage failure both preserve the draft exactly, mirroring the established `RouteListItem`/`RidingScreen` "disabled while in flight, so Cancel can never appear to undo work storage is still carrying out" precedent: Escape independently re-checks `isClearingRef.current` (since it bypasses `cancelDisabled`); a `clearDraft()` rejection shows a `role="alert"` "The draft could not be cleared on this device. Try again." message and closes the dialog, with focus restored to the trigger via a `useEffect` keyed on the error's own object identity (not an imperative call inside the `.catch()` block) — the trigger is still `disabled` in the DOM at the exact synchronous point `.catch()` runs, and `.focus()` on a still-disabled element silently no-ops, the same real, previously-found class of bug item 29's own End-ride implementation already documents.
    - **Testing.** `usePlanningRoute.test.ts` gained a 4-case `reset` block (no-op while idle, aborts + prevents late revival, clears a prior error/leg-progress state, cancels a scheduled recalculation debounce). A new `src/ui/planning/PlanningScreen.clearDraft.test.tsx` (13 cases, mirroring `PlanningScreen.saveAutosaveRace.test.tsx`'s exact mocked-repository/fake-timer/controlled-promise harness) covers: the dialog's exact copy and Cancel-focused state; Cancel and Escape both preserving state with no storage call; a full success case (genuinely clicking "Change" controls, not just seeding via mock — see the bug paragraph above) proving every field's reset including full undo/redo-history and routed-result removal; a `getPlanningPreferences()` rejection falling back safely with no error shown; a `clearDraft()` rejection preserving state, showing the accessible error, and a working focus-correct retry; a rapid double-confirm issuing exactly one `clearDraft()` call; Save-vs-Clear mutual exclusion; a pending autosave timer failing to resurrect the row; a delayed _original_ hydration read failing to resurrect old content after Clear (the scenario that actually exercises Clear's own `hasUserModifiedDraftFieldsRef` protection, distinct from item 31's existing rider-edits-before-hydration coverage); a delayed routing-provider result failing to restore the old route; the camera re-fit-exactly-once proof; and the renamed hydration-status terminology. `RidingScreen.test.tsx` gained a new terminology-focused test for the "could not be checked" catch path (previously entirely untested) and a positive assertion of the renamed reverse-confirmation dialog text; `SettingsScreen.test.tsx` gained one assertion for the renamed ferries hint. A new `e2e/clearPlanningDraft.spec.ts` (5 tests) proves, through the real UI and actual IndexedDB (`readPlanningDraftRow`): a meaningful draft — waypoints, a calculated route, a custom name, custom per-draft routing settings genuinely selected via the "Change" disclosure, and edit-copy provenance from a real "Edit copy in Planning" trip — opens the confirmation, is preserved exactly by Cancel, and is wiped to a genuinely fresh session using the real (untouched) current Settings defaults by Confirm, with the row proved absent across the complete autosave-debounce danger window via the established `assertPlanningDraftStaysCleared` polling pattern (duplicated locally, per this project's convention, from `editRouteAsPlanningCopy.spec.ts`/`reverseRoute.spec.ts`); a `page.reload()` after clearing does not resurrect the old draft or its calculated result; the fresh-session regional camera fit becomes available again after a genuine manual pan, exactly once; and a phone-viewport (390×844) test proves no horizontal overflow with the dialog open and a real ≥44×44px touch target on both the trigger and the dialog's own confirm button. Per this item's own testing paragraph, the delayed-hydration-read and delayed-routing-provider races are proved only at the component level (matching item 31's own precedent for its analogous races) — the brief's own "Unit/component" vs. "Real browser" section split scopes those two specifically to the former.
    - Files changed: `src/ui/planning/PlanningScreen.tsx`, `src/ui/planning/usePlanningRoute.ts`, `src/ui/riding/RidingScreen.tsx`, `src/ui/settings/SettingsScreen.tsx`; tests: `src/ui/planning/PlanningScreen.clearDraft.test.tsx` (new), `src/ui/planning/usePlanningRoute.test.ts`, `src/ui/planning/PlanningScreen.draftHydration.test.tsx`, `src/ui/riding/RidingScreen.test.tsx`, `src/ui/settings/SettingsScreen.test.tsx`, `e2e/clearPlanningDraft.spec.ts` (new), `e2e/reverseRoute.spec.ts`, `e2e/editRouteAsPlanningCopy.spec.ts`. No routing-provider payload, GPX format, service-worker policy, or IndexedDB schema change — `usePlanningRoute.reset()` and `freshSessionFramingToken` are pure in-memory React state, and `clearDraft()` already existed.
    - Verification: `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` (2239/2239 across 121 files), `npm run e2e` (111/111 across both the `chromium` and `android-chrome` projects), `npm run format:check` run last, immediately before committing — all pass. Version bumped to 0.3.28 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - Real-device iPhone and physical Android verification have not happened — see item 43's own ledger, which already lists this item; do not treat it as manually verified until each check under that ledger has actually been done.

38. **Reverse the current draft inside Planning — done**
    - Supersedes the pre-ride `Reverse route` entry point completed in item 27. Item 27's underlying mechanism — `reverseEditableWaypoints`, `suggestReversedRouteName`, the `<acn:planning>` GPX round-trip, the closed-loop/20-waypoint-cap/exact-provenance behaviour it proved — remains valid, completed history and is reused, not reimplemented; only _where the rider triggers reversal_ changed. See item 27 for the full historical implementation record; it is not restated here.
    - New workflow: open a saved/imported route → `Edit copy` (renamed from `Edit copy in Planning`; there is no competing edit destination in this PWA, so the shorter name is unambiguous — the confirmation dialog copy still explains that it opens/replaces the Planning draft) → in Planning, edit waypoints normally, or reverse the current draft via a new `Reverse route` action inside Planning itself, in the existing `role="group" aria-label="Waypoint actions"` row directly after `Return to start` and before the conditional `Add to end`. Riding's pre-ride panel's own former `Reverse route` button, `REVERSE_ROUTE_CONFIG`, and the whole `CopyOperationKind`/`CopyOperationConfig` parameterised-guard machinery that let the two pre-ride actions share one implementation are all deleted from `RidingScreen.tsx` — reversal no longer has a pre-ride entry point at all, and with only one copy action left (`performEditCopy`/`handleEditCopyClick`/`handleEditCopyCancel`), the kind-parameterised guard collapsed to a single `isEditCopyActionPendingRef`/`isEditCopyConfirmOpen`/`isEditCopyInFlight`/`editCopyError` set — a dead two-operation architecture was not kept merely to shrink the diff.
    - **Resolved undo/redo architecture (was the item's own "flag, not yet resolved" note; now settled).** `src/ui/planning/waypointHistory.ts`'s `WaypointHistoryState.past`/`present`/`future` were widened from bare `readonly Waypoint[]` entries to a small `WaypointDraftSnapshot { waypoints: readonly Waypoint[]; routeName: string }` — option (a) from the item's own two candidates, chosen over a second, reversal-scoped coupled mechanism because anything that must be undoable has to live in the one reducer that owns Undo/Redo; a second mechanism would itself be a second, parallel source of truth for "what does Undo do," the exact anti-pattern the item's own note warned against. Two new `WaypointAction` variants: `{type: "rename"; routeName: string}` mutates `present.routeName` only and **never** pushes a history entry (mirrors `select`'s existing "not every action needs to be undoable" precedent) — this is the literal mechanism keeping ordinary route-name _typing_ out of undo/redo, since the route-name `<input>`'s `onChange` now dispatches `rename` via plain `dispatch`, deliberately not the `dispatchWaypointAction` wrapper (typing a name must not clear an active warning/route-feature selection, unlike a genuine waypoint edit); and `{type: "reverse"}`, a normal history-creating action (like `append`) that pushes the current snapshot onto `past` and produces one new snapshot with both fields changed together — waypoints via a private `reverseDraftWaypoints` (reuses `reverseEditableWaypoints` for the coordinate-level reversal, reversing ids in lockstep via a plain array `.reverse()` rather than minting fresh ones with `createWaypointId`, since these are pre-existing, possibly-referenced live waypoints, not new placements) and the name via `suggestReversedRouteName`, guarded by a blank/whitespace-only check (`state.present.routeName.trim().length === 0` leaves the name exactly as-is — this guard lives in the reducer's `reverse` case, not inside `suggestReversedRouteName` itself, whose pure "no trimming, appends verbatim" contract is unchanged and still the caller's responsibility to special-case). `reset` gained a required `routeName` field (used by the hydration restore branch, the post-Save reset and Clear draft's own reset, all merged from two separate calls — `dispatchWaypointAction({type:"reset", waypoints:[]})` + `setRouteName(...)` — into one atomic dispatch). Every pre-existing action (`append`/`insertAfter`/`move`/`reorder`/`delete`/`returnToStart`) now carries `state.present.routeName` forward unchanged into its new snapshot — the mechanical but exhaustive part of the diff — so ordinary Undo/Redo never touches the name unexpectedly. `PlanningScreen.tsx`'s previously-separate `const [routeName, setRouteName] = useState("Planned route")` is deleted entirely; every read site became `state.present.routeName`/`state.present.waypoints` (autosave persist body and its dependency array, `waypointsRef`, `selectedIndex`, `waypointRoles`, `canReturnToStart`, `handleSave`/`handleExport`'s `buildPlanningProvenance` call and trimmed-name logic, `planningOverlay`, `describeCrosshairAction`, the Calculate button's `disabled` check, `WaypointList`'s props). Confirmed by construction, not just asserted: a `rename` reuses the same `present.waypoints` array reference (only `routeName` changes), so typing a name never retriggers `usePlanningRoute`'s separate recalculation-debounce effect, whose dependency is `waypoints` specifically — while the autosave-persist effect still correctly fires on a rename, since it depends on the whole `state.present` object, which does get a new reference on every `rename` (the object spread copies the array reference, not the array's own identity-triggering role).
    - **Correctness trace, verified against the actual reducer** (not merely asserted): (1) reversal → Undo restores the pre-reversal `{waypoints, name}` snapshot verbatim, since both were captured together in one object when `reverse` pushed onto `past`; (2) reversal → an ordinary edit (e.g. append) → Undo → Undo: the edit's own push carries the reversed name forward unchanged, so the first Undo reverts only the edit and keeps the reversed name, and the second Undo crosses the reversal boundary, restoring the original order and name together; (3) reversal → Undo → a new action clears `future` exactly as every other history-creating action already does. All three traces are proven directly by dedicated reducer-level tests in `waypointHistory.test.ts`, not deferred to component-level coverage alone.
    - **Explicit Calculate required after reversal — zero code changes to `usePlanningRoute.ts`.** `handleReverseRoute` calls `routing.reset()` (item 37's existing function) strictly after `dispatchWaypointAction({type:"reverse"})`, mirroring `handleClearDraftConfirm`'s own established ordering. Verified precisely why this suffices: `reset()` synchronously aborts any in-flight request, bumps the request-sequence ref, clears any scheduled debounce timer, and sets `routedResult` to `null`; a _separate_ effect (declared earlier in the file, keyed on `[routedResult]`) then sets `hasRoutedResultRef.current = false`; because both the dispatch and `reset()` run synchronously in one event handler (one React batch, one commit) and effects fire in declaration order per commit, the debounced recalculation-scheduling effect's own `if (!hasRoutedResultRef.current) return;` guard is already correct by the time it next runs — no new provider request is ever scheduled, satisfying "zero requests, including past the normal ~900ms debounce" with no changes to the hook itself. `isStale`'s own order-sensitive fingerprint check (unchanged) already made Save/Export unavailable the instant waypoint order changed, but only `reset()` stops the _automatic recalculation_ from firing — the two mechanisms serve genuinely different purposes here. After an explicit Calculate, reversed leg requests fall out of the existing per-leg resolution machinery for free (`buildRouteLegKey` is already directional over each leg's own endpoints) — no leg-cache or routing-adapter change was needed.
    - **`editCopyMeta`/`editCopyOperation` — deliberately left completely untouched by reversal**, confirmed against the full audit the item required. `editCopyMeta` describes _seed provenance_ ("this draft was created via Edit copy, from source route X"), not live edit history; `handleReverseRoute` never calls `setEditCopyMeta`. Consequences, all verified: `describeEditCopyNotice`, `storage/mapping.ts`'s `EditCopyOperation`/`isEditCopyOperation`, and `storage/db.ts`'s `editCopyOperation?: string` field all stay byte-identical — no IndexedDB schema bump, since this is a pure in-memory workflow change over already-additive optional fields. Since the pre-ride `REVERSE_ROUTE_CONFIG` (the sole writer of `operation: "reverse"`) is deleted, `"reverse"` becomes a **frozen, read-only legacy value** for pre-existing v0.3.17–v0.3.28 draft rows only; `fromStoredPlanningDraft`'s existing `?? "forward"` default already handles this correctly with no change. A freshly Edit-copied-then-Reversed-in-Planning draft therefore shows the **unchanged forward notice** ("Editable copy created from the route's original planning waypoints..." / "...estimated...") rather than a reverse-specific one — a deliberate behavioural difference from item 27 (whose reversal only ever happened at seed time, so its own dedicated reverse notice made sense then); this is not a contradiction, since the notice narrates how the draft was _seeded_, a historical fact unaffected by later edits, exactly as it already tolerates an ordinary append/delete without updating. `buildPlanningProvenance` already stamped from _live_ `state.present.waypoints` at Save/Export time, so a Save after Calculate on a reversed draft naturally produces `planningProvenance` with the reversed order, recovered exactly via the existing, unmodified `<acn:planning>` GPX extension — no new route/GPX field. Pre-existing tests that seed `editCopyOperation: "reverse"` directly via a mocked/raw stored draft row (`PlanningScreen.test.tsx`, `PlanningScreen.draftHydration.test.tsx`, `mapping.test.ts`) remain valid, now explicitly documented as legacy-read-compatibility coverage rather than a reachable current UI path.
    - Must-preserve behaviour, all confirmed unchanged: closed-loop reversal semantics (`[A,B,C,A]` → `[A,C,B,A]`, value-equal not reference-equal start/finish — proven directly by a dedicated reducer test and an e2e test); the existing 20-waypoint derivation cap (reversal only reorders an already-resolved, already-capped waypoint list from `resolveEditableWaypoints`/`deriveWaypointsFromRoute` at Edit-copy seed time — it never re-derives, so a reversed derived draft cannot exceed the cap merely because Reverse was pressed); exact `planningProvenance`-based recovery vs. geometry-derived fallback (both origins tested); the source saved/imported route is never mutated (proven by reopening it independently after saving a reversed copy). No confirmation dialog for the in-Planning Reverse route action (local + fully undoable) and no re-entrancy guard beyond its own `disabled` prop — deliberately matching Undo/Redo/Return to start's own lack of one; a rapid double-click simply double-reverses, harmlessly and fully undoably.
    - **Testing.** Pure/reducer: `waypointHistory.test.ts` gained `describe("rename", ...)` (no history entry, same `waypoints` array reference preserved, doesn't disturb selection) and `describe("reverse", ...)` (2/3-waypoint and closed-loop order reversal, ids reversed in lockstep not re-minted, named/blank/whitespace-only/already-suffixed name handling, exactly one history entry pushed with `future` cleared, selection cleared, no-op below 2 waypoints, and the three correctness-trace scenarios above at the reducer level) — every pre-existing append/insertAfter/move/reorder/delete/returnToStart/select/undo/redo/reset test was mechanically adapted to the new snapshot shape with no semantic change. `routeNaming.test.ts` gained one test proving `suggestReversedRouteName("")` still returns `" (reversed)"` verbatim — the pure contract is unchanged; the blank-draft guard lives one level up. Component: a new `src/ui/planning/PlanningScreen.reverseRoute.test.tsx` (mirroring `PlanningScreen.clearDraft.test.tsx`'s mocked-repository/fake-timer/controlled-promise harness, per this file's own established per-concern-split convention) covers disabled-below-2/enabled-at-2+, positioning inside the Waypoint-actions group, order+name reversal for named/blank/already-suffixed drafts persisted together via autosave, closed-loop reversal, clearing an active selection and a pending Move/Insert-after relocation, zero provider requests before Calculate (including past the debounce and across Undo/Redo of the reversal itself), `canSaveOrExportPlan` becoming `false` immediately after reversal with an explicit Calculate re-enabling Save/Export, a stale in-flight calculation result resolved after reversal failing to reappear, the notice staying unchanged for an Edit-copy-seeded draft, identical behaviour for a hand-planned draft with no `editCopyMeta`, and a fresh mount restoring a persisted reversed draft from storage. `RidingScreen.test.tsx`'s `describe("Edit copy in Planning", ...)` was renamed to `describe("Edit copy", ...)`, its shared-hint test rewritten to single-button framing, the entire `describe("Reverse route", ...)` block (6 tests) and two of the three "share a single in-flight guard" cross-kind tests deleted (no surviving equivalent with one action), the third (`"rapid double-click on Reverse route alone..."`) ported into the renamed `Edit copy` block retargeted at the sole remaining button, and one new test added positively asserting no `Reverse route` button exists in Riding at all. Browser: `e2e/reverseRoute.spec.ts` was reworked in place (not deleted and recreated) around a new `editCopyThenReverseInPlanning` helper (Edit copy, then press Planning's own Reverse route button), covering all ten required scenarios across 7 tests — zero requests until Calculate including past the debounce, atomic Undo/Redo with zero provider contact; Calculate sends the reversed leg coordinates and Save creates an independent route leaving the original unchanged and reopenable; the exact-provenance GPX export/offline-reimport/Edit-copy round trip; a derived arbitrary GPX capped at 20 waypoints; closed-loop reversal with the exact leg-by-leg reversed-and-reordered proof item 27 already established; reload recovering a reversed draft with zero automatic routing requests; and a phone-viewport (390×844) test proving the button is a real ≥44×44px touch target with no horizontal overflow. The old pre-ride confirmation-dialog test was deleted outright (no confirmation for the in-Planning action); `EXACT_REVERSE_NOTICE`/`DERIVED_REVERSE_NOTICE` were replaced with the unchanged forward-notice constants; the file's own camera-fit/marker-containment/manual-pan-survival proof was deliberately not re-added, since it exercises the Edit-copy seeding step alone (unchanged by this item) and is already fully proven by `editRouteAsPlanningCopy.spec.ts`. `e2e/editRouteAsPlanningCopy.spec.ts` and `e2e/clearPlanningDraft.spec.ts` had every `"Edit copy in Planning"` button-name query and rationale comment updated to `"Edit copy"`.
    - Files changed: `src/ui/planning/waypointHistory.ts` (+`.test.ts`), `src/ui/planning/PlanningScreen.tsx`, `src/domain/routeNaming.ts` (+`.test.ts`, doc-comment only), `src/ui/riding/RidingScreen.tsx` (+`.test.tsx`), `src/domain/editableWaypoints.ts`/`src/storage/mapping.ts`/`src/storage/db.ts` (doc-comment-only "Edit copy" renames), `e2e/reverseRoute.spec.ts`, `e2e/editRouteAsPlanningCopy.spec.ts`, `e2e/clearPlanningDraft.spec.ts`; new: `src/ui/planning/PlanningScreen.reverseRoute.test.tsx`.
    - Verification: `npm run format`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` (2269/2269 across 122 files), `npm run e2e` (112/112 across both the `chromium` and `android-chrome` projects), `npm run format:check` run last, immediately before committing — all pass. Version bumped to 0.3.29 (`package.json` and `package-lock.json`'s two matching `version` fields, hand-patched, following the established precedent).
    - Real-device iPhone and physical Android verification have not happened — see item 43's own ledger, which already lists this item; do not treat it as manually verified until each check under that ledger has actually been done.

### Map zoom-dependent route and waypoint presentation

39. **Low-zoom surface-warning width correction — done**
    - Direct follow-up to item 23 (Low-zoom route-colour and waypoint-marker legibility), which fixed ascent/descent overlay and neutral-base widths but left warning-layer widths unaddressed. No change to warning detection, categories, colours, dash patterns, selection semantics, details panels, or warning geometry (`src/map/warningLayer.ts` is untouched).
    - **Confirmed root cause**, and one correction to this item's own prior text: `routeWidthPolicy.ts`'s two zoom-recession families resolve identically at close zoom (15) but diverge below it — `recedingWidthStops` (multipliers 0.4 at overview zoom 6, 0.65 at regional zoom 11) recedes markedly faster than `legibleWidthStops` (0.6 at overview, 0.8 at regional). `MapView.tsx` applied `legibleWidthStops` — not `recedingWidthStops` as this item's own pre-implementation note incorrectly claimed — to the neutral base/casing (`acn-route-remaining-line`/`acn-route-completed-line`), the selected route-feature halo, _and_ every warning-related layer (`WARNING_SELECTED_PAINT`, width 13; each `WARNING_CATEGORY_PAINT` entry, widths 8–10). Warnings sharing the neutral base's gentle recession curve, rather than the faster-receding curve item 23 already gave the climb/descent colour overlays, is exactly why warnings remained visually dominant at low zoom in a way ascent/descent overlays no longer were since item 23.
    - **Chosen policy**: a third recession family, `warningWidthStops(closeWidthPx)` (`routeWidthPolicy.ts`), built via the existing `buildStops` helper with `WARNING_MULTIPLIERS = { overview: 0.45, regional: 0.6 }` — a distinct, warning-specific family, not a reuse of either existing one, since a direct swap onto `recedingWidthStops` would have narrowed the selected-warning halo below the selected route-feature halo at overview zoom (`13 × 0.4 = 5.2` vs `9 × 0.6 = 5.4`), inverting item 23's own established nested-ring priority. The chosen multipliers preserve every required inequality at every zoom stop:
      | width (px)                           | overview (×0.45) | regional (×0.6) | close |
      | ------------------------------------ | ---------------- | --------------- | ----- |
      | 8 (unknown-surface)                  | 3.6              | 4.8             | 8     |
      | 9 (other/ferry/questionable-surface) | 4.05             | 5.4             | 9     |
      | 10 (unsuitable-surface/obstacle)     | 4.5              | 6.0             | 10    |
      | 13 (selected warning)                | 5.85             | 7.8             | 13    |
      - Strictly narrower than `legibleWidthStops` at overview/regional, exactly equal at close, for every warning width — a warning now recedes rather than dominating a full-route overview.
      - Every category width stays wider than `recedingWidthStops(5)` (the climb/descent macro/micro overlay centre: 2/3.25/5) at every zoom, even the narrowest category (3.6 > 2, 4.8 > 3.25 at overview/regional) — a warning still visually outranks a climb/descent it overlaps.
      - Every category width stays wider than `legibleWidthStops(5)` (the neutral route base: 3/4/5) at every zoom, even the narrowest category (3.6 > 3, 4.8 > 4, 8 > 5).
      - `warningWidthStops(13)` (selected warning) stays wider than `legibleWidthStops(9)` (selected route-feature halo: 5.4/7.2/9) at every stop (5.85 > 5.4, 7.8 > 7.2, 13 > 9) — a selected warning still outranks a selected climb/descent.
      - `warningWidthStops(13)` stays wider than every warning category width at every stop, and proportional ordering within the warning family is preserved throughout — both hold by construction, since every warning width shares one multiplier applied to a strictly-ordered set of close-widths.
      - Treat these as first-pass, reasoned starting values pending real-device acceptance, mirroring this file's own established humility for `routeWidthPolicy.ts`'s existing constants.
    - **Applied consistently, with no Planning/Riding mode branch**: `WARNING_CATEGORY_PAINT`'s six entries and `WARNING_SELECTED_PAINT` now use `warningWidthStops` instead of `legibleWidthStops`; the neutral base, selected route-feature halo, climb/descent macro/micro overlays and Planning-preview layer are all unchanged. `RidingScreen.tsx` never passes a `warningOverlay` prop to `MapView` — warnings are a Planning-only UI/data feature (see "## Planning behaviour" above); Riding's pre-ride overview and active riding render the warning _layers_ unconditionally (as before) but their GeoJSON sources stay empty there, by existing, correct design, unchanged by this item. "Applied consistently" therefore means the shared paint constants are installed identically for every `MapView` caller by construction, proven directly by a new Planning/Riding parity test covering all six warning category layers.
    - **Files changed**: `src/map/routeWidthPolicy.ts` (new `WARNING_MULTIPLIERS`/`warningWidthStops`, updated doc comments), `src/map/MapView.tsx` (seven `legibleWidthStops` → `warningWidthStops` call sites, updated doc comments including the nested-ring rationale comment inside `addRouteAndPositionLayers`). No change to `src/map/warningLayer.ts`, `src/map/mapAdapter.ts`, or warning detection/normalisation code.
    - **Tests**: `routeWidthPolicy.test.ts` gained a `describe("warningWidthStops", ...)` block (9 tests: close-zoom-unchanged, ascending zoom stops, monotonicity, strictly-narrower-than-legible, wider-than-gradient-centre, wider-than-neutral-base, selected-warning-wider-than-selected-feature, selected-warning-wider-than-every-category, proportional ordering). `MapView.test.tsx`: the existing exhaustive per-layer paint test now expects `warningWidthStops` for the seven warning-related layers; a new test proves the real installed paint hierarchy (warning casings wider than the neutral base and macro overlay at overview/regional; the selected-warning halo wider than the selected route-feature halo at every stop); the Planning/Riding parity test's layer list was extended to all six warning category layers; a new "zoom-invariant warning source data" test (mirroring item 23's own pattern) proves a camera-only zoom change never touches warning GeoJSON source data; the fallback-style test gained assertions that the local fallback style installs the same `warningWidthStops` paints; a new test proves the full `WARNING_CATEGORIES_IN_PAINT_ORDER` add-order sequence, which no prior test covered exhaustively. `e2e/lowZoomLegibility.spec.ts` gained a `MOCK_WARNING_ORS_RESPONSE` fixture (surface codes 8 "Compacted gravel"/`questionable-surface` and 12 "Ground or mud"/`unsuitable-surface`, verified against `src/routing/surfaceCodes.ts`'s real code table) and three new tests: warning count/category/selection/details surviving a Planning zoom round trip; the same route surviving Save → pre-ride overview → Start riding across further zoom round trips (with an inline comment recording the Riding-has-no-warning-UI scope note above); and the warning staying selected and the route staying rendered under the local fallback style across a zoom round trip.
    - **Verification**: focused `npx vitest run src/map/routeWidthPolicy.test.ts src/map/MapView.test.tsx src/map/warningLayer.test.ts` (165/165 passed) and `npx playwright test e2e/lowZoomLegibility.spec.ts` (6/6 passed), with the three new e2e tests additionally repeated 5× under `--workers=1` (15/15 passed, no flakiness). Full gate (`npm run format`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test`, `npm run e2e`, `npm run format:check` run last) — see this file's own version-history discipline; results recorded at the time of the commit that shipped this item.
    - Real-device iPhone and physical Android visual acceptance have not happened — item 43's ledger already lists this item ("surface-warning legibility at overview and close zoom on a route carrying warnings"); do not treat it as manually verified until that check has actually been done.

### Ride lifecycle

40. **Riding information and action proximity**
    - Pre-ride: move `Recognised climbs` (item 13's `RidingClimbSelector`) so it sits directly under the route-profile/elevation area, with the selector and the selected climb's detailed profile (item 13's thirteenth-slice preview chart) kept spatially adjacent within that one area — today `RidingClimbSelector` and the `ride-profile-panel` elevation section are separate, sequential blocks in `RidingScreen.tsx`. This is a DOM-order/placement change only: every existing elevation-view behaviour (Full/2/5/10 km, Climb view availability, automatic current-climb entry, manual dismissal and re-entry, item 13's whole state machine) is unchanged — do not touch climb detection or progress calculation.
    - Active riding: move the `End ride` trigger (item 29) to the top of the active-Riding screen, with its confirmation card/dialog shown in the same top area so the trigger and its consequence stay close — today the active-riding End-ride row sits below the map, in the DOM slot `RidingClimbSelector` occupies pre-ride. Keep the button label exactly `End ride` (unchanged for both route-based Riding and, per item 42 below, future free roam). Preserve every existing failure/cancellation semantic from item 29 unchanged: cancellation and storage failure leave the ride active/resumable, with focus restored to the trigger and a retry path. `Finish ride` (item 29's `RidingRouteCompletionPanel`) remains the recognised route-completion action and continues to share the same successful finalisation path (`performFinalizeRide`) — this slice reorders where triggers sit, not the finalisation architecture itself.
    - Testing: prefer DOM-order/accessibility-tree assertions over CSS-only positioning assumptions. Existing climb-view (item 13) and End/Finish lifecycle suites (`RidingScreen.finishEndRide.test.tsx`, `RidingScreen.completionArming.test.tsx`, item 29) should pass unchanged wherever their assertions don't depend on the specific DOM position being moved — update only the assertions that do. Add phone-viewport browser coverage for the new ordering and confirmation placement.

41. **Ride launcher, explicit session recovery and post-finalisation route clearing**
    - Follow-up to item 6 (the existing "Ride, no selected route" empty screen offering `Choose a route`) and item 29 (End/Finish ride). Foundation for item 42 (free roam) below — this slice's session-storage shape should anticipate, without yet implementing, a route/free-roam discriminant.
    - Current architecture (confirmed by source inspection): `App.tsx`'s `screen`/`selectedRoute` are plain, unpersisted `useState` with no mount-time read of any persisted session state — `getActiveRideState()` is called only inside `useRideNavigation.ts`'s own mount-restore effect (reached only once `RidingScreen` is actually mounted with a `selectedRoute`) and read-only in `DiagnosticsScreen.tsx`. A reload therefore always returns to the Routes screen; "Resume riding" only becomes reachable once the rider manually reopens the _same_ route by ID. `useRideNavigation.ts`'s `finish()` (item 29) already clears `StoredRideState` and resets in-hook state, but nothing today clears `App.tsx`'s own `selectedRoute`/`screen` — so `App.tsx` needs a new integration point, not a change to `finish()`'s own storage-clear contract.
    - Required outcome: after a successful `End ride` or `Finish ride`, clear the active ride state (existing `finish()` behaviour, unchanged) **and** clear `App.tsx`'s transient `selectedRoute`, landing on an empty Ride launcher. Cancellation, finalisation failure, an ordinary tab change, PWA suspension, a GPS error, or a recoverable reload must never clear `selectedRoute` or any ride state — only a _successful_ End/Finish transition does. Never delete or alter the saved route itself, the Planning draft, or Settings — this only touches transient navigation state and the single active-ride-state row.
    - The launcher (reachable via the existing empty-Ride-screen path from item 6, now reached more often since finalisation lands there too) must inspect persisted active-session state on its own, not infer "no unfinished ride" merely from `selectedRoute === null` — since after a reload `selectedRoute` is _always_ null regardless of whether a resumable session exists. For an unfinished route session, resolve its saved route locally by `routeId` and offer `Resume route` (not "Resume riding" — no longer accurate once a route-less free-roam session, item 42, can also be resumed) alongside `End ride`, so the rider can abandon the session without first resuming GPS tracking. While an unfinished route session exists, do not offer `Start free roam` (item 42) — the rider must explicitly `End ride` first, a deliberately conservative guard against accidentally losing resumable progress. If the stored session references a `routeId` that no longer resolves to a saved route (deleted route, corrupted store), explain that it cannot be resumed and offer an explicit, confirmed `Discard unfinished ride` action — never silently delete the row without confirmation. All recovery must stay local/offline — never contact the routing provider merely to populate the launcher.
    - Architecture: keep one authoritative active-session lifecycle and storage-clear path — extend `useRideNavigation.ts`'s `finish()`/`clearActiveRideState()` machinery and `App.tsx`'s own new integration point, rather than building a second, competing End-ride implementation directly in `App.tsx`. Sequence storage-clear failures before any transient-state change, mirroring item 29's own "never claim success before the persisted state is actually proven cleared" discipline.
    - Testing: App/component/storage/browser coverage for each launcher state (no session, resumable route session, resumable-but-route-missing session), cancellation/failure paths, route resolution after a real `page.reload()`, the successful End/Finish → empty-launcher transition, and the missing-route discard-confirmation flow.

42. **Route-less free roam**
    - A larger, optional feature, deliberately kept as its own slice after item 41 (Ride launcher/session recovery) lands — must not be merged into a small visual cleanup slice, and must not begin before item 41's launcher/session architecture exists to build on.
    - User-visible behaviour: when no active session exists, the idle Ride launcher offers both `Choose a route` and `Start free roam`. Free roam shows the live GPS position on the map with no selected route; camera follows the rider's direction of movement (north-up before a reliable course is available, rotating to course once reliable, retaining the last reliable bearing rather than oscillating while stationary or course-unreliable) — mirroring, not necessarily reusing verbatim, route Riding's existing camera-follow principles and manual-recovery affordances (item 24's north-up/follow controls). Uses the same `End ride` label and top-of-screen confirmation placement as item 40's route-Riding change. An unfinished free-roam session offers `Resume free roam` plus `End ride`, mirroring item 41's route-session recovery. Starting free roam is unavailable while an unfinished route session exists (item 41); selecting a route while an unfinished free-roam session exists must not silently replace it — require deliberate finalisation or an explicit, separately specified replacement confirmation (design that confirmation at implementation time; not specified further here).
    - Persistence and lifecycle: suspension/reload recovery, stale-fix handling and wake lock behave like route Riding wherever route-independent (items 10, 29). Extend active-session storage with an explicit discriminated session kind (route vs. free-roam) rather than a sentinel `routeId` — `StoredRideState`'s existing fields are all optional and non-indexed beyond the original core set, so a new `kind` field fits the same additive, no-version-bump pattern already established repeatedly on this table (`wakeLockDesired`, `dismissedClimbFeatureId`, `completionArmed`, item 29). A missing `kind` on an existing stored row must map to `"route"` for backwards compatibility — confirm this mapping against `mapping.ts`'s existing `fromStoredRideState` defaulting conventions at implementation time rather than assuming the exact shape now. Persist only the minimal latest state needed for recovery (latest accepted fix, camera mode/state, last reliable bearing, wake-lock preference) — never a track, speed history, location history or ride history, per this project's explicit non-goals (see "Explicit non-goals" at the top of this file: no ride recording or location history). Free roam must not require OpenRouteService, a saved route, or any network access beyond ordinary map tiles, and must remain usable with the existing map-fallback behaviour.
    - Testing: unit-test the session-kind discriminant/mapping and legacy-row defaulting, camera course transitions, stationary/unreliable-course bearing retention, and lifecycle cleanup. Component/browser coverage should prove start, resume-after-reload, End ride, wake-lock cleanup, geolocation-watch cleanup, manual-camera-gesture/follow restoration, and the complete absence of any routing-provider request. Tests must not construct or assert any accumulated location array — the product must not retain one, and a test that builds one to "verify" behaviour would itself misrepresent the required architecture.

### Follow-up acceptance ledger

43. **Real-device acceptance ledger for items 33–42**
    - Not yet implemented; recorded here so a future manual-verification pass has an authoritative checklist once items 33–42 land, rather than being invented ad hoc. Do not add any of the following to the "Manual acceptance status" ledger above until each has actually been checked on the corresponding real device — automated (Playwright/Chromium-emulated) evidence and real-device evidence must stay explicitly distinct, per this file's own established convention (item 25's own `docs/android-chrome-acceptance.md` tagging discipline).
    - Installed iPhone Home Screen PWA, portrait orientation: Route Library density/ordering with the headings removed (item 33); the compact routing-preferences `Change` control and reordered Calculate/waypoint-action flow (item 36); `Clear draft` and the "draft" terminology throughout (item 37); reversing a draft from within Planning, in both a hand-planned and an edit-copy draft (item 38); surface-warning legibility at overview and close zoom on a route carrying warnings (item 39); climb-selector/route-profile proximity and the top-of-screen End-ride flow (item 40); every Ride-launcher state transition, including the missing-route discard path (item 41); and free roam's direction-following, stationary-bearing stability, suspension/resume, wake lock, End ride and battery-conscious behaviour on an actual ride (item 42). (Restored-draft/edit-copy camera framing, item 35, is now confirmed — see item 35's own entry and "Manual acceptance status" above.)
    - The still-outstanding normal road-bicycle field test for route Riding (recorded under "Manual acceptance status" above — live climb progress, completion-detection and arming thresholds from item 29's two slices) remains separately outstanding and is not superseded by anything in this entry. Free roam (item 42) needs its own dedicated bicycle field test, distinct from route Riding's. Real Android Chrome verification (distinct from Playwright's `android-chrome` Chromium-emulated project, item 25) is required for every item above once implemented — extend `docs/android-chrome-acceptance.md` with the equivalent launcher/free-roam/layout/recovery checks at that time, rather than inventing a second acceptance document.

### End-to-end test reliability

44. **`e2e/ridingCamera.spec.ts`'s manual-rotation/manual-gesture preconditions — done**
    - Found via a failed `Deploy to GitHub Pages` run, [31691965402](https://github.com/adf-MD/amazing-cycling-navigation/actions/runs/31691965402) (build job [94421062907](https://github.com/adf-MD/amazing-cycling-navigation/actions/runs/31691965402/job/94421062907)), for commit `0665d00` ("Fit Planning camera to restored waypoints", v0.3.26). Formatting, linting, type checking, unit/component tests and the production build all passed; the workflow's Pages setup/artifact-upload/deploy steps never ran because the E2E step failed first. The single failure was `e2e/ridingCamera.spec.ts:47:1`, "Riding: pressing Northwards twice, with a manual rotation in between, rotates back to north both times", timing out on `await expect.poll(() => mapContainer.getAttribute("data-camera-bearing")).not.toBe("0")` — the synthetic right-button `page.mouse` drag used only to establish that test's own intervening-manual-rotation precondition never caused MapLibre to publish a changed bearing, so the second Northwards press was never actually exercised.
    - `0665d00` touched only `PlanningScreen.tsx`, `MapView.tsx` (doc comments) and Planning-specific tests — it did not touch Riding production code or `e2e/ridingCamera.spec.ts` at all — so this is not evidence of a regression from that commit. It is a fresh instance of the exact narrow, CI-environment-sensitive MapLibre `DragRotateHandler`/missing-`moveend` failure mode already documented and fixed once for Planning in item 21 above. `e2e/ridingCamera.spec.ts` still carried the older, non-deterministic right-button-drag precondition in **both** of its tests (the Northwards one above, and "re-pressing Follow location with an unchanged GPS fix, after a manual gesture, resumes following") and needed the same hardening item 21 already gave Planning.
    - Confirmed safe by direct source inspection before changing anything: `mapAdapter.ts`'s `onUserCameraInteraction` (the sole "was this a genuine user gesture" mechanism) fires on MapLibre's `movestart` event whenever `event.originalEvent` is set — true for both a real pointer drag and a real keyboard-driven `easeTo`, and not true for this app's own programmatic `fitBounds`/`setCamera` calls. `RidingScreen.tsx:937` wires `onUserCameraInteraction={camera.reportUserInteraction}` — the identical generic mechanism Planning already uses to unpress its own North-up control (proven working from the keyboard path by item 21's own test). A keyboard-triggered ease is therefore indistinguishable, to production code, from a real drag for both the Northwards-unpress contract and Riding's own Follow-mode-pause contract. No production code changed.
    - **Fix.** Both tests now establish their manual-gesture precondition via a new local helper, `establishManualRotationAndPitch` (`e2e/ridingCamera.spec.ts`), mirroring `planning.spec.ts`'s own identically-named helper per this file's established no-shared-e2e-helpers-across-specs convention: focus the real `<canvas>` inside `[data-testid="map-container"]`, then drive MapLibre's own deterministic `KeyboardHandler` (`Shift+ArrowRight` then, once settled, `Shift+ArrowUp`) through its ordinary `easeTo`/`movestart`/`rotatestart`/`moveend` pipeline — eliminating the drag/inertia state machine by construction rather than retrying past it. Unlike Planning's helper, which assumes a bearing/pitch baseline of `"0"`, this version captures the map's own currently-settled bearing and pitch before pressing any key and polls for a genuine change from _that_ baseline — needed because the "re-pressing Follow location" test deliberately begins from its travel-up followed camera (pitch 35°, `FOLLOW_PITCH_DEGREES`), never from north-up/level. The Northwards test also gained an explicit `data-camera-pitch === "0"` baseline assertion and `aria-pressed` checks on `northButton` (unpressed after the manual gesture, pressed again after the second press) — real end-to-end proof of the same "manual rotation unpresses Northwards" contract `PlanningScreen.test.tsx` already covers at the mock level, through the real `onCameraSettled` path. The Follow-location test's existing `aria-pressed === "false"` and `not.toBe(followedBearing)` assertions immediately after the gesture were kept exactly as they were, even though they now prove a fact the helper's own internal poll already established, since removing the assertion that the manual change genuinely occurred was explicitly out of scope. `e2e/directionArrows.spec.ts`'s own independent real right-button-drag coverage of `DragRotateHandler` (its "arrows remain attached and correctly oriented after the map is rotated" test) is completely untouched — this fix removes the drag only where it was a precondition for unrelated behaviour, not the only remaining browser coverage of pointer rotation.
    - **Verification.** Focused: `npx playwright test e2e/ridingCamera.spec.ts` — 2/2 passed. Repeated stability, mirroring item 21's own method: `npx playwright test e2e/ridingCamera.spec.ts --repeat-each=20 --workers=1` — 40/40 passed, both with a normal reused dev server and again with `CI=1` set (forcing a fresh `vite preview` boot per `playwright.config.ts`'s `reuseExistingServer: !process.env.CI`) — 40/40 passed. Regression: `npx playwright test e2e/directionArrows.spec.ts` — 3/3 passed, including the real-drag rotation test. Full required verification (`npm run format`, `npm run lint`, `npm run typecheck`, `npm run build`, `npm test` — 2205/2205 across 120 files, `npm run e2e` — 104/104 across both the `chromium` and `android-chrome` projects, `npm run format:check` run last, immediately before committing) all pass.
    - No production code changed anywhere (`src/` is untouched); only `e2e/ridingCamera.spec.ts` and this entry changed. No version bump, matching item 21's and item 30's own established precedent for test-only hardening fixes.
    - Pushed to `main` as commit `b8cec2c` ("Harden Riding camera E2E manual-gesture precondition"). The resulting `Deploy to GitHub Pages` run completed successfully and the site redeployed — confirmed directly by the user via the GitHub Actions UI (this sandbox has no network access to GitHub Actions, so the exact run ID/URL was not independently fetched and is not recorded here). This confirms the diagnosis above: the only defect was this file's own flaky E2E precondition, not the production build or the Pages workflow configuration, which is why no production-code change was needed. Because `0665d00`'s own deploy attempt was blocked by that precondition failure, `0665d00` itself was never actually served from GitHub Pages on its own — `b8cec2c` (which carries `0665d00`'s changes forward unchanged, plus this fix) is the first commit whose deploy actually reached production.
